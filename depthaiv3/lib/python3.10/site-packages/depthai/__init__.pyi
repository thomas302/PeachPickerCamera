from depthai import filters as filters
from depthai import modelzoo as modelzoo
from depthai import nn_archive as nn_archive
from depthai import node as node

# Ensures that the stubs are picked up - thanks, numpy project
import typing
json = dict
from pathlib import Path
from typing import Set, Type, TypeVar
T = TypeVar('T')
import datetime
import numpy
import os
from . import filters as filters, modelzoo as modelzoo, nn_archive as nn_archive, node as node
from _typeshed import Incomplete
from depthai.filters.params import MedianFilter as MedianFilter
from typing import Callable, ClassVar, Iterable, Iterator, overload

LINE_LIST: PointsAnnotationType
LINE_LOOP: PointsAnnotationType
LINE_STRIP: PointsAnnotationType
POINTS: PointsAnnotationType
UNKNOWN: PointsAnnotationType
X_LINK_ALREADY_OPEN: XLinkError_t
X_LINK_ANY_PLATFORM: XLinkPlatform
X_LINK_ANY_PROTOCOL: XLinkProtocol
X_LINK_ANY_STATE: XLinkDeviceState
X_LINK_BOOTED: XLinkDeviceState
X_LINK_BOOTED_NON_EXCLUSIVE: XLinkDeviceState
X_LINK_BOOTLOADER: XLinkDeviceState
X_LINK_COMMUNICATION_FAIL: XLinkError_t
X_LINK_COMMUNICATION_NOT_OPEN: XLinkError_t
X_LINK_COMMUNICATION_UNKNOWN_ERROR: XLinkError_t
X_LINK_DEVICE_ALREADY_IN_USE: XLinkError_t
X_LINK_DEVICE_NOT_FOUND: XLinkError_t
X_LINK_ERROR: XLinkError_t
X_LINK_FLASH_BOOTED: XLinkDeviceState
X_LINK_GATE: XLinkDeviceState
X_LINK_GATE_BOOTED: XLinkDeviceState
X_LINK_GATE_SETUP: XLinkDeviceState
X_LINK_INIT_PCIE_ERROR: XLinkError_t
X_LINK_INIT_TCP_IP_ERROR: XLinkError_t
X_LINK_INIT_USB_ERROR: XLinkError_t
X_LINK_INSUFFICIENT_PERMISSIONS: XLinkError_t
X_LINK_IPC: XLinkProtocol
X_LINK_MYRIAD_2: XLinkPlatform
X_LINK_MYRIAD_X: XLinkPlatform
X_LINK_NMB_OF_PROTOCOLS: XLinkProtocol
X_LINK_NOT_IMPLEMENTED: XLinkError_t
X_LINK_OUT_OF_MEMORY: XLinkError_t
X_LINK_PCIE: XLinkProtocol
X_LINK_RVC3: XLinkPlatform
X_LINK_RVC4: XLinkPlatform
X_LINK_SUCCESS: XLinkError_t
X_LINK_TCP_IP: XLinkProtocol
X_LINK_TIMEOUT: XLinkError_t
X_LINK_UNBOOTED: XLinkDeviceState
X_LINK_USB_CDC: XLinkProtocol
X_LINK_USB_VSC: XLinkProtocol
__bootloader_version__: str
__build_datetime__: str
__commit__: str
__commit_datetime__: str
__device_rvc3_version__: str
__device_rvc4_version__: str
__device_version__: str
__init_subclass__: Callable
__version__: str
createSubnode: Callable

class ADatatype:
    """Abstract message"""
    def __init__(self) -> None:
        """__init__(self: depthai.ADatatype) -> None"""

class AprilTag:
    """AprilTag structure."""
    bottomLeft: Point2f
    bottomRight: Point2f
    decisionMargin: Incomplete
    hamming: int
    id: int
    topLeft: Point2f
    topRight: Point2f
    def __init__(self) -> None:
        """__init__(self: depthai.AprilTag) -> None"""

class AprilTagConfig(Buffer):
    """AprilTagConfig message."""

    class Family:
        """Supported AprilTag families.

        Members:

          TAG_36H11

          TAG_36H10

          TAG_25H9

          TAG_16H5

          TAG_CIR21H7

          TAG_STAND41H12"""
        __members__: ClassVar[dict] = ...  # read-only
        TAG_16H5: ClassVar[AprilTagConfig.Family] = ...
        TAG_25H9: ClassVar[AprilTagConfig.Family] = ...
        TAG_36H10: ClassVar[AprilTagConfig.Family] = ...
        TAG_36H11: ClassVar[AprilTagConfig.Family] = ...
        TAG_CIR21H7: ClassVar[AprilTagConfig.Family] = ...
        TAG_STAND41H12: ClassVar[AprilTagConfig.Family] = ...
        __entries: ClassVar[dict] = ...
        def __init__(self, value: int) -> None:
            """__init__(self: depthai.AprilTagConfig.Family, value: int) -> None"""
        def __eq__(self, other: object) -> bool:
            """__eq__(self: object, other: object) -> bool"""
        def __hash__(self) -> int:
            """__hash__(self: object) -> int"""
        def __index__(self) -> int:
            """__index__(self: depthai.AprilTagConfig.Family) -> int"""
        def __int__(self) -> int:
            """__int__(self: depthai.AprilTagConfig.Family) -> int"""
        def __ne__(self, other: object) -> bool:
            """__ne__(self: object, other: object) -> bool"""
        @property
        def name(self) -> str:
            """name(self: object) -> str

            name(self: object) -> str
            """
        @property
        def value(self) -> int:
            """(arg0: depthai.AprilTagConfig.Family) -> int"""

    class QuadThresholds:
        """AprilTag quad threshold parameters."""
        criticalDegree: float
        deglitch: bool
        maxLineFitMse: float
        maxNmaxima: int
        minClusterPixels: int
        minWhiteBlackDiff: int
        def __init__(self) -> None:
            """__init__(self: depthai.AprilTagConfig.QuadThresholds) -> None"""
    decodeSharpening: float
    family: AprilTagConfig.Family
    maxHammingDistance: int
    quadDecimate: int
    quadSigma: float
    quadThresholds: AprilTagConfig.QuadThresholds
    refineEdges: bool
    @overload
    def __init__(self) -> None:
        """__init__(*args, **kwargs)
        Overloaded function.

        1. __init__(self: depthai.AprilTagConfig) -> None

        2. __init__(self: depthai.AprilTagConfig) -> None
        """
    @overload
    def __init__(self) -> None:
        """__init__(*args, **kwargs)
        Overloaded function.

        1. __init__(self: depthai.AprilTagConfig) -> None

        2. __init__(self: depthai.AprilTagConfig) -> None
        """
    def setFamily(self, family: AprilTagConfig.Family) -> AprilTagConfig:
        """setFamily(self: depthai.AprilTagConfig, family: depthai.AprilTagConfig.Family) -> depthai.AprilTagConfig

        Parameter ``family``:
            AprilTag family
        """

class AprilTagProperties:
    """Specify properties for AprilTag"""
    initialConfig: AprilTagConfig
    inputConfigSync: bool
    numThreads: int
    def __init__(self, *args, **kwargs) -> None:
        """Initialize self.  See help(type(self)) for accurate signature."""

class AprilTags(Buffer):
    """AprilTags message."""
    aprilTags: list[AprilTag]
    def __init__(self) -> None:
        """__init__(self: depthai.AprilTags) -> None"""
    def getSequenceNum(self) -> int:
        """getSequenceNum(self: depthai.AprilTags) -> int

        Retrieves image sequence number
        """
    def getTimestamp(self) -> datetime.timedelta:
        """getTimestamp(self: depthai.AprilTags) -> datetime.timedelta

        Retrieves timestamp related to dai::Clock::now()
        """
    def getTimestampDevice(self) -> datetime.timedelta:
        """getTimestampDevice(self: depthai.AprilTags) -> datetime.timedelta

        Retrieves timestamp directly captured from device's monotonic clock, not
        synchronized to host time. Used mostly for debugging
        """

class Asset:
    """Asset is identified with string key and can store arbitrary binary data"""
    alignment: int
    data: numpy.ndarray[numpy.uint8]
    @overload
    def __init__(self) -> None:
        """__init__(*args, **kwargs)
        Overloaded function.

        1. __init__(self: depthai.Asset) -> None

        2. __init__(self: depthai.Asset, arg0: str) -> None
        """
    @overload
    def __init__(self, arg0: str) -> None:
        """__init__(*args, **kwargs)
        Overloaded function.

        1. __init__(self: depthai.Asset) -> None

        2. __init__(self: depthai.Asset, arg0: str) -> None
        """
    @property
    def key(self) -> str:
        """(self: depthai.Asset) -> str"""

class AssetManager:
    """AssetManager can store assets and serialize"""
    @overload
    def __init__(self) -> None:
        """__init__(*args, **kwargs)
        Overloaded function.

        1. __init__(self: depthai.AssetManager) -> None

        2. __init__(self: depthai.AssetManager, arg0: str) -> None
        """
    @overload
    def __init__(self, arg0: str) -> None:
        """__init__(*args, **kwargs)
        Overloaded function.

        1. __init__(self: depthai.AssetManager) -> None

        2. __init__(self: depthai.AssetManager, arg0: str) -> None
        """
    def addExisting(self, assets: list[Asset]) -> None:
        """addExisting(self: depthai.AssetManager, assets: list[depthai.Asset]) -> None

        Adds all assets in an array to the AssetManager

        Parameter ``assets``:
            Vector of assets to add
        """
    @overload
    def get(self, key: str) -> Asset:
        """get(*args, **kwargs)
        Overloaded function.

        1. get(self: depthai.AssetManager, key: str) -> depthai.Asset

        Returns:
            Asset assigned to the specified key or a nullptr otherwise

        2. get(self: depthai.AssetManager, key: str) -> depthai.Asset

        Returns:
            Asset assigned to the specified key or a nullptr otherwise
        """
    @overload
    def get(self, key: str) -> Asset:
        """get(*args, **kwargs)
        Overloaded function.

        1. get(self: depthai.AssetManager, key: str) -> depthai.Asset

        Returns:
            Asset assigned to the specified key or a nullptr otherwise

        2. get(self: depthai.AssetManager, key: str) -> depthai.Asset

        Returns:
            Asset assigned to the specified key or a nullptr otherwise
        """
    @overload
    def getAll(self) -> list[Asset]:
        """getAll(*args, **kwargs)
        Overloaded function.

        1. getAll(self: depthai.AssetManager) -> list[depthai.Asset]

        Returns:
            All asset stored in the AssetManager

        2. getAll(self: depthai.AssetManager) -> list[depthai.Asset]

        Returns:
            All asset stored in the AssetManager
        """
    @overload
    def getAll(self) -> list[Asset]:
        """getAll(*args, **kwargs)
        Overloaded function.

        1. getAll(self: depthai.AssetManager) -> list[depthai.Asset]

        Returns:
            All asset stored in the AssetManager

        2. getAll(self: depthai.AssetManager) -> list[depthai.Asset]

        Returns:
            All asset stored in the AssetManager
        """
    def getRootPath(self) -> str:
        """getRootPath(self: depthai.AssetManager) -> str

        Get root path of the asset manager

        Returns:
            Root path
        """
    def remove(self, key: str) -> None:
        """remove(self: depthai.AssetManager, key: str) -> None

        Removes asset with key

        Parameter ``key``:
            Key of asset to remove
        """
    @overload
    def set(self, asset: Asset) -> Asset:
        """set(*args, **kwargs)
        Overloaded function.

        1. set(self: depthai.AssetManager, asset: depthai.Asset) -> depthai.Asset

        Adds or overwrites an asset object to AssetManager.

        Parameter ``asset``:
            Asset to add

        Returns:
            Shared pointer to asset

        2. set(self: depthai.AssetManager, key: str, asset: depthai.Asset) -> depthai.Asset

        Adds or overwrites an asset object to AssetManager with a specified key. Key
        value will be assigned to an Asset as well

        Parameter ``key``:
            Key under which the asset should be stored

        Parameter ``asset``:
            Asset to store

        Returns:
            Shared pointer to asset

        3. set(self: depthai.AssetManager, key: str, path: os.PathLike, alignment: int = 64) -> depthai.Asset

        Loads file into asset manager under specified key.

        Parameter ``key``:
            Key under which the asset should be stored

        Parameter ``path``:
            Path to file which to load as asset

        Parameter ``alignment``:
            [Optional] alignment of asset data in asset storage. Default is 64B

        4. set(self: depthai.AssetManager, key: str, data: std::vector<unsigned char, std::allocator<unsigned char> >, alignment: int = 64) -> depthai.Asset

        Loads file into asset manager under specified key.

        Parameter ``key``:
            Key under which the asset should be stored

        Parameter ``data``:
            Asset data

        Parameter ``alignment``:
            [Optional] alignment of asset data in asset storage. Default is 64B

        Returns:
            Shared pointer to asset
        """
    @overload
    def set(self, key: str, asset: Asset) -> Asset:
        """set(*args, **kwargs)
        Overloaded function.

        1. set(self: depthai.AssetManager, asset: depthai.Asset) -> depthai.Asset

        Adds or overwrites an asset object to AssetManager.

        Parameter ``asset``:
            Asset to add

        Returns:
            Shared pointer to asset

        2. set(self: depthai.AssetManager, key: str, asset: depthai.Asset) -> depthai.Asset

        Adds or overwrites an asset object to AssetManager with a specified key. Key
        value will be assigned to an Asset as well

        Parameter ``key``:
            Key under which the asset should be stored

        Parameter ``asset``:
            Asset to store

        Returns:
            Shared pointer to asset

        3. set(self: depthai.AssetManager, key: str, path: os.PathLike, alignment: int = 64) -> depthai.Asset

        Loads file into asset manager under specified key.

        Parameter ``key``:
            Key under which the asset should be stored

        Parameter ``path``:
            Path to file which to load as asset

        Parameter ``alignment``:
            [Optional] alignment of asset data in asset storage. Default is 64B

        4. set(self: depthai.AssetManager, key: str, data: std::vector<unsigned char, std::allocator<unsigned char> >, alignment: int = 64) -> depthai.Asset

        Loads file into asset manager under specified key.

        Parameter ``key``:
            Key under which the asset should be stored

        Parameter ``data``:
            Asset data

        Parameter ``alignment``:
            [Optional] alignment of asset data in asset storage. Default is 64B

        Returns:
            Shared pointer to asset
        """
    @overload
    def set(self, key: str, path: os.PathLike, alignment: int = ...) -> Asset:
        """set(*args, **kwargs)
        Overloaded function.

        1. set(self: depthai.AssetManager, asset: depthai.Asset) -> depthai.Asset

        Adds or overwrites an asset object to AssetManager.

        Parameter ``asset``:
            Asset to add

        Returns:
            Shared pointer to asset

        2. set(self: depthai.AssetManager, key: str, asset: depthai.Asset) -> depthai.Asset

        Adds or overwrites an asset object to AssetManager with a specified key. Key
        value will be assigned to an Asset as well

        Parameter ``key``:
            Key under which the asset should be stored

        Parameter ``asset``:
            Asset to store

        Returns:
            Shared pointer to asset

        3. set(self: depthai.AssetManager, key: str, path: os.PathLike, alignment: int = 64) -> depthai.Asset

        Loads file into asset manager under specified key.

        Parameter ``key``:
            Key under which the asset should be stored

        Parameter ``path``:
            Path to file which to load as asset

        Parameter ``alignment``:
            [Optional] alignment of asset data in asset storage. Default is 64B

        4. set(self: depthai.AssetManager, key: str, data: std::vector<unsigned char, std::allocator<unsigned char> >, alignment: int = 64) -> depthai.Asset

        Loads file into asset manager under specified key.

        Parameter ``key``:
            Key under which the asset should be stored

        Parameter ``data``:
            Asset data

        Parameter ``alignment``:
            [Optional] alignment of asset data in asset storage. Default is 64B

        Returns:
            Shared pointer to asset
        """
    @overload
    def set(self, key: str, data, std, alignment: int = ...) -> Asset:
        """set(*args, **kwargs)
        Overloaded function.

        1. set(self: depthai.AssetManager, asset: depthai.Asset) -> depthai.Asset

        Adds or overwrites an asset object to AssetManager.

        Parameter ``asset``:
            Asset to add

        Returns:
            Shared pointer to asset

        2. set(self: depthai.AssetManager, key: str, asset: depthai.Asset) -> depthai.Asset

        Adds or overwrites an asset object to AssetManager with a specified key. Key
        value will be assigned to an Asset as well

        Parameter ``key``:
            Key under which the asset should be stored

        Parameter ``asset``:
            Asset to store

        Returns:
            Shared pointer to asset

        3. set(self: depthai.AssetManager, key: str, path: os.PathLike, alignment: int = 64) -> depthai.Asset

        Loads file into asset manager under specified key.

        Parameter ``key``:
            Key under which the asset should be stored

        Parameter ``path``:
            Path to file which to load as asset

        Parameter ``alignment``:
            [Optional] alignment of asset data in asset storage. Default is 64B

        4. set(self: depthai.AssetManager, key: str, data: std::vector<unsigned char, std::allocator<unsigned char> >, alignment: int = 64) -> depthai.Asset

        Loads file into asset manager under specified key.

        Parameter ``key``:
            Key under which the asset should be stored

        Parameter ``data``:
            Asset data

        Parameter ``alignment``:
            [Optional] alignment of asset data in asset storage. Default is 64B

        Returns:
            Shared pointer to asset
        """
    def size(self) -> int:
        """size(self: depthai.AssetManager) -> int

        Returns:
            Number of asset stored in the AssetManager
        """

class AutoCalibrationConfig(Buffer):
    """AutoCalibrationConfig message. Carries configuration for the automatic camera
    calibration algorithm. Defines parameters for periodic or one-time recalibration
    of the camera sensors."""

    class Mode:
        """Defines when the auto-calibration process should be triggered.

        Members:

          ON_START

          CONTINUOUS"""
        __members__: ClassVar[dict] = ...  # read-only
        CONTINUOUS: ClassVar[AutoCalibrationConfig.Mode] = ...
        ON_START: ClassVar[AutoCalibrationConfig.Mode] = ...
        __entries: ClassVar[dict] = ...
        def __init__(self, value: int) -> None:
            """__init__(self: depthai.AutoCalibrationConfig.Mode, value: int) -> None"""
        def __eq__(self, other: object) -> bool:
            """__eq__(self: object, other: object) -> bool"""
        def __hash__(self) -> int:
            """__hash__(self: object) -> int"""
        def __index__(self) -> int:
            """__index__(self: depthai.AutoCalibrationConfig.Mode) -> int"""
        def __int__(self) -> int:
            """__int__(self: depthai.AutoCalibrationConfig.Mode) -> int"""
        def __ne__(self, other: object) -> bool:
            """__ne__(self: object, other: object) -> bool"""
        @property
        def name(self) -> str:
            """name(self: object) -> str

            name(self: object) -> str
            """
        @property
        def value(self) -> int:
            """(arg0: depthai.AutoCalibrationConfig.Mode) -> int"""
    CONTINUOUS: ClassVar[AutoCalibrationConfig.Mode] = ...
    ON_START: ClassVar[AutoCalibrationConfig.Mode] = ...
    calibrationConfidenceThreshold: float
    dataConfidenceThreshold: float
    flashCalibration: bool
    maxImagesPerRecalibration: int
    maxIterations: int
    mode: AutoCalibrationConfig.Mode
    sleepingTime: int
    validationSetSize: int
    @overload
    def __init__(self) -> None:
        """__init__(*args, **kwargs)
        Overloaded function.

        1. __init__(self: depthai.AutoCalibrationConfig) -> None

        2. __init__(self: depthai.AutoCalibrationConfig, mode: depthai.AutoCalibrationConfig.Mode, sleepingTime: int, calibrationConfidenceThreshold: float, dataConfidenceThreshold: float, maxIterations: int, maxImagesPerRecalibration: int, validationSetSize: int, flashCalibration: bool) -> None
        """
    @overload
    def __init__(self, mode: AutoCalibrationConfig.Mode, sleepingTime: int, calibrationConfidenceThreshold: float, dataConfidenceThreshold: float, maxIterations: int, maxImagesPerRecalibration: int, validationSetSize: int, flashCalibration: bool) -> None:
        """__init__(*args, **kwargs)
        Overloaded function.

        1. __init__(self: depthai.AutoCalibrationConfig) -> None

        2. __init__(self: depthai.AutoCalibrationConfig, mode: depthai.AutoCalibrationConfig.Mode, sleepingTime: int, calibrationConfidenceThreshold: float, dataConfidenceThreshold: float, maxIterations: int, maxImagesPerRecalibration: int, validationSetSize: int, flashCalibration: bool) -> None
        """

class AutoCalibrationProperties:
    def __init__(self, *args, **kwargs) -> None:
        """Initialize self.  See help(type(self)) for accurate signature."""

class AutoCalibrationResult(Buffer):
    calibration: Incomplete
    calibrationConfidence: float
    dataConfidence: float
    passed: bool
    @overload
    def __init__(self) -> None:
        """__init__(*args, **kwargs)
        Overloaded function.

        1. __init__(self: depthai.AutoCalibrationResult) -> None

        2. __init__(self: depthai.AutoCalibrationResult, dataConfidence: float, calibrationConfidence: float, passed: bool, calibration: dai::CalibrationHandler) -> None
        """
    @overload
    def __init__(self, dataConfidence: float, calibrationConfidence: float, passed: bool, calibration) -> None:
        """__init__(*args, **kwargs)
        Overloaded function.

        1. __init__(self: depthai.AutoCalibrationResult) -> None

        2. __init__(self: depthai.AutoCalibrationResult, dataConfidence: float, calibrationConfidence: float, passed: bool, calibration: dai::CalibrationHandler) -> None
        """

class BenchmarkReport(Buffer):
    """BenchmarkReport message."""
    def __init__(self) -> None:
        """__init__(self: depthai.BenchmarkReport) -> None"""
    @property
    def averageLatency(self) -> float:
        """(arg0: depthai.BenchmarkReport) -> float"""
    @property
    def fps(self) -> float:
        """(arg0: depthai.BenchmarkReport) -> float"""
    @property
    def latencies(self) -> list[float]:
        """(arg0: depthai.BenchmarkReport) -> list[float]"""
    @property
    def numMessagesReceived(self) -> float:
        """(arg0: depthai.BenchmarkReport) -> float"""
    @property
    def timeTotal(self) -> float:
        """(arg0: depthai.BenchmarkReport) -> float"""

class BlockPipelineEvent:
    def __init__(self, *args, **kwargs) -> None:
        """Initialize self.  See help(type(self)) for accurate signature."""
    def cancel(self) -> None:
        """cancel(self: depthai.BlockPipelineEvent) -> None"""
    def setEndTimestamp(self, timestamp: datetime.timedelta) -> None:
        """setEndTimestamp(self: depthai.BlockPipelineEvent, timestamp: datetime.timedelta) -> None"""
    def setQueueSize(self, size: int) -> None:
        """setQueueSize(self: depthai.BlockPipelineEvent, size: int) -> None"""
    def __enter__(self) -> BlockPipelineEvent:
        """__enter__(self: depthai.BlockPipelineEvent) -> depthai.BlockPipelineEvent"""
    def __exit__(self, arg0: object, arg1: object, arg2: object) -> None:
        """__exit__(self: depthai.BlockPipelineEvent, arg0: object, arg1: object, arg2: object) -> None"""

class BoardConfig:
    class GPIO:
        """GPIO config"""

        class Direction:
            """

            Members:

              INPUT : 

              OUTPUT : """
            __members__: ClassVar[dict] = ...  # read-only
            INPUT: ClassVar[BoardConfig.GPIO.Direction] = ...
            OUTPUT: ClassVar[BoardConfig.GPIO.Direction] = ...
            __entries: ClassVar[dict] = ...
            def __init__(self, value: int) -> None:
                """__init__(self: depthai.BoardConfig.GPIO.Direction, value: int) -> None"""
            def __eq__(self, other: object) -> bool:
                """__eq__(self: object, other: object) -> bool"""
            def __hash__(self) -> int:
                """__hash__(self: object) -> int"""
            def __index__(self) -> int:
                """__index__(self: depthai.BoardConfig.GPIO.Direction) -> int"""
            def __int__(self) -> int:
                """__int__(self: depthai.BoardConfig.GPIO.Direction) -> int"""
            def __ne__(self, other: object) -> bool:
                """__ne__(self: object, other: object) -> bool"""
            @property
            def name(self) -> str:
                """name(self: object) -> str

                name(self: object) -> str
                """
            @property
            def value(self) -> int:
                """(arg0: depthai.BoardConfig.GPIO.Direction) -> int"""

        class Drive:
            """Drive strength in mA (2, 4, 8 and 12mA)

            Members:

              MA_2 : 

              MA_4 : 

              MA_8 : 

              MA_12 : """
            __members__: ClassVar[dict] = ...  # read-only
            MA_12: ClassVar[BoardConfig.GPIO.Drive] = ...
            MA_2: ClassVar[BoardConfig.GPIO.Drive] = ...
            MA_4: ClassVar[BoardConfig.GPIO.Drive] = ...
            MA_8: ClassVar[BoardConfig.GPIO.Drive] = ...
            __entries: ClassVar[dict] = ...
            def __init__(self, value: int) -> None:
                """__init__(self: depthai.BoardConfig.GPIO.Drive, value: int) -> None"""
            def __eq__(self, other: object) -> bool:
                """__eq__(self: object, other: object) -> bool"""
            def __hash__(self) -> int:
                """__hash__(self: object) -> int"""
            def __index__(self) -> int:
                """__index__(self: depthai.BoardConfig.GPIO.Drive) -> int"""
            def __int__(self) -> int:
                """__int__(self: depthai.BoardConfig.GPIO.Drive) -> int"""
            def __ne__(self, other: object) -> bool:
                """__ne__(self: object, other: object) -> bool"""
            @property
            def name(self) -> str:
                """name(self: object) -> str

                name(self: object) -> str
                """
            @property
            def value(self) -> int:
                """(arg0: depthai.BoardConfig.GPIO.Drive) -> int"""

        class Level:
            """

            Members:

              LOW : 

              HIGH : """
            __members__: ClassVar[dict] = ...  # read-only
            HIGH: ClassVar[BoardConfig.GPIO.Level] = ...
            LOW: ClassVar[BoardConfig.GPIO.Level] = ...
            __entries: ClassVar[dict] = ...
            def __init__(self, value: int) -> None:
                """__init__(self: depthai.BoardConfig.GPIO.Level, value: int) -> None"""
            def __eq__(self, other: object) -> bool:
                """__eq__(self: object, other: object) -> bool"""
            def __hash__(self) -> int:
                """__hash__(self: object) -> int"""
            def __index__(self) -> int:
                """__index__(self: depthai.BoardConfig.GPIO.Level) -> int"""
            def __int__(self) -> int:
                """__int__(self: depthai.BoardConfig.GPIO.Level) -> int"""
            def __ne__(self, other: object) -> bool:
                """__ne__(self: object, other: object) -> bool"""
            @property
            def name(self) -> str:
                """name(self: object) -> str

                name(self: object) -> str
                """
            @property
            def value(self) -> int:
                """(arg0: depthai.BoardConfig.GPIO.Level) -> int"""

        class Mode:
            """

            Members:

              ALT_MODE_0 : 

              ALT_MODE_1 : 

              ALT_MODE_2 : 

              ALT_MODE_3 : 

              ALT_MODE_4 : 

              ALT_MODE_5 : 

              ALT_MODE_6 : 

              DIRECT : """
            __members__: ClassVar[dict] = ...  # read-only
            ALT_MODE_0: ClassVar[BoardConfig.GPIO.Mode] = ...
            ALT_MODE_1: ClassVar[BoardConfig.GPIO.Mode] = ...
            ALT_MODE_2: ClassVar[BoardConfig.GPIO.Mode] = ...
            ALT_MODE_3: ClassVar[BoardConfig.GPIO.Mode] = ...
            ALT_MODE_4: ClassVar[BoardConfig.GPIO.Mode] = ...
            ALT_MODE_5: ClassVar[BoardConfig.GPIO.Mode] = ...
            ALT_MODE_6: ClassVar[BoardConfig.GPIO.Mode] = ...
            DIRECT: ClassVar[BoardConfig.GPIO.Mode] = ...
            __entries: ClassVar[dict] = ...
            def __init__(self, value: int) -> None:
                """__init__(self: depthai.BoardConfig.GPIO.Mode, value: int) -> None"""
            def __eq__(self, other: object) -> bool:
                """__eq__(self: object, other: object) -> bool"""
            def __hash__(self) -> int:
                """__hash__(self: object) -> int"""
            def __index__(self) -> int:
                """__index__(self: depthai.BoardConfig.GPIO.Mode) -> int"""
            def __int__(self) -> int:
                """__int__(self: depthai.BoardConfig.GPIO.Mode) -> int"""
            def __ne__(self, other: object) -> bool:
                """__ne__(self: object, other: object) -> bool"""
            @property
            def name(self) -> str:
                """name(self: object) -> str

                name(self: object) -> str
                """
            @property
            def value(self) -> int:
                """(arg0: depthai.BoardConfig.GPIO.Mode) -> int"""

        class Pull:
            """

            Members:

              NO_PULL : 

              PULL_UP : 

              PULL_DOWN : 

              BUS_KEEPER : """
            __members__: ClassVar[dict] = ...  # read-only
            BUS_KEEPER: ClassVar[BoardConfig.GPIO.Pull] = ...
            NO_PULL: ClassVar[BoardConfig.GPIO.Pull] = ...
            PULL_DOWN: ClassVar[BoardConfig.GPIO.Pull] = ...
            PULL_UP: ClassVar[BoardConfig.GPIO.Pull] = ...
            __entries: ClassVar[dict] = ...
            def __init__(self, value: int) -> None:
                """__init__(self: depthai.BoardConfig.GPIO.Pull, value: int) -> None"""
            def __eq__(self, other: object) -> bool:
                """__eq__(self: object, other: object) -> bool"""
            def __hash__(self) -> int:
                """__hash__(self: object) -> int"""
            def __index__(self) -> int:
                """__index__(self: depthai.BoardConfig.GPIO.Pull) -> int"""
            def __int__(self) -> int:
                """__int__(self: depthai.BoardConfig.GPIO.Pull) -> int"""
            def __ne__(self, other: object) -> bool:
                """__ne__(self: object, other: object) -> bool"""
            @property
            def name(self) -> str:
                """name(self: object) -> str

                name(self: object) -> str
                """
            @property
            def value(self) -> int:
                """(arg0: depthai.BoardConfig.GPIO.Pull) -> int"""
        ALT_MODE_0: ClassVar[BoardConfig.GPIO.Mode] = ...
        ALT_MODE_1: ClassVar[BoardConfig.GPIO.Mode] = ...
        ALT_MODE_2: ClassVar[BoardConfig.GPIO.Mode] = ...
        ALT_MODE_3: ClassVar[BoardConfig.GPIO.Mode] = ...
        ALT_MODE_4: ClassVar[BoardConfig.GPIO.Mode] = ...
        ALT_MODE_5: ClassVar[BoardConfig.GPIO.Mode] = ...
        ALT_MODE_6: ClassVar[BoardConfig.GPIO.Mode] = ...
        BUS_KEEPER: ClassVar[BoardConfig.GPIO.Pull] = ...
        DIRECT: ClassVar[BoardConfig.GPIO.Mode] = ...
        HIGH: ClassVar[BoardConfig.GPIO.Level] = ...
        INPUT: ClassVar[BoardConfig.GPIO.Direction] = ...
        LOW: ClassVar[BoardConfig.GPIO.Level] = ...
        MA_12: ClassVar[BoardConfig.GPIO.Drive] = ...
        MA_2: ClassVar[BoardConfig.GPIO.Drive] = ...
        MA_4: ClassVar[BoardConfig.GPIO.Drive] = ...
        MA_8: ClassVar[BoardConfig.GPIO.Drive] = ...
        NO_PULL: ClassVar[BoardConfig.GPIO.Pull] = ...
        OUTPUT: ClassVar[BoardConfig.GPIO.Direction] = ...
        PULL_DOWN: ClassVar[BoardConfig.GPIO.Pull] = ...
        PULL_UP: ClassVar[BoardConfig.GPIO.Pull] = ...
        direction: BoardConfig.GPIO.Direction
        drive: BoardConfig.GPIO.Drive
        level: BoardConfig.GPIO.Level
        mode: BoardConfig.GPIO.Mode
        pull: BoardConfig.GPIO.Pull
        schmitt: bool
        slewFast: bool
        @overload
        def __init__(self) -> None:
            """__init__(*args, **kwargs)
            Overloaded function.

            1. __init__(self: depthai.BoardConfig.GPIO) -> None

            2. __init__(self: depthai.BoardConfig.GPIO, arg0: depthai.BoardConfig.GPIO.Direction) -> None

            3. __init__(self: depthai.BoardConfig.GPIO, arg0: depthai.BoardConfig.GPIO.Direction, arg1: depthai.BoardConfig.GPIO.Level) -> None

            4. __init__(self: depthai.BoardConfig.GPIO, arg0: depthai.BoardConfig.GPIO.Direction, arg1: depthai.BoardConfig.GPIO.Level, arg2: depthai.BoardConfig.GPIO.Pull) -> None

            5. __init__(self: depthai.BoardConfig.GPIO, arg0: depthai.BoardConfig.GPIO.Direction, arg1: depthai.BoardConfig.GPIO.Mode) -> None

            6. __init__(self: depthai.BoardConfig.GPIO, arg0: depthai.BoardConfig.GPIO.Direction, arg1: depthai.BoardConfig.GPIO.Mode, arg2: depthai.BoardConfig.GPIO.Pull) -> None
            """
        @overload
        def __init__(self, arg0: BoardConfig.GPIO.Direction) -> None:
            """__init__(*args, **kwargs)
            Overloaded function.

            1. __init__(self: depthai.BoardConfig.GPIO) -> None

            2. __init__(self: depthai.BoardConfig.GPIO, arg0: depthai.BoardConfig.GPIO.Direction) -> None

            3. __init__(self: depthai.BoardConfig.GPIO, arg0: depthai.BoardConfig.GPIO.Direction, arg1: depthai.BoardConfig.GPIO.Level) -> None

            4. __init__(self: depthai.BoardConfig.GPIO, arg0: depthai.BoardConfig.GPIO.Direction, arg1: depthai.BoardConfig.GPIO.Level, arg2: depthai.BoardConfig.GPIO.Pull) -> None

            5. __init__(self: depthai.BoardConfig.GPIO, arg0: depthai.BoardConfig.GPIO.Direction, arg1: depthai.BoardConfig.GPIO.Mode) -> None

            6. __init__(self: depthai.BoardConfig.GPIO, arg0: depthai.BoardConfig.GPIO.Direction, arg1: depthai.BoardConfig.GPIO.Mode, arg2: depthai.BoardConfig.GPIO.Pull) -> None
            """
        @overload
        def __init__(self, arg0: BoardConfig.GPIO.Direction, arg1: BoardConfig.GPIO.Level) -> None:
            """__init__(*args, **kwargs)
            Overloaded function.

            1. __init__(self: depthai.BoardConfig.GPIO) -> None

            2. __init__(self: depthai.BoardConfig.GPIO, arg0: depthai.BoardConfig.GPIO.Direction) -> None

            3. __init__(self: depthai.BoardConfig.GPIO, arg0: depthai.BoardConfig.GPIO.Direction, arg1: depthai.BoardConfig.GPIO.Level) -> None

            4. __init__(self: depthai.BoardConfig.GPIO, arg0: depthai.BoardConfig.GPIO.Direction, arg1: depthai.BoardConfig.GPIO.Level, arg2: depthai.BoardConfig.GPIO.Pull) -> None

            5. __init__(self: depthai.BoardConfig.GPIO, arg0: depthai.BoardConfig.GPIO.Direction, arg1: depthai.BoardConfig.GPIO.Mode) -> None

            6. __init__(self: depthai.BoardConfig.GPIO, arg0: depthai.BoardConfig.GPIO.Direction, arg1: depthai.BoardConfig.GPIO.Mode, arg2: depthai.BoardConfig.GPIO.Pull) -> None
            """
        @overload
        def __init__(self, arg0: BoardConfig.GPIO.Direction, arg1: BoardConfig.GPIO.Level, arg2: BoardConfig.GPIO.Pull) -> None:
            """__init__(*args, **kwargs)
            Overloaded function.

            1. __init__(self: depthai.BoardConfig.GPIO) -> None

            2. __init__(self: depthai.BoardConfig.GPIO, arg0: depthai.BoardConfig.GPIO.Direction) -> None

            3. __init__(self: depthai.BoardConfig.GPIO, arg0: depthai.BoardConfig.GPIO.Direction, arg1: depthai.BoardConfig.GPIO.Level) -> None

            4. __init__(self: depthai.BoardConfig.GPIO, arg0: depthai.BoardConfig.GPIO.Direction, arg1: depthai.BoardConfig.GPIO.Level, arg2: depthai.BoardConfig.GPIO.Pull) -> None

            5. __init__(self: depthai.BoardConfig.GPIO, arg0: depthai.BoardConfig.GPIO.Direction, arg1: depthai.BoardConfig.GPIO.Mode) -> None

            6. __init__(self: depthai.BoardConfig.GPIO, arg0: depthai.BoardConfig.GPIO.Direction, arg1: depthai.BoardConfig.GPIO.Mode, arg2: depthai.BoardConfig.GPIO.Pull) -> None
            """
        @overload
        def __init__(self, arg0: BoardConfig.GPIO.Direction, arg1: BoardConfig.GPIO.Mode) -> None:
            """__init__(*args, **kwargs)
            Overloaded function.

            1. __init__(self: depthai.BoardConfig.GPIO) -> None

            2. __init__(self: depthai.BoardConfig.GPIO, arg0: depthai.BoardConfig.GPIO.Direction) -> None

            3. __init__(self: depthai.BoardConfig.GPIO, arg0: depthai.BoardConfig.GPIO.Direction, arg1: depthai.BoardConfig.GPIO.Level) -> None

            4. __init__(self: depthai.BoardConfig.GPIO, arg0: depthai.BoardConfig.GPIO.Direction, arg1: depthai.BoardConfig.GPIO.Level, arg2: depthai.BoardConfig.GPIO.Pull) -> None

            5. __init__(self: depthai.BoardConfig.GPIO, arg0: depthai.BoardConfig.GPIO.Direction, arg1: depthai.BoardConfig.GPIO.Mode) -> None

            6. __init__(self: depthai.BoardConfig.GPIO, arg0: depthai.BoardConfig.GPIO.Direction, arg1: depthai.BoardConfig.GPIO.Mode, arg2: depthai.BoardConfig.GPIO.Pull) -> None
            """
        @overload
        def __init__(self, arg0: BoardConfig.GPIO.Direction, arg1: BoardConfig.GPIO.Mode, arg2: BoardConfig.GPIO.Pull) -> None:
            """__init__(*args, **kwargs)
            Overloaded function.

            1. __init__(self: depthai.BoardConfig.GPIO) -> None

            2. __init__(self: depthai.BoardConfig.GPIO, arg0: depthai.BoardConfig.GPIO.Direction) -> None

            3. __init__(self: depthai.BoardConfig.GPIO, arg0: depthai.BoardConfig.GPIO.Direction, arg1: depthai.BoardConfig.GPIO.Level) -> None

            4. __init__(self: depthai.BoardConfig.GPIO, arg0: depthai.BoardConfig.GPIO.Direction, arg1: depthai.BoardConfig.GPIO.Level, arg2: depthai.BoardConfig.GPIO.Pull) -> None

            5. __init__(self: depthai.BoardConfig.GPIO, arg0: depthai.BoardConfig.GPIO.Direction, arg1: depthai.BoardConfig.GPIO.Mode) -> None

            6. __init__(self: depthai.BoardConfig.GPIO, arg0: depthai.BoardConfig.GPIO.Direction, arg1: depthai.BoardConfig.GPIO.Mode, arg2: depthai.BoardConfig.GPIO.Pull) -> None
            """

    class GPIOMap:
        def __init__(self) -> None:
            """__init__(self: depthai.BoardConfig.GPIOMap) -> None"""
        def items(self) -> BoardConfig.ItemsView:
            """items(self: depthai.BoardConfig.GPIOMap) -> depthai.BoardConfig.ItemsView"""
        def keys(self) -> BoardConfig.KeysView:
            """keys(self: depthai.BoardConfig.GPIOMap) -> depthai.BoardConfig.KeysView"""
        def values(self) -> BoardConfig.ValuesView:
            """values(self: depthai.BoardConfig.GPIOMap) -> depthai.BoardConfig.ValuesView"""
        def __bool__(self) -> bool:
            """__bool__(self: depthai.BoardConfig.GPIOMap) -> bool

            Check whether the map is nonempty
            """
        @overload
        def __contains__(self, arg0: int) -> bool:
            """__contains__(*args, **kwargs)
            Overloaded function.

            1. __contains__(self: depthai.BoardConfig.GPIOMap, arg0: int) -> bool

            2. __contains__(self: depthai.BoardConfig.GPIOMap, arg0: object) -> bool
            """
        @overload
        def __contains__(self, arg0: object) -> bool:
            """__contains__(*args, **kwargs)
            Overloaded function.

            1. __contains__(self: depthai.BoardConfig.GPIOMap, arg0: int) -> bool

            2. __contains__(self: depthai.BoardConfig.GPIOMap, arg0: object) -> bool
            """
        def __delitem__(self, arg0: int) -> None:
            """__delitem__(self: depthai.BoardConfig.GPIOMap, arg0: int) -> None"""
        def __getitem__(self, arg0: int) -> BoardConfig.GPIO:
            """__getitem__(self: depthai.BoardConfig.GPIOMap, arg0: int) -> depthai.BoardConfig.GPIO"""
        def __iter__(self) -> Iterator[int]:
            """__iter__(self: depthai.BoardConfig.GPIOMap) -> Iterator[int]"""
        def __len__(self) -> int:
            """__len__(self: depthai.BoardConfig.GPIOMap) -> int"""
        def __setitem__(self, arg0: int, arg1: BoardConfig.GPIO) -> None:
            """__setitem__(self: depthai.BoardConfig.GPIOMap, arg0: int, arg1: depthai.BoardConfig.GPIO) -> None"""

    class ItemsView:
        def __init__(self, *args, **kwargs) -> None:
            """Initialize self.  See help(type(self)) for accurate signature."""
        def __iter__(self) -> Iterator:
            """__iter__(self: depthai.BoardConfig.ItemsView) -> Iterator"""
        def __len__(self) -> int:
            """__len__(self: depthai.BoardConfig.ItemsView) -> int"""

    class KeysView:
        def __init__(self, *args, **kwargs) -> None:
            """Initialize self.  See help(type(self)) for accurate signature."""
        def __contains__(self, arg0: object) -> bool:
            """__contains__(self: depthai.BoardConfig.KeysView, arg0: object) -> bool"""
        def __iter__(self) -> Iterator:
            """__iter__(self: depthai.BoardConfig.KeysView) -> Iterator"""
        def __len__(self) -> int:
            """__len__(self: depthai.BoardConfig.KeysView) -> int"""

    class Network:
        """Network configuration"""
        mtu: int
        xlinkTcpNoDelay: bool
        def __init__(self) -> None:
            """__init__(self: depthai.BoardConfig.Network) -> None"""

    class UART:
        """UART instance config"""
        tmp: int
        def __init__(self) -> None:
            """__init__(self: depthai.BoardConfig.UART) -> None"""

    class UARTMap:
        def __init__(self) -> None:
            """__init__(self: depthai.BoardConfig.UARTMap) -> None"""
        def items(self) -> BoardConfig.ItemsView:
            """items(self: depthai.BoardConfig.UARTMap) -> depthai.BoardConfig.ItemsView"""
        def keys(self) -> BoardConfig.KeysView:
            """keys(self: depthai.BoardConfig.UARTMap) -> depthai.BoardConfig.KeysView"""
        def values(self) -> BoardConfig.ValuesView:
            """values(self: depthai.BoardConfig.UARTMap) -> depthai.BoardConfig.ValuesView"""
        def __bool__(self) -> bool:
            """__bool__(self: depthai.BoardConfig.UARTMap) -> bool

            Check whether the map is nonempty
            """
        @overload
        def __contains__(self, arg0: int) -> bool:
            """__contains__(*args, **kwargs)
            Overloaded function.

            1. __contains__(self: depthai.BoardConfig.UARTMap, arg0: int) -> bool

            2. __contains__(self: depthai.BoardConfig.UARTMap, arg0: object) -> bool
            """
        @overload
        def __contains__(self, arg0: object) -> bool:
            """__contains__(*args, **kwargs)
            Overloaded function.

            1. __contains__(self: depthai.BoardConfig.UARTMap, arg0: int) -> bool

            2. __contains__(self: depthai.BoardConfig.UARTMap, arg0: object) -> bool
            """
        def __delitem__(self, arg0: int) -> None:
            """__delitem__(self: depthai.BoardConfig.UARTMap, arg0: int) -> None"""
        def __getitem__(self, arg0: int) -> BoardConfig.UART:
            """__getitem__(self: depthai.BoardConfig.UARTMap, arg0: int) -> depthai.BoardConfig.UART"""
        def __iter__(self) -> Iterator[int]:
            """__iter__(self: depthai.BoardConfig.UARTMap) -> Iterator[int]"""
        def __len__(self) -> int:
            """__len__(self: depthai.BoardConfig.UARTMap) -> int"""
        def __setitem__(self, arg0: int, arg1: BoardConfig.UART) -> None:
            """__setitem__(self: depthai.BoardConfig.UARTMap, arg0: int, arg1: depthai.BoardConfig.UART) -> None"""

    class USB:
        """USB related config"""
        flashBootedPid: int
        flashBootedVid: int
        manufacturer: str
        maxSpeed: UsbSpeed
        pid: int
        productName: str
        vid: int
        def __init__(self) -> None:
            """__init__(self: depthai.BoardConfig.USB) -> None"""

    class UVC:
        """UVC configuration for USB descriptor"""
        cameraName: str
        enable: bool
        frameType: ImgFrame.Type
        height: int
        width: int
        @overload
        def __init__(self) -> None:
            """__init__(*args, **kwargs)
            Overloaded function.

            1. __init__(self: depthai.BoardConfig.UVC) -> None

            2. __init__(self: depthai.BoardConfig.UVC, arg0: int, arg1: int) -> None
            """
        @overload
        def __init__(self, arg0: int, arg1: int) -> None:
            """__init__(*args, **kwargs)
            Overloaded function.

            1. __init__(self: depthai.BoardConfig.UVC) -> None

            2. __init__(self: depthai.BoardConfig.UVC, arg0: int, arg1: int) -> None
            """

    class ValuesView:
        def __init__(self, *args, **kwargs) -> None:
            """Initialize self.  See help(type(self)) for accurate signature."""
        def __iter__(self) -> Iterator:
            """__iter__(self: depthai.BoardConfig.ValuesView) -> Iterator"""
        def __len__(self) -> int:
            """__len__(self: depthai.BoardConfig.ValuesView) -> int"""
    emmc: bool | None
    gpio: BoardConfig.GPIOMap
    logDevicePrints: bool | None
    logPath: str | None
    logSizeMax: int | None
    logVerbosity: LogLevel | None
    mipi4LaneRgb: bool | None
    network: BoardConfig.Network
    pcieInternalClock: bool | None
    sysctl: list[str]
    uart: BoardConfig.UARTMap
    usb: BoardConfig.USB
    usb3PhyInternalClock: bool | None
    uvc: BoardConfig.UVC | None
    watchdogInitialDelayMs: int | None
    watchdogTimeoutMs: int | None
    def __init__(self) -> None:
        """__init__(self: depthai.BoardConfig) -> None"""

class Buffer(ADatatype):
    """Base message - buffer of binary data"""
    @overload
    def __init__(self) -> None:
        """__init__(*args, **kwargs)
        Overloaded function.

        1. __init__(self: depthai.Buffer) -> None

        2. __init__(self: depthai.Buffer, arg0: int) -> None
        """
    @overload
    def __init__(self, arg0: int) -> None:
        """__init__(*args, **kwargs)
        Overloaded function.

        1. __init__(self: depthai.Buffer) -> None

        2. __init__(self: depthai.Buffer, arg0: int) -> None
        """
    def getData(self) -> numpy.ndarray[numpy.uint8]:
        """getData(self: object) -> numpy.ndarray[numpy.uint8]

        Get non-owning reference to internal buffer

        Returns:
            Reference to internal buffer
        """
    def getSequenceNum(self) -> int:
        """getSequenceNum(self: depthai.Buffer) -> int

        Retrieves image sequence number
        """
    def getTimestamp(self) -> datetime.timedelta:
        """getTimestamp(self: depthai.Buffer) -> datetime.timedelta

        Retrieves timestamp related to dai::Clock::now()
        """
    def getTimestampDevice(self) -> datetime.timedelta:
        """getTimestampDevice(self: depthai.Buffer) -> datetime.timedelta

        Retrieves timestamp directly captured from device's monotonic clock, not
        synchronized to host time. Used mostly for debugging
        """
    def getVisualizationMessage(self) -> ImgAnnotations | ImgFrame | None:
        """getVisualizationMessage(self: depthai.Buffer) -> Union[depthai.ImgAnnotations, depthai.ImgFrame, None]

        Get visualizable message

        Returns:
            Visualizable message, either ImgFrame, ImgAnnotations or std::monostate
            (None)
        """
    @overload
    def setData(self, arg0, std) -> None:
        """setData(*args, **kwargs)
        Overloaded function.

        1. setData(self: depthai.Buffer, arg0: std::vector<unsigned char, std::allocator<unsigned char> >) -> None

        Parameter ``data``:
            Copies data to internal buffer

        2. setData(self: depthai.Buffer, arg0: numpy.ndarray[numpy.uint8]) -> None

        Parameter ``data``:
            Copies data to internal buffer

        3. setData(self: depthai.Buffer, arg0: Buffer) -> None

        Parameter ``data``:
            Copies data to internal buffer
        """
    @overload
    def setData(self, arg0: numpy.ndarray[numpy.uint8]) -> None:
        """setData(*args, **kwargs)
        Overloaded function.

        1. setData(self: depthai.Buffer, arg0: std::vector<unsigned char, std::allocator<unsigned char> >) -> None

        Parameter ``data``:
            Copies data to internal buffer

        2. setData(self: depthai.Buffer, arg0: numpy.ndarray[numpy.uint8]) -> None

        Parameter ``data``:
            Copies data to internal buffer

        3. setData(self: depthai.Buffer, arg0: Buffer) -> None

        Parameter ``data``:
            Copies data to internal buffer
        """
    @overload
    def setData(self, arg0: Buffer) -> None:
        """setData(*args, **kwargs)
        Overloaded function.

        1. setData(self: depthai.Buffer, arg0: std::vector<unsigned char, std::allocator<unsigned char> >) -> None

        Parameter ``data``:
            Copies data to internal buffer

        2. setData(self: depthai.Buffer, arg0: numpy.ndarray[numpy.uint8]) -> None

        Parameter ``data``:
            Copies data to internal buffer

        3. setData(self: depthai.Buffer, arg0: Buffer) -> None

        Parameter ``data``:
            Copies data to internal buffer
        """
    def setSequenceNum(self, sequenceNum: int) -> None:
        """setSequenceNum(self: depthai.Buffer, sequenceNum: int) -> None

        Sets image sequence number
        """
    def setTimestamp(self, timestamp: datetime.timedelta) -> None:
        """setTimestamp(self: depthai.Buffer, timestamp: datetime.timedelta) -> None

        Sets image timestamp related to dai::Clock::now()
        """
    def setTimestampDevice(self, timestampDevice: datetime.timedelta) -> None:
        """setTimestampDevice(self: depthai.Buffer, timestampDevice: datetime.timedelta) -> None

        Sets image timestamp related to dai::Clock::now()
        """

class CalibrationHandler:
    """CalibrationHandler is an interface to read/load/write structured calibration and
    device data. The following fields are protected and aren't allowed to be
    overridden by default: - boardName - boardRev - boardConf - hardwareConf -
    batchName - batchTime - boardOptions - productName"""
    @overload
    def __init__(self) -> None:
        """__init__(*args, **kwargs)
        Overloaded function.

        1. __init__(self: depthai.CalibrationHandler) -> None

        2. __init__(self: depthai.CalibrationHandler, eepromDataPath: os.PathLike, validateExtrinsics: bool = None) -> None

        Construct a new Calibration Handler object using the eeprom json file created
        from calibration procedure.

        Parameter ``eepromDataPath``:
            takes the full path to the json file containing the calibration and device
            info.

        Parameter ``validateCalibration``:
            Enable internal check for extrinsics cycling links or dangling references.

        3. __init__(self: depthai.CalibrationHandler, calibrationDataPath: os.PathLike, boardConfigPath: os.PathLike, validateExtrinsics: bool = None) -> None

        Construct a new Calibration Handler object using the board config json file and
        .calib binary files created using gen1 calibration.

        Parameter ``calibrationDataPath``:
            Full Path to the .calib binary file from the gen1 calibration. (Supports
            only Version 5)

        Parameter ``boardConfigPath``:
            Full Path to the board config json file containing device information.

        Parameter ``validateCalibration``:
            Enable internal check for extrinsics cycling links or dangling references.

        4. __init__(self: depthai.CalibrationHandler, eepromData: depthai.EepromData, validateExtrinsics: bool = None) -> None

        Construct a new Calibration Handler object from EepromData object.

        Parameter ``eepromData``:
            EepromData data structure containing the calibration data.

        Parameter ``validateCalibration``:
            Enable internal check for extrinsics cycling links or dangling references.
        """
    @overload
    def __init__(self, eepromDataPath: os.PathLike, validateExtrinsics: bool = ...) -> None:
        """__init__(*args, **kwargs)
        Overloaded function.

        1. __init__(self: depthai.CalibrationHandler) -> None

        2. __init__(self: depthai.CalibrationHandler, eepromDataPath: os.PathLike, validateExtrinsics: bool = None) -> None

        Construct a new Calibration Handler object using the eeprom json file created
        from calibration procedure.

        Parameter ``eepromDataPath``:
            takes the full path to the json file containing the calibration and device
            info.

        Parameter ``validateCalibration``:
            Enable internal check for extrinsics cycling links or dangling references.

        3. __init__(self: depthai.CalibrationHandler, calibrationDataPath: os.PathLike, boardConfigPath: os.PathLike, validateExtrinsics: bool = None) -> None

        Construct a new Calibration Handler object using the board config json file and
        .calib binary files created using gen1 calibration.

        Parameter ``calibrationDataPath``:
            Full Path to the .calib binary file from the gen1 calibration. (Supports
            only Version 5)

        Parameter ``boardConfigPath``:
            Full Path to the board config json file containing device information.

        Parameter ``validateCalibration``:
            Enable internal check for extrinsics cycling links or dangling references.

        4. __init__(self: depthai.CalibrationHandler, eepromData: depthai.EepromData, validateExtrinsics: bool = None) -> None

        Construct a new Calibration Handler object from EepromData object.

        Parameter ``eepromData``:
            EepromData data structure containing the calibration data.

        Parameter ``validateCalibration``:
            Enable internal check for extrinsics cycling links or dangling references.
        """
    @overload
    def __init__(self, calibrationDataPath: os.PathLike, boardConfigPath: os.PathLike, validateExtrinsics: bool = ...) -> None:
        """__init__(*args, **kwargs)
        Overloaded function.

        1. __init__(self: depthai.CalibrationHandler) -> None

        2. __init__(self: depthai.CalibrationHandler, eepromDataPath: os.PathLike, validateExtrinsics: bool = None) -> None

        Construct a new Calibration Handler object using the eeprom json file created
        from calibration procedure.

        Parameter ``eepromDataPath``:
            takes the full path to the json file containing the calibration and device
            info.

        Parameter ``validateCalibration``:
            Enable internal check for extrinsics cycling links or dangling references.

        3. __init__(self: depthai.CalibrationHandler, calibrationDataPath: os.PathLike, boardConfigPath: os.PathLike, validateExtrinsics: bool = None) -> None

        Construct a new Calibration Handler object using the board config json file and
        .calib binary files created using gen1 calibration.

        Parameter ``calibrationDataPath``:
            Full Path to the .calib binary file from the gen1 calibration. (Supports
            only Version 5)

        Parameter ``boardConfigPath``:
            Full Path to the board config json file containing device information.

        Parameter ``validateCalibration``:
            Enable internal check for extrinsics cycling links or dangling references.

        4. __init__(self: depthai.CalibrationHandler, eepromData: depthai.EepromData, validateExtrinsics: bool = None) -> None

        Construct a new Calibration Handler object from EepromData object.

        Parameter ``eepromData``:
            EepromData data structure containing the calibration data.

        Parameter ``validateCalibration``:
            Enable internal check for extrinsics cycling links or dangling references.
        """
    @overload
    def __init__(self, eepromData: EepromData, validateExtrinsics: bool = ...) -> None:
        """__init__(*args, **kwargs)
        Overloaded function.

        1. __init__(self: depthai.CalibrationHandler) -> None

        2. __init__(self: depthai.CalibrationHandler, eepromDataPath: os.PathLike, validateExtrinsics: bool = None) -> None

        Construct a new Calibration Handler object using the eeprom json file created
        from calibration procedure.

        Parameter ``eepromDataPath``:
            takes the full path to the json file containing the calibration and device
            info.

        Parameter ``validateCalibration``:
            Enable internal check for extrinsics cycling links or dangling references.

        3. __init__(self: depthai.CalibrationHandler, calibrationDataPath: os.PathLike, boardConfigPath: os.PathLike, validateExtrinsics: bool = None) -> None

        Construct a new Calibration Handler object using the board config json file and
        .calib binary files created using gen1 calibration.

        Parameter ``calibrationDataPath``:
            Full Path to the .calib binary file from the gen1 calibration. (Supports
            only Version 5)

        Parameter ``boardConfigPath``:
            Full Path to the board config json file containing device information.

        Parameter ``validateCalibration``:
            Enable internal check for extrinsics cycling links or dangling references.

        4. __init__(self: depthai.CalibrationHandler, eepromData: depthai.EepromData, validateExtrinsics: bool = None) -> None

        Construct a new Calibration Handler object from EepromData object.

        Parameter ``eepromData``:
            EepromData data structure containing the calibration data.

        Parameter ``validateCalibration``:
            Enable internal check for extrinsics cycling links or dangling references.
        """
    def eepromToJson(self) -> json:
        """eepromToJson(self: depthai.CalibrationHandler) -> json

        Get JSON representation of calibration data

        Returns:
            JSON structure
        """
    def eepromToJsonFile(self, destPath: os.PathLike) -> bool:
        """eepromToJsonFile(self: depthai.CalibrationHandler, destPath: os.PathLike) -> bool

        Write raw calibration/board data to json file.

        Parameter ``destPath``:
            Full path to the json file in which raw calibration data will be stored

        Returns:
            True on success, false otherwise
        """
    @staticmethod
    def fromJson(eepromDataJson: json, validateExtrinsics: bool | None = ...) -> CalibrationHandler:
        """fromJson(eepromDataJson: json, validateExtrinsics: Optional[bool] = None) -> depthai.CalibrationHandler

        Construct a new Calibration Handler object from JSON EepromData.

        Parameter ``eepromDataJson``:
            EepromData as JSON

        Parameter ``validateCalibration``:
            Enable internal check for extrinsics cycling links or dangling references.
        """
    def getBaselineDistance(self, cam1: CameraBoardSocket = ..., cam2: CameraBoardSocket = ..., useSpecTranslation: bool = ..., unit: LengthUnit = ...) -> float:
        """getBaselineDistance(self: depthai.CalibrationHandler, cam1: depthai.CameraBoardSocket = <CameraBoardSocket.???: 2>, cam2: depthai.CameraBoardSocket = <CameraBoardSocket.???: 1>, useSpecTranslation: bool = True, unit: depthai.LengthUnit = <LengthUnit.CENTIMETER: 1>) -> float

        Get the baseline distance between two specified cameras. By default it will get
        the baseline between CameraBoardSocket.RIGHT and CameraBoardSocket.LEFT.

        Parameter ``cam1``:
            First camera

        Parameter ``cam2``:
            Second camera

        Parameter ``useSpecTranslation``:
            Enabling this bool uses the translation information from the board design
            data (not the calibration data)

        Parameter ``unit``:
            Units of the returned baseline distance (default: centimeters)

        Returns:
            baseline distance
        """
    def getCameraExtrinsics(self, srcCamera: CameraBoardSocket, dstCamera: CameraBoardSocket, useSpecTranslation: bool = ..., unit: LengthUnit = ...) -> list[list[float]]:
        """getCameraExtrinsics(self: depthai.CalibrationHandler, srcCamera: depthai.CameraBoardSocket, dstCamera: depthai.CameraBoardSocket, useSpecTranslation: bool = False, unit: depthai.LengthUnit = <LengthUnit.CENTIMETER: 1>) -> list[list[float]]

        Get the Camera Extrinsics object between two cameras from the calibration data
        if there is a linked connection between any two cameras then the relative
        rotation and translation is returned by this function.

        Parameter ``srcCamera``:
            Camera Id of the camera which will be considered as origin.

        Parameter ``dstCamera``:
            Camera Id of the destination camera to which we are fetching the rotation
            and translation from the SrcCamera

        Parameter ``useSpecTranslation``:
            Enabling this bool uses the translation information from the board design
            data

        Parameter ``unit``:
            Units of the returned translation (default: centimeters)

        Returns:
            a transformationMatrix which is 4x4 in homogeneous coordinate system

        Matrix representation of transformation matrix \\f[ \\text{Transformation Matrix}
        = \\left [ \\begin{matrix} r_{00} & r_{01} & r_{02} & T_x \\\\ r_{10} & r_{11} &
        r_{12} & T_y \\\\ r_{20} & r_{21} & r_{22} & T_z \\\\ 0 & 0 & 0 & 1 \\end{matrix}
        \\right ] \\f]
        """
    def getCameraIntrinsics(self, cameraId: CameraBoardSocket, resizeWidth: int = ..., resizeHeight: int = ..., topLeftPixelId: Point2f = ..., bottomRightPixelId: Point2f = ..., keepAspectRatio: bool = ...) -> list[list[float]]:
        """getCameraIntrinsics(*args, **kwargs)
        Overloaded function.

        1. getCameraIntrinsics(self: depthai.CalibrationHandler, cameraId: depthai.CameraBoardSocket, resizeWidth: int = -1, resizeHeight: int = -1, topLeftPixelId: depthai.Point2f = <depthai.Point2f object at 0xff8e0c698c30>, bottomRightPixelId: depthai.Point2f = <depthai.Point2f object at 0xff8e0c698c70>, keepAspectRatio: bool = True) -> list[list[float]]

        Get the Camera Intrinsics object

        Parameter ``cameraId``:
            Uses the cameraId to identify which camera intrinsics to return

        Parameter ``resizewidth``:
            resized width of the image for which intrinsics is requested. resizewidth =
            -1 represents width is same as default intrinsics

        Parameter ``resizeHeight``:
            resized height of the image for which intrinsics is requested. resizeHeight
            = -1 represents height is same as default intrinsics

        Parameter ``topLeftPixelId``:
            (x, y) point represents the top left corner coordinates of the cropped image
            which is used to modify the intrinsics for the respective cropped image

        Parameter ``bottomRightPixelId``:
            (x, y) point represents the bottom right corner coordinates of the cropped
            image which is used to modify the intrinsics for the respective cropped
            image

        Parameter ``keepAspectRatio``:
            Enabling this will scale on width or height depending on which provides the
            max resolution and crops the remaining part of the other side

        Returns:
            Represents the 3x3 intrinsics matrix of the respective camera at the
            requested size and crop dimensions.

        Matrix representation of intrinsic matrix \\f[ \\text{Intrinsic Matrix} = \\left [
        \\begin{matrix} f_x & 0 & c_x \\\\ 0 & f_y & c_y \\\\ 0 & 0 & 1 \\end{matrix} \\right ]
        \\f]

        2. getCameraIntrinsics(self: depthai.CalibrationHandler, cameraId: depthai.CameraBoardSocket, destShape: depthai.Size2f, topLeftPixelId: depthai.Point2f = <depthai.Point2f object at 0xff8e121e71b0>, bottomRightPixelId: depthai.Point2f = <depthai.Point2f object at 0xff8e121e54b0>, keepAspectRatio: bool = True) -> list[list[float]]

        Get the Camera Intrinsics object

        Parameter ``cameraId``:
            Uses the cameraId to identify which camera intrinsics to return

        Parameter ``destShape``:
            resized width and height of the image for which intrinsics is requested.

        Parameter ``topLeftPixelId``:
            (x, y) point represents the top left corner coordinates of the cropped image
            which is used to modify the intrinsics for the respective cropped image

        Parameter ``bottomRightPixelId``:
            (x, y) point represents the bottom right corner coordinates of the cropped
            image which is used to modify the intrinsics for the respective cropped
            image

        Parameter ``keepAspectRatio``:
            Enabling this will scale on width or height depending on which provides the
            max resolution and crops the remaining part of the other side

        Returns:
            Represents the 3x3 intrinsics matrix of the respective camera at the
            requested size and crop dimensions.

        Matrix representation of intrinsic matrix \\f[ \\text{Intrinsic Matrix} = \\left [
        \\begin{matrix} f_x & 0 & c_x \\\\ 0 & f_y & c_y \\\\ 0 & 0 & 1 \\end{matrix} \\right ]
        \\f]

        3. getCameraIntrinsics(self: depthai.CalibrationHandler, cameraId: depthai.CameraBoardSocket, destShape: tuple[int, int], topLeftPixelId: depthai.Point2f = <depthai.Point2f object at 0xff8e121a0f70>, bottomRightPixelId: depthai.Point2f = <depthai.Point2f object at 0xff8e0c698fb0>, keepAspectRatio: bool = True) -> list[list[float]]

        Get the Camera Intrinsics object

        Parameter ``cameraId``:
            Uses the cameraId to identify which camera intrinsics to return

        Parameter ``destShape``:
            resized width and height of the image for which intrinsics is requested.

        Parameter ``topLeftPixelId``:
            (x, y) point represents the top left corner coordinates of the cropped image
            which is used to modify the intrinsics for the respective cropped image

        Parameter ``bottomRightPixelId``:
            (x, y) point represents the bottom right corner coordinates of the cropped
            image which is used to modify the intrinsics for the respective cropped
            image

        Parameter ``keepAspectRatio``:
            Enabling this will scale on width or height depending on which provides the
            max resolution and crops the remaining part of the other side

        Returns:
            Represents the 3x3 intrinsics matrix of the respective camera at the
            requested size and crop dimensions.

        Matrix representation of intrinsic matrix \\f[ \\text{Intrinsic Matrix} = \\left [
        \\begin{matrix} f_x & 0 & c_x \\\\ 0 & f_y & c_y \\\\ 0 & 0 & 1 \\end{matrix} \\right ]
        \\f]
        """
    def getCameraRotationMatrix(self, srcCamera: CameraBoardSocket, dstCamera: CameraBoardSocket) -> list[list[float]]:
        """getCameraRotationMatrix(self: depthai.CalibrationHandler, srcCamera: depthai.CameraBoardSocket, dstCamera: depthai.CameraBoardSocket) -> list[list[float]]

        Get the Camera rotation matrix between two cameras from the calibration data.

        Parameter ``srcCamera``:
            Camera Id of the camera which will be considered as origin.

        Parameter ``dstCamera``:
            Camera Id of the destination camera to which we are fetching the rotation
            vector from the SrcCamera

        Returns:
            a 3x3 rotation matrix Matrix representation of rotation matrix \\f[
            \\text{Rotation Matrix} = \\left [ \\begin{matrix} r_{00} & r_{01} & r_{02}\\\\\n            r_{10} & r_{11} & r_{12}\\\\ r_{20} & r_{21} & r_{22}\\\\ \\end{matrix} \\right ]
            \\f]
        """
    def getCameraToImuExtrinsics(self, cameraId: CameraBoardSocket, useSpecTranslation: bool = ..., unit: LengthUnit = ...) -> list[list[float]]:
        """getCameraToImuExtrinsics(self: depthai.CalibrationHandler, cameraId: depthai.CameraBoardSocket, useSpecTranslation: bool = False, unit: depthai.LengthUnit = <LengthUnit.CENTIMETER: 1>) -> list[list[float]]

        Get the Camera To Imu Extrinsics object From the data loaded if there is a
        linked connection between IMU and the given camera then there relative rotation
        and translation from the camera to IMU is returned.

        Parameter ``cameraId``:
            Camera Id of the camera which will be considered as origin. from which
            Transformation matrix to the IMU will be found

        Parameter ``useSpecTranslation``:
            Enabling this bool uses the translation information from the board design
            data

        Parameter ``unit``:
            Units of the returned translation (default: centimeters)

        Returns:
            Returns a transformationMatrix which is 4x4 in homogeneous coordinate system

        Matrix representation of transformation matrix \\f[ \\text{Transformation Matrix}
        = \\left [ \\begin{matrix} r_{00} & r_{01} & r_{02} & T_x \\\\ r_{10} & r_{11} &
        r_{12} & T_y \\\\ r_{20} & r_{21} & r_{22} & T_z \\\\ 0 & 0 & 0 & 1 \\end{matrix}
        \\right ] \\f]
        """
    def getCameraTranslationVector(self, srcCamera: CameraBoardSocket, dstCamera: CameraBoardSocket, useSpecTranslation: bool = ..., unit: LengthUnit = ...) -> list[float]:
        """getCameraTranslationVector(self: depthai.CalibrationHandler, srcCamera: depthai.CameraBoardSocket, dstCamera: depthai.CameraBoardSocket, useSpecTranslation: bool = True, unit: depthai.LengthUnit = <LengthUnit.CENTIMETER: 1>) -> list[float]

        Get the Camera translation vector between two cameras from the calibration data.

        Parameter ``srcCamera``:
            Camera Id of the camera which will be considered as origin.

        Parameter ``dstCamera``:
            Camera Id of the destination camera to which we are fetching the translation
            vector from the SrcCamera

        Parameter ``useSpecTranslation``:
            Disabling this bool uses the translation information from the calibration
            data (not the board design data)

        Parameter ``unit``:
            Units of the returned translation (default: centimeters)

        Returns:
            a translation vector like [x, y, z]
        """
    def getDefaultIntrinsics(self, cameraId: CameraBoardSocket) -> tuple[list[list[float]], int, int]:
        """getDefaultIntrinsics(self: depthai.CalibrationHandler, cameraId: depthai.CameraBoardSocket) -> tuple[list[list[float]], int, int]

        Get the Default Intrinsics object

        Parameter ``cameraId``:
            Uses the cameraId to identify which camera intrinsics to return

        Returns:
            Represents the 3x3 intrinsics matrix of the respective camera along with
            width and height at which it was calibrated.

        Matrix representation of intrinsic matrix \\f[ \\text{Intrinsic Matrix} = \\left [
        \\begin{matrix} f_x & 0 & c_x \\\\ 0 & f_y & c_y \\\\ 0 & 0 & 1 \\end{matrix} \\right ]
        \\f]
        """
    def getDistortionCoefficients(self, cameraId: CameraBoardSocket) -> list[float]:
        """getDistortionCoefficients(self: depthai.CalibrationHandler, cameraId: depthai.CameraBoardSocket) -> list[float]

        Get the Distortion Coefficients object

        Parameter ``cameraId``:
            Uses the cameraId to identify which distortion Coefficients to return.

        Returns:
            the distortion coefficients of the requested camera in this order:
            [k1,k2,p1,p2,k3,k4,k5,k6,s1,s2,s3,s4,τx,τy] for CameraModel::Perspective or
            [k1, k2, k3, k4] for CameraModel::Fisheye see
            https://docs.opencv.org/4.5.4/d9/d0c/group__calib3d.html for Perspective
            model (Rational Polynomial Model) see
            https://docs.opencv.org/4.5.4/db/d58/group__calib3d__fisheye.html for
            Fisheye model
        """
    def getDistortionModel(self, cameraId: CameraBoardSocket) -> CameraModel:
        """getDistortionModel(self: depthai.CalibrationHandler, cameraId: depthai.CameraBoardSocket) -> depthai.CameraModel

        Get the distortion model of the given camera

        Parameter ``cameraId``:
            of the camera with lens position is requested.

        Returns:
            lens position of the camera with given cameraId at which it was calibrated.
        """
    def getEepromData(self) -> EepromData:
        """getEepromData(self: depthai.CalibrationHandler) -> depthai.EepromData

        Get the Eeprom Data object

        Returns:
            EepromData object which contains the raw calibration data
        """
    def getFov(self, cameraId: CameraBoardSocket, useSpec: bool = ...) -> float:
        """getFov(self: depthai.CalibrationHandler, cameraId: depthai.CameraBoardSocket, useSpec: bool = True) -> float

        Get the Fov of the camera

        Parameter ``cameraId``:
            of the camera of which we are fetching fov.

        Parameter ``useSpec``:
            Disabling this bool will calculate the fov based on intrinsics (focal
            length, image width), instead of getting it from the camera specs

        Returns:
            field of view of the camera with given cameraId.
        """
    def getHousingCalibration(self, srcCamera: CameraBoardSocket, housingCS: HousingCoordinateSystem, useSpecTranslation: bool = ..., unit: LengthUnit = ...) -> list[list[float]]:
        """getHousingCalibration(self: depthai.CalibrationHandler, srcCamera: depthai.CameraBoardSocket, housingCS: depthai.HousingCoordinateSystem, useSpecTranslation: bool = True, unit: depthai.LengthUnit = <LengthUnit.CENTIMETER: 1>) -> list[list[float]]

        Get the transformation matrix between a camera and a chosen housing coordinate
        system. The returned 4x4 homogeneous transformation matrix maps points from the
        camera's coordinate system into the specified housing coordinate system.

        The transformation consists of a rotation matrix and translation vector
        extracted either from the calibration data or from the board design
        (specification) data, depending on the `useSpecTranslation` flag.

        Parameter ``srcCamera``:
            Camera whose coordinate frame will be treated as the origin.

        Parameter ``housingCS``:
            The housing coordinate system to which the camera transformation is
            requested (e.g. VESA_RIGHT, FRONT_COVER_LEFT, etc.).

        Parameter ``useSpecTranslation``:
            If true, uses board-design (spec) translation values. If false, uses
            calibrated translation values.

        Parameter ``unit``:
            Units of the returned translation (default: centimeters)

        Returns:
            A 4x4 homogeneous transformation matrix.

        Matrix representation of the transformation: \\f[ \\text{Transformation Matrix} =
        \\left[ \\begin{matrix} r_{00} & r_{01} & r_{02} & T_x \\\\ r_{10} & r_{11} & r_{12}
        & T_y \\\\ r_{20} & r_{21} & r_{22} & T_z \\\\ 0 & 0 & 0 & 1 \\end{matrix} \\right]
        \\f]
        """
    def getImuToCameraExtrinsics(self, cameraId: CameraBoardSocket, useSpecTranslation: bool = ..., unit: LengthUnit = ...) -> list[list[float]]:
        """getImuToCameraExtrinsics(self: depthai.CalibrationHandler, cameraId: depthai.CameraBoardSocket, useSpecTranslation: bool = False, unit: depthai.LengthUnit = <LengthUnit.CENTIMETER: 1>) -> list[list[float]]

        Get the Imu To Camera Extrinsics object from the data loaded if there is a
        linked connection between IMU and the given camera then there relative rotation
        and translation from the IMU to Camera is returned.

        Parameter ``cameraId``:
            Camera Id of the camera which will be considered as destination. To which
            Transformation matrix from the IMU will be found.

        Parameter ``useSpecTranslation``:
            Enabling this bool uses the translation information from the board design
            data

        Parameter ``unit``:
            Units of the returned translation (default: centimeters)

        Returns:
            Returns a transformationMatrix which is 4x4 in homogeneous coordinate system

        Matrix representation of transformation matrix \\f[ \\text{Transformation Matrix}
        = \\left [ \\begin{matrix} r_{00} & r_{01} & r_{02} & T_x \\\\ r_{10} & r_{11} &
        r_{12} & T_y \\\\ r_{20} & r_{21} & r_{22} & T_z \\\\ 0 & 0 & 0 & 1 \\end{matrix}
        \\right ] \\f]
        """
    def getLensPosition(self, cameraId: CameraBoardSocket) -> int:
        """getLensPosition(self: depthai.CalibrationHandler, cameraId: depthai.CameraBoardSocket) -> int

        Get the lens position of the given camera

        Parameter ``cameraId``:
            of the camera with lens position is requested.

        Returns:
            lens position of the camera with given cameraId at which it was calibrated.
        """
    def getStereoLeftCameraId(self) -> CameraBoardSocket:
        """getStereoLeftCameraId(self: depthai.CalibrationHandler) -> depthai.CameraBoardSocket

        Get the camera id of the camera which is used as left camera of the stereo setup

        Returns:
            cameraID of the camera used as left camera
        """
    def getStereoLeftRectificationRotation(self) -> list[list[float]]:
        """getStereoLeftRectificationRotation(self: depthai.CalibrationHandler) -> list[list[float]]

        Get the Stereo Left Rectification Rotation object

        Returns:
            returns a 3x3 rectification rotation matrix
        """
    def getStereoRightCameraId(self) -> CameraBoardSocket:
        """getStereoRightCameraId(self: depthai.CalibrationHandler) -> depthai.CameraBoardSocket

        Get the camera id of the camera which is used as right camera of the stereo
        setup

        Returns:
            cameraID of the camera used as right camera
        """
    def getStereoRightRectificationRotation(self) -> list[list[float]]:
        """getStereoRightRectificationRotation(self: depthai.CalibrationHandler) -> list[list[float]]

        Get the Stereo Right Rectification Rotation object

        Returns:
            returns a 3x3 rectification rotation matrix
        """
    @overload
    def setBoardInfo(self, boardName: str, boardRev: str) -> None:
        """setBoardInfo(*args, **kwargs)
        Overloaded function.

        1. setBoardInfo(self: depthai.CalibrationHandler, boardName: str, boardRev: str) -> None

        Set the Board Info object

        Parameter ``version``:
            Sets the version of the Calibration data(Current version is 6)

        Parameter ``boardName``:
            Sets your board name.

        Parameter ``boardRev``:
            set your board revision id.

        2. setBoardInfo(self: depthai.CalibrationHandler, productName: str, boardName: str, boardRev: str, boardConf: str, hardwareConf: str, batchName: str, batchTime: int, boardOptions: int, boardCustom: str = '') -> None

        Set the Board Info object. Creates version 7 EEPROM data

        Parameter ``productName``:
            Sets product name (alias).

        Parameter ``boardName``:
            Sets board name.

        Parameter ``boardRev``:
            Sets board revision id.

        Parameter ``boardConf``:
            Sets board configuration id.

        Parameter ``hardwareConf``:
            Sets hardware configuration id.

        Parameter ``batchName``:
            Sets batch name.

        Parameter ``batchTime``:
            Sets batch time (unix timestamp).

        Parameter ``boardCustom``:
            Sets a custom board (Default empty string).

        3. setBoardInfo(self: depthai.CalibrationHandler, deviceName: str, productName: str, boardName: str, boardRev: str, boardConf: str, hardwareConf: str, batchName: str, batchTime: int, boardOptions: int, boardCustom: str = '') -> None

        Set the Board Info object. Creates version 7 EEPROM data

        Parameter ``deviceName``:
            Sets device name.

        Parameter ``productName``:
            Sets product name (alias).

        Parameter ``boardName``:
            Sets board name.

        Parameter ``boardRev``:
            Sets board revision id.

        Parameter ``boardConf``:
            Sets board configuration id.

        Parameter ``hardwareConf``:
            Sets hardware configuration id.

        Parameter ``batchName``:
            Sets batch name. Not supported anymore

        Parameter ``batchTime``:
            Sets batch time (unix timestamp).

        Parameter ``boardCustom``:
            Sets a custom board (Default empty string).
        """
    @overload
    def setBoardInfo(self, productName: str, boardName: str, boardRev: str, boardConf: str, hardwareConf: str, batchName: str, batchTime: int, boardOptions: int, boardCustom: str = ...) -> None:
        """setBoardInfo(*args, **kwargs)
        Overloaded function.

        1. setBoardInfo(self: depthai.CalibrationHandler, boardName: str, boardRev: str) -> None

        Set the Board Info object

        Parameter ``version``:
            Sets the version of the Calibration data(Current version is 6)

        Parameter ``boardName``:
            Sets your board name.

        Parameter ``boardRev``:
            set your board revision id.

        2. setBoardInfo(self: depthai.CalibrationHandler, productName: str, boardName: str, boardRev: str, boardConf: str, hardwareConf: str, batchName: str, batchTime: int, boardOptions: int, boardCustom: str = '') -> None

        Set the Board Info object. Creates version 7 EEPROM data

        Parameter ``productName``:
            Sets product name (alias).

        Parameter ``boardName``:
            Sets board name.

        Parameter ``boardRev``:
            Sets board revision id.

        Parameter ``boardConf``:
            Sets board configuration id.

        Parameter ``hardwareConf``:
            Sets hardware configuration id.

        Parameter ``batchName``:
            Sets batch name.

        Parameter ``batchTime``:
            Sets batch time (unix timestamp).

        Parameter ``boardCustom``:
            Sets a custom board (Default empty string).

        3. setBoardInfo(self: depthai.CalibrationHandler, deviceName: str, productName: str, boardName: str, boardRev: str, boardConf: str, hardwareConf: str, batchName: str, batchTime: int, boardOptions: int, boardCustom: str = '') -> None

        Set the Board Info object. Creates version 7 EEPROM data

        Parameter ``deviceName``:
            Sets device name.

        Parameter ``productName``:
            Sets product name (alias).

        Parameter ``boardName``:
            Sets board name.

        Parameter ``boardRev``:
            Sets board revision id.

        Parameter ``boardConf``:
            Sets board configuration id.

        Parameter ``hardwareConf``:
            Sets hardware configuration id.

        Parameter ``batchName``:
            Sets batch name. Not supported anymore

        Parameter ``batchTime``:
            Sets batch time (unix timestamp).

        Parameter ``boardCustom``:
            Sets a custom board (Default empty string).
        """
    @overload
    def setBoardInfo(self, deviceName: str, productName: str, boardName: str, boardRev: str, boardConf: str, hardwareConf: str, batchName: str, batchTime: int, boardOptions: int, boardCustom: str = ...) -> None:
        """setBoardInfo(*args, **kwargs)
        Overloaded function.

        1. setBoardInfo(self: depthai.CalibrationHandler, boardName: str, boardRev: str) -> None

        Set the Board Info object

        Parameter ``version``:
            Sets the version of the Calibration data(Current version is 6)

        Parameter ``boardName``:
            Sets your board name.

        Parameter ``boardRev``:
            set your board revision id.

        2. setBoardInfo(self: depthai.CalibrationHandler, productName: str, boardName: str, boardRev: str, boardConf: str, hardwareConf: str, batchName: str, batchTime: int, boardOptions: int, boardCustom: str = '') -> None

        Set the Board Info object. Creates version 7 EEPROM data

        Parameter ``productName``:
            Sets product name (alias).

        Parameter ``boardName``:
            Sets board name.

        Parameter ``boardRev``:
            Sets board revision id.

        Parameter ``boardConf``:
            Sets board configuration id.

        Parameter ``hardwareConf``:
            Sets hardware configuration id.

        Parameter ``batchName``:
            Sets batch name.

        Parameter ``batchTime``:
            Sets batch time (unix timestamp).

        Parameter ``boardCustom``:
            Sets a custom board (Default empty string).

        3. setBoardInfo(self: depthai.CalibrationHandler, deviceName: str, productName: str, boardName: str, boardRev: str, boardConf: str, hardwareConf: str, batchName: str, batchTime: int, boardOptions: int, boardCustom: str = '') -> None

        Set the Board Info object. Creates version 7 EEPROM data

        Parameter ``deviceName``:
            Sets device name.

        Parameter ``productName``:
            Sets product name (alias).

        Parameter ``boardName``:
            Sets board name.

        Parameter ``boardRev``:
            Sets board revision id.

        Parameter ``boardConf``:
            Sets board configuration id.

        Parameter ``hardwareConf``:
            Sets hardware configuration id.

        Parameter ``batchName``:
            Sets batch name. Not supported anymore

        Parameter ``batchTime``:
            Sets batch time (unix timestamp).

        Parameter ``boardCustom``:
            Sets a custom board (Default empty string).
        """
    def setCameraExtrinsics(self, srcCameraId: CameraBoardSocket, destCameraId: CameraBoardSocket, rotationMatrix: list[list[float]], translation: list[float], specTranslation: list[float] = ...) -> None:
        """setCameraExtrinsics(self: depthai.CalibrationHandler, srcCameraId: depthai.CameraBoardSocket, destCameraId: depthai.CameraBoardSocket, rotationMatrix: list[list[float]], translation: list[float], specTranslation: list[float] = [0.0, 0.0, 0.0]) -> None

        Set the Camera Extrinsics object

        Parameter ``srcCameraId``:
            Camera Id of the camera which will be considered as relative origin.

        Parameter ``destCameraId``:
            Camera Id of the camera which will be considered as destination from
            srcCameraId.

        Parameter ``rotationMatrix``:
            Rotation between srcCameraId and destCameraId origins.

        Parameter ``translation``:
            Translation between srcCameraId and destCameraId origins.

        Parameter ``specTranslation``:
            Translation between srcCameraId and destCameraId origins from the design.
        """
    def setCameraIntrinsics(self, cameraId: CameraBoardSocket, intrinsics: list[list[float]], frameSize: Size2f) -> None:
        """setCameraIntrinsics(*args, **kwargs)
        Overloaded function.

        1. setCameraIntrinsics(self: depthai.CalibrationHandler, cameraId: depthai.CameraBoardSocket, intrinsics: list[list[float]], frameSize: depthai.Size2f) -> None

        Set the Camera Intrinsics object

        Parameter ``cameraId``:
            CameraId of the camera for which Camera intrinsics are being loaded

        Parameter ``intrinsics``:
            3x3 intrinsics matrix

        Parameter ``frameSize``:
            Represents the width and height of the image at which intrinsics are
            calculated.

        Matrix representation of intrinsic matrix \\f[ \\text{Intrinsic Matrix} = \\left [
        \\begin{matrix} f_x & 0 & c_x \\\\ 0 & f_y & c_y \\\\ 0 & 0 & 1 \\end{matrix} \\right ]
        \\f]

        2. setCameraIntrinsics(self: depthai.CalibrationHandler, cameraId: depthai.CameraBoardSocket, intrinsics: list[list[float]], width: int, height: int) -> None

        Set the Camera Intrinsics object

        Parameter ``cameraId``:
            CameraId of the camera for which Camera intrinsics are being loaded

        Parameter ``intrinsics``:
            3x3 intrinsics matrix

        Parameter ``width``:
            Represents the width of the image at which intrinsics are calculated.

        Parameter ``height``:
            Represents the height of the image at which intrinsics are calculated.

        Matrix representation of intrinsic matrix \\f[ \\text{Intrinsic Matrix} = \\left [
        \\begin{matrix} f_x & 0 & c_x \\\\ 0 & f_y & c_y \\\\ 0 & 0 & 1 \\end{matrix} \\right ]
        \\f]

        3. setCameraIntrinsics(self: depthai.CalibrationHandler, cameraId: depthai.CameraBoardSocket, intrinsics: list[list[float]], frameSize: tuple[int, int]) -> None

        Set the Camera Intrinsics object

        Parameter ``cameraId``:
            CameraId of the camera for which Camera intrinsics are being loaded

        Parameter ``intrinsics``:
            3x3 intrinsics matrix

        Parameter ``frameSize``:
            Represents the width and height of the image at which intrinsics are
            calculated.

        Matrix representation of intrinsic matrix \\f[ \\text{Intrinsic Matrix} = \\left [
        \\begin{matrix} f_x & 0 & c_x \\\\ 0 & f_y & c_y \\\\ 0 & 0 & 1 \\end{matrix} \\right ]
        \\f]
        """
    def setCameraType(self, cameraId: CameraBoardSocket, cameraModel: CameraModel) -> None:
        """setCameraType(self: depthai.CalibrationHandler, cameraId: depthai.CameraBoardSocket, cameraModel: depthai.CameraModel) -> None

        Set the Camera Type object

        Parameter ``cameraId``:
            CameraId of the camera for which cameraModel Type is being updated.

        Parameter ``cameraModel``:
            Type of the model the camera represents
        """
    def setDeviceName(self, deviceName: str) -> None:
        """setDeviceName(self: depthai.CalibrationHandler, deviceName: str) -> None

        Set the deviceName which responses to getDeviceName of Device

        Parameter ``deviceName``:
            Sets device name.
        """
    def setDistortionCoefficients(self, cameraId: CameraBoardSocket, distortionCoefficients: list[float]) -> None:
        """setDistortionCoefficients(self: depthai.CalibrationHandler, cameraId: depthai.CameraBoardSocket, distortionCoefficients: list[float]) -> None

        Sets the distortion Coefficients obtained from camera calibration

        Parameter ``cameraId``:
            Camera Id of the camera for which distortion coefficients are computed

        Parameter ``distortionCoefficients``:
            Distortion Coefficients of the respective Camera.
        """
    def setFov(self, cameraId: CameraBoardSocket, hfov: float) -> None:
        """setFov(self: depthai.CalibrationHandler, cameraId: depthai.CameraBoardSocket, hfov: float) -> None

        Set the Fov of the Camera

        Parameter ``cameraId``:
            Camera Id of the camera

        Parameter ``hfov``:
            Horizontal fov of the camera from Camera Datasheet
        """
    def setImuExtrinsics(self, destCameraId: CameraBoardSocket, rotationMatrix: list[list[float]], translation: list[float], specTranslation: list[float] = ...) -> None:
        """setImuExtrinsics(self: depthai.CalibrationHandler, destCameraId: depthai.CameraBoardSocket, rotationMatrix: list[list[float]], translation: list[float], specTranslation: list[float] = [0.0, 0.0, 0.0]) -> None

        Set the Imu to Camera Extrinsics object

        Parameter ``destCameraId``:
            Camera Id of the camera which will be considered as destination from IMU.

        Parameter ``rotationMatrix``:
            Rotation between srcCameraId and destCameraId origins.

        Parameter ``translation``:
            Translation between IMU and destCameraId origins.

        Parameter ``specTranslation``:
            Translation between IMU and destCameraId origins from the design.
        """
    def setLensPosition(self, cameraId: CameraBoardSocket, lensPosition: int) -> None:
        """setLensPosition(self: depthai.CalibrationHandler, cameraId: depthai.CameraBoardSocket, lensPosition: int) -> None

        Sets the distortion Coefficients obtained from camera calibration

        Parameter ``cameraId``:
            Camera Id of the camera

        Parameter ``lensPosition``:
            lens posiotion value of the camera at the time of calibration
        """
    def setProductName(self, productName: str) -> None:
        """setProductName(self: depthai.CalibrationHandler, productName: str) -> None

        Set the productName which acts as alisas for users to identify the device

        Parameter ``productName``:
            Sets product name (alias).
        """
    def setStereoLeft(self, cameraId: CameraBoardSocket, rectifiedRotation: list[list[float]]) -> None:
        """setStereoLeft(self: depthai.CalibrationHandler, cameraId: depthai.CameraBoardSocket, rectifiedRotation: list[list[float]]) -> None

        Set the Stereo Left Rectification object

        Parameter ``cameraId``:
            CameraId of the camera which will be used as left Camera of stereo Setup

        Parameter ``rectifiedRotation``:
            Rectification rotation of the left camera required for feature matching

        Homography of the Left Rectification = Intrinsics_right * rectifiedRotation *
        inv(Intrinsics_left)
        """
    def setStereoRight(self, cameraId: CameraBoardSocket, rectifiedRotation: list[list[float]]) -> None:
        """setStereoRight(self: depthai.CalibrationHandler, cameraId: depthai.CameraBoardSocket, rectifiedRotation: list[list[float]]) -> None

        Set the Stereo Right Rectification object

        Parameter ``cameraId``:
            CameraId of the camera which will be used as left Camera of stereo Setup

        Parameter ``rectifiedRotation``:
            Rectification rotation of the left camera required for feature matching

        Homography of the Right Rectification = Intrinsics_right * rectifiedRotation *
        inv(Intrinsics_right)
        """
    def validateCalibrationHandler(self, throwOnError: bool = ...) -> None:
        """validateCalibrationHandler(self: depthai.CalibrationHandler, throwOnError: bool = True) -> None

        Validate Calibration handler properties and how they are set, so there is no: -
        Cycling links - Missing links - Dangling connections

        Parameter ``throwOnError``:
            Throw runtime error on failture.
        """

class CalibrationMetrics(Buffer):
    calibrationConfidence: float
    dataConfidence: float
    def __init__(self) -> None:
        """__init__(self: depthai.CalibrationMetrics) -> None"""

class CalibrationQuality(Buffer):
    info: str
    qualityData: CalibrationQualityData | None
    @overload
    def __init__(self) -> None:
        """__init__(*args, **kwargs)
        Overloaded function.

        1. __init__(self: depthai.CalibrationQuality) -> None

        2. __init__(self: depthai.CalibrationQuality, qualityData: depthai.CalibrationQualityData, info: str) -> None

        3. __init__(self: depthai.CalibrationQuality, info: str) -> None
        """
    @overload
    def __init__(self, qualityData: CalibrationQualityData, info: str) -> None:
        """__init__(*args, **kwargs)
        Overloaded function.

        1. __init__(self: depthai.CalibrationQuality) -> None

        2. __init__(self: depthai.CalibrationQuality, qualityData: depthai.CalibrationQualityData, info: str) -> None

        3. __init__(self: depthai.CalibrationQuality, info: str) -> None
        """
    @overload
    def __init__(self, info: str) -> None:
        """__init__(*args, **kwargs)
        Overloaded function.

        1. __init__(self: depthai.CalibrationQuality) -> None

        2. __init__(self: depthai.CalibrationQuality, qualityData: depthai.CalibrationQualityData, info: str) -> None

        3. __init__(self: depthai.CalibrationQuality, info: str) -> None
        """

class CalibrationQualityData:
    depthErrorDifference: list[float]
    rotationChange: Incomplete
    sampsonErrorCurrent: float
    sampsonErrorNew: float
    def __init__(self) -> None:
        """__init__(self: depthai.CalibrationQualityData) -> None"""

class CameraBoardSocket:
    """Which Camera socket to use.

    AUTO denotes that the decision will be made by device

    Members:

      AUTO

      CAM_A

      CAM_B

      CAM_C

      CAM_D

      VERTICAL

      CAM_E

      CAM_F

      CAM_G

      CAM_H

      RGB : **Deprecated:** Use CAM_A or address camera by name instead

      LEFT : **Deprecated:** Use CAM_B or address camera by name instead

      RIGHT : **Deprecated:** Use CAM_C or address camera by name instead

      CENTER : **Deprecated:** Use CAM_A or address camera by name instead"""
    CENTER: ClassVar[CameraBoardSocket] = ...  # read-only
    LEFT: ClassVar[CameraBoardSocket] = ...  # read-only
    RGB: ClassVar[CameraBoardSocket] = ...  # read-only
    RIGHT: ClassVar[CameraBoardSocket] = ...  # read-only
    __members__: ClassVar[dict] = ...  # read-only
    AUTO: ClassVar[CameraBoardSocket] = ...
    CAM_A: ClassVar[CameraBoardSocket] = ...
    CAM_B: ClassVar[CameraBoardSocket] = ...
    CAM_C: ClassVar[CameraBoardSocket] = ...
    CAM_D: ClassVar[CameraBoardSocket] = ...
    CAM_E: ClassVar[CameraBoardSocket] = ...
    CAM_F: ClassVar[CameraBoardSocket] = ...
    CAM_G: ClassVar[CameraBoardSocket] = ...
    CAM_H: ClassVar[CameraBoardSocket] = ...
    VERTICAL: ClassVar[CameraBoardSocket] = ...
    __entries: ClassVar[dict] = ...
    def __init__(self, value: int) -> None:
        """__init__(self: depthai.CameraBoardSocket, value: int) -> None"""
    def __eq__(self, other: object) -> bool:
        """__eq__(self: object, other: object) -> bool"""
    def __hash__(self) -> int:
        """__hash__(self: object) -> int"""
    def __index__(self) -> int:
        """__index__(self: depthai.CameraBoardSocket) -> int"""
    def __int__(self) -> int:
        """__int__(self: depthai.CameraBoardSocket) -> int"""
    def __ne__(self, other: object) -> bool:
        """__ne__(self: object, other: object) -> bool"""
    @property
    def name(self) -> str:
        """name(self: object) -> str

        name(self: object) -> str
        """
    @property
    def value(self) -> int:
        """(arg0: depthai.CameraBoardSocket) -> int"""

class CameraControl(Buffer):
    """CameraControl message. Specifies various camera control commands like:

    - Still capture

    - Auto/manual focus

    - Auto/manual white balance

    - Auto/manual exposure

    - Anti banding

    - ...

    By default the camera enables 3A, with auto-focus in `CONTINUOUS_VIDEO` mode,
    auto-white-balance in `AUTO` mode, and auto-exposure with anti-banding for 50Hz
    mains frequency."""

    class AntiBandingMode:
        """

        Members:

          OFF

          MAINS_50_HZ

          MAINS_60_HZ

          AUTO"""
        __members__: ClassVar[dict] = ...  # read-only
        AUTO: ClassVar[CameraControl.AntiBandingMode] = ...
        MAINS_50_HZ: ClassVar[CameraControl.AntiBandingMode] = ...
        MAINS_60_HZ: ClassVar[CameraControl.AntiBandingMode] = ...
        OFF: ClassVar[CameraControl.AntiBandingMode] = ...
        __entries: ClassVar[dict] = ...
        def __init__(self, value: int) -> None:
            """__init__(self: depthai.CameraControl.AntiBandingMode, value: int) -> None"""
        def __eq__(self, other: object) -> bool:
            """__eq__(self: object, other: object) -> bool"""
        def __hash__(self) -> int:
            """__hash__(self: object) -> int"""
        def __index__(self) -> int:
            """__index__(self: depthai.CameraControl.AntiBandingMode) -> int"""
        def __int__(self) -> int:
            """__int__(self: depthai.CameraControl.AntiBandingMode) -> int"""
        def __ne__(self, other: object) -> bool:
            """__ne__(self: object, other: object) -> bool"""
        @property
        def name(self) -> str:
            """name(self: object) -> str

            name(self: object) -> str
            """
        @property
        def value(self) -> int:
            """(arg0: depthai.CameraControl.AntiBandingMode) -> int"""

    class AutoFocusMode:
        """

        Members:

          OFF

          AUTO

          MACRO

          CONTINUOUS_VIDEO

          CONTINUOUS_PICTURE

          EDOF"""
        __members__: ClassVar[dict] = ...  # read-only
        AUTO: ClassVar[CameraControl.AutoFocusMode] = ...
        CONTINUOUS_PICTURE: ClassVar[CameraControl.AutoFocusMode] = ...
        CONTINUOUS_VIDEO: ClassVar[CameraControl.AutoFocusMode] = ...
        EDOF: ClassVar[CameraControl.AutoFocusMode] = ...
        MACRO: ClassVar[CameraControl.AutoFocusMode] = ...
        OFF: ClassVar[CameraControl.AutoFocusMode] = ...
        __entries: ClassVar[dict] = ...
        def __init__(self, value: int) -> None:
            """__init__(self: depthai.CameraControl.AutoFocusMode, value: int) -> None"""
        def __eq__(self, other: object) -> bool:
            """__eq__(self: object, other: object) -> bool"""
        def __hash__(self) -> int:
            """__hash__(self: object) -> int"""
        def __index__(self) -> int:
            """__index__(self: depthai.CameraControl.AutoFocusMode) -> int"""
        def __int__(self) -> int:
            """__int__(self: depthai.CameraControl.AutoFocusMode) -> int"""
        def __ne__(self, other: object) -> bool:
            """__ne__(self: object, other: object) -> bool"""
        @property
        def name(self) -> str:
            """name(self: object) -> str

            name(self: object) -> str
            """
        @property
        def value(self) -> int:
            """(arg0: depthai.CameraControl.AutoFocusMode) -> int"""

    class AutoWhiteBalanceMode:
        """

        Members:

          OFF

          AUTO

          INCANDESCENT

          FLUORESCENT

          WARM_FLUORESCENT

          DAYLIGHT

          CLOUDY_DAYLIGHT

          TWILIGHT

          SHADE"""
        __members__: ClassVar[dict] = ...  # read-only
        AUTO: ClassVar[CameraControl.AutoWhiteBalanceMode] = ...
        CLOUDY_DAYLIGHT: ClassVar[CameraControl.AutoWhiteBalanceMode] = ...
        DAYLIGHT: ClassVar[CameraControl.AutoWhiteBalanceMode] = ...
        FLUORESCENT: ClassVar[CameraControl.AutoWhiteBalanceMode] = ...
        INCANDESCENT: ClassVar[CameraControl.AutoWhiteBalanceMode] = ...
        OFF: ClassVar[CameraControl.AutoWhiteBalanceMode] = ...
        SHADE: ClassVar[CameraControl.AutoWhiteBalanceMode] = ...
        TWILIGHT: ClassVar[CameraControl.AutoWhiteBalanceMode] = ...
        WARM_FLUORESCENT: ClassVar[CameraControl.AutoWhiteBalanceMode] = ...
        __entries: ClassVar[dict] = ...
        def __init__(self, value: int) -> None:
            """__init__(self: depthai.CameraControl.AutoWhiteBalanceMode, value: int) -> None"""
        def __eq__(self, other: object) -> bool:
            """__eq__(self: object, other: object) -> bool"""
        def __hash__(self) -> int:
            """__hash__(self: object) -> int"""
        def __index__(self) -> int:
            """__index__(self: depthai.CameraControl.AutoWhiteBalanceMode) -> int"""
        def __int__(self) -> int:
            """__int__(self: depthai.CameraControl.AutoWhiteBalanceMode) -> int"""
        def __ne__(self, other: object) -> bool:
            """__ne__(self: object, other: object) -> bool"""
        @property
        def name(self) -> str:
            """name(self: object) -> str

            name(self: object) -> str
            """
        @property
        def value(self) -> int:
            """(arg0: depthai.CameraControl.AutoWhiteBalanceMode) -> int"""

    class CaptureIntent:
        """

        Members:

          CUSTOM

          PREVIEW

          STILL_CAPTURE

          VIDEO_RECORD

          VIDEO_SNAPSHOT

          ZERO_SHUTTER_LAG"""
        __members__: ClassVar[dict] = ...  # read-only
        CUSTOM: ClassVar[CameraControl.CaptureIntent] = ...
        PREVIEW: ClassVar[CameraControl.CaptureIntent] = ...
        STILL_CAPTURE: ClassVar[CameraControl.CaptureIntent] = ...
        VIDEO_RECORD: ClassVar[CameraControl.CaptureIntent] = ...
        VIDEO_SNAPSHOT: ClassVar[CameraControl.CaptureIntent] = ...
        ZERO_SHUTTER_LAG: ClassVar[CameraControl.CaptureIntent] = ...
        __entries: ClassVar[dict] = ...
        def __init__(self, value: int) -> None:
            """__init__(self: depthai.CameraControl.CaptureIntent, value: int) -> None"""
        def __eq__(self, other: object) -> bool:
            """__eq__(self: object, other: object) -> bool"""
        def __hash__(self) -> int:
            """__hash__(self: object) -> int"""
        def __index__(self) -> int:
            """__index__(self: depthai.CameraControl.CaptureIntent) -> int"""
        def __int__(self) -> int:
            """__int__(self: depthai.CameraControl.CaptureIntent) -> int"""
        def __ne__(self, other: object) -> bool:
            """__ne__(self: object, other: object) -> bool"""
        @property
        def name(self) -> str:
            """name(self: object) -> str

            name(self: object) -> str
            """
        @property
        def value(self) -> int:
            """(arg0: depthai.CameraControl.CaptureIntent) -> int"""

    class Command:
        """

        Members:

          START_STREAM

          STOP_STREAM

          STILL_CAPTURE

          MOVE_LENS

          AF_TRIGGER

          AE_MANUAL

          AE_AUTO

          AWB_MODE

          SCENE_MODE

          ANTIBANDING_MODE

          EXPOSURE_COMPENSATION

          AE_LOCK

          AE_TARGET_FPS_RANGE

          AWB_LOCK

          CAPTURE_INTENT

          CONTROL_MODE

          FRAME_DURATION

          SENSITIVITY

          EFFECT_MODE

          AF_MODE

          NOISE_REDUCTION_STRENGTH

          SATURATION

          BRIGHTNESS

          STREAM_FORMAT

          RESOLUTION

          SHARPNESS

          CUSTOM_USECASE

          CUSTOM_CAPT_MODE

          CUSTOM_EXP_BRACKETS

          CUSTOM_CAPTURE

          CONTRAST

          AE_REGION

          AF_REGION

          LUMA_DENOISE

          CHROMA_DENOISE

          WB_COLOR_TEMP"""
        __members__: ClassVar[dict] = ...  # read-only
        AE_AUTO: ClassVar[CameraControl.Command] = ...
        AE_LOCK: ClassVar[CameraControl.Command] = ...
        AE_MANUAL: ClassVar[CameraControl.Command] = ...
        AE_REGION: ClassVar[CameraControl.Command] = ...
        AE_TARGET_FPS_RANGE: ClassVar[CameraControl.Command] = ...
        AF_MODE: ClassVar[CameraControl.Command] = ...
        AF_REGION: ClassVar[CameraControl.Command] = ...
        AF_TRIGGER: ClassVar[CameraControl.Command] = ...
        ANTIBANDING_MODE: ClassVar[CameraControl.Command] = ...
        AWB_LOCK: ClassVar[CameraControl.Command] = ...
        AWB_MODE: ClassVar[CameraControl.Command] = ...
        BRIGHTNESS: ClassVar[CameraControl.Command] = ...
        CAPTURE_INTENT: ClassVar[CameraControl.Command] = ...
        CHROMA_DENOISE: ClassVar[CameraControl.Command] = ...
        CONTRAST: ClassVar[CameraControl.Command] = ...
        CONTROL_MODE: ClassVar[CameraControl.Command] = ...
        CUSTOM_CAPTURE: ClassVar[CameraControl.Command] = ...
        CUSTOM_CAPT_MODE: ClassVar[CameraControl.Command] = ...
        CUSTOM_EXP_BRACKETS: ClassVar[CameraControl.Command] = ...
        CUSTOM_USECASE: ClassVar[CameraControl.Command] = ...
        EFFECT_MODE: ClassVar[CameraControl.Command] = ...
        EXPOSURE_COMPENSATION: ClassVar[CameraControl.Command] = ...
        FRAME_DURATION: ClassVar[CameraControl.Command] = ...
        LUMA_DENOISE: ClassVar[CameraControl.Command] = ...
        MOVE_LENS: ClassVar[CameraControl.Command] = ...
        NOISE_REDUCTION_STRENGTH: ClassVar[CameraControl.Command] = ...
        RESOLUTION: ClassVar[CameraControl.Command] = ...
        SATURATION: ClassVar[CameraControl.Command] = ...
        SCENE_MODE: ClassVar[CameraControl.Command] = ...
        SENSITIVITY: ClassVar[CameraControl.Command] = ...
        SHARPNESS: ClassVar[CameraControl.Command] = ...
        START_STREAM: ClassVar[CameraControl.Command] = ...
        STILL_CAPTURE: ClassVar[CameraControl.Command] = ...
        STOP_STREAM: ClassVar[CameraControl.Command] = ...
        STREAM_FORMAT: ClassVar[CameraControl.Command] = ...
        WB_COLOR_TEMP: ClassVar[CameraControl.Command] = ...
        __entries: ClassVar[dict] = ...
        def __init__(self, value: int) -> None:
            """__init__(self: depthai.CameraControl.Command, value: int) -> None"""
        def __eq__(self, other: object) -> bool:
            """__eq__(self: object, other: object) -> bool"""
        def __hash__(self) -> int:
            """__hash__(self: object) -> int"""
        def __index__(self) -> int:
            """__index__(self: depthai.CameraControl.Command) -> int"""
        def __int__(self) -> int:
            """__int__(self: depthai.CameraControl.Command) -> int"""
        def __ne__(self, other: object) -> bool:
            """__ne__(self: object, other: object) -> bool"""
        @property
        def name(self) -> str:
            """name(self: object) -> str

            name(self: object) -> str
            """
        @property
        def value(self) -> int:
            """(arg0: depthai.CameraControl.Command) -> int"""

    class ControlMode:
        """

        Members:

          OFF

          AUTO

          USE_SCENE_MODE"""
        __members__: ClassVar[dict] = ...  # read-only
        AUTO: ClassVar[CameraControl.ControlMode] = ...
        OFF: ClassVar[CameraControl.ControlMode] = ...
        USE_SCENE_MODE: ClassVar[CameraControl.ControlMode] = ...
        __entries: ClassVar[dict] = ...
        def __init__(self, value: int) -> None:
            """__init__(self: depthai.CameraControl.ControlMode, value: int) -> None"""
        def __eq__(self, other: object) -> bool:
            """__eq__(self: object, other: object) -> bool"""
        def __hash__(self) -> int:
            """__hash__(self: object) -> int"""
        def __index__(self) -> int:
            """__index__(self: depthai.CameraControl.ControlMode) -> int"""
        def __int__(self) -> int:
            """__int__(self: depthai.CameraControl.ControlMode) -> int"""
        def __ne__(self, other: object) -> bool:
            """__ne__(self: object, other: object) -> bool"""
        @property
        def name(self) -> str:
            """name(self: object) -> str

            name(self: object) -> str
            """
        @property
        def value(self) -> int:
            """(arg0: depthai.CameraControl.ControlMode) -> int"""

    class EffectMode:
        """

        Members:

          OFF

          MONO

          NEGATIVE

          SOLARIZE

          SEPIA

          POSTERIZE

          WHITEBOARD

          BLACKBOARD

          AQUA"""
        __members__: ClassVar[dict] = ...  # read-only
        AQUA: ClassVar[CameraControl.EffectMode] = ...
        BLACKBOARD: ClassVar[CameraControl.EffectMode] = ...
        MONO: ClassVar[CameraControl.EffectMode] = ...
        NEGATIVE: ClassVar[CameraControl.EffectMode] = ...
        OFF: ClassVar[CameraControl.EffectMode] = ...
        POSTERIZE: ClassVar[CameraControl.EffectMode] = ...
        SEPIA: ClassVar[CameraControl.EffectMode] = ...
        SOLARIZE: ClassVar[CameraControl.EffectMode] = ...
        WHITEBOARD: ClassVar[CameraControl.EffectMode] = ...
        __entries: ClassVar[dict] = ...
        def __init__(self, value: int) -> None:
            """__init__(self: depthai.CameraControl.EffectMode, value: int) -> None"""
        def __eq__(self, other: object) -> bool:
            """__eq__(self: object, other: object) -> bool"""
        def __hash__(self) -> int:
            """__hash__(self: object) -> int"""
        def __index__(self) -> int:
            """__index__(self: depthai.CameraControl.EffectMode) -> int"""
        def __int__(self) -> int:
            """__int__(self: depthai.CameraControl.EffectMode) -> int"""
        def __ne__(self, other: object) -> bool:
            """__ne__(self: object, other: object) -> bool"""
        @property
        def name(self) -> str:
            """name(self: object) -> str

            name(self: object) -> str
            """
        @property
        def value(self) -> int:
            """(arg0: depthai.CameraControl.EffectMode) -> int"""

    class FrameSyncMode:
        """

        Members:

          OFF

          OUTPUT

          INPUT"""
        __members__: ClassVar[dict] = ...  # read-only
        INPUT: ClassVar[CameraControl.FrameSyncMode] = ...
        OFF: ClassVar[CameraControl.FrameSyncMode] = ...
        OUTPUT: ClassVar[CameraControl.FrameSyncMode] = ...
        __entries: ClassVar[dict] = ...
        def __init__(self, value: int) -> None:
            """__init__(self: depthai.CameraControl.FrameSyncMode, value: int) -> None"""
        def __eq__(self, other: object) -> bool:
            """__eq__(self: object, other: object) -> bool"""
        def __hash__(self) -> int:
            """__hash__(self: object) -> int"""
        def __index__(self) -> int:
            """__index__(self: depthai.CameraControl.FrameSyncMode) -> int"""
        def __int__(self) -> int:
            """__int__(self: depthai.CameraControl.FrameSyncMode) -> int"""
        def __ne__(self, other: object) -> bool:
            """__ne__(self: object, other: object) -> bool"""
        @property
        def name(self) -> str:
            """name(self: object) -> str

            name(self: object) -> str
            """
        @property
        def value(self) -> int:
            """(arg0: depthai.CameraControl.FrameSyncMode) -> int"""

    class SceneMode:
        """

        Members:

          UNSUPPORTED

          FACE_PRIORITY

          ACTION

          PORTRAIT

          LANDSCAPE

          NIGHT

          NIGHT_PORTRAIT

          THEATRE

          BEACH

          SNOW

          SUNSET

          STEADYPHOTO

          FIREWORKS

          SPORTS

          PARTY

          CANDLELIGHT

          BARCODE"""
        __members__: ClassVar[dict] = ...  # read-only
        ACTION: ClassVar[CameraControl.SceneMode] = ...
        BARCODE: ClassVar[CameraControl.SceneMode] = ...
        BEACH: ClassVar[CameraControl.SceneMode] = ...
        CANDLELIGHT: ClassVar[CameraControl.SceneMode] = ...
        FACE_PRIORITY: ClassVar[CameraControl.SceneMode] = ...
        FIREWORKS: ClassVar[CameraControl.SceneMode] = ...
        LANDSCAPE: ClassVar[CameraControl.SceneMode] = ...
        NIGHT: ClassVar[CameraControl.SceneMode] = ...
        NIGHT_PORTRAIT: ClassVar[CameraControl.SceneMode] = ...
        PARTY: ClassVar[CameraControl.SceneMode] = ...
        PORTRAIT: ClassVar[CameraControl.SceneMode] = ...
        SNOW: ClassVar[CameraControl.SceneMode] = ...
        SPORTS: ClassVar[CameraControl.SceneMode] = ...
        STEADYPHOTO: ClassVar[CameraControl.SceneMode] = ...
        SUNSET: ClassVar[CameraControl.SceneMode] = ...
        THEATRE: ClassVar[CameraControl.SceneMode] = ...
        UNSUPPORTED: ClassVar[CameraControl.SceneMode] = ...
        __entries: ClassVar[dict] = ...
        def __init__(self, value: int) -> None:
            """__init__(self: depthai.CameraControl.SceneMode, value: int) -> None"""
        def __eq__(self, other: object) -> bool:
            """__eq__(self: object, other: object) -> bool"""
        def __hash__(self) -> int:
            """__hash__(self: object) -> int"""
        def __index__(self) -> int:
            """__index__(self: depthai.CameraControl.SceneMode) -> int"""
        def __int__(self) -> int:
            """__int__(self: depthai.CameraControl.SceneMode) -> int"""
        def __ne__(self, other: object) -> bool:
            """__ne__(self: object, other: object) -> bool"""
        @property
        def name(self) -> str:
            """name(self: object) -> str

            name(self: object) -> str
            """
        @property
        def value(self) -> int:
            """(arg0: depthai.CameraControl.SceneMode) -> int"""
    aeLockMode: bool
    aeMaxExposureTimeUs: int
    afRegion: Incomplete
    antiBandingMode: CameraControl.AntiBandingMode
    autoFocusMode: CameraControl.AutoFocusMode
    awbLockMode: bool
    awbMode: CameraControl.AutoWhiteBalanceMode
    brightness: int
    captureIntent: CameraControl.CaptureIntent
    chromaDenoise: int
    cmdMask: int
    contrast: int
    controlMode: CameraControl.ControlMode
    effectMode: CameraControl.EffectMode
    expCompensation: int
    expManual: Incomplete
    lensPosition: int
    lumaDenoise: int
    saturation: int
    sceneMode: CameraControl.SceneMode
    sharpness: int
    wbColorTemp: int
    @overload
    def __init__(self) -> None:
        """__init__(*args, **kwargs)
        Overloaded function.

        1. __init__(self: depthai.CameraControl) -> None

        2. __init__(self: depthai.CameraControl) -> None
        """
    @overload
    def __init__(self) -> None:
        """__init__(*args, **kwargs)
        Overloaded function.

        1. __init__(self: depthai.CameraControl) -> None

        2. __init__(self: depthai.CameraControl) -> None
        """
    def clearCommand(self, arg0: CameraControl.Command) -> None:
        """clearCommand(self: depthai.CameraControl, arg0: depthai.CameraControl.Command) -> None"""
    def clearMiscControls(self) -> None:
        """clearMiscControls(self: depthai.CameraControl) -> None

        Clear the list of miscellaneous controls set by `setControl`
        """
    def getCaptureStill(self) -> bool:
        """getCaptureStill(self: depthai.CameraControl) -> bool

        Check whether command to capture a still is set

        Returns:
            True if capture still command is set
        """
    def getCommand(self, arg0: CameraControl.Command) -> bool:
        """getCommand(self: depthai.CameraControl, arg0: depthai.CameraControl.Command) -> bool"""
    def getExposureTime(self) -> datetime.timedelta:
        """getExposureTime(self: depthai.CameraControl) -> datetime.timedelta

        Retrieves exposure time
        """
    def getHdr(self) -> bool:
        """getHdr(self: depthai.CameraControl) -> bool

        Whether or not HDR (High Dynamic Range) mode is enabled

        Returns:
            True if HDR mode is enabled, false otherwise
        """
    def getLensPosition(self) -> int:
        """getLensPosition(self: depthai.CameraControl) -> int

        Retrieves lens position, range 0..255. Returns -1 if not available
        """
    def getLensPositionRaw(self) -> float:
        """getLensPositionRaw(self: depthai.CameraControl) -> float

        Retrieves lens position, range 0.0f..1.0f.
        """
    def getMiscControls(self) -> list[tuple[str, str]]:
        """getMiscControls(self: depthai.CameraControl) -> list[tuple[str, str]]

        Get the list of miscellaneous controls set by `setControl`

        Returns:
            A list of <key, value> pairs as strings
        """
    def getSensitivity(self) -> int:
        """getSensitivity(self: depthai.CameraControl) -> int

        Retrieves sensitivity, as an ISO value
        """
    def setAntiBandingMode(self, mode: CameraControl.AntiBandingMode) -> CameraControl:
        """setAntiBandingMode(self: depthai.CameraControl, mode: depthai.CameraControl.AntiBandingMode) -> depthai.CameraControl

        Set a command to specify anti-banding mode. Anti-banding / anti-flicker works in
        auto-exposure mode, by controlling the exposure time to be applied in multiples
        of half the mains period, for example in multiple of 10ms for 50Hz (period 20ms)
        AC-powered illumination sources.

        If the scene would be too bright for the smallest exposure step (10ms in the
        example, with ISO at a minimum of 100), anti-banding is not effective.

        Parameter ``mode``:
            Anti-banding mode to use. Default: `MAINS_50_HZ`
        """
    def setAutoExposureCompensation(self, compensation: int) -> CameraControl:
        """setAutoExposureCompensation(self: depthai.CameraControl, compensation: int) -> depthai.CameraControl

        Set a command to specify auto exposure compensation. This modifies the
        brightness target with positive numbers making the image brighter and negative
        numbers making the image darker.

        Parameter ``compensation``:
            Compensation value between -9..9, default 0
        """
    def setAutoExposureEnable(self) -> CameraControl:
        """setAutoExposureEnable(self: depthai.CameraControl) -> depthai.CameraControl

        Set a command to enable auto exposure
        """
    @overload
    def setAutoExposureLimit(self, maxExposureTimeUs: int) -> CameraControl:
        """setAutoExposureLimit(*args, **kwargs)
        Overloaded function.

        1. setAutoExposureLimit(self: depthai.CameraControl, maxExposureTimeUs: int) -> depthai.CameraControl

        Set a command to specify the maximum exposure time limit for auto-exposure. By
        default the AE algorithm prioritizes increasing exposure over ISO, up to around
        frame-time (subject to further limits imposed by anti-banding)

        Parameter ``maxExposureTimeUs``:
            Maximum exposure time in microseconds

        2. setAutoExposureLimit(self: depthai.CameraControl, maxExposureTime: datetime.timedelta) -> depthai.CameraControl

        Set a command to specify the maximum exposure time limit for auto-exposure. By
        default the AE algorithm prioritizes increasing exposure over ISO, up to around
        frame-time (subject to further limits imposed by anti-banding)

        Parameter ``maxExposureTime``:
            Maximum exposure time
        """
    @overload
    def setAutoExposureLimit(self, maxExposureTime: datetime.timedelta) -> CameraControl:
        """setAutoExposureLimit(*args, **kwargs)
        Overloaded function.

        1. setAutoExposureLimit(self: depthai.CameraControl, maxExposureTimeUs: int) -> depthai.CameraControl

        Set a command to specify the maximum exposure time limit for auto-exposure. By
        default the AE algorithm prioritizes increasing exposure over ISO, up to around
        frame-time (subject to further limits imposed by anti-banding)

        Parameter ``maxExposureTimeUs``:
            Maximum exposure time in microseconds

        2. setAutoExposureLimit(self: depthai.CameraControl, maxExposureTime: datetime.timedelta) -> depthai.CameraControl

        Set a command to specify the maximum exposure time limit for auto-exposure. By
        default the AE algorithm prioritizes increasing exposure over ISO, up to around
        frame-time (subject to further limits imposed by anti-banding)

        Parameter ``maxExposureTime``:
            Maximum exposure time
        """
    def setAutoExposureLock(self, lock: bool) -> CameraControl:
        """setAutoExposureLock(self: depthai.CameraControl, lock: bool) -> depthai.CameraControl

        Set a command to stop the auto-exposure algorithm. The latest AE sensor
        configuration is kept.

        Parameter ``lock``:
            Auto exposure lock mode enabled or disabled
        """
    def setAutoExposureRegion(self, startX: int, startY: int, width: int, height: int) -> CameraControl:
        """setAutoExposureRegion(self: depthai.CameraControl, startX: int, startY: int, width: int, height: int) -> depthai.CameraControl

        Set a command to specify auto exposure region in pixels. Note: the region should
        be mapped to the configured sensor resolution, before ISP scaling

        Parameter ``startX``:
            X coordinate of top left corner of region

        Parameter ``startY``:
            Y coordinate of top left corner of region

        Parameter ``width``:
            Region width

        Parameter ``height``:
            Region height
        """
    def setAutoFocusLensRange(self, infinityPosition: int, macroPosition: int) -> CameraControl:
        """setAutoFocusLensRange(self: depthai.CameraControl, infinityPosition: int, macroPosition: int) -> depthai.CameraControl

        Set autofocus lens range, `infinityPosition < macroPosition`, valid values
        `0..255`. May help to improve autofocus in case the lens adjustment is not
        typical/tuned
        """
    def setAutoFocusMode(self, mode: CameraControl.AutoFocusMode) -> CameraControl:
        """setAutoFocusMode(self: depthai.CameraControl, mode: depthai.CameraControl.AutoFocusMode) -> depthai.CameraControl

        Set a command to specify autofocus mode. Default `CONTINUOUS_VIDEO`
        """
    def setAutoFocusRegion(self, startX: int, startY: int, width: int, height: int) -> CameraControl:
        """setAutoFocusRegion(self: depthai.CameraControl, startX: int, startY: int, width: int, height: int) -> depthai.CameraControl

        Set a command to specify focus region in pixels. Note: the region should be
        mapped to the configured sensor resolution, before ISP scaling

        Parameter ``startX``:
            X coordinate of top left corner of region

        Parameter ``startY``:
            Y coordinate of top left corner of region

        Parameter ``width``:
            Region width

        Parameter ``height``:
            Region height
        """
    def setAutoFocusTrigger(self) -> CameraControl:
        """setAutoFocusTrigger(self: depthai.CameraControl) -> depthai.CameraControl

        Set a command to trigger autofocus
        """
    def setAutoWhiteBalanceLock(self, lock: bool) -> CameraControl:
        """setAutoWhiteBalanceLock(self: depthai.CameraControl, lock: bool) -> depthai.CameraControl

        Set a command to specify auto white balance lock

        Parameter ``lock``:
            Auto white balance lock mode enabled or disabled
        """
    def setAutoWhiteBalanceMode(self, mode: CameraControl.AutoWhiteBalanceMode) -> CameraControl:
        """setAutoWhiteBalanceMode(self: depthai.CameraControl, mode: depthai.CameraControl.AutoWhiteBalanceMode) -> depthai.CameraControl

        Set a command to specify auto white balance mode

        Parameter ``mode``:
            Auto white balance mode to use. Default `AUTO`
        """
    def setBrightness(self, value: int) -> CameraControl:
        """setBrightness(self: depthai.CameraControl, value: int) -> depthai.CameraControl

        Set a command to adjust image brightness

        Parameter ``value``:
            Brightness, range -10..10, default 0
        """
    def setCaptureIntent(self, mode: CameraControl.CaptureIntent) -> CameraControl:
        """setCaptureIntent(self: depthai.CameraControl, mode: depthai.CameraControl.CaptureIntent) -> depthai.CameraControl

        Set a command to specify capture intent mode

        Parameter ``mode``:
            Capture intent mode
        """
    def setCaptureStill(self, capture: bool) -> CameraControl:
        """setCaptureStill(self: depthai.CameraControl, capture: bool) -> depthai.CameraControl

        Set a command to capture a still image
        """
    def setChromaDenoise(self, value: int) -> CameraControl:
        """setChromaDenoise(self: depthai.CameraControl, value: int) -> depthai.CameraControl

        Set a command to adjust chroma denoise amount

        Parameter ``value``:
            Chroma denoise amount, range 0..4, default 1
        """
    def setCommand(self, arg0: CameraControl.Command, arg1: bool) -> None:
        """setCommand(self: depthai.CameraControl, arg0: depthai.CameraControl.Command, arg1: bool) -> None"""
    def setContrast(self, value: int) -> CameraControl:
        """setContrast(self: depthai.CameraControl, value: int) -> depthai.CameraControl

        Set a command to adjust image contrast

        Parameter ``value``:
            Contrast, range -10..10, default 0
        """
    def setControlMode(self, mode: CameraControl.ControlMode) -> CameraControl:
        """setControlMode(self: depthai.CameraControl, mode: depthai.CameraControl.ControlMode) -> depthai.CameraControl

        Set a command to specify control mode

        Parameter ``mode``:
            Control mode
        """
    def setEffectMode(self, mode: CameraControl.EffectMode) -> CameraControl:
        """setEffectMode(self: depthai.CameraControl, mode: depthai.CameraControl.EffectMode) -> depthai.CameraControl

        Set a command to specify effect mode

        Parameter ``mode``:
            Effect mode
        """
    def setExternalTrigger(self, numFramesBurst: int, numFramesDiscard: int) -> CameraControl:
        """setExternalTrigger(self: depthai.CameraControl, numFramesBurst: int, numFramesDiscard: int) -> depthai.CameraControl

        Set a command to enable external trigger snapshot mode

        A rising edge on the sensor FSIN pin will make it capture a sequence of
        `numFramesBurst` frames. First `numFramesDiscard` will be skipped as configured
        (can be set to 0 as well), as they may have degraded quality
        """
    def setFrameSyncMode(self, mode: CameraControl.FrameSyncMode) -> CameraControl:
        """setFrameSyncMode(self: depthai.CameraControl, mode: depthai.CameraControl.FrameSyncMode) -> depthai.CameraControl

        Set the frame sync mode for continuous streaming operation mode, translating to
        how the camera pin FSIN/FSYNC is used: input/output/disabled
        """
    def setHdr(self, enable: bool) -> CameraControl:
        """setHdr(self: depthai.CameraControl, enable: bool) -> depthai.CameraControl

        Whether or not to enable HDR (High Dynamic Range) mode

        Parameter ``enable``:
            True to enable HDR mode, false to disable
        """
    def setLumaDenoise(self, value: int) -> CameraControl:
        """setLumaDenoise(self: depthai.CameraControl, value: int) -> depthai.CameraControl

        Set a command to adjust luma denoise amount

        Parameter ``value``:
            Luma denoise amount, range 0..4, default 1
        """
    @overload
    def setManualExposure(self, exposureTimeUs: int, sensitivityIso: int) -> CameraControl:
        """setManualExposure(*args, **kwargs)
        Overloaded function.

        1. setManualExposure(self: depthai.CameraControl, exposureTimeUs: int, sensitivityIso: int) -> depthai.CameraControl

        Set a command to manually specify exposure

        Parameter ``exposureTimeUs``:
            Exposure time in microseconds

        Parameter ``sensitivityIso``:
            Sensitivity as ISO value, usual range 100..1600

        2. setManualExposure(self: depthai.CameraControl, exposureTime: datetime.timedelta, sensitivityIso: int) -> depthai.CameraControl

        Set a command to manually specify exposure

        Parameter ``exposureTime``:
            Exposure time

        Parameter ``sensitivityIso``:
            Sensitivity as ISO value, usual range 100..1600
        """
    @overload
    def setManualExposure(self, exposureTime: datetime.timedelta, sensitivityIso: int) -> CameraControl:
        """setManualExposure(*args, **kwargs)
        Overloaded function.

        1. setManualExposure(self: depthai.CameraControl, exposureTimeUs: int, sensitivityIso: int) -> depthai.CameraControl

        Set a command to manually specify exposure

        Parameter ``exposureTimeUs``:
            Exposure time in microseconds

        Parameter ``sensitivityIso``:
            Sensitivity as ISO value, usual range 100..1600

        2. setManualExposure(self: depthai.CameraControl, exposureTime: datetime.timedelta, sensitivityIso: int) -> depthai.CameraControl

        Set a command to manually specify exposure

        Parameter ``exposureTime``:
            Exposure time

        Parameter ``sensitivityIso``:
            Sensitivity as ISO value, usual range 100..1600
        """
    def setManualFocus(self, lensPosition: int) -> CameraControl:
        """setManualFocus(self: depthai.CameraControl, lensPosition: int) -> depthai.CameraControl

        Set a command to specify manual focus position

        Parameter ``lensPosition``:
            specify lens position 0..255
        """
    def setManualFocusRaw(self, lensPositionRaw: float) -> CameraControl:
        """setManualFocusRaw(self: depthai.CameraControl, lensPositionRaw: float) -> depthai.CameraControl

        Set a command to specify manual focus position (more precise control).

        Parameter ``lensPositionRaw``:
            specify lens position 0.0f .. 1.0f

        Returns:
            CameraControl&
        """
    def setManualWhiteBalance(self, colorTemperatureK: int) -> CameraControl:
        """setManualWhiteBalance(self: depthai.CameraControl, colorTemperatureK: int) -> depthai.CameraControl

        Set a command to manually specify white-balance color correction

        Parameter ``colorTemperatureK``:
            Light source color temperature in kelvins, range 1000..12000
        """
    @overload
    def setMisc(self, control: str, value: str) -> CameraControl:
        """setMisc(*args, **kwargs)
        Overloaded function.

        1. setMisc(self: depthai.CameraControl, control: str, value: str) -> depthai.CameraControl

        Set a miscellaneous control. The controls set by this function get appended to a
        list, processed after the standard controls

        Parameter ``control``:
            Control name

        Parameter ``value``:
            Value as a string

        2. setMisc(self: depthai.CameraControl, control: str, value: int) -> depthai.CameraControl

        Set a miscellaneous control. The controls set by this function get appended to a
        list, processed after the standard controls

        Parameter ``control``:
            Control name

        Parameter ``value``:
            Value as an integer number

        3. setMisc(self: depthai.CameraControl, control: str, value: float) -> depthai.CameraControl

        Set a miscellaneous control. The controls set by this function get appended to a
        list, processed after the standard controls

        Parameter ``control``:
            Control name

        Parameter ``value``:
            Value as a floating point number
        """
    @overload
    def setMisc(self, control: str, value: int) -> CameraControl:
        """setMisc(*args, **kwargs)
        Overloaded function.

        1. setMisc(self: depthai.CameraControl, control: str, value: str) -> depthai.CameraControl

        Set a miscellaneous control. The controls set by this function get appended to a
        list, processed after the standard controls

        Parameter ``control``:
            Control name

        Parameter ``value``:
            Value as a string

        2. setMisc(self: depthai.CameraControl, control: str, value: int) -> depthai.CameraControl

        Set a miscellaneous control. The controls set by this function get appended to a
        list, processed after the standard controls

        Parameter ``control``:
            Control name

        Parameter ``value``:
            Value as an integer number

        3. setMisc(self: depthai.CameraControl, control: str, value: float) -> depthai.CameraControl

        Set a miscellaneous control. The controls set by this function get appended to a
        list, processed after the standard controls

        Parameter ``control``:
            Control name

        Parameter ``value``:
            Value as a floating point number
        """
    @overload
    def setMisc(self, control: str, value: float) -> CameraControl:
        """setMisc(*args, **kwargs)
        Overloaded function.

        1. setMisc(self: depthai.CameraControl, control: str, value: str) -> depthai.CameraControl

        Set a miscellaneous control. The controls set by this function get appended to a
        list, processed after the standard controls

        Parameter ``control``:
            Control name

        Parameter ``value``:
            Value as a string

        2. setMisc(self: depthai.CameraControl, control: str, value: int) -> depthai.CameraControl

        Set a miscellaneous control. The controls set by this function get appended to a
        list, processed after the standard controls

        Parameter ``control``:
            Control name

        Parameter ``value``:
            Value as an integer number

        3. setMisc(self: depthai.CameraControl, control: str, value: float) -> depthai.CameraControl

        Set a miscellaneous control. The controls set by this function get appended to a
        list, processed after the standard controls

        Parameter ``control``:
            Control name

        Parameter ``value``:
            Value as a floating point number
        """
    def setSaturation(self, value: int) -> CameraControl:
        """setSaturation(self: depthai.CameraControl, value: int) -> depthai.CameraControl

        Set a command to adjust image saturation

        Parameter ``value``:
            Saturation, range -10..10, default 0
        """
    def setSceneMode(self, mode: CameraControl.SceneMode) -> CameraControl:
        """setSceneMode(self: depthai.CameraControl, mode: depthai.CameraControl.SceneMode) -> depthai.CameraControl

        Set a command to specify scene mode

        Parameter ``mode``:
            Scene mode
        """
    def setSharpness(self, value: int) -> CameraControl:
        """setSharpness(self: depthai.CameraControl, value: int) -> depthai.CameraControl

        Set a command to adjust image sharpness

        Parameter ``value``:
            Sharpness, range 0..4, default 1
        """
    def setStartStreaming(self) -> CameraControl:
        """setStartStreaming(self: depthai.CameraControl) -> depthai.CameraControl

        Set a command to start streaming
        """
    def setStopStreaming(self) -> CameraControl:
        """setStopStreaming(self: depthai.CameraControl) -> depthai.CameraControl

        Set a command to stop streaming
        """
    def setStrobeDisable(self) -> CameraControl:
        """setStrobeDisable(self: depthai.CameraControl) -> depthai.CameraControl

        Disable STROBE output
        """
    def setStrobeExternal(self, gpioNumber: int, activeLevel: int) -> CameraControl:
        """setStrobeExternal(self: depthai.CameraControl, gpioNumber: int, activeLevel: int) -> depthai.CameraControl

        Enable STROBE output driven by a MyriadX GPIO, optionally configuring the
        polarity This normally requires a FSIN/FSYNC/trigger input for MyriadX (usually
        GPIO 41), to generate timings
        """
    def setStrobeSensor(self, activeLevel: int) -> CameraControl:
        """setStrobeSensor(self: depthai.CameraControl, activeLevel: int) -> depthai.CameraControl

        Enable STROBE output on sensor pin, optionally configuring the polarity. Note:
        for many sensors the polarity is high-active and not configurable
        """

class CameraExposureOffset:
    """Members:

      START

      MIDDLE

      END"""
    __members__: ClassVar[dict] = ...  # read-only
    END: ClassVar[CameraExposureOffset] = ...
    MIDDLE: ClassVar[CameraExposureOffset] = ...
    START: ClassVar[CameraExposureOffset] = ...
    __entries: ClassVar[dict] = ...
    def __init__(self, value: int) -> None:
        """__init__(self: depthai.CameraExposureOffset, value: int) -> None"""
    def __eq__(self, other: object) -> bool:
        """__eq__(self: object, other: object) -> bool"""
    def __hash__(self) -> int:
        """__hash__(self: object) -> int"""
    def __index__(self) -> int:
        """__index__(self: depthai.CameraExposureOffset) -> int"""
    def __int__(self) -> int:
        """__int__(self: depthai.CameraExposureOffset) -> int"""
    def __ne__(self, other: object) -> bool:
        """__ne__(self: object, other: object) -> bool"""
    @property
    def name(self) -> str:
        """name(self: object) -> str

        name(self: object) -> str
        """
    @property
    def value(self) -> int:
        """(arg0: depthai.CameraExposureOffset) -> int"""

class CameraFeatures:
    """CameraFeatures structure

    Characterizes detected cameras on board"""
    calibrationResolution: CameraSensorConfig | None
    configs: list[CameraSensorConfig]
    hasAutofocus: bool
    hasAutofocusIC: bool
    height: int
    name: str
    orientation: CameraImageOrientation
    sensorName: str
    socket: CameraBoardSocket
    supportedTypes: list[CameraSensorType]
    width: int
    def __init__(self) -> None:
        """__init__(self: depthai.CameraFeatures) -> None"""

class CameraImageOrientation:
    """Camera sensor image orientation / pixel readout. This exposes direct sensor
    settings. 90 or 270 degrees rotation is not available.

    AUTO denotes that the decision will be made by device (e.g. on OAK-1/megaAI:
    ROTATE_180_DEG).

    Members:

      AUTO

      NORMAL

      HORIZONTAL_MIRROR

      VERTICAL_FLIP

      ROTATE_180_DEG"""
    __members__: ClassVar[dict] = ...  # read-only
    AUTO: ClassVar[CameraImageOrientation] = ...
    HORIZONTAL_MIRROR: ClassVar[CameraImageOrientation] = ...
    NORMAL: ClassVar[CameraImageOrientation] = ...
    ROTATE_180_DEG: ClassVar[CameraImageOrientation] = ...
    VERTICAL_FLIP: ClassVar[CameraImageOrientation] = ...
    __entries: ClassVar[dict] = ...
    def __init__(self, value: int) -> None:
        """__init__(self: depthai.CameraImageOrientation, value: int) -> None"""
    def __eq__(self, other: object) -> bool:
        """__eq__(self: object, other: object) -> bool"""
    def __hash__(self) -> int:
        """__hash__(self: object) -> int"""
    def __index__(self) -> int:
        """__index__(self: depthai.CameraImageOrientation) -> int"""
    def __int__(self) -> int:
        """__int__(self: depthai.CameraImageOrientation) -> int"""
    def __ne__(self, other: object) -> bool:
        """__ne__(self: object, other: object) -> bool"""
    @property
    def name(self) -> str:
        """name(self: object) -> str

        name(self: object) -> str
        """
    @property
    def value(self) -> int:
        """(arg0: depthai.CameraImageOrientation) -> int"""

class CameraInfo:
    """CameraInfo structure"""
    cameraType: CameraModel
    distortionCoeff: list[float]
    extrinsics: Extrinsics
    height: int
    intrinsicMatrix: list[list[float]]
    specHfovDeg: float
    width: int
    def __init__(self) -> None:
        """__init__(self: depthai.CameraInfo) -> None"""

class CameraModel:
    """Which CameraModel to initialize the calibration with.

    Members:

      Perspective

      Fisheye

      Equirectangular

      RadialDivision"""
    __members__: ClassVar[dict] = ...  # read-only
    Equirectangular: ClassVar[CameraModel] = ...
    Fisheye: ClassVar[CameraModel] = ...
    Perspective: ClassVar[CameraModel] = ...
    RadialDivision: ClassVar[CameraModel] = ...
    __entries: ClassVar[dict] = ...
    def __init__(self, value: int) -> None:
        """__init__(self: depthai.CameraModel, value: int) -> None"""
    def __eq__(self, other: object) -> bool:
        """__eq__(self: object, other: object) -> bool"""
    def __hash__(self) -> int:
        """__hash__(self: object) -> int"""
    def __index__(self) -> int:
        """__index__(self: depthai.CameraModel) -> int"""
    def __int__(self) -> int:
        """__int__(self: depthai.CameraModel) -> int"""
    def __ne__(self, other: object) -> bool:
        """__ne__(self: object, other: object) -> bool"""
    @property
    def name(self) -> str:
        """name(self: object) -> str

        name(self: object) -> str
        """
    @property
    def value(self) -> int:
        """(arg0: depthai.CameraModel) -> int"""

class CameraSensorConfig:
    """Sensor config"""
    fov: Rect
    height: int
    maxFps: float
    minFps: float
    type: CameraSensorType
    width: int
    def __init__(self) -> None:
        """__init__(self: depthai.CameraSensorConfig) -> None"""

class CameraSensorType:
    """Camera sensor type

    Members:

      COLOR

      MONO

      TOF

      THERMAL"""
    __members__: ClassVar[dict] = ...  # read-only
    COLOR: ClassVar[CameraSensorType] = ...
    MONO: ClassVar[CameraSensorType] = ...
    THERMAL: ClassVar[CameraSensorType] = ...
    TOF: ClassVar[CameraSensorType] = ...
    __entries: ClassVar[dict] = ...
    def __init__(self, value: int) -> None:
        """__init__(self: depthai.CameraSensorType, value: int) -> None"""
    def __eq__(self, other: object) -> bool:
        """__eq__(self: object, other: object) -> bool"""
    def __hash__(self) -> int:
        """__hash__(self: object) -> int"""
    def __index__(self) -> int:
        """__index__(self: depthai.CameraSensorType) -> int"""
    def __int__(self) -> int:
        """__int__(self: depthai.CameraSensorType) -> int"""
    def __ne__(self, other: object) -> bool:
        """__ne__(self: object, other: object) -> bool"""
    @property
    def name(self) -> str:
        """name(self: object) -> str

        name(self: object) -> str
        """
    @property
    def value(self) -> int:
        """(arg0: depthai.CameraSensorType) -> int"""

class Capability:
    def __init__(self, *args, **kwargs) -> None:
        """Initialize self.  See help(type(self)) for accurate signature."""

class CapabilityRangeFloat:
    def __init__(self, *args, **kwargs) -> None:
        """Initialize self.  See help(type(self)) for accurate signature."""
    def discrete(self, arg0: list[float]) -> None:
        """discrete(self: depthai.CapabilityRangeFloat, arg0: list[float]) -> None"""
    def fixed(self, arg0: float) -> None:
        """fixed(self: depthai.CapabilityRangeFloat, arg0: float) -> None"""
    @overload
    def minMax(self, arg0: tuple[float, float]) -> None:
        """minMax(*args, **kwargs)
        Overloaded function.

        1. minMax(self: depthai.CapabilityRangeFloat, arg0: tuple[float, float]) -> None

        2. minMax(self: depthai.CapabilityRangeFloat, arg0: tuple[float, float]) -> None

        3. minMax(self: depthai.CapabilityRangeFloat, arg0: float, arg1: float) -> None
        """
    @overload
    def minMax(self, arg0: tuple[float, float]) -> None:
        """minMax(*args, **kwargs)
        Overloaded function.

        1. minMax(self: depthai.CapabilityRangeFloat, arg0: tuple[float, float]) -> None

        2. minMax(self: depthai.CapabilityRangeFloat, arg0: tuple[float, float]) -> None

        3. minMax(self: depthai.CapabilityRangeFloat, arg0: float, arg1: float) -> None
        """
    @overload
    def minMax(self, arg0: float, arg1: float) -> None:
        """minMax(*args, **kwargs)
        Overloaded function.

        1. minMax(self: depthai.CapabilityRangeFloat, arg0: tuple[float, float]) -> None

        2. minMax(self: depthai.CapabilityRangeFloat, arg0: tuple[float, float]) -> None

        3. minMax(self: depthai.CapabilityRangeFloat, arg0: float, arg1: float) -> None
        """

class CapabilityRangeFloatPair:
    def __init__(self, *args, **kwargs) -> None:
        """Initialize self.  See help(type(self)) for accurate signature."""
    def discrete(self, arg0: list[tuple[float, float]]) -> None:
        """discrete(self: depthai.CapabilityRangeFloatPair, arg0: list[tuple[float, float]]) -> None"""
    def fixed(self, arg0: tuple[float, float]) -> None:
        """fixed(self: depthai.CapabilityRangeFloatPair, arg0: tuple[float, float]) -> None"""
    @overload
    def minMax(self, arg0: tuple[tuple[float, float], tuple[float, float]]) -> None:
        """minMax(*args, **kwargs)
        Overloaded function.

        1. minMax(self: depthai.CapabilityRangeFloatPair, arg0: tuple[tuple[float, float], tuple[float, float]]) -> None

        2. minMax(self: depthai.CapabilityRangeFloatPair, arg0: tuple[tuple[float, float], tuple[float, float]]) -> None

        3. minMax(self: depthai.CapabilityRangeFloatPair, arg0: tuple[float, float], arg1: tuple[float, float]) -> None
        """
    @overload
    def minMax(self, arg0: tuple[tuple[float, float], tuple[float, float]]) -> None:
        """minMax(*args, **kwargs)
        Overloaded function.

        1. minMax(self: depthai.CapabilityRangeFloatPair, arg0: tuple[tuple[float, float], tuple[float, float]]) -> None

        2. minMax(self: depthai.CapabilityRangeFloatPair, arg0: tuple[tuple[float, float], tuple[float, float]]) -> None

        3. minMax(self: depthai.CapabilityRangeFloatPair, arg0: tuple[float, float], arg1: tuple[float, float]) -> None
        """
    @overload
    def minMax(self, arg0: tuple[float, float], arg1: tuple[float, float]) -> None:
        """minMax(*args, **kwargs)
        Overloaded function.

        1. minMax(self: depthai.CapabilityRangeFloatPair, arg0: tuple[tuple[float, float], tuple[float, float]]) -> None

        2. minMax(self: depthai.CapabilityRangeFloatPair, arg0: tuple[tuple[float, float], tuple[float, float]]) -> None

        3. minMax(self: depthai.CapabilityRangeFloatPair, arg0: tuple[float, float], arg1: tuple[float, float]) -> None
        """

class CapabilityRangeUint:
    def __init__(self, *args, **kwargs) -> None:
        """Initialize self.  See help(type(self)) for accurate signature."""
    def discrete(self, arg0: list[int]) -> None:
        """discrete(self: depthai.CapabilityRangeUint, arg0: list[int]) -> None"""
    def fixed(self, arg0: int) -> None:
        """fixed(self: depthai.CapabilityRangeUint, arg0: int) -> None"""
    @overload
    def minMax(self, arg0: tuple[int, int]) -> None:
        """minMax(*args, **kwargs)
        Overloaded function.

        1. minMax(self: depthai.CapabilityRangeUint, arg0: tuple[int, int]) -> None

        2. minMax(self: depthai.CapabilityRangeUint, arg0: tuple[int, int]) -> None

        3. minMax(self: depthai.CapabilityRangeUint, arg0: int, arg1: int) -> None
        """
    @overload
    def minMax(self, arg0: tuple[int, int]) -> None:
        """minMax(*args, **kwargs)
        Overloaded function.

        1. minMax(self: depthai.CapabilityRangeUint, arg0: tuple[int, int]) -> None

        2. minMax(self: depthai.CapabilityRangeUint, arg0: tuple[int, int]) -> None

        3. minMax(self: depthai.CapabilityRangeUint, arg0: int, arg1: int) -> None
        """
    @overload
    def minMax(self, arg0: int, arg1: int) -> None:
        """minMax(*args, **kwargs)
        Overloaded function.

        1. minMax(self: depthai.CapabilityRangeUint, arg0: tuple[int, int]) -> None

        2. minMax(self: depthai.CapabilityRangeUint, arg0: tuple[int, int]) -> None

        3. minMax(self: depthai.CapabilityRangeUint, arg0: int, arg1: int) -> None
        """

class CapabilityRangeUintPair:
    def __init__(self, *args, **kwargs) -> None:
        """Initialize self.  See help(type(self)) for accurate signature."""
    def discrete(self, arg0: list[tuple[int, int]]) -> None:
        """discrete(self: depthai.CapabilityRangeUintPair, arg0: list[tuple[int, int]]) -> None"""
    def fixed(self, arg0: tuple[int, int]) -> None:
        """fixed(self: depthai.CapabilityRangeUintPair, arg0: tuple[int, int]) -> None"""
    @overload
    def minMax(self, arg0: tuple[tuple[int, int], tuple[int, int]]) -> None:
        """minMax(*args, **kwargs)
        Overloaded function.

        1. minMax(self: depthai.CapabilityRangeUintPair, arg0: tuple[tuple[int, int], tuple[int, int]]) -> None

        2. minMax(self: depthai.CapabilityRangeUintPair, arg0: tuple[tuple[int, int], tuple[int, int]]) -> None

        3. minMax(self: depthai.CapabilityRangeUintPair, arg0: tuple[int, int], arg1: tuple[int, int]) -> None
        """
    @overload
    def minMax(self, arg0: tuple[tuple[int, int], tuple[int, int]]) -> None:
        """minMax(*args, **kwargs)
        Overloaded function.

        1. minMax(self: depthai.CapabilityRangeUintPair, arg0: tuple[tuple[int, int], tuple[int, int]]) -> None

        2. minMax(self: depthai.CapabilityRangeUintPair, arg0: tuple[tuple[int, int], tuple[int, int]]) -> None

        3. minMax(self: depthai.CapabilityRangeUintPair, arg0: tuple[int, int], arg1: tuple[int, int]) -> None
        """
    @overload
    def minMax(self, arg0: tuple[int, int], arg1: tuple[int, int]) -> None:
        """minMax(*args, **kwargs)
        Overloaded function.

        1. minMax(self: depthai.CapabilityRangeUintPair, arg0: tuple[tuple[int, int], tuple[int, int]]) -> None

        2. minMax(self: depthai.CapabilityRangeUintPair, arg0: tuple[tuple[int, int], tuple[int, int]]) -> None

        3. minMax(self: depthai.CapabilityRangeUintPair, arg0: tuple[int, int], arg1: tuple[int, int]) -> None
        """

class ChipTemperature:
    """Chip temperature information.

    Multiple temperature measurement points and their average"""
    average: float
    css: float
    dss: float
    mss: float
    upa: float
    def __init__(self) -> None:
        """__init__(self: depthai.ChipTemperature) -> None"""

class ChipTemperatureRVC4:
    """Chip temperature information.

    Multiple temperature measurement points and their average"""
    average: float
    camera: float
    cpuss: float
    ddr: float
    gpuss: float
    mdmss: float
    video: float
    def __init__(self) -> None:
        """__init__(self: depthai.ChipTemperatureRVC4) -> None"""

class CircleAnnotation:
    diameter: float
    fillColor: Color
    outlineColor: Color
    position: Point2f
    thickness: float
    def __init__(self) -> None:
        """__init__(self: depthai.CircleAnnotation) -> None"""

class Clock:
    def __init__(self, *args, **kwargs) -> None:
        """Initialize self.  See help(type(self)) for accurate signature."""
    def now(self) -> datetime.timedelta:
        """now() -> datetime.timedelta"""

class Color:
    """Color structure

    r,g,b,a color values with values in range [0.0, 1.0]"""
    a: float
    b: float
    g: float
    r: float
    @overload
    def __init__(self) -> None:
        """__init__(*args, **kwargs)
        Overloaded function.

        1. __init__(self: depthai.Color) -> None

        2. __init__(self: depthai.Color, r: float, g: float, b: float, a: float = 1.0) -> None
        """
    @overload
    def __init__(self, r: float, g: float, b: float, a: float = ...) -> None:
        """__init__(*args, **kwargs)
        Overloaded function.

        1. __init__(self: depthai.Color) -> None

        2. __init__(self: depthai.Color, r: float, g: float, b: float, a: float = 1.0) -> None
        """

class ColorCameraProperties:
    """Specify properties for ColorCamera such as camera ID, ..."""

    class ColorOrder:
        """For 24 bit color these can be either RGB or BGR

        Members:

          BGR

          RGB"""
        __members__: ClassVar[dict] = ...  # read-only
        BGR: ClassVar[ColorCameraProperties.ColorOrder] = ...
        RGB: ClassVar[ColorCameraProperties.ColorOrder] = ...
        __entries: ClassVar[dict] = ...
        def __init__(self, value: int) -> None:
            """__init__(self: depthai.ColorCameraProperties.ColorOrder, value: int) -> None"""
        def __eq__(self, other: object) -> bool:
            """__eq__(self: object, other: object) -> bool"""
        def __hash__(self) -> int:
            """__hash__(self: object) -> int"""
        def __index__(self) -> int:
            """__index__(self: depthai.ColorCameraProperties.ColorOrder) -> int"""
        def __int__(self) -> int:
            """__int__(self: depthai.ColorCameraProperties.ColorOrder) -> int"""
        def __ne__(self, other: object) -> bool:
            """__ne__(self: object, other: object) -> bool"""
        @property
        def name(self) -> str:
            """name(self: object) -> str

            name(self: object) -> str
            """
        @property
        def value(self) -> int:
            """(arg0: depthai.ColorCameraProperties.ColorOrder) -> int"""

    class SensorResolution:
        """Select the camera sensor resolution

        Members:

          THE_1080_P

          THE_1200_P

          THE_4_K

          THE_5_MP

          THE_12_MP

          THE_4000X3000

          THE_13_MP

          THE_5312X6000

          THE_48_MP

          THE_720_P

          THE_800_P

          THE_240X180

          THE_1280X962

          THE_2000X1500

          THE_2028X1520

          THE_2104X1560

          THE_1440X1080

          THE_1352X1012

          THE_2024X1520"""
        __members__: ClassVar[dict] = ...  # read-only
        THE_1080_P: ClassVar[ColorCameraProperties.SensorResolution] = ...
        THE_1200_P: ClassVar[ColorCameraProperties.SensorResolution] = ...
        THE_1280X962: ClassVar[ColorCameraProperties.SensorResolution] = ...
        THE_12_MP: ClassVar[ColorCameraProperties.SensorResolution] = ...
        THE_1352X1012: ClassVar[ColorCameraProperties.SensorResolution] = ...
        THE_13_MP: ClassVar[ColorCameraProperties.SensorResolution] = ...
        THE_1440X1080: ClassVar[ColorCameraProperties.SensorResolution] = ...
        THE_2000X1500: ClassVar[ColorCameraProperties.SensorResolution] = ...
        THE_2024X1520: ClassVar[ColorCameraProperties.SensorResolution] = ...
        THE_2028X1520: ClassVar[ColorCameraProperties.SensorResolution] = ...
        THE_2104X1560: ClassVar[ColorCameraProperties.SensorResolution] = ...
        THE_240X180: ClassVar[ColorCameraProperties.SensorResolution] = ...
        THE_4000X3000: ClassVar[ColorCameraProperties.SensorResolution] = ...
        THE_48_MP: ClassVar[ColorCameraProperties.SensorResolution] = ...
        THE_4_K: ClassVar[ColorCameraProperties.SensorResolution] = ...
        THE_5312X6000: ClassVar[ColorCameraProperties.SensorResolution] = ...
        THE_5_MP: ClassVar[ColorCameraProperties.SensorResolution] = ...
        THE_720_P: ClassVar[ColorCameraProperties.SensorResolution] = ...
        THE_800_P: ClassVar[ColorCameraProperties.SensorResolution] = ...
        __entries: ClassVar[dict] = ...
        def __init__(self, value: int) -> None:
            """__init__(self: depthai.ColorCameraProperties.SensorResolution, value: int) -> None"""
        def __eq__(self, other: object) -> bool:
            """__eq__(self: object, other: object) -> bool"""
        def __hash__(self) -> int:
            """__hash__(self: object) -> int"""
        def __index__(self) -> int:
            """__index__(self: depthai.ColorCameraProperties.SensorResolution) -> int"""
        def __int__(self) -> int:
            """__int__(self: depthai.ColorCameraProperties.SensorResolution) -> int"""
        def __ne__(self, other: object) -> bool:
            """__ne__(self: object, other: object) -> bool"""
        @property
        def name(self) -> str:
            """name(self: object) -> str

            name(self: object) -> str
            """
        @property
        def value(self) -> int:
            """(arg0: depthai.ColorCameraProperties.SensorResolution) -> int"""

    class WarpMeshSource:
        """Warp mesh source

        Members:

          AUTO

          NONE

          CALIBRATION

          URI"""
        __members__: ClassVar[dict] = ...  # read-only
        AUTO: ClassVar[ColorCameraProperties.WarpMeshSource] = ...
        CALIBRATION: ClassVar[ColorCameraProperties.WarpMeshSource] = ...
        NONE: ClassVar[ColorCameraProperties.WarpMeshSource] = ...
        URI: ClassVar[ColorCameraProperties.WarpMeshSource] = ...
        __entries: ClassVar[dict] = ...
        def __init__(self, value: int) -> None:
            """__init__(self: depthai.ColorCameraProperties.WarpMeshSource, value: int) -> None"""
        def __eq__(self, other: object) -> bool:
            """__eq__(self: object, other: object) -> bool"""
        def __hash__(self) -> int:
            """__hash__(self: object) -> int"""
        def __index__(self) -> int:
            """__index__(self: depthai.ColorCameraProperties.WarpMeshSource) -> int"""
        def __int__(self) -> int:
            """__int__(self: depthai.ColorCameraProperties.WarpMeshSource) -> int"""
        def __ne__(self, other: object) -> bool:
            """__ne__(self: object, other: object) -> bool"""
        @property
        def name(self) -> str:
            """name(self: object) -> str

            name(self: object) -> str
            """
        @property
        def value(self) -> int:
            """(arg0: depthai.ColorCameraProperties.WarpMeshSource) -> int"""
    boardSocket: CameraBoardSocket
    calibAlpha: float
    eventFilter: list[FrameEvent]
    fps: float
    imageOrientation: CameraImageOrientation
    initialControl: CameraControl
    isp3aFps: int
    ispScale: Incomplete
    numFramesPoolIsp: int
    numFramesPoolPreview: int
    numFramesPoolRaw: int
    numFramesPoolStill: int
    numFramesPoolVideo: int
    previewHeight: int
    previewKeepAspectRatio: bool
    previewWidth: int
    resolution: ColorCameraProperties.SensorResolution
    sensorCropX: float
    sensorCropY: float
    stillHeight: int
    stillWidth: int
    videoHeight: int
    videoWidth: int
    warpMeshHeight: int
    warpMeshSource: ColorCameraProperties.WarpMeshSource
    warpMeshStepHeight: int
    warpMeshStepWidth: int
    warpMeshUri: str
    warpMeshWidth: int
    def __init__(self, *args, **kwargs) -> None:
        """Initialize self.  See help(type(self)) for accurate signature."""

class Colormap:
    """Camera sensor type

    Members:

      NONE

      JET

      TURBO

      STEREO_JET

      STEREO_TURBO"""
    __members__: ClassVar[dict] = ...  # read-only
    JET: ClassVar[Colormap] = ...
    NONE: ClassVar[Colormap] = ...
    STEREO_JET: ClassVar[Colormap] = ...
    STEREO_TURBO: ClassVar[Colormap] = ...
    TURBO: ClassVar[Colormap] = ...
    __entries: ClassVar[dict] = ...
    def __init__(self, value: int) -> None:
        """__init__(self: depthai.Colormap, value: int) -> None"""
    def __eq__(self, other: object) -> bool:
        """__eq__(self: object, other: object) -> bool"""
    def __hash__(self) -> int:
        """__hash__(self: object) -> int"""
    def __index__(self) -> int:
        """__index__(self: depthai.Colormap) -> int"""
    def __int__(self) -> int:
        """__int__(self: depthai.Colormap) -> int"""
    def __ne__(self, other: object) -> bool:
        """__ne__(self: object, other: object) -> bool"""
    @property
    def name(self) -> str:
        """name(self: object) -> str

        name(self: object) -> str
        """
    @property
    def value(self) -> int:
        """(arg0: depthai.Colormap) -> int"""

class CoverageData(Buffer):
    coverageAcquired: float
    coveragePerCellA: list[list[float]]
    coveragePerCellB: list[list[float]]
    dataAcquired: float
    meanCoverage: float
    def __init__(self) -> None:
        """__init__(self: depthai.CoverageData) -> None"""

class CpuUsage:
    """CpuUsage structure

    Average usage in percent and time span of the average (since last query)"""
    average: float
    msTime: int
    def __init__(self) -> None:
        """__init__(self: depthai.CpuUsage) -> None"""

class CrashDump:
    class CrashReport:
        class ErrorSourceInfo:
            class AssertContext:
                fileName: str
                functionName: str
                line: int
                def __init__(self) -> None:
                    """__init__(self: depthai.CrashDump.CrashReport.ErrorSourceInfo.AssertContext) -> None"""

            class TrapContext:
                trapAddress: int
                trapName: str
                trapNumber: int
                def __init__(self) -> None:
                    """__init__(self: depthai.CrashDump.CrashReport.ErrorSourceInfo.TrapContext) -> None"""
            assertContext: CrashDump.CrashReport.ErrorSourceInfo.AssertContext
            errorId: int
            trapContext: CrashDump.CrashReport.ErrorSourceInfo.TrapContext
            def __init__(self) -> None:
                """__init__(self: depthai.CrashDump.CrashReport.ErrorSourceInfo) -> None"""

        class ThreadCallstack:
            class CallstackContext:
                callSite: int
                calledTarget: int
                context: str
                framePointer: int
                def __init__(self) -> None:
                    """__init__(self: depthai.CrashDump.CrashReport.ThreadCallstack.CallstackContext) -> None"""
            callStack: list[CrashDump.CrashReport.ThreadCallstack.CallstackContext]
            instructionPointer: int
            stackBottom: int
            stackPointer: int
            stackTop: int
            threadId: int
            threadName: str
            threadStatus: str
            def __init__(self) -> None:
                """__init__(self: depthai.CrashDump.CrashReport.ThreadCallstack) -> None"""
        crashedThreadId: int
        errorSource: str
        prints: list[str]
        processor: ProcessorType
        statusFlags: int
        threadCallstack: list[CrashDump.CrashReport.ThreadCallstack]
        timerRaw: int
        uptimeNs: int
        def __init__(self) -> None:
            """__init__(self: depthai.CrashDump.CrashReport) -> None"""
    crashReports: list[CrashDump.CrashReport]
    depthaiCommitHash: str
    deviceId: str
    def __init__(self) -> None:
        """__init__(self: depthai.CrashDump) -> None"""
    def serializeToJson(self) -> json:
        """serializeToJson(self: depthai.CrashDump) -> json"""

class DatatypeEnum:
    """

    Members:

      ADatatype

      Buffer

      ImgFrame

      EncodedFrame

      NNData

      ImageManipConfig

      CameraControl

      ImgDetections

      SpatialImgDetections

      SegmentationParserConfig

      SegmentationMask

      SystemInformation

      SystemInformationRVC4

      SpatialLocationCalculatorConfig

      SpatialLocationCalculatorData

      EdgeDetectorConfig

      AprilTagConfig

      AprilTags

      Tracklets

      IMUData

      StereoDepthConfig

      FeatureTrackerConfig

      ThermalConfig

      ToFConfig

      VppConfig

      TrackedFeatures

      BenchmarkReport

      MessageGroup

      TransformData

      PointCloudConfig

      PointCloudData

      ImageAlignConfig

      ImgAnnotations

      MapData

      RGBDData

      PipelineEvent

      PipelineState

      ImageFiltersConfig

      ToFDepthConfidenceFilterConfig

      DynamicCalibrationControl

      DynamicCalibrationResult

      AutoCalibrationConfig

      AutoCalibrationResult

      CalibrationQuality

      CoverageData"""
    __members__: ClassVar[dict] = ...  # read-only
    ADatatype: ClassVar[DatatypeEnum] = ...
    AprilTagConfig: ClassVar[DatatypeEnum] = ...
    AprilTags: ClassVar[DatatypeEnum] = ...
    AutoCalibrationConfig: ClassVar[DatatypeEnum] = ...
    AutoCalibrationResult: ClassVar[DatatypeEnum] = ...
    BenchmarkReport: ClassVar[DatatypeEnum] = ...
    Buffer: ClassVar[DatatypeEnum] = ...
    CalibrationQuality: ClassVar[DatatypeEnum] = ...
    CameraControl: ClassVar[DatatypeEnum] = ...
    CoverageData: ClassVar[DatatypeEnum] = ...
    DynamicCalibrationControl: ClassVar[DatatypeEnum] = ...
    DynamicCalibrationResult: ClassVar[DatatypeEnum] = ...
    EdgeDetectorConfig: ClassVar[DatatypeEnum] = ...
    EncodedFrame: ClassVar[DatatypeEnum] = ...
    FeatureTrackerConfig: ClassVar[DatatypeEnum] = ...
    IMUData: ClassVar[DatatypeEnum] = ...
    ImageAlignConfig: ClassVar[DatatypeEnum] = ...
    ImageFiltersConfig: ClassVar[DatatypeEnum] = ...
    ImageManipConfig: ClassVar[DatatypeEnum] = ...
    ImgAnnotations: ClassVar[DatatypeEnum] = ...
    ImgDetections: ClassVar[DatatypeEnum] = ...
    ImgFrame: ClassVar[DatatypeEnum] = ...
    MapData: ClassVar[DatatypeEnum] = ...
    MessageGroup: ClassVar[DatatypeEnum] = ...
    NNData: ClassVar[DatatypeEnum] = ...
    PipelineEvent: ClassVar[DatatypeEnum] = ...
    PipelineState: ClassVar[DatatypeEnum] = ...
    PointCloudConfig: ClassVar[DatatypeEnum] = ...
    PointCloudData: ClassVar[DatatypeEnum] = ...
    RGBDData: ClassVar[DatatypeEnum] = ...
    SegmentationMask: ClassVar[DatatypeEnum] = ...
    SegmentationParserConfig: ClassVar[DatatypeEnum] = ...
    SpatialImgDetections: ClassVar[DatatypeEnum] = ...
    SpatialLocationCalculatorConfig: ClassVar[DatatypeEnum] = ...
    SpatialLocationCalculatorData: ClassVar[DatatypeEnum] = ...
    StereoDepthConfig: ClassVar[DatatypeEnum] = ...
    SystemInformation: ClassVar[DatatypeEnum] = ...
    SystemInformationRVC4: ClassVar[DatatypeEnum] = ...
    ThermalConfig: ClassVar[DatatypeEnum] = ...
    ToFConfig: ClassVar[DatatypeEnum] = ...
    ToFDepthConfidenceFilterConfig: ClassVar[DatatypeEnum] = ...
    TrackedFeatures: ClassVar[DatatypeEnum] = ...
    Tracklets: ClassVar[DatatypeEnum] = ...
    TransformData: ClassVar[DatatypeEnum] = ...
    VppConfig: ClassVar[DatatypeEnum] = ...
    __entries: ClassVar[dict] = ...
    def __init__(self, value: int) -> None:
        """__init__(self: depthai.DatatypeEnum, value: int) -> None"""
    def __eq__(self, other: object) -> bool:
        """__eq__(self: object, other: object) -> bool"""
    def __hash__(self) -> int:
        """__hash__(self: object) -> int"""
    def __index__(self) -> int:
        """__index__(self: depthai.DatatypeEnum) -> int"""
    def __int__(self) -> int:
        """__int__(self: depthai.DatatypeEnum) -> int"""
    def __ne__(self, other: object) -> bool:
        """__ne__(self: object, other: object) -> bool"""
    @property
    def name(self) -> str:
        """name(self: object) -> str

        name(self: object) -> str
        """
    @property
    def value(self) -> int:
        """(arg0: depthai.DatatypeEnum) -> int"""

class DepthUnit:
    """Measurement unit for depth and calibration data.

    Members:

      METER

      CENTIMETER

      MILLIMETER

      INCH

      FOOT

      CUSTOM"""
    __members__: ClassVar[dict] = ...  # read-only
    CENTIMETER: ClassVar[LengthUnit] = ...
    CUSTOM: ClassVar[LengthUnit] = ...
    FOOT: ClassVar[LengthUnit] = ...
    INCH: ClassVar[LengthUnit] = ...
    METER: ClassVar[LengthUnit] = ...
    MILLIMETER: ClassVar[LengthUnit] = ...
    __entries: ClassVar[dict] = ...
    def __init__(self, value: int) -> None:
        """__init__(self: depthai.LengthUnit, value: int) -> None"""
    def __eq__(self, other: object) -> bool:
        """__eq__(self: object, other: object) -> bool"""
    def __hash__(self) -> int:
        """__hash__(self: object) -> int"""
    def __index__(self) -> int:
        """__index__(self: depthai.LengthUnit) -> int"""
    def __int__(self) -> int:
        """__int__(self: depthai.LengthUnit) -> int"""
    def __ne__(self, other: object) -> bool:
        """__ne__(self: object, other: object) -> bool"""
    @property
    def name(self) -> str:
        """name(self: object) -> str

        name(self: object) -> str
        """
    @property
    def value(self) -> int:
        """(arg0: depthai.LengthUnit) -> int"""

class DetectionNetworkType:
    """Members:

      YOLO

      MOBILENET"""
    __members__: ClassVar[dict] = ...  # read-only
    MOBILENET: ClassVar[DetectionNetworkType] = ...
    YOLO: ClassVar[DetectionNetworkType] = ...
    __entries: ClassVar[dict] = ...
    def __init__(self, value: int) -> None:
        """__init__(self: depthai.DetectionNetworkType, value: int) -> None"""
    def __eq__(self, other: object) -> bool:
        """__eq__(self: object, other: object) -> bool"""
    def __hash__(self) -> int:
        """__hash__(self: object) -> int"""
    def __index__(self) -> int:
        """__index__(self: depthai.DetectionNetworkType) -> int"""
    def __int__(self) -> int:
        """__int__(self: depthai.DetectionNetworkType) -> int"""
    def __ne__(self, other: object) -> bool:
        """__ne__(self: object, other: object) -> bool"""
    @property
    def name(self) -> str:
        """name(self: object) -> str

        name(self: object) -> str
        """
    @property
    def value(self) -> int:
        """(arg0: depthai.DetectionNetworkType) -> int"""

class DetectionParserOptions:
    """DetectionParserOptions

    Specifies how to parse output of detection networks"""
    anchorMasks: dict[str, list[int]]
    anchors: list[float]
    anchorsV2: list[list[list[float]]]
    classes: int
    confidenceThreshold: float
    coordinates: int
    decodeKeypoints: bool
    decodingFamily: YoloDecodingFamily
    iouThreshold: float
    keypointEdges: Incomplete
    nnFamily: DetectionNetworkType
    numKeypoints: int | None
    outputNames: list[str]
    def __init__(self, *args, **kwargs) -> None:
        """Initialize self.  See help(type(self)) for accurate signature."""

class DetectionParserProperties:
    """Specify properties for DetectionParser"""
    parser: DetectionParserOptions
    def __init__(self, *args, **kwargs) -> None:
        """Initialize self.  See help(type(self)) for accurate signature."""

class Device(DeviceBase):
    """Represents the DepthAI device with the methods to interact with it. Implements
    the host-side queues to connect with XLinkIn and XLinkOut nodes"""

    class Config:
        """Device specific configuration"""
        board: BoardConfig
        logLevel: LogLevel | None
        nonExclusiveMode: bool
        outputLogLevel: LogLevel | None
        version: OpenVINO.Version
        def __init__(self) -> None:
            """__init__(self: depthai.Device.Config) -> None"""

    class ReconnectionStatus:
        """

        Members:

          RECONNECT_FAILED

          RECONNECTED

          RECONNECTING"""
        __members__: ClassVar[dict] = ...  # read-only
        RECONNECTED: ClassVar[Device.ReconnectionStatus] = ...
        RECONNECTING: ClassVar[Device.ReconnectionStatus] = ...
        RECONNECT_FAILED: ClassVar[Device.ReconnectionStatus] = ...
        __entries: ClassVar[dict] = ...
        def __init__(self, value: int) -> None:
            """__init__(self: depthai.Device.ReconnectionStatus, value: int) -> None"""
        def __eq__(self, other: object) -> bool:
            """__eq__(self: object, other: object) -> bool"""
        def __hash__(self) -> int:
            """__hash__(self: object) -> int"""
        def __index__(self) -> int:
            """__index__(self: depthai.Device.ReconnectionStatus) -> int"""
        def __int__(self) -> int:
            """__int__(self: depthai.Device.ReconnectionStatus) -> int"""
        def __ne__(self, other: object) -> bool:
            """__ne__(self: object, other: object) -> bool"""
        @property
        def name(self) -> str:
            """name(self: object) -> str

            name(self: object) -> str
            """
        @property
        def value(self) -> int:
            """(arg0: depthai.Device.ReconnectionStatus) -> int"""
    @overload
    def __init__(self) -> None:
        """__init__(*args, **kwargs)
        Overloaded function.

        1. __init__(self: depthai.Device) -> None

        Connects to any available device with a DEFAULT_SEARCH_TIME timeout. Uses
        OpenVINO version OpenVINO::VERSION_UNIVERSAL

        2. __init__(self: depthai.Device, maxUsbSpeed: depthai.UsbSpeed) -> None

        Connects to device

        Parameter ``maxUsbSpeed``:
            Maximum allowed USB speed

        3. __init__(self: depthai.Device, deviceInfo: depthai.DeviceInfo, maxUsbSpeed: depthai.UsbSpeed) -> None

        Connects to device specified by devInfo.

        Parameter ``devInfo``:
            DeviceInfo which specifies which device to connect to

        Parameter ``maxUsbSpeed``:
            Maximum allowed USB speed

        4. __init__(self: depthai.Device, deviceDesc: depthai.DeviceInfo, pathToCmd: os.PathLike) -> None

        Connects to device specified by devInfo.

        Parameter ``devInfo``:
            DeviceInfo which specifies which device to connect to

        Parameter ``pathToCmd``:
            Path to custom device firmware

        5. __init__(self: depthai.Device, config: depthai.Device.Config) -> None

        Connects to any available device with custom config.

        Parameter ``config``:
            Device custom configuration to boot with

        6. __init__(self: depthai.Device, config: depthai.Device.Config, deviceInfo: depthai.DeviceInfo) -> None

        Connects to device 'devInfo' with custom config.

        Parameter ``config``:
            Device custom configuration to boot with

        Parameter ``devInfo``:
            DeviceInfo which specifies which device to connect to

        7. __init__(self: depthai.Device, deviceInfo: depthai.DeviceInfo) -> None

        Connects to any available device with a DEFAULT_SEARCH_TIME timeout. Uses
        OpenVINO version OpenVINO::VERSION_UNIVERSAL

        Parameter ``devInfo``:
            DeviceInfo which specifies which device to connect to

        8. __init__(self: depthai.Device, deviceInfo: depthai.DeviceInfo, maxUsbSpeed: depthai.UsbSpeed) -> None

        Connects to any available device with a DEFAULT_SEARCH_TIME timeout. Uses
        OpenVINO version OpenVINO::VERSION_UNIVERSAL

        Parameter ``nameOrDeviceId``:
            Creates DeviceInfo with nameOrDeviceId to connect to

        9. __init__(self: depthai.Device, nameOrDeviceId: str) -> None

        Connects to any available device with a DEFAULT_SEARCH_TIME timeout. Uses
        OpenVINO version OpenVINO::VERSION_UNIVERSAL

        Parameter ``nameOrDeviceId``:
            Creates DeviceInfo with nameOrDeviceId to connect to

        Parameter ``maxUsbSpeed``:
            Maximum allowed USB speed

        10. __init__(self: depthai.Device, nameOrDeviceId: str, maxUsbSpeed: depthai.UsbSpeed) -> None

        Connects to device specified by devInfo.

        Parameter ``config``:
            Config with which the device will be booted with

        Parameter ``maxUsbSpeed``:
            Maximum allowed USB speed
        """
    @overload
    def __init__(self, maxUsbSpeed: UsbSpeed) -> None:
        """__init__(*args, **kwargs)
        Overloaded function.

        1. __init__(self: depthai.Device) -> None

        Connects to any available device with a DEFAULT_SEARCH_TIME timeout. Uses
        OpenVINO version OpenVINO::VERSION_UNIVERSAL

        2. __init__(self: depthai.Device, maxUsbSpeed: depthai.UsbSpeed) -> None

        Connects to device

        Parameter ``maxUsbSpeed``:
            Maximum allowed USB speed

        3. __init__(self: depthai.Device, deviceInfo: depthai.DeviceInfo, maxUsbSpeed: depthai.UsbSpeed) -> None

        Connects to device specified by devInfo.

        Parameter ``devInfo``:
            DeviceInfo which specifies which device to connect to

        Parameter ``maxUsbSpeed``:
            Maximum allowed USB speed

        4. __init__(self: depthai.Device, deviceDesc: depthai.DeviceInfo, pathToCmd: os.PathLike) -> None

        Connects to device specified by devInfo.

        Parameter ``devInfo``:
            DeviceInfo which specifies which device to connect to

        Parameter ``pathToCmd``:
            Path to custom device firmware

        5. __init__(self: depthai.Device, config: depthai.Device.Config) -> None

        Connects to any available device with custom config.

        Parameter ``config``:
            Device custom configuration to boot with

        6. __init__(self: depthai.Device, config: depthai.Device.Config, deviceInfo: depthai.DeviceInfo) -> None

        Connects to device 'devInfo' with custom config.

        Parameter ``config``:
            Device custom configuration to boot with

        Parameter ``devInfo``:
            DeviceInfo which specifies which device to connect to

        7. __init__(self: depthai.Device, deviceInfo: depthai.DeviceInfo) -> None

        Connects to any available device with a DEFAULT_SEARCH_TIME timeout. Uses
        OpenVINO version OpenVINO::VERSION_UNIVERSAL

        Parameter ``devInfo``:
            DeviceInfo which specifies which device to connect to

        8. __init__(self: depthai.Device, deviceInfo: depthai.DeviceInfo, maxUsbSpeed: depthai.UsbSpeed) -> None

        Connects to any available device with a DEFAULT_SEARCH_TIME timeout. Uses
        OpenVINO version OpenVINO::VERSION_UNIVERSAL

        Parameter ``nameOrDeviceId``:
            Creates DeviceInfo with nameOrDeviceId to connect to

        9. __init__(self: depthai.Device, nameOrDeviceId: str) -> None

        Connects to any available device with a DEFAULT_SEARCH_TIME timeout. Uses
        OpenVINO version OpenVINO::VERSION_UNIVERSAL

        Parameter ``nameOrDeviceId``:
            Creates DeviceInfo with nameOrDeviceId to connect to

        Parameter ``maxUsbSpeed``:
            Maximum allowed USB speed

        10. __init__(self: depthai.Device, nameOrDeviceId: str, maxUsbSpeed: depthai.UsbSpeed) -> None

        Connects to device specified by devInfo.

        Parameter ``config``:
            Config with which the device will be booted with

        Parameter ``maxUsbSpeed``:
            Maximum allowed USB speed
        """
    @overload
    def __init__(self, deviceInfo: DeviceInfo, maxUsbSpeed: UsbSpeed) -> None:
        """__init__(*args, **kwargs)
        Overloaded function.

        1. __init__(self: depthai.Device) -> None

        Connects to any available device with a DEFAULT_SEARCH_TIME timeout. Uses
        OpenVINO version OpenVINO::VERSION_UNIVERSAL

        2. __init__(self: depthai.Device, maxUsbSpeed: depthai.UsbSpeed) -> None

        Connects to device

        Parameter ``maxUsbSpeed``:
            Maximum allowed USB speed

        3. __init__(self: depthai.Device, deviceInfo: depthai.DeviceInfo, maxUsbSpeed: depthai.UsbSpeed) -> None

        Connects to device specified by devInfo.

        Parameter ``devInfo``:
            DeviceInfo which specifies which device to connect to

        Parameter ``maxUsbSpeed``:
            Maximum allowed USB speed

        4. __init__(self: depthai.Device, deviceDesc: depthai.DeviceInfo, pathToCmd: os.PathLike) -> None

        Connects to device specified by devInfo.

        Parameter ``devInfo``:
            DeviceInfo which specifies which device to connect to

        Parameter ``pathToCmd``:
            Path to custom device firmware

        5. __init__(self: depthai.Device, config: depthai.Device.Config) -> None

        Connects to any available device with custom config.

        Parameter ``config``:
            Device custom configuration to boot with

        6. __init__(self: depthai.Device, config: depthai.Device.Config, deviceInfo: depthai.DeviceInfo) -> None

        Connects to device 'devInfo' with custom config.

        Parameter ``config``:
            Device custom configuration to boot with

        Parameter ``devInfo``:
            DeviceInfo which specifies which device to connect to

        7. __init__(self: depthai.Device, deviceInfo: depthai.DeviceInfo) -> None

        Connects to any available device with a DEFAULT_SEARCH_TIME timeout. Uses
        OpenVINO version OpenVINO::VERSION_UNIVERSAL

        Parameter ``devInfo``:
            DeviceInfo which specifies which device to connect to

        8. __init__(self: depthai.Device, deviceInfo: depthai.DeviceInfo, maxUsbSpeed: depthai.UsbSpeed) -> None

        Connects to any available device with a DEFAULT_SEARCH_TIME timeout. Uses
        OpenVINO version OpenVINO::VERSION_UNIVERSAL

        Parameter ``nameOrDeviceId``:
            Creates DeviceInfo with nameOrDeviceId to connect to

        9. __init__(self: depthai.Device, nameOrDeviceId: str) -> None

        Connects to any available device with a DEFAULT_SEARCH_TIME timeout. Uses
        OpenVINO version OpenVINO::VERSION_UNIVERSAL

        Parameter ``nameOrDeviceId``:
            Creates DeviceInfo with nameOrDeviceId to connect to

        Parameter ``maxUsbSpeed``:
            Maximum allowed USB speed

        10. __init__(self: depthai.Device, nameOrDeviceId: str, maxUsbSpeed: depthai.UsbSpeed) -> None

        Connects to device specified by devInfo.

        Parameter ``config``:
            Config with which the device will be booted with

        Parameter ``maxUsbSpeed``:
            Maximum allowed USB speed
        """
    @overload
    def __init__(self, deviceDesc: DeviceInfo, pathToCmd: os.PathLike) -> None:
        """__init__(*args, **kwargs)
        Overloaded function.

        1. __init__(self: depthai.Device) -> None

        Connects to any available device with a DEFAULT_SEARCH_TIME timeout. Uses
        OpenVINO version OpenVINO::VERSION_UNIVERSAL

        2. __init__(self: depthai.Device, maxUsbSpeed: depthai.UsbSpeed) -> None

        Connects to device

        Parameter ``maxUsbSpeed``:
            Maximum allowed USB speed

        3. __init__(self: depthai.Device, deviceInfo: depthai.DeviceInfo, maxUsbSpeed: depthai.UsbSpeed) -> None

        Connects to device specified by devInfo.

        Parameter ``devInfo``:
            DeviceInfo which specifies which device to connect to

        Parameter ``maxUsbSpeed``:
            Maximum allowed USB speed

        4. __init__(self: depthai.Device, deviceDesc: depthai.DeviceInfo, pathToCmd: os.PathLike) -> None

        Connects to device specified by devInfo.

        Parameter ``devInfo``:
            DeviceInfo which specifies which device to connect to

        Parameter ``pathToCmd``:
            Path to custom device firmware

        5. __init__(self: depthai.Device, config: depthai.Device.Config) -> None

        Connects to any available device with custom config.

        Parameter ``config``:
            Device custom configuration to boot with

        6. __init__(self: depthai.Device, config: depthai.Device.Config, deviceInfo: depthai.DeviceInfo) -> None

        Connects to device 'devInfo' with custom config.

        Parameter ``config``:
            Device custom configuration to boot with

        Parameter ``devInfo``:
            DeviceInfo which specifies which device to connect to

        7. __init__(self: depthai.Device, deviceInfo: depthai.DeviceInfo) -> None

        Connects to any available device with a DEFAULT_SEARCH_TIME timeout. Uses
        OpenVINO version OpenVINO::VERSION_UNIVERSAL

        Parameter ``devInfo``:
            DeviceInfo which specifies which device to connect to

        8. __init__(self: depthai.Device, deviceInfo: depthai.DeviceInfo, maxUsbSpeed: depthai.UsbSpeed) -> None

        Connects to any available device with a DEFAULT_SEARCH_TIME timeout. Uses
        OpenVINO version OpenVINO::VERSION_UNIVERSAL

        Parameter ``nameOrDeviceId``:
            Creates DeviceInfo with nameOrDeviceId to connect to

        9. __init__(self: depthai.Device, nameOrDeviceId: str) -> None

        Connects to any available device with a DEFAULT_SEARCH_TIME timeout. Uses
        OpenVINO version OpenVINO::VERSION_UNIVERSAL

        Parameter ``nameOrDeviceId``:
            Creates DeviceInfo with nameOrDeviceId to connect to

        Parameter ``maxUsbSpeed``:
            Maximum allowed USB speed

        10. __init__(self: depthai.Device, nameOrDeviceId: str, maxUsbSpeed: depthai.UsbSpeed) -> None

        Connects to device specified by devInfo.

        Parameter ``config``:
            Config with which the device will be booted with

        Parameter ``maxUsbSpeed``:
            Maximum allowed USB speed
        """
    @overload
    def __init__(self, config: Device.Config) -> None:
        """__init__(*args, **kwargs)
        Overloaded function.

        1. __init__(self: depthai.Device) -> None

        Connects to any available device with a DEFAULT_SEARCH_TIME timeout. Uses
        OpenVINO version OpenVINO::VERSION_UNIVERSAL

        2. __init__(self: depthai.Device, maxUsbSpeed: depthai.UsbSpeed) -> None

        Connects to device

        Parameter ``maxUsbSpeed``:
            Maximum allowed USB speed

        3. __init__(self: depthai.Device, deviceInfo: depthai.DeviceInfo, maxUsbSpeed: depthai.UsbSpeed) -> None

        Connects to device specified by devInfo.

        Parameter ``devInfo``:
            DeviceInfo which specifies which device to connect to

        Parameter ``maxUsbSpeed``:
            Maximum allowed USB speed

        4. __init__(self: depthai.Device, deviceDesc: depthai.DeviceInfo, pathToCmd: os.PathLike) -> None

        Connects to device specified by devInfo.

        Parameter ``devInfo``:
            DeviceInfo which specifies which device to connect to

        Parameter ``pathToCmd``:
            Path to custom device firmware

        5. __init__(self: depthai.Device, config: depthai.Device.Config) -> None

        Connects to any available device with custom config.

        Parameter ``config``:
            Device custom configuration to boot with

        6. __init__(self: depthai.Device, config: depthai.Device.Config, deviceInfo: depthai.DeviceInfo) -> None

        Connects to device 'devInfo' with custom config.

        Parameter ``config``:
            Device custom configuration to boot with

        Parameter ``devInfo``:
            DeviceInfo which specifies which device to connect to

        7. __init__(self: depthai.Device, deviceInfo: depthai.DeviceInfo) -> None

        Connects to any available device with a DEFAULT_SEARCH_TIME timeout. Uses
        OpenVINO version OpenVINO::VERSION_UNIVERSAL

        Parameter ``devInfo``:
            DeviceInfo which specifies which device to connect to

        8. __init__(self: depthai.Device, deviceInfo: depthai.DeviceInfo, maxUsbSpeed: depthai.UsbSpeed) -> None

        Connects to any available device with a DEFAULT_SEARCH_TIME timeout. Uses
        OpenVINO version OpenVINO::VERSION_UNIVERSAL

        Parameter ``nameOrDeviceId``:
            Creates DeviceInfo with nameOrDeviceId to connect to

        9. __init__(self: depthai.Device, nameOrDeviceId: str) -> None

        Connects to any available device with a DEFAULT_SEARCH_TIME timeout. Uses
        OpenVINO version OpenVINO::VERSION_UNIVERSAL

        Parameter ``nameOrDeviceId``:
            Creates DeviceInfo with nameOrDeviceId to connect to

        Parameter ``maxUsbSpeed``:
            Maximum allowed USB speed

        10. __init__(self: depthai.Device, nameOrDeviceId: str, maxUsbSpeed: depthai.UsbSpeed) -> None

        Connects to device specified by devInfo.

        Parameter ``config``:
            Config with which the device will be booted with

        Parameter ``maxUsbSpeed``:
            Maximum allowed USB speed
        """
    @overload
    def __init__(self, config: Device.Config, deviceInfo: DeviceInfo) -> None:
        """__init__(*args, **kwargs)
        Overloaded function.

        1. __init__(self: depthai.Device) -> None

        Connects to any available device with a DEFAULT_SEARCH_TIME timeout. Uses
        OpenVINO version OpenVINO::VERSION_UNIVERSAL

        2. __init__(self: depthai.Device, maxUsbSpeed: depthai.UsbSpeed) -> None

        Connects to device

        Parameter ``maxUsbSpeed``:
            Maximum allowed USB speed

        3. __init__(self: depthai.Device, deviceInfo: depthai.DeviceInfo, maxUsbSpeed: depthai.UsbSpeed) -> None

        Connects to device specified by devInfo.

        Parameter ``devInfo``:
            DeviceInfo which specifies which device to connect to

        Parameter ``maxUsbSpeed``:
            Maximum allowed USB speed

        4. __init__(self: depthai.Device, deviceDesc: depthai.DeviceInfo, pathToCmd: os.PathLike) -> None

        Connects to device specified by devInfo.

        Parameter ``devInfo``:
            DeviceInfo which specifies which device to connect to

        Parameter ``pathToCmd``:
            Path to custom device firmware

        5. __init__(self: depthai.Device, config: depthai.Device.Config) -> None

        Connects to any available device with custom config.

        Parameter ``config``:
            Device custom configuration to boot with

        6. __init__(self: depthai.Device, config: depthai.Device.Config, deviceInfo: depthai.DeviceInfo) -> None

        Connects to device 'devInfo' with custom config.

        Parameter ``config``:
            Device custom configuration to boot with

        Parameter ``devInfo``:
            DeviceInfo which specifies which device to connect to

        7. __init__(self: depthai.Device, deviceInfo: depthai.DeviceInfo) -> None

        Connects to any available device with a DEFAULT_SEARCH_TIME timeout. Uses
        OpenVINO version OpenVINO::VERSION_UNIVERSAL

        Parameter ``devInfo``:
            DeviceInfo which specifies which device to connect to

        8. __init__(self: depthai.Device, deviceInfo: depthai.DeviceInfo, maxUsbSpeed: depthai.UsbSpeed) -> None

        Connects to any available device with a DEFAULT_SEARCH_TIME timeout. Uses
        OpenVINO version OpenVINO::VERSION_UNIVERSAL

        Parameter ``nameOrDeviceId``:
            Creates DeviceInfo with nameOrDeviceId to connect to

        9. __init__(self: depthai.Device, nameOrDeviceId: str) -> None

        Connects to any available device with a DEFAULT_SEARCH_TIME timeout. Uses
        OpenVINO version OpenVINO::VERSION_UNIVERSAL

        Parameter ``nameOrDeviceId``:
            Creates DeviceInfo with nameOrDeviceId to connect to

        Parameter ``maxUsbSpeed``:
            Maximum allowed USB speed

        10. __init__(self: depthai.Device, nameOrDeviceId: str, maxUsbSpeed: depthai.UsbSpeed) -> None

        Connects to device specified by devInfo.

        Parameter ``config``:
            Config with which the device will be booted with

        Parameter ``maxUsbSpeed``:
            Maximum allowed USB speed
        """
    @overload
    def __init__(self, deviceInfo: DeviceInfo) -> None:
        """__init__(*args, **kwargs)
        Overloaded function.

        1. __init__(self: depthai.Device) -> None

        Connects to any available device with a DEFAULT_SEARCH_TIME timeout. Uses
        OpenVINO version OpenVINO::VERSION_UNIVERSAL

        2. __init__(self: depthai.Device, maxUsbSpeed: depthai.UsbSpeed) -> None

        Connects to device

        Parameter ``maxUsbSpeed``:
            Maximum allowed USB speed

        3. __init__(self: depthai.Device, deviceInfo: depthai.DeviceInfo, maxUsbSpeed: depthai.UsbSpeed) -> None

        Connects to device specified by devInfo.

        Parameter ``devInfo``:
            DeviceInfo which specifies which device to connect to

        Parameter ``maxUsbSpeed``:
            Maximum allowed USB speed

        4. __init__(self: depthai.Device, deviceDesc: depthai.DeviceInfo, pathToCmd: os.PathLike) -> None

        Connects to device specified by devInfo.

        Parameter ``devInfo``:
            DeviceInfo which specifies which device to connect to

        Parameter ``pathToCmd``:
            Path to custom device firmware

        5. __init__(self: depthai.Device, config: depthai.Device.Config) -> None

        Connects to any available device with custom config.

        Parameter ``config``:
            Device custom configuration to boot with

        6. __init__(self: depthai.Device, config: depthai.Device.Config, deviceInfo: depthai.DeviceInfo) -> None

        Connects to device 'devInfo' with custom config.

        Parameter ``config``:
            Device custom configuration to boot with

        Parameter ``devInfo``:
            DeviceInfo which specifies which device to connect to

        7. __init__(self: depthai.Device, deviceInfo: depthai.DeviceInfo) -> None

        Connects to any available device with a DEFAULT_SEARCH_TIME timeout. Uses
        OpenVINO version OpenVINO::VERSION_UNIVERSAL

        Parameter ``devInfo``:
            DeviceInfo which specifies which device to connect to

        8. __init__(self: depthai.Device, deviceInfo: depthai.DeviceInfo, maxUsbSpeed: depthai.UsbSpeed) -> None

        Connects to any available device with a DEFAULT_SEARCH_TIME timeout. Uses
        OpenVINO version OpenVINO::VERSION_UNIVERSAL

        Parameter ``nameOrDeviceId``:
            Creates DeviceInfo with nameOrDeviceId to connect to

        9. __init__(self: depthai.Device, nameOrDeviceId: str) -> None

        Connects to any available device with a DEFAULT_SEARCH_TIME timeout. Uses
        OpenVINO version OpenVINO::VERSION_UNIVERSAL

        Parameter ``nameOrDeviceId``:
            Creates DeviceInfo with nameOrDeviceId to connect to

        Parameter ``maxUsbSpeed``:
            Maximum allowed USB speed

        10. __init__(self: depthai.Device, nameOrDeviceId: str, maxUsbSpeed: depthai.UsbSpeed) -> None

        Connects to device specified by devInfo.

        Parameter ``config``:
            Config with which the device will be booted with

        Parameter ``maxUsbSpeed``:
            Maximum allowed USB speed
        """
    @overload
    def __init__(self, deviceInfo: DeviceInfo, maxUsbSpeed: UsbSpeed) -> None:
        """__init__(*args, **kwargs)
        Overloaded function.

        1. __init__(self: depthai.Device) -> None

        Connects to any available device with a DEFAULT_SEARCH_TIME timeout. Uses
        OpenVINO version OpenVINO::VERSION_UNIVERSAL

        2. __init__(self: depthai.Device, maxUsbSpeed: depthai.UsbSpeed) -> None

        Connects to device

        Parameter ``maxUsbSpeed``:
            Maximum allowed USB speed

        3. __init__(self: depthai.Device, deviceInfo: depthai.DeviceInfo, maxUsbSpeed: depthai.UsbSpeed) -> None

        Connects to device specified by devInfo.

        Parameter ``devInfo``:
            DeviceInfo which specifies which device to connect to

        Parameter ``maxUsbSpeed``:
            Maximum allowed USB speed

        4. __init__(self: depthai.Device, deviceDesc: depthai.DeviceInfo, pathToCmd: os.PathLike) -> None

        Connects to device specified by devInfo.

        Parameter ``devInfo``:
            DeviceInfo which specifies which device to connect to

        Parameter ``pathToCmd``:
            Path to custom device firmware

        5. __init__(self: depthai.Device, config: depthai.Device.Config) -> None

        Connects to any available device with custom config.

        Parameter ``config``:
            Device custom configuration to boot with

        6. __init__(self: depthai.Device, config: depthai.Device.Config, deviceInfo: depthai.DeviceInfo) -> None

        Connects to device 'devInfo' with custom config.

        Parameter ``config``:
            Device custom configuration to boot with

        Parameter ``devInfo``:
            DeviceInfo which specifies which device to connect to

        7. __init__(self: depthai.Device, deviceInfo: depthai.DeviceInfo) -> None

        Connects to any available device with a DEFAULT_SEARCH_TIME timeout. Uses
        OpenVINO version OpenVINO::VERSION_UNIVERSAL

        Parameter ``devInfo``:
            DeviceInfo which specifies which device to connect to

        8. __init__(self: depthai.Device, deviceInfo: depthai.DeviceInfo, maxUsbSpeed: depthai.UsbSpeed) -> None

        Connects to any available device with a DEFAULT_SEARCH_TIME timeout. Uses
        OpenVINO version OpenVINO::VERSION_UNIVERSAL

        Parameter ``nameOrDeviceId``:
            Creates DeviceInfo with nameOrDeviceId to connect to

        9. __init__(self: depthai.Device, nameOrDeviceId: str) -> None

        Connects to any available device with a DEFAULT_SEARCH_TIME timeout. Uses
        OpenVINO version OpenVINO::VERSION_UNIVERSAL

        Parameter ``nameOrDeviceId``:
            Creates DeviceInfo with nameOrDeviceId to connect to

        Parameter ``maxUsbSpeed``:
            Maximum allowed USB speed

        10. __init__(self: depthai.Device, nameOrDeviceId: str, maxUsbSpeed: depthai.UsbSpeed) -> None

        Connects to device specified by devInfo.

        Parameter ``config``:
            Config with which the device will be booted with

        Parameter ``maxUsbSpeed``:
            Maximum allowed USB speed
        """
    @overload
    def __init__(self, nameOrDeviceId: str) -> None:
        """__init__(*args, **kwargs)
        Overloaded function.

        1. __init__(self: depthai.Device) -> None

        Connects to any available device with a DEFAULT_SEARCH_TIME timeout. Uses
        OpenVINO version OpenVINO::VERSION_UNIVERSAL

        2. __init__(self: depthai.Device, maxUsbSpeed: depthai.UsbSpeed) -> None

        Connects to device

        Parameter ``maxUsbSpeed``:
            Maximum allowed USB speed

        3. __init__(self: depthai.Device, deviceInfo: depthai.DeviceInfo, maxUsbSpeed: depthai.UsbSpeed) -> None

        Connects to device specified by devInfo.

        Parameter ``devInfo``:
            DeviceInfo which specifies which device to connect to

        Parameter ``maxUsbSpeed``:
            Maximum allowed USB speed

        4. __init__(self: depthai.Device, deviceDesc: depthai.DeviceInfo, pathToCmd: os.PathLike) -> None

        Connects to device specified by devInfo.

        Parameter ``devInfo``:
            DeviceInfo which specifies which device to connect to

        Parameter ``pathToCmd``:
            Path to custom device firmware

        5. __init__(self: depthai.Device, config: depthai.Device.Config) -> None

        Connects to any available device with custom config.

        Parameter ``config``:
            Device custom configuration to boot with

        6. __init__(self: depthai.Device, config: depthai.Device.Config, deviceInfo: depthai.DeviceInfo) -> None

        Connects to device 'devInfo' with custom config.

        Parameter ``config``:
            Device custom configuration to boot with

        Parameter ``devInfo``:
            DeviceInfo which specifies which device to connect to

        7. __init__(self: depthai.Device, deviceInfo: depthai.DeviceInfo) -> None

        Connects to any available device with a DEFAULT_SEARCH_TIME timeout. Uses
        OpenVINO version OpenVINO::VERSION_UNIVERSAL

        Parameter ``devInfo``:
            DeviceInfo which specifies which device to connect to

        8. __init__(self: depthai.Device, deviceInfo: depthai.DeviceInfo, maxUsbSpeed: depthai.UsbSpeed) -> None

        Connects to any available device with a DEFAULT_SEARCH_TIME timeout. Uses
        OpenVINO version OpenVINO::VERSION_UNIVERSAL

        Parameter ``nameOrDeviceId``:
            Creates DeviceInfo with nameOrDeviceId to connect to

        9. __init__(self: depthai.Device, nameOrDeviceId: str) -> None

        Connects to any available device with a DEFAULT_SEARCH_TIME timeout. Uses
        OpenVINO version OpenVINO::VERSION_UNIVERSAL

        Parameter ``nameOrDeviceId``:
            Creates DeviceInfo with nameOrDeviceId to connect to

        Parameter ``maxUsbSpeed``:
            Maximum allowed USB speed

        10. __init__(self: depthai.Device, nameOrDeviceId: str, maxUsbSpeed: depthai.UsbSpeed) -> None

        Connects to device specified by devInfo.

        Parameter ``config``:
            Config with which the device will be booted with

        Parameter ``maxUsbSpeed``:
            Maximum allowed USB speed
        """
    @overload
    def __init__(self, nameOrDeviceId: str, maxUsbSpeed: UsbSpeed) -> None:
        """__init__(*args, **kwargs)
        Overloaded function.

        1. __init__(self: depthai.Device) -> None

        Connects to any available device with a DEFAULT_SEARCH_TIME timeout. Uses
        OpenVINO version OpenVINO::VERSION_UNIVERSAL

        2. __init__(self: depthai.Device, maxUsbSpeed: depthai.UsbSpeed) -> None

        Connects to device

        Parameter ``maxUsbSpeed``:
            Maximum allowed USB speed

        3. __init__(self: depthai.Device, deviceInfo: depthai.DeviceInfo, maxUsbSpeed: depthai.UsbSpeed) -> None

        Connects to device specified by devInfo.

        Parameter ``devInfo``:
            DeviceInfo which specifies which device to connect to

        Parameter ``maxUsbSpeed``:
            Maximum allowed USB speed

        4. __init__(self: depthai.Device, deviceDesc: depthai.DeviceInfo, pathToCmd: os.PathLike) -> None

        Connects to device specified by devInfo.

        Parameter ``devInfo``:
            DeviceInfo which specifies which device to connect to

        Parameter ``pathToCmd``:
            Path to custom device firmware

        5. __init__(self: depthai.Device, config: depthai.Device.Config) -> None

        Connects to any available device with custom config.

        Parameter ``config``:
            Device custom configuration to boot with

        6. __init__(self: depthai.Device, config: depthai.Device.Config, deviceInfo: depthai.DeviceInfo) -> None

        Connects to device 'devInfo' with custom config.

        Parameter ``config``:
            Device custom configuration to boot with

        Parameter ``devInfo``:
            DeviceInfo which specifies which device to connect to

        7. __init__(self: depthai.Device, deviceInfo: depthai.DeviceInfo) -> None

        Connects to any available device with a DEFAULT_SEARCH_TIME timeout. Uses
        OpenVINO version OpenVINO::VERSION_UNIVERSAL

        Parameter ``devInfo``:
            DeviceInfo which specifies which device to connect to

        8. __init__(self: depthai.Device, deviceInfo: depthai.DeviceInfo, maxUsbSpeed: depthai.UsbSpeed) -> None

        Connects to any available device with a DEFAULT_SEARCH_TIME timeout. Uses
        OpenVINO version OpenVINO::VERSION_UNIVERSAL

        Parameter ``nameOrDeviceId``:
            Creates DeviceInfo with nameOrDeviceId to connect to

        9. __init__(self: depthai.Device, nameOrDeviceId: str) -> None

        Connects to any available device with a DEFAULT_SEARCH_TIME timeout. Uses
        OpenVINO version OpenVINO::VERSION_UNIVERSAL

        Parameter ``nameOrDeviceId``:
            Creates DeviceInfo with nameOrDeviceId to connect to

        Parameter ``maxUsbSpeed``:
            Maximum allowed USB speed

        10. __init__(self: depthai.Device, nameOrDeviceId: str, maxUsbSpeed: depthai.UsbSpeed) -> None

        Connects to device specified by devInfo.

        Parameter ``config``:
            Config with which the device will be booted with

        Parameter ``maxUsbSpeed``:
            Maximum allowed USB speed
        """
    def getPlatform(self) -> Platform:
        """getPlatform(self: depthai.Device) -> depthai.Platform

        Get the platform of the connected device

        Returns:
            Platform Platform enum
        """
    def getPlatformAsString(self) -> str:
        """getPlatformAsString(self: depthai.Device) -> str

        Get the platform of the connected device as string

        Returns:
            std::string String representation of Platform
        """

class DeviceBase:
    """The core of depthai device for RAII, connects to device and maintains watchdog,
    timesync, ..."""
    @overload
    def __init__(self) -> None:
        """__init__(*args, **kwargs)
        Overloaded function.

        1. __init__(self: depthai.DeviceBase) -> None

        Connects to any available device with a DEFAULT_SEARCH_TIME timeout. Uses
        OpenVINO version OpenVINO::VERSION_UNIVERSAL

        2. __init__(self: depthai.DeviceBase, maxUsbSpeed: depthai.UsbSpeed) -> None

        Connects to device

        Parameter ``maxUsbSpeed``:
            Maximum allowed USB speed

        3. __init__(self: depthai.DeviceBase, deviceInfo: depthai.DeviceInfo, maxUsbSpeed: depthai.UsbSpeed) -> None

        Connects to device specified by devInfo.

        Parameter ``devInfo``:
            DeviceInfo which specifies which device to connect to

        Parameter ``maxUsbSpeed``:
            Maximum allowed USB speed

        4. __init__(self: depthai.DeviceBase, deviceDesc: depthai.DeviceInfo, pathToCmd: os.PathLike) -> None

        Connects to device specified by devInfo.

        Parameter ``devInfo``:
            DeviceInfo which specifies which device to connect to

        Parameter ``pathToCmd``:
            Path to custom device firmware

        5. __init__(self: depthai.DeviceBase, config: depthai.Device.Config) -> None

        Connects to any available device with custom config.

        Parameter ``config``:
            Device custom configuration to boot with

        6. __init__(self: depthai.DeviceBase, config: depthai.Device.Config, deviceInfo: depthai.DeviceInfo) -> None

        Connects to device 'devInfo' with custom config.

        Parameter ``config``:
            Device custom configuration to boot with

        Parameter ``devInfo``:
            DeviceInfo which specifies which device to connect to

        7. __init__(self: depthai.DeviceBase, deviceInfo: depthai.DeviceInfo) -> None

        Connects to any available device with a DEFAULT_SEARCH_TIME timeout. Uses
        OpenVINO version OpenVINO::VERSION_UNIVERSAL

        Parameter ``devInfo``:
            DeviceInfo which specifies which device to connect to

        8. __init__(self: depthai.DeviceBase, deviceInfo: depthai.DeviceInfo, maxUsbSpeed: depthai.UsbSpeed) -> None

        Connects to any available device with a DEFAULT_SEARCH_TIME timeout. Uses
        OpenVINO version OpenVINO::VERSION_UNIVERSAL

        Parameter ``nameOrDeviceId``:
            Creates DeviceInfo with nameOrDeviceId to connect to

        9. __init__(self: depthai.DeviceBase, nameOrDeviceId: str) -> None

        Connects to any available device with a DEFAULT_SEARCH_TIME timeout. Uses
        OpenVINO version OpenVINO::VERSION_UNIVERSAL

        Parameter ``nameOrDeviceId``:
            Creates DeviceInfo with nameOrDeviceId to connect to

        Parameter ``maxUsbSpeed``:
            Maximum allowed USB speed

        10. __init__(self: depthai.DeviceBase, nameOrDeviceId: str, maxUsbSpeed: depthai.UsbSpeed) -> None

        Connects to device specified by devInfo.

        Parameter ``config``:
            Config with which the device will be booted with

        Parameter ``maxUsbSpeed``:
            Maximum allowed USB speed
        """
    @overload
    def __init__(self, maxUsbSpeed: UsbSpeed) -> None:
        """__init__(*args, **kwargs)
        Overloaded function.

        1. __init__(self: depthai.DeviceBase) -> None

        Connects to any available device with a DEFAULT_SEARCH_TIME timeout. Uses
        OpenVINO version OpenVINO::VERSION_UNIVERSAL

        2. __init__(self: depthai.DeviceBase, maxUsbSpeed: depthai.UsbSpeed) -> None

        Connects to device

        Parameter ``maxUsbSpeed``:
            Maximum allowed USB speed

        3. __init__(self: depthai.DeviceBase, deviceInfo: depthai.DeviceInfo, maxUsbSpeed: depthai.UsbSpeed) -> None

        Connects to device specified by devInfo.

        Parameter ``devInfo``:
            DeviceInfo which specifies which device to connect to

        Parameter ``maxUsbSpeed``:
            Maximum allowed USB speed

        4. __init__(self: depthai.DeviceBase, deviceDesc: depthai.DeviceInfo, pathToCmd: os.PathLike) -> None

        Connects to device specified by devInfo.

        Parameter ``devInfo``:
            DeviceInfo which specifies which device to connect to

        Parameter ``pathToCmd``:
            Path to custom device firmware

        5. __init__(self: depthai.DeviceBase, config: depthai.Device.Config) -> None

        Connects to any available device with custom config.

        Parameter ``config``:
            Device custom configuration to boot with

        6. __init__(self: depthai.DeviceBase, config: depthai.Device.Config, deviceInfo: depthai.DeviceInfo) -> None

        Connects to device 'devInfo' with custom config.

        Parameter ``config``:
            Device custom configuration to boot with

        Parameter ``devInfo``:
            DeviceInfo which specifies which device to connect to

        7. __init__(self: depthai.DeviceBase, deviceInfo: depthai.DeviceInfo) -> None

        Connects to any available device with a DEFAULT_SEARCH_TIME timeout. Uses
        OpenVINO version OpenVINO::VERSION_UNIVERSAL

        Parameter ``devInfo``:
            DeviceInfo which specifies which device to connect to

        8. __init__(self: depthai.DeviceBase, deviceInfo: depthai.DeviceInfo, maxUsbSpeed: depthai.UsbSpeed) -> None

        Connects to any available device with a DEFAULT_SEARCH_TIME timeout. Uses
        OpenVINO version OpenVINO::VERSION_UNIVERSAL

        Parameter ``nameOrDeviceId``:
            Creates DeviceInfo with nameOrDeviceId to connect to

        9. __init__(self: depthai.DeviceBase, nameOrDeviceId: str) -> None

        Connects to any available device with a DEFAULT_SEARCH_TIME timeout. Uses
        OpenVINO version OpenVINO::VERSION_UNIVERSAL

        Parameter ``nameOrDeviceId``:
            Creates DeviceInfo with nameOrDeviceId to connect to

        Parameter ``maxUsbSpeed``:
            Maximum allowed USB speed

        10. __init__(self: depthai.DeviceBase, nameOrDeviceId: str, maxUsbSpeed: depthai.UsbSpeed) -> None

        Connects to device specified by devInfo.

        Parameter ``config``:
            Config with which the device will be booted with

        Parameter ``maxUsbSpeed``:
            Maximum allowed USB speed
        """
    @overload
    def __init__(self, deviceInfo: DeviceInfo, maxUsbSpeed: UsbSpeed) -> None:
        """__init__(*args, **kwargs)
        Overloaded function.

        1. __init__(self: depthai.DeviceBase) -> None

        Connects to any available device with a DEFAULT_SEARCH_TIME timeout. Uses
        OpenVINO version OpenVINO::VERSION_UNIVERSAL

        2. __init__(self: depthai.DeviceBase, maxUsbSpeed: depthai.UsbSpeed) -> None

        Connects to device

        Parameter ``maxUsbSpeed``:
            Maximum allowed USB speed

        3. __init__(self: depthai.DeviceBase, deviceInfo: depthai.DeviceInfo, maxUsbSpeed: depthai.UsbSpeed) -> None

        Connects to device specified by devInfo.

        Parameter ``devInfo``:
            DeviceInfo which specifies which device to connect to

        Parameter ``maxUsbSpeed``:
            Maximum allowed USB speed

        4. __init__(self: depthai.DeviceBase, deviceDesc: depthai.DeviceInfo, pathToCmd: os.PathLike) -> None

        Connects to device specified by devInfo.

        Parameter ``devInfo``:
            DeviceInfo which specifies which device to connect to

        Parameter ``pathToCmd``:
            Path to custom device firmware

        5. __init__(self: depthai.DeviceBase, config: depthai.Device.Config) -> None

        Connects to any available device with custom config.

        Parameter ``config``:
            Device custom configuration to boot with

        6. __init__(self: depthai.DeviceBase, config: depthai.Device.Config, deviceInfo: depthai.DeviceInfo) -> None

        Connects to device 'devInfo' with custom config.

        Parameter ``config``:
            Device custom configuration to boot with

        Parameter ``devInfo``:
            DeviceInfo which specifies which device to connect to

        7. __init__(self: depthai.DeviceBase, deviceInfo: depthai.DeviceInfo) -> None

        Connects to any available device with a DEFAULT_SEARCH_TIME timeout. Uses
        OpenVINO version OpenVINO::VERSION_UNIVERSAL

        Parameter ``devInfo``:
            DeviceInfo which specifies which device to connect to

        8. __init__(self: depthai.DeviceBase, deviceInfo: depthai.DeviceInfo, maxUsbSpeed: depthai.UsbSpeed) -> None

        Connects to any available device with a DEFAULT_SEARCH_TIME timeout. Uses
        OpenVINO version OpenVINO::VERSION_UNIVERSAL

        Parameter ``nameOrDeviceId``:
            Creates DeviceInfo with nameOrDeviceId to connect to

        9. __init__(self: depthai.DeviceBase, nameOrDeviceId: str) -> None

        Connects to any available device with a DEFAULT_SEARCH_TIME timeout. Uses
        OpenVINO version OpenVINO::VERSION_UNIVERSAL

        Parameter ``nameOrDeviceId``:
            Creates DeviceInfo with nameOrDeviceId to connect to

        Parameter ``maxUsbSpeed``:
            Maximum allowed USB speed

        10. __init__(self: depthai.DeviceBase, nameOrDeviceId: str, maxUsbSpeed: depthai.UsbSpeed) -> None

        Connects to device specified by devInfo.

        Parameter ``config``:
            Config with which the device will be booted with

        Parameter ``maxUsbSpeed``:
            Maximum allowed USB speed
        """
    @overload
    def __init__(self, deviceDesc: DeviceInfo, pathToCmd: os.PathLike) -> None:
        """__init__(*args, **kwargs)
        Overloaded function.

        1. __init__(self: depthai.DeviceBase) -> None

        Connects to any available device with a DEFAULT_SEARCH_TIME timeout. Uses
        OpenVINO version OpenVINO::VERSION_UNIVERSAL

        2. __init__(self: depthai.DeviceBase, maxUsbSpeed: depthai.UsbSpeed) -> None

        Connects to device

        Parameter ``maxUsbSpeed``:
            Maximum allowed USB speed

        3. __init__(self: depthai.DeviceBase, deviceInfo: depthai.DeviceInfo, maxUsbSpeed: depthai.UsbSpeed) -> None

        Connects to device specified by devInfo.

        Parameter ``devInfo``:
            DeviceInfo which specifies which device to connect to

        Parameter ``maxUsbSpeed``:
            Maximum allowed USB speed

        4. __init__(self: depthai.DeviceBase, deviceDesc: depthai.DeviceInfo, pathToCmd: os.PathLike) -> None

        Connects to device specified by devInfo.

        Parameter ``devInfo``:
            DeviceInfo which specifies which device to connect to

        Parameter ``pathToCmd``:
            Path to custom device firmware

        5. __init__(self: depthai.DeviceBase, config: depthai.Device.Config) -> None

        Connects to any available device with custom config.

        Parameter ``config``:
            Device custom configuration to boot with

        6. __init__(self: depthai.DeviceBase, config: depthai.Device.Config, deviceInfo: depthai.DeviceInfo) -> None

        Connects to device 'devInfo' with custom config.

        Parameter ``config``:
            Device custom configuration to boot with

        Parameter ``devInfo``:
            DeviceInfo which specifies which device to connect to

        7. __init__(self: depthai.DeviceBase, deviceInfo: depthai.DeviceInfo) -> None

        Connects to any available device with a DEFAULT_SEARCH_TIME timeout. Uses
        OpenVINO version OpenVINO::VERSION_UNIVERSAL

        Parameter ``devInfo``:
            DeviceInfo which specifies which device to connect to

        8. __init__(self: depthai.DeviceBase, deviceInfo: depthai.DeviceInfo, maxUsbSpeed: depthai.UsbSpeed) -> None

        Connects to any available device with a DEFAULT_SEARCH_TIME timeout. Uses
        OpenVINO version OpenVINO::VERSION_UNIVERSAL

        Parameter ``nameOrDeviceId``:
            Creates DeviceInfo with nameOrDeviceId to connect to

        9. __init__(self: depthai.DeviceBase, nameOrDeviceId: str) -> None

        Connects to any available device with a DEFAULT_SEARCH_TIME timeout. Uses
        OpenVINO version OpenVINO::VERSION_UNIVERSAL

        Parameter ``nameOrDeviceId``:
            Creates DeviceInfo with nameOrDeviceId to connect to

        Parameter ``maxUsbSpeed``:
            Maximum allowed USB speed

        10. __init__(self: depthai.DeviceBase, nameOrDeviceId: str, maxUsbSpeed: depthai.UsbSpeed) -> None

        Connects to device specified by devInfo.

        Parameter ``config``:
            Config with which the device will be booted with

        Parameter ``maxUsbSpeed``:
            Maximum allowed USB speed
        """
    @overload
    def __init__(self, config: Device.Config) -> None:
        """__init__(*args, **kwargs)
        Overloaded function.

        1. __init__(self: depthai.DeviceBase) -> None

        Connects to any available device with a DEFAULT_SEARCH_TIME timeout. Uses
        OpenVINO version OpenVINO::VERSION_UNIVERSAL

        2. __init__(self: depthai.DeviceBase, maxUsbSpeed: depthai.UsbSpeed) -> None

        Connects to device

        Parameter ``maxUsbSpeed``:
            Maximum allowed USB speed

        3. __init__(self: depthai.DeviceBase, deviceInfo: depthai.DeviceInfo, maxUsbSpeed: depthai.UsbSpeed) -> None

        Connects to device specified by devInfo.

        Parameter ``devInfo``:
            DeviceInfo which specifies which device to connect to

        Parameter ``maxUsbSpeed``:
            Maximum allowed USB speed

        4. __init__(self: depthai.DeviceBase, deviceDesc: depthai.DeviceInfo, pathToCmd: os.PathLike) -> None

        Connects to device specified by devInfo.

        Parameter ``devInfo``:
            DeviceInfo which specifies which device to connect to

        Parameter ``pathToCmd``:
            Path to custom device firmware

        5. __init__(self: depthai.DeviceBase, config: depthai.Device.Config) -> None

        Connects to any available device with custom config.

        Parameter ``config``:
            Device custom configuration to boot with

        6. __init__(self: depthai.DeviceBase, config: depthai.Device.Config, deviceInfo: depthai.DeviceInfo) -> None

        Connects to device 'devInfo' with custom config.

        Parameter ``config``:
            Device custom configuration to boot with

        Parameter ``devInfo``:
            DeviceInfo which specifies which device to connect to

        7. __init__(self: depthai.DeviceBase, deviceInfo: depthai.DeviceInfo) -> None

        Connects to any available device with a DEFAULT_SEARCH_TIME timeout. Uses
        OpenVINO version OpenVINO::VERSION_UNIVERSAL

        Parameter ``devInfo``:
            DeviceInfo which specifies which device to connect to

        8. __init__(self: depthai.DeviceBase, deviceInfo: depthai.DeviceInfo, maxUsbSpeed: depthai.UsbSpeed) -> None

        Connects to any available device with a DEFAULT_SEARCH_TIME timeout. Uses
        OpenVINO version OpenVINO::VERSION_UNIVERSAL

        Parameter ``nameOrDeviceId``:
            Creates DeviceInfo with nameOrDeviceId to connect to

        9. __init__(self: depthai.DeviceBase, nameOrDeviceId: str) -> None

        Connects to any available device with a DEFAULT_SEARCH_TIME timeout. Uses
        OpenVINO version OpenVINO::VERSION_UNIVERSAL

        Parameter ``nameOrDeviceId``:
            Creates DeviceInfo with nameOrDeviceId to connect to

        Parameter ``maxUsbSpeed``:
            Maximum allowed USB speed

        10. __init__(self: depthai.DeviceBase, nameOrDeviceId: str, maxUsbSpeed: depthai.UsbSpeed) -> None

        Connects to device specified by devInfo.

        Parameter ``config``:
            Config with which the device will be booted with

        Parameter ``maxUsbSpeed``:
            Maximum allowed USB speed
        """
    @overload
    def __init__(self, config: Device.Config, deviceInfo: DeviceInfo) -> None:
        """__init__(*args, **kwargs)
        Overloaded function.

        1. __init__(self: depthai.DeviceBase) -> None

        Connects to any available device with a DEFAULT_SEARCH_TIME timeout. Uses
        OpenVINO version OpenVINO::VERSION_UNIVERSAL

        2. __init__(self: depthai.DeviceBase, maxUsbSpeed: depthai.UsbSpeed) -> None

        Connects to device

        Parameter ``maxUsbSpeed``:
            Maximum allowed USB speed

        3. __init__(self: depthai.DeviceBase, deviceInfo: depthai.DeviceInfo, maxUsbSpeed: depthai.UsbSpeed) -> None

        Connects to device specified by devInfo.

        Parameter ``devInfo``:
            DeviceInfo which specifies which device to connect to

        Parameter ``maxUsbSpeed``:
            Maximum allowed USB speed

        4. __init__(self: depthai.DeviceBase, deviceDesc: depthai.DeviceInfo, pathToCmd: os.PathLike) -> None

        Connects to device specified by devInfo.

        Parameter ``devInfo``:
            DeviceInfo which specifies which device to connect to

        Parameter ``pathToCmd``:
            Path to custom device firmware

        5. __init__(self: depthai.DeviceBase, config: depthai.Device.Config) -> None

        Connects to any available device with custom config.

        Parameter ``config``:
            Device custom configuration to boot with

        6. __init__(self: depthai.DeviceBase, config: depthai.Device.Config, deviceInfo: depthai.DeviceInfo) -> None

        Connects to device 'devInfo' with custom config.

        Parameter ``config``:
            Device custom configuration to boot with

        Parameter ``devInfo``:
            DeviceInfo which specifies which device to connect to

        7. __init__(self: depthai.DeviceBase, deviceInfo: depthai.DeviceInfo) -> None

        Connects to any available device with a DEFAULT_SEARCH_TIME timeout. Uses
        OpenVINO version OpenVINO::VERSION_UNIVERSAL

        Parameter ``devInfo``:
            DeviceInfo which specifies which device to connect to

        8. __init__(self: depthai.DeviceBase, deviceInfo: depthai.DeviceInfo, maxUsbSpeed: depthai.UsbSpeed) -> None

        Connects to any available device with a DEFAULT_SEARCH_TIME timeout. Uses
        OpenVINO version OpenVINO::VERSION_UNIVERSAL

        Parameter ``nameOrDeviceId``:
            Creates DeviceInfo with nameOrDeviceId to connect to

        9. __init__(self: depthai.DeviceBase, nameOrDeviceId: str) -> None

        Connects to any available device with a DEFAULT_SEARCH_TIME timeout. Uses
        OpenVINO version OpenVINO::VERSION_UNIVERSAL

        Parameter ``nameOrDeviceId``:
            Creates DeviceInfo with nameOrDeviceId to connect to

        Parameter ``maxUsbSpeed``:
            Maximum allowed USB speed

        10. __init__(self: depthai.DeviceBase, nameOrDeviceId: str, maxUsbSpeed: depthai.UsbSpeed) -> None

        Connects to device specified by devInfo.

        Parameter ``config``:
            Config with which the device will be booted with

        Parameter ``maxUsbSpeed``:
            Maximum allowed USB speed
        """
    @overload
    def __init__(self, deviceInfo: DeviceInfo) -> None:
        """__init__(*args, **kwargs)
        Overloaded function.

        1. __init__(self: depthai.DeviceBase) -> None

        Connects to any available device with a DEFAULT_SEARCH_TIME timeout. Uses
        OpenVINO version OpenVINO::VERSION_UNIVERSAL

        2. __init__(self: depthai.DeviceBase, maxUsbSpeed: depthai.UsbSpeed) -> None

        Connects to device

        Parameter ``maxUsbSpeed``:
            Maximum allowed USB speed

        3. __init__(self: depthai.DeviceBase, deviceInfo: depthai.DeviceInfo, maxUsbSpeed: depthai.UsbSpeed) -> None

        Connects to device specified by devInfo.

        Parameter ``devInfo``:
            DeviceInfo which specifies which device to connect to

        Parameter ``maxUsbSpeed``:
            Maximum allowed USB speed

        4. __init__(self: depthai.DeviceBase, deviceDesc: depthai.DeviceInfo, pathToCmd: os.PathLike) -> None

        Connects to device specified by devInfo.

        Parameter ``devInfo``:
            DeviceInfo which specifies which device to connect to

        Parameter ``pathToCmd``:
            Path to custom device firmware

        5. __init__(self: depthai.DeviceBase, config: depthai.Device.Config) -> None

        Connects to any available device with custom config.

        Parameter ``config``:
            Device custom configuration to boot with

        6. __init__(self: depthai.DeviceBase, config: depthai.Device.Config, deviceInfo: depthai.DeviceInfo) -> None

        Connects to device 'devInfo' with custom config.

        Parameter ``config``:
            Device custom configuration to boot with

        Parameter ``devInfo``:
            DeviceInfo which specifies which device to connect to

        7. __init__(self: depthai.DeviceBase, deviceInfo: depthai.DeviceInfo) -> None

        Connects to any available device with a DEFAULT_SEARCH_TIME timeout. Uses
        OpenVINO version OpenVINO::VERSION_UNIVERSAL

        Parameter ``devInfo``:
            DeviceInfo which specifies which device to connect to

        8. __init__(self: depthai.DeviceBase, deviceInfo: depthai.DeviceInfo, maxUsbSpeed: depthai.UsbSpeed) -> None

        Connects to any available device with a DEFAULT_SEARCH_TIME timeout. Uses
        OpenVINO version OpenVINO::VERSION_UNIVERSAL

        Parameter ``nameOrDeviceId``:
            Creates DeviceInfo with nameOrDeviceId to connect to

        9. __init__(self: depthai.DeviceBase, nameOrDeviceId: str) -> None

        Connects to any available device with a DEFAULT_SEARCH_TIME timeout. Uses
        OpenVINO version OpenVINO::VERSION_UNIVERSAL

        Parameter ``nameOrDeviceId``:
            Creates DeviceInfo with nameOrDeviceId to connect to

        Parameter ``maxUsbSpeed``:
            Maximum allowed USB speed

        10. __init__(self: depthai.DeviceBase, nameOrDeviceId: str, maxUsbSpeed: depthai.UsbSpeed) -> None

        Connects to device specified by devInfo.

        Parameter ``config``:
            Config with which the device will be booted with

        Parameter ``maxUsbSpeed``:
            Maximum allowed USB speed
        """
    @overload
    def __init__(self, deviceInfo: DeviceInfo, maxUsbSpeed: UsbSpeed) -> None:
        """__init__(*args, **kwargs)
        Overloaded function.

        1. __init__(self: depthai.DeviceBase) -> None

        Connects to any available device with a DEFAULT_SEARCH_TIME timeout. Uses
        OpenVINO version OpenVINO::VERSION_UNIVERSAL

        2. __init__(self: depthai.DeviceBase, maxUsbSpeed: depthai.UsbSpeed) -> None

        Connects to device

        Parameter ``maxUsbSpeed``:
            Maximum allowed USB speed

        3. __init__(self: depthai.DeviceBase, deviceInfo: depthai.DeviceInfo, maxUsbSpeed: depthai.UsbSpeed) -> None

        Connects to device specified by devInfo.

        Parameter ``devInfo``:
            DeviceInfo which specifies which device to connect to

        Parameter ``maxUsbSpeed``:
            Maximum allowed USB speed

        4. __init__(self: depthai.DeviceBase, deviceDesc: depthai.DeviceInfo, pathToCmd: os.PathLike) -> None

        Connects to device specified by devInfo.

        Parameter ``devInfo``:
            DeviceInfo which specifies which device to connect to

        Parameter ``pathToCmd``:
            Path to custom device firmware

        5. __init__(self: depthai.DeviceBase, config: depthai.Device.Config) -> None

        Connects to any available device with custom config.

        Parameter ``config``:
            Device custom configuration to boot with

        6. __init__(self: depthai.DeviceBase, config: depthai.Device.Config, deviceInfo: depthai.DeviceInfo) -> None

        Connects to device 'devInfo' with custom config.

        Parameter ``config``:
            Device custom configuration to boot with

        Parameter ``devInfo``:
            DeviceInfo which specifies which device to connect to

        7. __init__(self: depthai.DeviceBase, deviceInfo: depthai.DeviceInfo) -> None

        Connects to any available device with a DEFAULT_SEARCH_TIME timeout. Uses
        OpenVINO version OpenVINO::VERSION_UNIVERSAL

        Parameter ``devInfo``:
            DeviceInfo which specifies which device to connect to

        8. __init__(self: depthai.DeviceBase, deviceInfo: depthai.DeviceInfo, maxUsbSpeed: depthai.UsbSpeed) -> None

        Connects to any available device with a DEFAULT_SEARCH_TIME timeout. Uses
        OpenVINO version OpenVINO::VERSION_UNIVERSAL

        Parameter ``nameOrDeviceId``:
            Creates DeviceInfo with nameOrDeviceId to connect to

        9. __init__(self: depthai.DeviceBase, nameOrDeviceId: str) -> None

        Connects to any available device with a DEFAULT_SEARCH_TIME timeout. Uses
        OpenVINO version OpenVINO::VERSION_UNIVERSAL

        Parameter ``nameOrDeviceId``:
            Creates DeviceInfo with nameOrDeviceId to connect to

        Parameter ``maxUsbSpeed``:
            Maximum allowed USB speed

        10. __init__(self: depthai.DeviceBase, nameOrDeviceId: str, maxUsbSpeed: depthai.UsbSpeed) -> None

        Connects to device specified by devInfo.

        Parameter ``config``:
            Config with which the device will be booted with

        Parameter ``maxUsbSpeed``:
            Maximum allowed USB speed
        """
    @overload
    def __init__(self, nameOrDeviceId: str) -> None:
        """__init__(*args, **kwargs)
        Overloaded function.

        1. __init__(self: depthai.DeviceBase) -> None

        Connects to any available device with a DEFAULT_SEARCH_TIME timeout. Uses
        OpenVINO version OpenVINO::VERSION_UNIVERSAL

        2. __init__(self: depthai.DeviceBase, maxUsbSpeed: depthai.UsbSpeed) -> None

        Connects to device

        Parameter ``maxUsbSpeed``:
            Maximum allowed USB speed

        3. __init__(self: depthai.DeviceBase, deviceInfo: depthai.DeviceInfo, maxUsbSpeed: depthai.UsbSpeed) -> None

        Connects to device specified by devInfo.

        Parameter ``devInfo``:
            DeviceInfo which specifies which device to connect to

        Parameter ``maxUsbSpeed``:
            Maximum allowed USB speed

        4. __init__(self: depthai.DeviceBase, deviceDesc: depthai.DeviceInfo, pathToCmd: os.PathLike) -> None

        Connects to device specified by devInfo.

        Parameter ``devInfo``:
            DeviceInfo which specifies which device to connect to

        Parameter ``pathToCmd``:
            Path to custom device firmware

        5. __init__(self: depthai.DeviceBase, config: depthai.Device.Config) -> None

        Connects to any available device with custom config.

        Parameter ``config``:
            Device custom configuration to boot with

        6. __init__(self: depthai.DeviceBase, config: depthai.Device.Config, deviceInfo: depthai.DeviceInfo) -> None

        Connects to device 'devInfo' with custom config.

        Parameter ``config``:
            Device custom configuration to boot with

        Parameter ``devInfo``:
            DeviceInfo which specifies which device to connect to

        7. __init__(self: depthai.DeviceBase, deviceInfo: depthai.DeviceInfo) -> None

        Connects to any available device with a DEFAULT_SEARCH_TIME timeout. Uses
        OpenVINO version OpenVINO::VERSION_UNIVERSAL

        Parameter ``devInfo``:
            DeviceInfo which specifies which device to connect to

        8. __init__(self: depthai.DeviceBase, deviceInfo: depthai.DeviceInfo, maxUsbSpeed: depthai.UsbSpeed) -> None

        Connects to any available device with a DEFAULT_SEARCH_TIME timeout. Uses
        OpenVINO version OpenVINO::VERSION_UNIVERSAL

        Parameter ``nameOrDeviceId``:
            Creates DeviceInfo with nameOrDeviceId to connect to

        9. __init__(self: depthai.DeviceBase, nameOrDeviceId: str) -> None

        Connects to any available device with a DEFAULT_SEARCH_TIME timeout. Uses
        OpenVINO version OpenVINO::VERSION_UNIVERSAL

        Parameter ``nameOrDeviceId``:
            Creates DeviceInfo with nameOrDeviceId to connect to

        Parameter ``maxUsbSpeed``:
            Maximum allowed USB speed

        10. __init__(self: depthai.DeviceBase, nameOrDeviceId: str, maxUsbSpeed: depthai.UsbSpeed) -> None

        Connects to device specified by devInfo.

        Parameter ``config``:
            Config with which the device will be booted with

        Parameter ``maxUsbSpeed``:
            Maximum allowed USB speed
        """
    @overload
    def __init__(self, nameOrDeviceId: str, maxUsbSpeed: UsbSpeed) -> None:
        """__init__(*args, **kwargs)
        Overloaded function.

        1. __init__(self: depthai.DeviceBase) -> None

        Connects to any available device with a DEFAULT_SEARCH_TIME timeout. Uses
        OpenVINO version OpenVINO::VERSION_UNIVERSAL

        2. __init__(self: depthai.DeviceBase, maxUsbSpeed: depthai.UsbSpeed) -> None

        Connects to device

        Parameter ``maxUsbSpeed``:
            Maximum allowed USB speed

        3. __init__(self: depthai.DeviceBase, deviceInfo: depthai.DeviceInfo, maxUsbSpeed: depthai.UsbSpeed) -> None

        Connects to device specified by devInfo.

        Parameter ``devInfo``:
            DeviceInfo which specifies which device to connect to

        Parameter ``maxUsbSpeed``:
            Maximum allowed USB speed

        4. __init__(self: depthai.DeviceBase, deviceDesc: depthai.DeviceInfo, pathToCmd: os.PathLike) -> None

        Connects to device specified by devInfo.

        Parameter ``devInfo``:
            DeviceInfo which specifies which device to connect to

        Parameter ``pathToCmd``:
            Path to custom device firmware

        5. __init__(self: depthai.DeviceBase, config: depthai.Device.Config) -> None

        Connects to any available device with custom config.

        Parameter ``config``:
            Device custom configuration to boot with

        6. __init__(self: depthai.DeviceBase, config: depthai.Device.Config, deviceInfo: depthai.DeviceInfo) -> None

        Connects to device 'devInfo' with custom config.

        Parameter ``config``:
            Device custom configuration to boot with

        Parameter ``devInfo``:
            DeviceInfo which specifies which device to connect to

        7. __init__(self: depthai.DeviceBase, deviceInfo: depthai.DeviceInfo) -> None

        Connects to any available device with a DEFAULT_SEARCH_TIME timeout. Uses
        OpenVINO version OpenVINO::VERSION_UNIVERSAL

        Parameter ``devInfo``:
            DeviceInfo which specifies which device to connect to

        8. __init__(self: depthai.DeviceBase, deviceInfo: depthai.DeviceInfo, maxUsbSpeed: depthai.UsbSpeed) -> None

        Connects to any available device with a DEFAULT_SEARCH_TIME timeout. Uses
        OpenVINO version OpenVINO::VERSION_UNIVERSAL

        Parameter ``nameOrDeviceId``:
            Creates DeviceInfo with nameOrDeviceId to connect to

        9. __init__(self: depthai.DeviceBase, nameOrDeviceId: str) -> None

        Connects to any available device with a DEFAULT_SEARCH_TIME timeout. Uses
        OpenVINO version OpenVINO::VERSION_UNIVERSAL

        Parameter ``nameOrDeviceId``:
            Creates DeviceInfo with nameOrDeviceId to connect to

        Parameter ``maxUsbSpeed``:
            Maximum allowed USB speed

        10. __init__(self: depthai.DeviceBase, nameOrDeviceId: str, maxUsbSpeed: depthai.UsbSpeed) -> None

        Connects to device specified by devInfo.

        Parameter ``config``:
            Config with which the device will be booted with

        Parameter ``maxUsbSpeed``:
            Maximum allowed USB speed
        """
    def addLogCallback(self, callback: Callable[[LogMessage], None]) -> int:
        """addLogCallback(self: depthai.DeviceBase, callback: Callable[[depthai.LogMessage], None]) -> int

        Add a callback for device logging. The callback will be called from a separate
        thread with the LogMessage being passed.

        Parameter ``callback``:
            Callback to call whenever a log message arrives

        Returns:
            Id which can be used to later remove the callback
        """
    def close(self) -> None:
        """close(self: depthai.DeviceBase) -> None

        Closes the connection to device. Better alternative is the usage of context manager: `with depthai.Device(pipeline) as device:`
        """
    def crashDevice(self) -> None:
        """crashDevice(self: depthai.DeviceBase) -> None

        Crashes the device $.. warning::

        ONLY FOR TESTING PURPOSES, it causes an unrecoverable crash on the device
        """
    def factoryResetCalibration(self) -> None:
        """factoryResetCalibration(self: depthai.DeviceBase) -> None

        Factory reset EEPROM data if factory backup is available.

        Throws:
            std::runtime_error If factory reset was unsuccessful
        """
    def flashCalibration(self, arg0: CalibrationHandler) -> None:
        """flashCalibration(self: depthai.DeviceBase, arg0: depthai.CalibrationHandler) -> None

        Stores the Calibration and Device information to the Device EEPROM

        Throws:
            std::runtime_error if failed to flash the calibration

        Parameter ``calibrationObj``:
            CalibrationHandler object which is loaded with calibration information.
        """
    def flashEepromClear(self) -> None:
        """flashEepromClear(self: depthai.DeviceBase) -> None

        Destructive action, deletes User area EEPROM contents Requires PROTECTED
        permissions

        Throws:
            std::runtime_error if failed to flash the calibration

        Returns:
            True on successful flash, false on failure
        """
    def flashFactoryCalibration(self, arg0: CalibrationHandler) -> None:
        """flashFactoryCalibration(self: depthai.DeviceBase, arg0: depthai.CalibrationHandler) -> None

        Stores the Calibration and Device information to the Device EEPROM in Factory
        area To perform this action, correct env variable must be set

        Throws:
            std::runtime_error if failed to flash the calibration

        Returns:
            True on successful flash, false on failure
        """
    def flashFactoryEepromClear(self) -> None:
        """flashFactoryEepromClear(self: depthai.DeviceBase) -> None

        Destructive action, deletes Factory area EEPROM contents Requires FACTORY
        PROTECTED permissions

        Throws:
            std::runtime_error if failed to flash the calibration

        Returns:
            True on successful flash, false on failure
        """
    @staticmethod
    def getAllAvailableDevices() -> list[DeviceInfo]:
        """getAllAvailableDevices() -> list[depthai.DeviceInfo]

        Returns all available devices

        Returns:
            Vector of available devices
        """
    @staticmethod
    def getAllConnectedDevices() -> list[DeviceInfo]:
        """getAllConnectedDevices() -> list[depthai.DeviceInfo]

        Returns information of all connected devices. The devices could be both
        connectable as well as already connected to devices.

        Returns:
            Vector of connected device information
        """
    @overload
    @staticmethod
    def getAnyAvailableDevice(timeout: datetime.timedelta) -> tuple[bool, DeviceInfo]:
        """getAnyAvailableDevice(*args, **kwargs)
        Overloaded function.

        1. getAnyAvailableDevice(timeout: datetime.timedelta) -> tuple[bool, depthai.DeviceInfo]

        Waits for any available device with a timeout

        Parameter ``timeout``:
            duration of time to wait for the any device

        Returns:
            Tuple of bool and DeviceInfo. Bool specifies if device was found. DeviceInfo
            specifies the found device

        2. getAnyAvailableDevice() -> tuple[bool, depthai.DeviceInfo]

        Gets any available device

        Returns:
            Tuple of bool and DeviceInfo. Bool specifies if device was found. DeviceInfo
            specifies the found device
        """
    @overload
    @staticmethod
    def getAnyAvailableDevice() -> tuple[bool, DeviceInfo]:
        """getAnyAvailableDevice(*args, **kwargs)
        Overloaded function.

        1. getAnyAvailableDevice(timeout: datetime.timedelta) -> tuple[bool, depthai.DeviceInfo]

        Waits for any available device with a timeout

        Parameter ``timeout``:
            duration of time to wait for the any device

        Returns:
            Tuple of bool and DeviceInfo. Bool specifies if device was found. DeviceInfo
            specifies the found device

        2. getAnyAvailableDevice() -> tuple[bool, depthai.DeviceInfo]

        Gets any available device

        Returns:
            Tuple of bool and DeviceInfo. Bool specifies if device was found. DeviceInfo
            specifies the found device
        """
    def getAvailableStereoPairs(self) -> list[StereoPair]:
        """getAvailableStereoPairs(self: depthai.DeviceBase) -> list[depthai.StereoPair]

        Get stereo pairs taking into account the calibration and connected cameras.

        @note This method will always return a subset of `getStereoPairs`.

        Returns:
            Vector of stereo pairs
        """
    def getBootloaderVersion(self) -> Version | None:
        """getBootloaderVersion(self: depthai.DeviceBase) -> Optional[depthai.Version]

        Gets Bootloader version if it was booted through Bootloader

        Returns:
            DeviceBootloader::Version if booted through Bootloader or none otherwise
        """
    def getCalibration(self) -> CalibrationHandler:
        """getCalibration(self: depthai.DeviceBase) -> depthai.CalibrationHandler

        Retrieves the CalibrationHandler object containing the non-persistent
        calibration

        Throws:
            std::runtime_error if failed to get the calibration

        Returns:
            The CalibrationHandler object containing the non-persistent calibration
        """
    def getCameraSensorNames(self) -> dict[CameraBoardSocket, str]:
        """getCameraSensorNames(self: depthai.DeviceBase) -> dict[depthai.CameraBoardSocket, str]

        Get sensor names for cameras that are connected to the device

        Returns:
            Map/dictionary with camera sensor names, indexed by socket
        """
    def getChipTemperature(self) -> ChipTemperature:
        """getChipTemperature(self: depthai.DeviceBase) -> depthai.ChipTemperature

        Retrieves current chip temperature as measured by device

        Returns:
            Temperature of various onboard sensors
        """
    def getCmxMemoryUsage(self) -> MemoryInfo:
        """getCmxMemoryUsage(self: depthai.DeviceBase) -> depthai.MemoryInfo

        Retrieves current CMX memory information from device

        Returns:
            Used, remaining and total cmx memory
        """
    def getConnectedCameraFeatures(self) -> list[CameraFeatures]:
        """getConnectedCameraFeatures(self: depthai.DeviceBase) -> list[depthai.CameraFeatures]

        Get cameras that are connected to the device with their features/properties

        Returns:
            Vector of connected camera features
        """
    def getConnectedCameras(self) -> list[CameraBoardSocket]:
        """getConnectedCameras(self: depthai.DeviceBase) -> list[depthai.CameraBoardSocket]

        Get cameras that are connected to the device

        Returns:
            Vector of connected cameras
        """
    def getConnectedIMU(self) -> str:
        """getConnectedIMU(self: depthai.DeviceBase) -> str

        Get connected IMU type

        Returns:
            IMU type
        """
    def getConnectionInterfaces(self) -> list[connectionInterface]:
        """getConnectionInterfaces(self: depthai.DeviceBase) -> list[depthai.connectionInterface]

        Get connection interfaces for device

        Returns:
            Vector of connection type
        """
    def getCrashDump(self, clearCrashDump: bool = ...) -> CrashDump:
        """getCrashDump(self: depthai.DeviceBase, clearCrashDump: bool = True) -> depthai.CrashDump

        Retrieves crash dump for debugging.
        """
    def getDdrMemoryUsage(self) -> MemoryInfo:
        """getDdrMemoryUsage(self: depthai.DeviceBase) -> depthai.MemoryInfo

        Retrieves current DDR memory information from device

        Returns:
            Used, remaining and total ddr memory
        """
    @staticmethod
    def getDeviceById(deviceId: str) -> tuple[bool, DeviceInfo]:
        """getDeviceById(deviceId: str) -> tuple[bool, depthai.DeviceInfo]

        Finds a device by Device ID. Example: 14442C10D13EABCE00

        Parameter ``deviceId``:
            Device ID which uniquely specifies a device

        Returns:
            Tuple of bool and DeviceInfo. Bool specifies if device was found. DeviceInfo
            specifies the found device
        """
    def getDeviceId(self) -> str:
        """getDeviceId(self: depthai.DeviceBase) -> str

        Get DeviceId of device

        Returns:
            DeviceId of connected device
        """
    def getDeviceInfo(self) -> DeviceInfo:
        """getDeviceInfo(self: depthai.DeviceBase) -> depthai.DeviceInfo

        Get the Device Info object o the device which is currently running

        Returns:
            DeviceInfo of the current device in execution
        """
    def getDeviceName(self) -> object:
        """getDeviceName(self: depthai.DeviceBase) -> object

        Get device name if available

        Returns:
            device name or empty string if not available
        """
    @staticmethod
    def getEmbeddedDeviceBinary(*args, **kwargs):
        """getEmbeddedDeviceBinary(*args, **kwargs)
        Overloaded function.

        1. getEmbeddedDeviceBinary(usb2Mode: bool, version: depthai.OpenVINO.Version = <Version.???: 7>) -> std::vector<unsigned char, std::allocator<unsigned char> >

        Gets device firmware binary for a specific OpenVINO version

        Parameter ``usb2Mode``:
            USB2 mode firmware

        Parameter ``version``:
            Version of OpenVINO which firmware will support

        Returns:
            Firmware binary

        2. getEmbeddedDeviceBinary(config: depthai.Device.Config) -> std::vector<unsigned char, std::allocator<unsigned char> >

        Gets device firmware binary for a specific configuration

        Parameter ``config``:
            FW with applied configuration

        Returns:
            Firmware binary
        """
    def getEmbeddedIMUFirmwareVersion(self) -> Version:
        """getEmbeddedIMUFirmwareVersion(self: depthai.DeviceBase) -> depthai.Version

        Get embedded IMU firmware version to which IMU can be upgraded

        Returns:
            Get embedded IMU firmware version to which IMU can be upgraded.
        """
    def getExternalFrameSyncRole(self) -> ExternalFrameSyncRole:
        """getExternalFrameSyncRole(self: depthai.DeviceBase) -> depthai.ExternalFrameSyncRole

        Gets external frame sync role for the device

        Returns:
            Gets external frame sync role
        """
    @staticmethod
    def getFirstAvailableDevice(skipInvalidDevices: bool = ...) -> tuple[bool, DeviceInfo]:
        """getFirstAvailableDevice(skipInvalidDevices: bool = True) -> tuple[bool, depthai.DeviceInfo]

        Gets first available device. Device can be either in XLINK_UNBOOTED or
        XLINK_BOOTLOADER state

        Returns:
            Tuple of bool and DeviceInfo. Bool specifies if device was found. DeviceInfo
            specifies the found device
        """
    @staticmethod
    def getGlobalProfilingData() -> ProfilingData:
        """getGlobalProfilingData() -> depthai.ProfilingData

        Get current global accumulated profiling data

        Returns:
            ProfilingData from all devices
        """
    def getIMUFirmwareUpdateStatus(self) -> tuple[bool, float]:
        """getIMUFirmwareUpdateStatus(self: depthai.DeviceBase) -> tuple[bool, float]

        Get IMU firmware update status

        Returns:
            Whether IMU firmware update is done and last firmware update progress as
            percentage. return value true and 100 means that the update was successful
            return value true and other than 100 means that the update failed
        """
    def getIMUFirmwareVersion(self) -> Version:
        """getIMUFirmwareVersion(self: depthai.DeviceBase) -> depthai.Version

        Get connected IMU firmware version

        Returns:
            IMU firmware version
        """
    def getIrDrivers(self) -> list[tuple[str, int, int]]:
        '''getIrDrivers(self: depthai.DeviceBase) -> list[tuple[str, int, int]]

        Retrieves detected IR laser/LED drivers.

        Returns:
            Vector of tuples containing: driver name, I2C bus, I2C address. For OAK-D-
            Pro it should be `[{"LM3644", 2, 0x63}]`
        '''
    def getLeonCssCpuUsage(self) -> CpuUsage:
        """getLeonCssCpuUsage(self: depthai.DeviceBase) -> depthai.CpuUsage

        Retrieves average CSS Leon CPU usage

        Returns:
            Average CPU usage and sampling duration
        """
    def getLeonCssHeapUsage(self) -> MemoryInfo:
        """getLeonCssHeapUsage(self: depthai.DeviceBase) -> depthai.MemoryInfo

        Retrieves current CSS Leon CPU heap information from device

        Returns:
            Used, remaining and total heap memory
        """
    def getLeonMssCpuUsage(self) -> CpuUsage:
        """getLeonMssCpuUsage(self: depthai.DeviceBase) -> depthai.CpuUsage

        Retrieves average MSS Leon CPU usage

        Returns:
            Average CPU usage and sampling duration
        """
    def getLeonMssHeapUsage(self) -> MemoryInfo:
        """getLeonMssHeapUsage(self: depthai.DeviceBase) -> depthai.MemoryInfo

        Retrieves current MSS Leon CPU heap information from device

        Returns:
            Used, remaining and total heap memory
        """
    def getLogLevel(self) -> LogLevel:
        """getLogLevel(self: depthai.DeviceBase) -> depthai.LogLevel

        Gets current logging severity level of the device.

        Returns:
            Logging severity level
        """
    def getLogOutputLevel(self) -> LogLevel:
        """getLogOutputLevel(self: depthai.DeviceBase) -> depthai.LogLevel

        Gets logging level which decides printing level to standard output.

        Returns:
            Standard output printing severity
        """
    def getMxId(self) -> str:
        """getMxId(self: depthai.DeviceBase) -> str

        Get MxId of device

        Returns:
            MxId of connected device
        """
    def getProcessMemoryUsage(self) -> int:
        """getProcessMemoryUsage(self: depthai.DeviceBase) -> int

        Retrieves current Rss memory usage of the device process

        Returns:
            Current Rss memory used
        """
    def getProductName(self) -> object:
        """getProductName(self: depthai.DeviceBase) -> object

        Get product name if available

        Returns:
            product name or empty string if not available
        """
    def getProfilingData(self) -> ProfilingData:
        """getProfilingData(self: depthai.DeviceBase) -> depthai.ProfilingData

        Get current accumulated profiling data

        Returns:
            ProfilingData from the specific device
        """
    def getState(self) -> CrashDump:
        """getState(self: depthai.DeviceBase) -> depthai.CrashDump

        Retrieves current device state in a crash dump format. It halts the device
        temporarily and might affect the running pipeline, it's best to close the device
        after this operation. Supported only on RVC2.
        """
    def getStereoPairs(self) -> list[StereoPair]:
        """getStereoPairs(self: depthai.DeviceBase) -> list[depthai.StereoPair]

        Get stereo pairs based on the device type.

        Returns:
            Vector of stereo pairs
        """
    def getSystemInformationLoggingRate(self) -> float:
        '''getSystemInformationLoggingRate(self: depthai.DeviceBase) -> float

        Gets current rate of system information logging ("info" severity) in Hz.

        Returns:
            Logging rate in Hz
        '''
    def getUsbSpeed(self) -> UsbSpeed:
        """getUsbSpeed(self: depthai.DeviceBase) -> depthai.UsbSpeed

        Retrieves USB connection speed

        Returns:
            USB connection speed of connected device if applicable. Unknown otherwise.
        """
    def getXLinkChunkSize(self) -> int:
        """getXLinkChunkSize(self: depthai.DeviceBase) -> int

        Gets current XLink chunk size.

        Returns:
            XLink chunk size in bytes
        """
    def hasCrashDump(self) -> bool:
        """hasCrashDump(self: depthai.DeviceBase) -> bool

        Retrieves whether the is crash dump stored on device or not.
        """
    def isClosed(self) -> bool:
        """isClosed(self: depthai.DeviceBase) -> bool

        Is the device already closed (or disconnected)

        .. warning::
            This function is thread-unsafe and may return outdated incorrect values. It
            is only meant for use in simple single-threaded code. Well written code
            should handle exceptions when calling any DepthAI apis to handle hardware
            events and multithreaded use.
        """
    def isEepromAvailable(self) -> bool:
        """isEepromAvailable(self: depthai.DeviceBase) -> bool

        Check if EEPROM is available

        Returns:
            True if EEPROM is present on board, false otherwise
        """
    def isNeuralDepthSupported(self) -> bool:
        """isNeuralDepthSupported(self: depthai.DeviceBase) -> bool

        Checks if Neural Depth is supported on the device

        Returns:
            True if supported, false otherwise
        """
    def isPipelineRunning(self) -> bool:
        """isPipelineRunning(self: depthai.DeviceBase) -> bool

        Checks if devices pipeline is already running

        Returns:
            True if running, false otherwise
        """
    def readCalibration(self) -> CalibrationHandler:
        """readCalibration(self: depthai.DeviceBase) -> depthai.CalibrationHandler

        Fetches the EEPROM data from the device and loads it into CalibrationHandler
        object If no calibration is flashed, it returns default

        Returns:
            The CalibrationHandler object containing the calibration currently flashed
            on device EEPROM
        """
    def readCalibration2(self) -> CalibrationHandler:
        """readCalibration2(self: depthai.DeviceBase) -> depthai.CalibrationHandler

        Fetches the EEPROM data from the device and loads it into CalibrationHandler
        object

        Throws:
            std::runtime_error if no calibration is flashed

        Returns:
            The CalibrationHandler object containing the calibration currently flashed
            on device EEPROM
        """
    def readCalibrationOrDefault(self) -> CalibrationHandler:
        """readCalibrationOrDefault(self: depthai.DeviceBase) -> depthai.CalibrationHandler

        Fetches the EEPROM data from the device and loads it into CalibrationHandler
        object If no calibration is flashed, it returns default

        Returns:
            The CalibrationHandler object containing the calibration currently flashed
            on device EEPROM
        """
    def readCalibrationRaw(self) -> bytes:
        """readCalibrationRaw(self: depthai.DeviceBase) -> bytes

        Fetches the raw EEPROM data from User area

        Throws:
            std::runtime_error if any error occurred

        Returns:
            Binary dump of User area EEPROM data
        """
    def readFactoryCalibration(self) -> CalibrationHandler:
        """readFactoryCalibration(self: depthai.DeviceBase) -> depthai.CalibrationHandler

        Fetches the EEPROM data from Factory area and loads it into CalibrationHandler
        object

        Throws:
            std::runtime_error if no calibration is flashed

        Returns:
            The CalibrationHandler object containing the calibration currently flashed
            on device EEPROM in Factory Area
        """
    def readFactoryCalibrationOrDefault(self) -> CalibrationHandler:
        """readFactoryCalibrationOrDefault(self: depthai.DeviceBase) -> depthai.CalibrationHandler

        Fetches the EEPROM data from Factory area and loads it into CalibrationHandler
        object If no calibration is flashed, it returns default

        Returns:
            The CalibrationHandler object containing the calibration currently flashed
            on device EEPROM in Factory Area
        """
    def readFactoryCalibrationRaw(self) -> bytes:
        """readFactoryCalibrationRaw(self: depthai.DeviceBase) -> bytes

        Fetches the raw EEPROM data from Factory area

        Throws:
            std::runtime_error if any error occurred

        Returns:
            Binary dump of Factory area EEPROM data
        """
    def removeLogCallback(self, callbackId: int) -> bool:
        """removeLogCallback(self: depthai.DeviceBase, callbackId: int) -> bool

        Removes a callback

        Parameter ``callbackId``:
            Id of callback to be removed

        Returns:
            True if callback was removed, false otherwise
        """
    def setCalibration(self, arg0: CalibrationHandler) -> None:
        """setCalibration(self: depthai.DeviceBase, arg0: depthai.CalibrationHandler) -> None

        Sets the Calibration at runtime. This is not persistent and will be lost after
        device reset.

        Throws:
            std::runtime_error if failed to set the calibration

        Parameter ``calibrationObj``:
            CalibrationHandler object which is loaded with calibration information.
        """
    def setExternalFrameSyncRole(self, role: ExternalFrameSyncRole) -> tuple[bool, str]:
        """setExternalFrameSyncRole(self: depthai.DeviceBase, role: depthai.ExternalFrameSyncRole) -> tuple[bool, str]

        Sets external frame sync role for the device

        Parameter ``role``:
            External frame sync role to be set, AUTO_DETECT by default

        Returns:
            Tuple of bool and string. Bool specifies if role was set without failures.
            String is the error message describing the failure reason.
        """
    def setExternalStrobeEnable(self, enable: bool) -> None:
        """setExternalStrobeEnable(self: depthai.DeviceBase, enable: bool) -> None

        Set whether the external strobe should be enabled. External strobe signal is low
        for the duration of exposure, and high for the rest of the frame.

        Parameter ``enable``:
            Enables or disables strobe
        """
    def setExternalStrobeRelativeLimits(self, min: float, max: float) -> tuple[bool, str]:
        """setExternalStrobeRelativeLimits(self: depthai.DeviceBase, min: float, max: float) -> tuple[bool, str]

        Sets the relative external strobe limits. Limits the strobe duty cycle, between
        0 and 1, as a fraction of the whole period. 0 means always off, 1 means always
        on. The rising edge of the strobe signal is always synced to end of exposure.
        The falling edge of the strobe signal is then limited according to the min and
        max values. Default values are 0.005 and 0.995

        Parameter ``min``:
            Minimum strobe value

        Parameter ``max``:
            Maximum strobe value

        Returns:
            Tuple of bool and string. Bool specifies if role was set without failures.
            String is the error message describing the failure reason.
        """
    def setIrFloodLightIntensity(self, intensity: float, mask: int = ...) -> bool:
        """setIrFloodLightIntensity(self: depthai.DeviceBase, intensity: float, mask: int = -1) -> bool

        Sets the intensity of the IR Flood Light. Limits: Intensity is directly
        normalized to 0 - 1500mA current. The duty cycle is 30% when exposure time is
        longer than 30% frame time. Otherwise, duty cycle is 100% of exposure time. The
        duty cycle is controlled by the `left` camera STROBE, aligned to start of
        exposure. The emitter is turned off by default

        Parameter ``intensity``:
            Intensity on range 0 to 1, that will determine brightness, 0 or negative to
            turn off

        Parameter ``mask``:
            Optional mask to modify only Left (0x1) or Right (0x2) sides on OAK-D-Pro-W-
            DEV

        Returns:
            True on success, false if not found or other failure
        """
    def setIrLaserDotProjectorIntensity(self, intensity: float, mask: int = ...) -> bool:
        """setIrLaserDotProjectorIntensity(self: depthai.DeviceBase, intensity: float, mask: int = -1) -> bool

        Sets the intensity of the IR Laser Dot Projector. Limits: up to 765mA at 30%
        frame time duty cycle when exposure time is longer than 30% frame time.
        Otherwise, duty cycle is 100% of exposure time, with current increased up to max
        1200mA to make up for shorter duty cycle. The duty cycle is controlled by `left`
        camera STROBE, aligned to start of exposure. The emitter is turned off by
        default

        Parameter ``intensity``:
            Intensity on range 0 to 1, that will determine brightness. 0 or negative to
            turn off

        Parameter ``mask``:
            Optional mask to modify only Left (0x1) or Right (0x2) sides on OAK-D-Pro-W-
            DEV

        Returns:
            True on success, false if not found or other failure
        """
    def setLogLevel(self, level: LogLevel) -> None:
        """setLogLevel(self: depthai.DeviceBase, level: depthai.LogLevel) -> None

        Sets the devices logging severity level. This level affects which logs are
        transferred from device to host.

        Parameter ``level``:
            Logging severity
        """
    def setLogOutputLevel(self, level: LogLevel) -> None:
        """setLogOutputLevel(self: depthai.DeviceBase, level: depthai.LogLevel) -> None

        Sets logging level which decides printing level to standard output. If lower
        than setLogLevel, no messages will be printed

        Parameter ``level``:
            Standard output printing severity
        """
    def setMaxReconnectionAttempts(self, maxAttempts: int, callback: Callable[[Device.ReconnectionStatus], None] = ...) -> None:
        """setMaxReconnectionAttempts(self: depthai.DeviceBase, maxAttempts: int, callback: Callable[[depthai.Device.ReconnectionStatus], None] = None) -> None

        Sets max number of automatic reconnection attempts

        Parameter ``maxAttempts``:
            Maximum number of reconnection attempts, 0 to disable reconnection

        Parameter ``callBack``:
            Callback to be called when reconnection is attempted
        """
    def setSystemInformationLoggingRate(self, rateHz: float) -> None:
        '''setSystemInformationLoggingRate(self: depthai.DeviceBase, rateHz: float) -> None

        Sets rate of system information logging ("info" severity). Default 1Hz If
        parameter is less or equal to zero, then system information logging will be
        disabled

        Parameter ``rateHz``:
            Logging rate in Hz
        '''
    @overload
    def setTimesync(self, arg0: datetime.timedelta, arg1: int, arg2: bool) -> None:
        """setTimesync(*args, **kwargs)
        Overloaded function.

        1. setTimesync(self: depthai.DeviceBase, arg0: datetime.timedelta, arg1: int, arg2: bool) -> None

        Configures Timesync service on device. It keeps host and device clocks in sync
        First time timesync is started it waits until the initial sync is completed
        Afterwards the function changes the following parameters

        Parameter ``period``:
            Interval between timesync runs

        Parameter ``numSamples``:
            Number of timesync samples per run which are used to compute a better value.
            Set to zero to disable timesync

        Parameter ``random``:
            If true partial timesync requests will be performed at random intervals,
            otherwise at fixed intervals

        2. setTimesync(self: depthai.DeviceBase, enable: bool) -> None

        Enables or disables Timesync service on device. It keeps host and device clocks
        in sync.

        Parameter ``enable``:
            Enables or disables consistent timesyncing
        """
    @overload
    def setTimesync(self, enable: bool) -> None:
        """setTimesync(*args, **kwargs)
        Overloaded function.

        1. setTimesync(self: depthai.DeviceBase, arg0: datetime.timedelta, arg1: int, arg2: bool) -> None

        Configures Timesync service on device. It keeps host and device clocks in sync
        First time timesync is started it waits until the initial sync is completed
        Afterwards the function changes the following parameters

        Parameter ``period``:
            Interval between timesync runs

        Parameter ``numSamples``:
            Number of timesync samples per run which are used to compute a better value.
            Set to zero to disable timesync

        Parameter ``random``:
            If true partial timesync requests will be performed at random intervals,
            otherwise at fixed intervals

        2. setTimesync(self: depthai.DeviceBase, enable: bool) -> None

        Enables or disables Timesync service on device. It keeps host and device clocks
        in sync.

        Parameter ``enable``:
            Enables or disables consistent timesyncing
        """
    def setXLinkChunkSize(self, sizeBytes: int) -> None:
        """setXLinkChunkSize(self: depthai.DeviceBase, sizeBytes: int) -> None

        Sets the chunk size for splitting device-sent XLink packets. A larger value
        could increase performance, and 0 disables chunking. A negative value is
        ignored. Device defaults are configured per protocol, currently 64*1024 for both
        USB and Ethernet.

        Parameter ``sizeBytes``:
            XLink chunk size in bytes
        """
    def startIMUFirmwareUpdate(self, forceUpdate: bool = ...) -> bool:
        """startIMUFirmwareUpdate(self: depthai.DeviceBase, forceUpdate: bool = False) -> bool

        Starts IMU firmware update asynchronously only if IMU node is not running. If
        current firmware version is the same as embedded firmware version then it's no-
        op. Can be overridden by forceUpdate parameter. State of firmware update can be
        monitored using getIMUFirmwareUpdateStatus API.

        Parameter ``forceUpdate``:
            Force firmware update or not. Will perform FW update regardless of current
            version and embedded firmware version.

        Returns:
            Returns whether firmware update can be started. Returns false if IMU node is
            started.
        """
    def tryFlashCalibration(self, calibrationDataHandler: CalibrationHandler) -> bool:
        """tryFlashCalibration(self: depthai.DeviceBase, calibrationDataHandler: depthai.CalibrationHandler) -> bool

        Stores the Calibration and Device information to the Device EEPROM

        Parameter ``calibrationObj``:
            CalibrationHandler object which is loaded with calibration information.

        Returns:
            true on successful flash, false on failure
        """
    def __enter__(self) -> DeviceBase:
        """__enter__(self: depthai.DeviceBase) -> depthai.DeviceBase"""
    def __exit__(self, arg0: object, arg1: object, arg2: object) -> None:
        """__exit__(self: depthai.DeviceBase, arg0: object, arg1: object, arg2: object) -> None"""

class DeviceBootloader:
    """Represents the DepthAI bootloader with the methods to interact with it."""

    class ApplicationInfo:
        applicationName: str
        firmwareVersion: str
        hasApplication: bool
        def __init__(self) -> None:
            """__init__(self: depthai.DeviceBootloader.ApplicationInfo) -> None"""

    class Config:
        appMem: DeviceBootloader.Memory
        network: DeviceBootloader.NetworkConfig
        usb: DeviceBootloader.UsbConfig
        def __init__(self) -> None:
            """__init__(self: depthai.DeviceBootloader.Config) -> None"""
        def fromJson(self) -> DeviceBootloader.Config:
            """fromJson(self: json) -> depthai.DeviceBootloader.Config"""
        def getDnsAltIPv4(self) -> str:
            """getDnsAltIPv4(self: depthai.DeviceBootloader.Config) -> str"""
        def getDnsIPv4(self) -> str:
            """getDnsIPv4(self: depthai.DeviceBootloader.Config) -> str"""
        def getIPv4(self) -> str:
            """getIPv4(self: depthai.DeviceBootloader.Config) -> str"""
        def getIPv4Gateway(self) -> str:
            """getIPv4Gateway(self: depthai.DeviceBootloader.Config) -> str"""
        def getIPv4Mask(self) -> str:
            """getIPv4Mask(self: depthai.DeviceBootloader.Config) -> str"""
        def getMacAddress(self) -> str:
            """getMacAddress(self: depthai.DeviceBootloader.Config) -> str"""
        def getNetworkTimeout(self) -> datetime.timedelta:
            """getNetworkTimeout(self: depthai.DeviceBootloader.Config) -> datetime.timedelta"""
        def getUsbMaxSpeed(self) -> UsbSpeed:
            """getUsbMaxSpeed(self: depthai.DeviceBootloader.Config) -> depthai.UsbSpeed"""
        def getUsbTimeout(self) -> datetime.timedelta:
            """getUsbTimeout(self: depthai.DeviceBootloader.Config) -> datetime.timedelta"""
        def isStaticIPV4(self) -> bool:
            """isStaticIPV4(self: depthai.DeviceBootloader.Config) -> bool"""
        def setDnsIPv4(self, arg0: str, arg1: str) -> None:
            """setDnsIPv4(self: depthai.DeviceBootloader.Config, arg0: str, arg1: str) -> None"""
        def setDynamicIPv4(self, arg0: str, arg1: str, arg2: str) -> None:
            """setDynamicIPv4(self: depthai.DeviceBootloader.Config, arg0: str, arg1: str, arg2: str) -> None"""
        def setMacAddress(self, arg0: str) -> None:
            """setMacAddress(self: depthai.DeviceBootloader.Config, arg0: str) -> None"""
        def setNetworkTimeout(self, arg0: datetime.timedelta) -> None:
            """setNetworkTimeout(self: depthai.DeviceBootloader.Config, arg0: datetime.timedelta) -> None"""
        def setStaticIPv4(self, arg0: str, arg1: str, arg2: str) -> None:
            """setStaticIPv4(self: depthai.DeviceBootloader.Config, arg0: str, arg1: str, arg2: str) -> None"""
        def setUsbMaxSpeed(self, arg0: UsbSpeed) -> None:
            """setUsbMaxSpeed(self: depthai.DeviceBootloader.Config, arg0: depthai.UsbSpeed) -> None"""
        def setUsbTimeout(self, arg0: datetime.timedelta) -> None:
            """setUsbTimeout(self: depthai.DeviceBootloader.Config, arg0: datetime.timedelta) -> None"""
        def toJson(self) -> json:
            """toJson(self: depthai.DeviceBootloader.Config) -> json"""

    class Memory:
        """Members:

          AUTO

          FLASH

          EMMC"""
        __members__: ClassVar[dict] = ...  # read-only
        AUTO: ClassVar[DeviceBootloader.Memory] = ...
        EMMC: ClassVar[DeviceBootloader.Memory] = ...
        FLASH: ClassVar[DeviceBootloader.Memory] = ...
        __entries: ClassVar[dict] = ...
        def __init__(self, value: int) -> None:
            """__init__(self: depthai.DeviceBootloader.Memory, value: int) -> None"""
        def __eq__(self, other: object) -> bool:
            """__eq__(self: object, other: object) -> bool"""
        def __hash__(self) -> int:
            """__hash__(self: object) -> int"""
        def __index__(self) -> int:
            """__index__(self: depthai.DeviceBootloader.Memory) -> int"""
        def __int__(self) -> int:
            """__int__(self: depthai.DeviceBootloader.Memory) -> int"""
        def __ne__(self, other: object) -> bool:
            """__ne__(self: object, other: object) -> bool"""
        @property
        def name(self) -> str:
            """name(self: object) -> str

            name(self: object) -> str
            """
        @property
        def value(self) -> int:
            """(arg0: depthai.DeviceBootloader.Memory) -> int"""

    class MemoryInfo:
        available: bool
        info: str
        size: int
        def __init__(self) -> None:
            """__init__(self: depthai.DeviceBootloader.MemoryInfo) -> None"""

    class NetworkConfig:
        ipv4: int
        ipv4Dns: int
        ipv4DnsAlt: int
        ipv4Gateway: int
        ipv4Mask: int
        ipv6: Incomplete
        ipv6Dns: Incomplete
        ipv6DnsAlt: Incomplete
        ipv6Gateway: Incomplete
        ipv6Prefix: int
        mac: Incomplete
        staticIpv4: bool
        staticIpv6: bool
        timeoutMs: int
        def __init__(self) -> None:
            """__init__(self: depthai.DeviceBootloader.NetworkConfig) -> None"""

    class Section:
        """Members:

          AUTO

          HEADER

          BOOTLOADER

          BOOTLOADER_CONFIG

          APPLICATION"""
        __members__: ClassVar[dict] = ...  # read-only
        APPLICATION: ClassVar[DeviceBootloader.Section] = ...
        AUTO: ClassVar[DeviceBootloader.Section] = ...
        BOOTLOADER: ClassVar[DeviceBootloader.Section] = ...
        BOOTLOADER_CONFIG: ClassVar[DeviceBootloader.Section] = ...
        HEADER: ClassVar[DeviceBootloader.Section] = ...
        __entries: ClassVar[dict] = ...
        def __init__(self, value: int) -> None:
            """__init__(self: depthai.DeviceBootloader.Section, value: int) -> None"""
        def __eq__(self, other: object) -> bool:
            """__eq__(self: object, other: object) -> bool"""
        def __hash__(self) -> int:
            """__hash__(self: object) -> int"""
        def __index__(self) -> int:
            """__index__(self: depthai.DeviceBootloader.Section) -> int"""
        def __int__(self) -> int:
            """__int__(self: depthai.DeviceBootloader.Section) -> int"""
        def __ne__(self, other: object) -> bool:
            """__ne__(self: object, other: object) -> bool"""
        @property
        def name(self) -> str:
            """name(self: object) -> str

            name(self: object) -> str
            """
        @property
        def value(self) -> int:
            """(arg0: depthai.DeviceBootloader.Section) -> int"""

    class Type:
        """Members:

          AUTO

          USB

          NETWORK"""
        __members__: ClassVar[dict] = ...  # read-only
        AUTO: ClassVar[DeviceBootloader.Type] = ...
        NETWORK: ClassVar[DeviceBootloader.Type] = ...
        USB: ClassVar[DeviceBootloader.Type] = ...
        __entries: ClassVar[dict] = ...
        def __init__(self, value: int) -> None:
            """__init__(self: depthai.DeviceBootloader.Type, value: int) -> None"""
        def __eq__(self, other: object) -> bool:
            """__eq__(self: object, other: object) -> bool"""
        def __hash__(self) -> int:
            """__hash__(self: object) -> int"""
        def __index__(self) -> int:
            """__index__(self: depthai.DeviceBootloader.Type) -> int"""
        def __int__(self) -> int:
            """__int__(self: depthai.DeviceBootloader.Type) -> int"""
        def __ne__(self, other: object) -> bool:
            """__ne__(self: object, other: object) -> bool"""
        @property
        def name(self) -> str:
            """name(self: object) -> str

            name(self: object) -> str
            """
        @property
        def value(self) -> int:
            """(arg0: depthai.DeviceBootloader.Type) -> int"""

    class UsbConfig:
        maxUsbSpeed: int
        pid: int
        timeoutMs: int
        vid: int
        def __init__(self) -> None:
            """__init__(self: depthai.DeviceBootloader.UsbConfig) -> None"""
    def __init__(self, devInfo: DeviceInfo, allowFlashingBootloader: bool = ...) -> None:
        """__init__(*args, **kwargs)
        Overloaded function.

        1. __init__(self: depthai.DeviceBootloader, devInfo: depthai.DeviceInfo, allowFlashingBootloader: bool = False) -> None

        Connects to device in bootloader of specified type. Throws if it wasn't
        possible. This constructor will automatically boot into specified bootloader
        type if not already running

        Parameter ``devInfo``:
            DeviceInfo of which to boot or connect to

        Parameter ``type``:
            Type of bootloader to boot/connect to.

        Parameter ``allowFlashingBootloader``:
            Set to true to allow flashing the devices bootloader. Defaults to false

        2. __init__(self: depthai.DeviceBootloader, devInfo: depthai.DeviceInfo, pathToCmd: os.PathLike, allowFlashingBootloader: bool = False) -> None

        Connects to or boots device in bootloader mode depending on devInfo state with a
        custom bootloader firmware.

        Parameter ``devInfo``:
            DeviceInfo of which to boot or connect to

        Parameter ``pathToBootloader``:
            Custom bootloader firmware to boot

        Parameter ``allowFlashingBootloader``:
            Set to true to allow flashing the devices bootloader. Defaults to false

        3. __init__(self: depthai.DeviceBootloader, nameOrDeviceId: str, allowFlashingBootloader: bool = False) -> None

        Connects to device with specified name/device id

        Parameter ``nameOrDeviceId``:
            Creates DeviceInfo with nameOrDeviceId to connect to

        Parameter ``allowFlashingBootloader``:
            Set to true to allow flashing the devices bootloader. Defaults to false
        """
    def bootMemory(self, fw, std) -> None:
        """bootMemory(self: depthai.DeviceBootloader, fw: std::vector<unsigned char, std::allocator<unsigned char> >) -> None

        Boots a custom FW in memory

        Parameter ``fw``:
            $Throws:

        A runtime exception if there are any communication issues
        """
    def bootUsbRomBootloader(self) -> None:
        """bootUsbRomBootloader(self: depthai.DeviceBootloader) -> None

        Boots into integrated ROM bootloader in USB mode

        Throws:
            A runtime exception if there are any communication issues
        """
    def close(self) -> None:
        """close(self: depthai.DeviceBootloader) -> None

        Closes the connection to device. Better alternative is the usage of context manager: `with depthai.DeviceBootloader(deviceInfo) as bootloader:`
        """
    @staticmethod
    def createDepthaiApplicationPackage(*args, **kwargs):
        """createDepthaiApplicationPackage(*args, **kwargs)
        Overloaded function.

        1. createDepthaiApplicationPackage(pipeline: depthai.Pipeline, pathToCmd: os.PathLike = PosixPath('.'), compress: bool = False, applicationName: str = '', checkChecksum: bool = False) -> std::vector<unsigned char, std::allocator<unsigned char> >

        Creates application package which can be flashed to depthai device.

        Parameter ``pipeline``:
            Pipeline from which to create the application package

        Parameter ``pathToCmd``:
            Optional path to custom device firmware

        Parameter ``compress``:
            Optional boolean which specifies if contents should be compressed

        Parameter ``applicationName``:
            Optional name the application that is flashed

        Returns:
            Depthai application package

        2. createDepthaiApplicationPackage(pipeline: depthai.Pipeline, compress: bool, applicationName: str = '', checkChecksum: bool = False) -> std::vector<unsigned char, std::allocator<unsigned char> >

        Creates application package which can be flashed to depthai device.

        Parameter ``pipeline``:
            Pipeline from which to create the application package

        Parameter ``compress``:
            Specifies if contents should be compressed

        Parameter ``applicationName``:
            Name the application that is flashed

        Returns:
            Depthai application package
        """
    @overload
    def flash(self, progressCallback: Callable[[float], None], pipeline: Pipeline, compress: bool = ..., applicationName: str = ..., memory: DeviceBootloader.Memory = ..., checkChecksum: bool = ...) -> tuple[bool, str]:
        """flash(*args, **kwargs)
        Overloaded function.

        1. flash(self: depthai.DeviceBootloader, progressCallback: Callable[[float], None], pipeline: depthai.Pipeline, compress: bool = False, applicationName: str = '', memory: depthai.DeviceBootloader.Memory = <Memory.AUTO: -1>, checkChecksum: bool = False) -> tuple[bool, str]

        Flashes a given pipeline to the device.

        Parameter ``progressCallback``:
            Callback that sends back a value between 0..1 which signifies current
            flashing progress

        Parameter ``pipeline``:
            Pipeline to flash to the board

        Parameter ``compress``:
            Compresses application to reduce needed memory size

        Parameter ``applicationName``:
            Name the application that is flashed

        2. flash(self: depthai.DeviceBootloader, pipeline: depthai.Pipeline, compress: bool = False, applicationName: str = '', memory: depthai.DeviceBootloader.Memory = <Memory.AUTO: -1>, checkChecksum: bool = False) -> tuple[bool, str]

        Flashes a given pipeline to the device.

        Parameter ``pipeline``:
            Pipeline to flash to the board

        Parameter ``compress``:
            Compresses application to reduce needed memory size

        Parameter ``applicationName``:
            Optional name the application that is flashed
        """
    @overload
    def flash(self, pipeline: Pipeline, compress: bool = ..., applicationName: str = ..., memory: DeviceBootloader.Memory = ..., checkChecksum: bool = ...) -> tuple[bool, str]:
        """flash(*args, **kwargs)
        Overloaded function.

        1. flash(self: depthai.DeviceBootloader, progressCallback: Callable[[float], None], pipeline: depthai.Pipeline, compress: bool = False, applicationName: str = '', memory: depthai.DeviceBootloader.Memory = <Memory.AUTO: -1>, checkChecksum: bool = False) -> tuple[bool, str]

        Flashes a given pipeline to the device.

        Parameter ``progressCallback``:
            Callback that sends back a value between 0..1 which signifies current
            flashing progress

        Parameter ``pipeline``:
            Pipeline to flash to the board

        Parameter ``compress``:
            Compresses application to reduce needed memory size

        Parameter ``applicationName``:
            Name the application that is flashed

        2. flash(self: depthai.DeviceBootloader, pipeline: depthai.Pipeline, compress: bool = False, applicationName: str = '', memory: depthai.DeviceBootloader.Memory = <Memory.AUTO: -1>, checkChecksum: bool = False) -> tuple[bool, str]

        Flashes a given pipeline to the device.

        Parameter ``pipeline``:
            Pipeline to flash to the board

        Parameter ``compress``:
            Compresses application to reduce needed memory size

        Parameter ``applicationName``:
            Optional name the application that is flashed
        """
    def flashBootHeader(self, memory: DeviceBootloader.Memory, frequency: int = ..., location: int = ..., dummyCycles: int = ..., offset: int = ...) -> tuple[bool, str]:
        """flashBootHeader(self: depthai.DeviceBootloader, memory: depthai.DeviceBootloader.Memory, frequency: int = -1, location: int = -1, dummyCycles: int = -1, offset: int = -1) -> tuple[bool, str]

        Flash optimized boot header

        Parameter ``memory``:
            Which memory to flasht the header to

        Parameter ``frequency``:
            SPI specific parameter, frequency in MHz

        Parameter ``location``:
            Target location the header should boot to. Default to location of bootloader

        Parameter ``dummyCycles``:
            SPI specific parameter

        Parameter ``offset``:
            Offset in memory to flash the header to. Defaults to offset of boot header

        Returns:
            status as std::tuple<bool, std::string>
        """
    @overload
    def flashBootloader(self, progressCallback: Callable[[float], None], path: os.PathLike = ...) -> tuple[bool, str]:
        """flashBootloader(*args, **kwargs)
        Overloaded function.

        1. flashBootloader(self: depthai.DeviceBootloader, progressCallback: Callable[[float], None], path: os.PathLike = '') -> tuple[bool, str]

        Flashes bootloader to the current board

        Parameter ``progressCallback``:
            Callback that sends back a value between 0..1 which signifies current
            flashing progress

        Parameter ``path``:
            Optional parameter to custom bootloader to flash

        2. flashBootloader(self: depthai.DeviceBootloader, memory: depthai.DeviceBootloader.Memory, type: depthai.DeviceBootloader.Type, progressCallback: Callable[[float], None], path: os.PathLike = '') -> tuple[bool, str]

        Flash selected bootloader to the current board

        Parameter ``memory``:
            Memory to flash

        Parameter ``type``:
            Bootloader type to flash

        Parameter ``progressCallback``:
            Callback that sends back a value between 0..1 which signifies current
            flashing progress

        Parameter ``path``:
            Optional parameter to custom bootloader to flash
        """
    @overload
    def flashBootloader(self, memory: DeviceBootloader.Memory, type: DeviceBootloader.Type, progressCallback: Callable[[float], None], path: os.PathLike = ...) -> tuple[bool, str]:
        """flashBootloader(*args, **kwargs)
        Overloaded function.

        1. flashBootloader(self: depthai.DeviceBootloader, progressCallback: Callable[[float], None], path: os.PathLike = '') -> tuple[bool, str]

        Flashes bootloader to the current board

        Parameter ``progressCallback``:
            Callback that sends back a value between 0..1 which signifies current
            flashing progress

        Parameter ``path``:
            Optional parameter to custom bootloader to flash

        2. flashBootloader(self: depthai.DeviceBootloader, memory: depthai.DeviceBootloader.Memory, type: depthai.DeviceBootloader.Type, progressCallback: Callable[[float], None], path: os.PathLike = '') -> tuple[bool, str]

        Flash selected bootloader to the current board

        Parameter ``memory``:
            Memory to flash

        Parameter ``type``:
            Bootloader type to flash

        Parameter ``progressCallback``:
            Callback that sends back a value between 0..1 which signifies current
            flashing progress

        Parameter ``path``:
            Optional parameter to custom bootloader to flash
        """
    def flashClear(self, memory: DeviceBootloader.Memory = ...) -> tuple[bool, str]:
        """flashClear(self: depthai.DeviceBootloader, memory: depthai.DeviceBootloader.Memory = <Memory.AUTO: -1>) -> tuple[bool, str]

        Clears flashed application on the device, by removing SBR boot structure Doesn't
        remove fast boot header capability to still boot the application
        """
    def flashConfig(self, config: DeviceBootloader.Config, memory: DeviceBootloader.Memory = ..., type: DeviceBootloader.Type = ...) -> tuple[bool, str]:
        """flashConfig(self: depthai.DeviceBootloader, config: depthai.DeviceBootloader.Config, memory: depthai.DeviceBootloader.Memory = <Memory.AUTO: -1>, type: depthai.DeviceBootloader.Type = <Type.AUTO: -1>) -> tuple[bool, str]

        Flashes configuration to bootloader

        Parameter ``configData``:
            Configuration structure

        Parameter ``memory``:
            Optional - to which memory flash configuration

        Parameter ``type``:
            Optional - for which type of bootloader to flash configuration
        """
    def flashConfigClear(self, memory: DeviceBootloader.Memory = ..., type: DeviceBootloader.Type = ...) -> tuple[bool, str]:
        """flashConfigClear(self: depthai.DeviceBootloader, memory: depthai.DeviceBootloader.Memory = <Memory.AUTO: -1>, type: depthai.DeviceBootloader.Type = <Type.AUTO: -1>) -> tuple[bool, str]

        Clears configuration data

        Parameter ``memory``:
            Optional - on which memory to clear configuration data

        Parameter ``type``:
            Optional - for which type of bootloader to clear configuration data
        """
    def flashConfigData(self, configData: json, memory: DeviceBootloader.Memory = ..., type: DeviceBootloader.Type = ...) -> tuple[bool, str]:
        """flashConfigData(self: depthai.DeviceBootloader, configData: json, memory: depthai.DeviceBootloader.Memory = <Memory.AUTO: -1>, type: depthai.DeviceBootloader.Type = <Type.AUTO: -1>) -> tuple[bool, str]

        Flashes configuration data to bootloader

        Parameter ``configData``:
            Unstructured configuration data

        Parameter ``memory``:
            Optional - to which memory flash configuration

        Parameter ``type``:
            Optional - for which type of bootloader to flash configuration
        """
    def flashConfigFile(self, configData: os.PathLike, memory: DeviceBootloader.Memory = ..., type: DeviceBootloader.Type = ...) -> tuple[bool, str]:
        """flashConfigFile(self: depthai.DeviceBootloader, configData: os.PathLike, memory: depthai.DeviceBootloader.Memory = <Memory.AUTO: -1>, type: depthai.DeviceBootloader.Type = <Type.AUTO: -1>) -> tuple[bool, str]

        Flashes configuration data to bootloader

        Parameter ``configPath``:
            Unstructured configuration data

        Parameter ``memory``:
            Optional - to which memory flash configuration

        Parameter ``type``:
            Optional - for which type of bootloader to flash configuration
        """
    @overload
    def flashCustom(self, memory: DeviceBootloader.Memory, offset: int, data, std, progressCallback: Callable[[float], None] = ...) -> tuple[bool, str]:
        """flashCustom(*args, **kwargs)
        Overloaded function.

        1. flashCustom(self: depthai.DeviceBootloader, memory: depthai.DeviceBootloader.Memory, offset: int, data: std::vector<unsigned char, std::allocator<unsigned char> >, progressCallback: Callable[[float], None] = None) -> tuple[bool, str]

        Flash arbitrary data at custom offset in specified memory

        Parameter ``memory``:
            Memory to flash

        Parameter ``offset``:
            Offset at which to flash the given data in bytes

        Parameter ``progressCallback``:
            Callback that sends back a value between 0..1 which signifies current
            flashing progress

        Parameter ``data``:
            Data to flash

        2. flashCustom(self: depthai.DeviceBootloader, memory: depthai.DeviceBootloader.Memory, offset: int, filename: str, progressCallback: Callable[[float], None] = None) -> tuple[bool, str]

        Flash arbitrary data at custom offset in specified memory

        Parameter ``memory``:
            Memory to flash

        Parameter ``offset``:
            Offset at which to flash the given data in bytes

        Parameter ``progressCallback``:
            Callback that sends back a value between 0..1 which signifies current
            flashing progress

        Parameter ``data``:
            Data to flash
        """
    @overload
    def flashCustom(self, memory: DeviceBootloader.Memory, offset: int, filename: str, progressCallback: Callable[[float], None] = ...) -> tuple[bool, str]:
        """flashCustom(*args, **kwargs)
        Overloaded function.

        1. flashCustom(self: depthai.DeviceBootloader, memory: depthai.DeviceBootloader.Memory, offset: int, data: std::vector<unsigned char, std::allocator<unsigned char> >, progressCallback: Callable[[float], None] = None) -> tuple[bool, str]

        Flash arbitrary data at custom offset in specified memory

        Parameter ``memory``:
            Memory to flash

        Parameter ``offset``:
            Offset at which to flash the given data in bytes

        Parameter ``progressCallback``:
            Callback that sends back a value between 0..1 which signifies current
            flashing progress

        Parameter ``data``:
            Data to flash

        2. flashCustom(self: depthai.DeviceBootloader, memory: depthai.DeviceBootloader.Memory, offset: int, filename: str, progressCallback: Callable[[float], None] = None) -> tuple[bool, str]

        Flash arbitrary data at custom offset in specified memory

        Parameter ``memory``:
            Memory to flash

        Parameter ``offset``:
            Offset at which to flash the given data in bytes

        Parameter ``progressCallback``:
            Callback that sends back a value between 0..1 which signifies current
            flashing progress

        Parameter ``data``:
            Data to flash
        """
    @overload
    def flashDepthaiApplicationPackage(self, progressCallback: Callable[[float], None], package, std, memory: DeviceBootloader.Memory = ...) -> tuple[bool, str]:
        """flashDepthaiApplicationPackage(*args, **kwargs)
        Overloaded function.

        1. flashDepthaiApplicationPackage(self: depthai.DeviceBootloader, progressCallback: Callable[[float], None], package: std::vector<unsigned char, std::allocator<unsigned char> >, memory: depthai.DeviceBootloader.Memory = <Memory.AUTO: -1>) -> tuple[bool, str]

        Flashes a specific depthai application package that was generated using
        createDepthaiApplicationPackage or saveDepthaiApplicationPackage

        Parameter ``progressCallback``:
            Callback that sends back a value between 0..1 which signifies current
            flashing progress

        Parameter ``package``:
            Depthai application package to flash to the board

        2. flashDepthaiApplicationPackage(self: depthai.DeviceBootloader, package: std::vector<unsigned char, std::allocator<unsigned char> >, memory: depthai.DeviceBootloader.Memory = <Memory.AUTO: -1>) -> tuple[bool, str]

        Flashes a specific depthai application package that was generated using
        createDepthaiApplicationPackage or saveDepthaiApplicationPackage

        Parameter ``package``:
            Depthai application package to flash to the board
        """
    @overload
    def flashDepthaiApplicationPackage(self, package, std, memory: DeviceBootloader.Memory = ...) -> tuple[bool, str]:
        """flashDepthaiApplicationPackage(*args, **kwargs)
        Overloaded function.

        1. flashDepthaiApplicationPackage(self: depthai.DeviceBootloader, progressCallback: Callable[[float], None], package: std::vector<unsigned char, std::allocator<unsigned char> >, memory: depthai.DeviceBootloader.Memory = <Memory.AUTO: -1>) -> tuple[bool, str]

        Flashes a specific depthai application package that was generated using
        createDepthaiApplicationPackage or saveDepthaiApplicationPackage

        Parameter ``progressCallback``:
            Callback that sends back a value between 0..1 which signifies current
            flashing progress

        Parameter ``package``:
            Depthai application package to flash to the board

        2. flashDepthaiApplicationPackage(self: depthai.DeviceBootloader, package: std::vector<unsigned char, std::allocator<unsigned char> >, memory: depthai.DeviceBootloader.Memory = <Memory.AUTO: -1>) -> tuple[bool, str]

        Flashes a specific depthai application package that was generated using
        createDepthaiApplicationPackage or saveDepthaiApplicationPackage

        Parameter ``package``:
            Depthai application package to flash to the board
        """
    def flashFastBootHeader(self, memory: DeviceBootloader.Memory, frequency: int = ..., location: int = ..., dummyCycles: int = ..., offset: int = ...) -> tuple[bool, str]:
        """flashFastBootHeader(self: depthai.DeviceBootloader, memory: depthai.DeviceBootloader.Memory, frequency: int = -1, location: int = -1, dummyCycles: int = -1, offset: int = -1) -> tuple[bool, str]

        Flash fast boot header. Application must already be present in flash, or
        location must be specified manually. Note - Can soft brick your device if
        firmware location changes.

        Parameter ``memory``:
            Which memory to flash the header to

        Parameter ``frequency``:
            SPI specific parameter, frequency in MHz

        Parameter ``location``:
            Target location the header should boot to. Default to location of bootloader

        Parameter ``dummyCycles``:
            SPI specific parameter

        Parameter ``offset``:
            Offset in memory to flash the header to. Defaults to offset of boot header

        Returns:
            status as std::tuple<bool, std::string>
        """
    def flashGpioModeBootHeader(self, memory: DeviceBootloader.Memory, mode: int) -> tuple[bool, str]:
        """flashGpioModeBootHeader(self: depthai.DeviceBootloader, memory: depthai.DeviceBootloader.Memory, mode: int) -> tuple[bool, str]

        Flash boot header which boots same as equivalent GPIO mode would

        Parameter ``gpioMode``:
            GPIO mode equivalent
        """
    def flashUsbRecoveryBootHeader(self, memory: DeviceBootloader.Memory) -> tuple[bool, str]:
        """flashUsbRecoveryBootHeader(self: depthai.DeviceBootloader, memory: depthai.DeviceBootloader.Memory) -> tuple[bool, str]

        Flash USB recovery boot header. Switches to USB ROM Bootloader

        Parameter ``memory``:
            Which memory to flash the header to
        """
    def flashUserBootloader(self, progressCallback: Callable[[float], None], path: os.PathLike = ...) -> tuple[bool, str]:
        """flashUserBootloader(self: depthai.DeviceBootloader, progressCallback: Callable[[float], None], path: os.PathLike = '') -> tuple[bool, str]

        Flashes user bootloader to the current board. Available for NETWORK bootloader
        type

        Parameter ``progressCallback``:
            Callback that sends back a value between 0..1 which signifies current
            flashing progress

        Parameter ``path``:
            Optional parameter to custom bootloader to flash
        """
    @staticmethod
    def getAllAvailableDevices() -> list[DeviceInfo]:
        """getAllAvailableDevices() -> list[depthai.DeviceInfo]

        Searches for connected devices in either UNBOOTED or BOOTLOADER states.

        Returns:
            Vector of all found devices
        """
    @staticmethod
    def getEmbeddedBootloaderBinary(*args, **kwargs):
        """getEmbeddedBootloaderBinary(arg0: depthai.DeviceBootloader.Type) -> std::vector<unsigned char, std::allocator<unsigned char> >

        Returns:
            Embedded bootloader binary
        """
    @staticmethod
    def getEmbeddedBootloaderVersion() -> Version:
        """getEmbeddedBootloaderVersion() -> depthai.Version

        Returns:
            Embedded bootloader version
        """
    @staticmethod
    def getFirstAvailableDevice() -> tuple[bool, DeviceInfo]:
        """getFirstAvailableDevice() -> tuple[bool, depthai.DeviceInfo]

        Searches for connected devices in either UNBOOTED or BOOTLOADER states and
        returns first available.

        Returns:
            Tuple of boolean and DeviceInfo. If found boolean is true and DeviceInfo
            describes the device. Otherwise false
        """
    def getMemoryInfo(self, arg0: DeviceBootloader.Memory) -> DeviceBootloader.MemoryInfo:
        """getMemoryInfo(self: depthai.DeviceBootloader, arg0: depthai.DeviceBootloader.Memory) -> depthai.DeviceBootloader.MemoryInfo

        Retrieves information about specified memory

        Parameter ``memory``:
            Specifies which memory to query
        """
    def getType(self) -> DeviceBootloader.Type:
        """getType(self: depthai.DeviceBootloader) -> depthai.DeviceBootloader.Type

        Returns:
            Type of currently connected bootloader
        """
    def getVersion(self) -> Version:
        """getVersion(self: depthai.DeviceBootloader) -> depthai.Version

        Returns:
            Version of current running bootloader
        """
    def isAllowedFlashingBootloader(self) -> bool:
        """isAllowedFlashingBootloader(self: depthai.DeviceBootloader) -> bool

        Returns:
            True if allowed to flash bootloader
        """
    def isEmbeddedVersion(self) -> bool:
        """isEmbeddedVersion(self: depthai.DeviceBootloader) -> bool

        Returns:
            True when bootloader was booted using latest bootloader integrated in the
            library. False when bootloader is already running on the device and just
            connected to.
        """
    def isUserBootloader(self) -> bool:
        """isUserBootloader(self: depthai.DeviceBootloader) -> bool

        Retrieves whether current bootloader is User Bootloader (B out of A/B
        configuration)
        """
    def isUserBootloaderSupported(self) -> bool:
        """isUserBootloaderSupported(self: depthai.DeviceBootloader) -> bool

        Checks whether User Bootloader is supported with current bootloader

        Returns:
            true of User Bootloader is supported, false otherwise
        """
    def readApplicationInfo(self, memory: DeviceBootloader.Memory) -> DeviceBootloader.ApplicationInfo:
        """readApplicationInfo(self: depthai.DeviceBootloader, memory: depthai.DeviceBootloader.Memory) -> depthai.DeviceBootloader.ApplicationInfo

        Reads information about flashed application in specified memory from device

        Parameter ``memory``:
            Specifies which memory to query
        """
    def readConfig(self, memory: DeviceBootloader.Memory = ..., type: DeviceBootloader.Type = ...) -> DeviceBootloader.Config:
        """readConfig(self: depthai.DeviceBootloader, memory: depthai.DeviceBootloader.Memory = <Memory.AUTO: -1>, type: depthai.DeviceBootloader.Type = <Type.AUTO: -1>) -> depthai.DeviceBootloader.Config

        Reads configuration from bootloader

        Parameter ``memory``:
            Optional - from which memory to read configuration

        Parameter ``type``:
            Optional - from which type of bootloader to read configuration

        Returns:
            Configuration structure
        """
    def readConfigData(self, memory: DeviceBootloader.Memory = ..., type: DeviceBootloader.Type = ...) -> json:
        """readConfigData(self: depthai.DeviceBootloader, memory: depthai.DeviceBootloader.Memory = <Memory.AUTO: -1>, type: depthai.DeviceBootloader.Type = <Type.AUTO: -1>) -> json

        Reads configuration data from bootloader

        Returns:
            Unstructured configuration data

        Parameter ``memory``:
            Optional - from which memory to read configuration data

        Parameter ``type``:
            Optional - from which type of bootloader to read configuration data
        """
    def readCustom(self, memory: DeviceBootloader.Memory, offset: int, size: int, filename: str, progressCallback: Callable[[float], None] = ...) -> tuple[bool, str]:
        """readCustom(*args, **kwargs)
        Overloaded function.

        1. readCustom(self: depthai.DeviceBootloader, memory: depthai.DeviceBootloader.Memory, offset: int, size: int, filename: str, progressCallback: Callable[[float], None] = None) -> tuple[bool, str]

        Reads arbitrary data at custom offset in specified memory

        Parameter ``memory``:
            Memory to read

        Parameter ``offset``:
            Offset at which to read the specified bytes

        Parameter ``size``:
            Number of bytes to read

        Parameter ``data``:
            Data to read to. Must be at least 'size' number of bytes big

        Parameter ``progressCallback``:
            Callback that sends back a value between 0..1 which signifies current
            reading progress

        2. readCustom(self: depthai.DeviceBootloader, memory: depthai.DeviceBootloader.Memory, offset: int, size: int, progressCallback: Callable[[float], None] = None) -> tuple[bool, str, std::vector<unsigned char, std::allocator<unsigned char> >]

        Reads arbitrary data at custom offset in specified memory

        Parameter ``memory``:
            Memory to read

        Parameter ``offset``:
            Offset at which to read the specified bytes

        Parameter ``size``:
            Number of bytes to read

        Parameter ``data``:
            Data to read to. Must be at least 'size' number of bytes big

        Parameter ``progressCallback``:
            Callback that sends back a value between 0..1 which signifies current
            reading progress
        """
    @overload
    @staticmethod
    def saveDepthaiApplicationPackage(path: os.PathLike, pipeline: Pipeline, pathToCmd: os.PathLike = ..., compress: bool = ..., applicationName: str = ..., checkChecksum: bool = ...) -> None:
        """saveDepthaiApplicationPackage(*args, **kwargs)
        Overloaded function.

        1. saveDepthaiApplicationPackage(path: os.PathLike, pipeline: depthai.Pipeline, pathToCmd: os.PathLike = PosixPath('.'), compress: bool = False, applicationName: str = '', checkChecksum: bool = False) -> None

        Saves application package to a file which can be flashed to depthai device.

        Parameter ``path``:
            Path where to save the application package

        Parameter ``pipeline``:
            Pipeline from which to create the application package

        Parameter ``pathToCmd``:
            Optional path to custom device firmware

        Parameter ``compress``:
            Optional boolean which specifies if contents should be compressed

        Parameter ``applicationName``:
            Optional name the application that is flashed

        2. saveDepthaiApplicationPackage(path: os.PathLike, pipeline: depthai.Pipeline, compress: bool, applicationName: str = '', checkChecksum: bool = False) -> None

        Saves application package to a file which can be flashed to depthai device.

        Parameter ``path``:
            Path where to save the application package

        Parameter ``pipeline``:
            Pipeline from which to create the application package

        Parameter ``compress``:
            Specifies if contents should be compressed

        Parameter ``applicationName``:
            Optional name the application that is flashed
        """
    @overload
    @staticmethod
    def saveDepthaiApplicationPackage(path: os.PathLike, pipeline: Pipeline, compress: bool, applicationName: str = ..., checkChecksum: bool = ...) -> None:
        """saveDepthaiApplicationPackage(*args, **kwargs)
        Overloaded function.

        1. saveDepthaiApplicationPackage(path: os.PathLike, pipeline: depthai.Pipeline, pathToCmd: os.PathLike = PosixPath('.'), compress: bool = False, applicationName: str = '', checkChecksum: bool = False) -> None

        Saves application package to a file which can be flashed to depthai device.

        Parameter ``path``:
            Path where to save the application package

        Parameter ``pipeline``:
            Pipeline from which to create the application package

        Parameter ``pathToCmd``:
            Optional path to custom device firmware

        Parameter ``compress``:
            Optional boolean which specifies if contents should be compressed

        Parameter ``applicationName``:
            Optional name the application that is flashed

        2. saveDepthaiApplicationPackage(path: os.PathLike, pipeline: depthai.Pipeline, compress: bool, applicationName: str = '', checkChecksum: bool = False) -> None

        Saves application package to a file which can be flashed to depthai device.

        Parameter ``path``:
            Path where to save the application package

        Parameter ``pipeline``:
            Pipeline from which to create the application package

        Parameter ``compress``:
            Specifies if contents should be compressed

        Parameter ``applicationName``:
            Optional name the application that is flashed
        """
    def __enter__(self) -> DeviceBootloader:
        """__enter__(self: depthai.DeviceBootloader) -> depthai.DeviceBootloader"""
    def __exit__(self, arg0: object, arg1: object, arg2: object) -> None:
        """__exit__(self: depthai.DeviceBootloader, arg0: object, arg1: object, arg2: object) -> None"""

class DeviceDesc:
    mxid: str
    name: str
    platform: XLinkPlatform
    protocol: XLinkProtocol
    state: XLinkDeviceState
    status: XLinkError_t
    def __init__(self) -> None:
        """__init__(self: depthai.DeviceDesc) -> None"""

class DeviceInfo:
    """Describes a connected device"""
    deviceId: str
    name: str
    platform: XLinkPlatform
    protocol: XLinkProtocol
    state: XLinkDeviceState
    status: XLinkError_t
    @overload
    def __init__(self) -> None:
        """__init__(*args, **kwargs)
        Overloaded function.

        1. __init__(self: depthai.DeviceInfo) -> None

        2. __init__(self: depthai.DeviceInfo, name: str, deviceId: str, state: depthai.XLinkDeviceState, protocol: depthai.XLinkProtocol, platform: depthai.XLinkPlatform, status: depthai.XLinkError_t) -> None

        3. __init__(self: depthai.DeviceInfo, deviceIdOrName: str) -> None

        Creates a DeviceInfo by checking whether supplied parameter is a DeviceID or
        IP/USB name

        Parameter ``deviceIdOrName``:
            Either DeviceId, IP Address or USB port name

        4. __init__(self: depthai.DeviceInfo, arg0: depthai.DeviceDesc) -> None
        """
    @overload
    def __init__(self, name: str, deviceId: str, state: XLinkDeviceState, protocol: XLinkProtocol, platform: XLinkPlatform, status: XLinkError_t) -> None:
        """__init__(*args, **kwargs)
        Overloaded function.

        1. __init__(self: depthai.DeviceInfo) -> None

        2. __init__(self: depthai.DeviceInfo, name: str, deviceId: str, state: depthai.XLinkDeviceState, protocol: depthai.XLinkProtocol, platform: depthai.XLinkPlatform, status: depthai.XLinkError_t) -> None

        3. __init__(self: depthai.DeviceInfo, deviceIdOrName: str) -> None

        Creates a DeviceInfo by checking whether supplied parameter is a DeviceID or
        IP/USB name

        Parameter ``deviceIdOrName``:
            Either DeviceId, IP Address or USB port name

        4. __init__(self: depthai.DeviceInfo, arg0: depthai.DeviceDesc) -> None
        """
    @overload
    def __init__(self, deviceIdOrName: str) -> None:
        """__init__(*args, **kwargs)
        Overloaded function.

        1. __init__(self: depthai.DeviceInfo) -> None

        2. __init__(self: depthai.DeviceInfo, name: str, deviceId: str, state: depthai.XLinkDeviceState, protocol: depthai.XLinkProtocol, platform: depthai.XLinkPlatform, status: depthai.XLinkError_t) -> None

        3. __init__(self: depthai.DeviceInfo, deviceIdOrName: str) -> None

        Creates a DeviceInfo by checking whether supplied parameter is a DeviceID or
        IP/USB name

        Parameter ``deviceIdOrName``:
            Either DeviceId, IP Address or USB port name

        4. __init__(self: depthai.DeviceInfo, arg0: depthai.DeviceDesc) -> None
        """
    @overload
    def __init__(self, arg0: DeviceDesc) -> None:
        """__init__(*args, **kwargs)
        Overloaded function.

        1. __init__(self: depthai.DeviceInfo) -> None

        2. __init__(self: depthai.DeviceInfo, name: str, deviceId: str, state: depthai.XLinkDeviceState, protocol: depthai.XLinkProtocol, platform: depthai.XLinkPlatform, status: depthai.XLinkError_t) -> None

        3. __init__(self: depthai.DeviceInfo, deviceIdOrName: str) -> None

        Creates a DeviceInfo by checking whether supplied parameter is a DeviceID or
        IP/USB name

        Parameter ``deviceIdOrName``:
            Either DeviceId, IP Address or USB port name

        4. __init__(self: depthai.DeviceInfo, arg0: depthai.DeviceDesc) -> None
        """
    def getDeviceId(self) -> str:
        """getDeviceId(self: depthai.DeviceInfo) -> str"""
    def getXLinkDeviceDesc(self) -> DeviceDesc:
        """getXLinkDeviceDesc(self: depthai.DeviceInfo) -> depthai.DeviceDesc"""

class DeviceModelZoo:
    """On device models, relevant for RVC4 platform

    Members:

      NEURAL_DEPTH_LARGE

      NEURAL_DEPTH_MEDIUM

      NEURAL_DEPTH_SMALL

      NEURAL_DEPTH_NANO"""
    __members__: ClassVar[dict] = ...  # read-only
    NEURAL_DEPTH_LARGE: ClassVar[DeviceModelZoo] = ...
    NEURAL_DEPTH_MEDIUM: ClassVar[DeviceModelZoo] = ...
    NEURAL_DEPTH_NANO: ClassVar[DeviceModelZoo] = ...
    NEURAL_DEPTH_SMALL: ClassVar[DeviceModelZoo] = ...
    __entries: ClassVar[dict] = ...
    def __init__(self, value: int) -> None:
        """__init__(self: depthai.DeviceModelZoo, value: int) -> None"""
    def __eq__(self, other: object) -> bool:
        """__eq__(self: object, other: object) -> bool"""
    def __hash__(self) -> int:
        """__hash__(self: object) -> int"""
    def __index__(self) -> int:
        """__index__(self: depthai.DeviceModelZoo) -> int"""
    def __int__(self) -> int:
        """__int__(self: depthai.DeviceModelZoo) -> int"""
    def __ne__(self, other: object) -> bool:
        """__ne__(self: object, other: object) -> bool"""
    @property
    def name(self) -> str:
        """name(self: object) -> str

        name(self: object) -> str
        """
    @property
    def value(self) -> int:
        """(arg0: depthai.DeviceModelZoo) -> int"""

class DeviceNode(ThreadedNode):
    def __init__(self, *args, **kwargs) -> None:
        """Initialize self.  See help(type(self)) for accurate signature."""

class DeviceNodeGroup(DeviceNode):
    def __init__(self, *args, **kwargs) -> None:
        """Initialize self.  See help(type(self)) for accurate signature."""

class DynamicCalibrationControl(Buffer):
    class Commands:
        class ApplyCalibration:
            calibration: Incomplete
            flash: bool
            @overload
            def __init__(self) -> None:
                """__init__(*args, **kwargs)
                Overloaded function.

                1. __init__(self: depthai.DynamicCalibrationControl.Commands.ApplyCalibration) -> None

                2. __init__(self: depthai.DynamicCalibrationControl.Commands.ApplyCalibration, calibration: dai::CalibrationHandler, flash: bool = False) -> None
                """
            @overload
            def __init__(self, calibration, flash: bool = ...) -> None:
                """__init__(*args, **kwargs)
                Overloaded function.

                1. __init__(self: depthai.DynamicCalibrationControl.Commands.ApplyCalibration) -> None

                2. __init__(self: depthai.DynamicCalibrationControl.Commands.ApplyCalibration, calibration: dai::CalibrationHandler, flash: bool = False) -> None
                """

        class Calibrate:
            force: bool
            def __init__(self, force: bool = ...) -> None:
                """__init__(self: depthai.DynamicCalibrationControl.Commands.Calibrate, force: bool = False) -> None"""

        class CalibrationQuality:
            force: bool
            def __init__(self, force: bool = ...) -> None:
                """__init__(self: depthai.DynamicCalibrationControl.Commands.CalibrationQuality, force: bool = False) -> None"""

        class ComputeCalibrationMetrics:
            calibration: Incomplete
            def __init__(self, calibration) -> None:
                """__init__(self: depthai.DynamicCalibrationControl.Commands.ComputeCalibrationMetrics, calibration: dai::CalibrationHandler) -> None"""

        class LoadImage:
            def __init__(self) -> None:
                """__init__(self: depthai.DynamicCalibrationControl.Commands.LoadImage) -> None"""

        class ResetData:
            def __init__(self) -> None:
                """__init__(self: depthai.DynamicCalibrationControl.Commands.ResetData) -> None"""

        class SetPerformanceMode:
            performanceMode: DynamicCalibrationControl.PerformanceMode
            def __init__(self, performanceMode: DynamicCalibrationControl.PerformanceMode) -> None:
                """__init__(self: depthai.DynamicCalibrationControl.Commands.SetPerformanceMode, performanceMode: depthai.DynamicCalibrationControl.PerformanceMode) -> None"""

        class StartCalibration:
            calibrationPeriod: float
            loadImagePeriod: float
            def __init__(self, loadImagePeriod: float = ..., calibrationPeriod: float = ...) -> None:
                """__init__(self: depthai.DynamicCalibrationControl.Commands.StartCalibration, loadImagePeriod: float = 0.5, calibrationPeriod: float = 5.0) -> None"""

        class StopCalibration:
            def __init__(self) -> None:
                """__init__(self: depthai.DynamicCalibrationControl.Commands.StopCalibration) -> None"""
        def __init__(self, *args, **kwargs) -> None:
            """Initialize self.  See help(type(self)) for accurate signature."""

    class PerformanceMode:
        """Members:

          DEFAULT

          STATIC_SCENERY

          OPTIMIZE_SPEED

          OPTIMIZE_PERFORMANCE

          SKIP_CHECKS"""
        __members__: ClassVar[dict] = ...  # read-only
        DEFAULT: ClassVar[DynamicCalibrationControl.PerformanceMode] = ...
        OPTIMIZE_PERFORMANCE: ClassVar[DynamicCalibrationControl.PerformanceMode] = ...
        OPTIMIZE_SPEED: ClassVar[DynamicCalibrationControl.PerformanceMode] = ...
        SKIP_CHECKS: ClassVar[DynamicCalibrationControl.PerformanceMode] = ...
        STATIC_SCENERY: ClassVar[DynamicCalibrationControl.PerformanceMode] = ...
        __entries: ClassVar[dict] = ...
        def __init__(self, value: int) -> None:
            """__init__(self: depthai.DynamicCalibrationControl.PerformanceMode, value: int) -> None"""
        def __eq__(self, other: object) -> bool:
            """__eq__(self: object, other: object) -> bool"""
        def __hash__(self) -> int:
            """__hash__(self: object) -> int"""
        def __index__(self) -> int:
            """__index__(self: depthai.DynamicCalibrationControl.PerformanceMode) -> int"""
        def __int__(self) -> int:
            """__int__(self: depthai.DynamicCalibrationControl.PerformanceMode) -> int"""
        def __ne__(self, other: object) -> bool:
            """__ne__(self: object, other: object) -> bool"""
        @property
        def name(self) -> str:
            """name(self: object) -> str

            name(self: object) -> str
            """
        @property
        def value(self) -> int:
            """(arg0: depthai.DynamicCalibrationControl.PerformanceMode) -> int"""
    DEFAULT: ClassVar[DynamicCalibrationControl.PerformanceMode] = ...
    OPTIMIZE_PERFORMANCE: ClassVar[DynamicCalibrationControl.PerformanceMode] = ...
    OPTIMIZE_SPEED: ClassVar[DynamicCalibrationControl.PerformanceMode] = ...
    SKIP_CHECKS: ClassVar[DynamicCalibrationControl.PerformanceMode] = ...
    STATIC_SCENERY: ClassVar[DynamicCalibrationControl.PerformanceMode] = ...
    @overload
    def __init__(self) -> None:
        """__init__(*args, **kwargs)
        Overloaded function.

        1. __init__(self: depthai.DynamicCalibrationControl) -> None

        2. __init__(self: depthai.DynamicCalibrationControl, arg0: depthai.DynamicCalibrationControl.Commands.Calibrate) -> None

        3. __init__(self: depthai.DynamicCalibrationControl, arg0: depthai.DynamicCalibrationControl.Commands.CalibrationQuality) -> None

        4. __init__(self: depthai.DynamicCalibrationControl, arg0: depthai.DynamicCalibrationControl.Commands.StartCalibration) -> None

        5. __init__(self: depthai.DynamicCalibrationControl, arg0: depthai.DynamicCalibrationControl.Commands.StopCalibration) -> None

        6. __init__(self: depthai.DynamicCalibrationControl, arg0: depthai.DynamicCalibrationControl.Commands.LoadImage) -> None

        7. __init__(self: depthai.DynamicCalibrationControl, arg0: depthai.DynamicCalibrationControl.Commands.ApplyCalibration) -> None

        8. __init__(self: depthai.DynamicCalibrationControl, arg0: depthai.DynamicCalibrationControl.Commands.ResetData) -> None

        9. __init__(self: depthai.DynamicCalibrationControl, arg0: depthai.DynamicCalibrationControl.Commands.SetPerformanceMode) -> None
        """
    @overload
    def __init__(self, arg0: DynamicCalibrationControl.Commands.Calibrate) -> None:
        """__init__(*args, **kwargs)
        Overloaded function.

        1. __init__(self: depthai.DynamicCalibrationControl) -> None

        2. __init__(self: depthai.DynamicCalibrationControl, arg0: depthai.DynamicCalibrationControl.Commands.Calibrate) -> None

        3. __init__(self: depthai.DynamicCalibrationControl, arg0: depthai.DynamicCalibrationControl.Commands.CalibrationQuality) -> None

        4. __init__(self: depthai.DynamicCalibrationControl, arg0: depthai.DynamicCalibrationControl.Commands.StartCalibration) -> None

        5. __init__(self: depthai.DynamicCalibrationControl, arg0: depthai.DynamicCalibrationControl.Commands.StopCalibration) -> None

        6. __init__(self: depthai.DynamicCalibrationControl, arg0: depthai.DynamicCalibrationControl.Commands.LoadImage) -> None

        7. __init__(self: depthai.DynamicCalibrationControl, arg0: depthai.DynamicCalibrationControl.Commands.ApplyCalibration) -> None

        8. __init__(self: depthai.DynamicCalibrationControl, arg0: depthai.DynamicCalibrationControl.Commands.ResetData) -> None

        9. __init__(self: depthai.DynamicCalibrationControl, arg0: depthai.DynamicCalibrationControl.Commands.SetPerformanceMode) -> None
        """
    @overload
    def __init__(self, arg0: DynamicCalibrationControl.Commands.CalibrationQuality) -> None:
        """__init__(*args, **kwargs)
        Overloaded function.

        1. __init__(self: depthai.DynamicCalibrationControl) -> None

        2. __init__(self: depthai.DynamicCalibrationControl, arg0: depthai.DynamicCalibrationControl.Commands.Calibrate) -> None

        3. __init__(self: depthai.DynamicCalibrationControl, arg0: depthai.DynamicCalibrationControl.Commands.CalibrationQuality) -> None

        4. __init__(self: depthai.DynamicCalibrationControl, arg0: depthai.DynamicCalibrationControl.Commands.StartCalibration) -> None

        5. __init__(self: depthai.DynamicCalibrationControl, arg0: depthai.DynamicCalibrationControl.Commands.StopCalibration) -> None

        6. __init__(self: depthai.DynamicCalibrationControl, arg0: depthai.DynamicCalibrationControl.Commands.LoadImage) -> None

        7. __init__(self: depthai.DynamicCalibrationControl, arg0: depthai.DynamicCalibrationControl.Commands.ApplyCalibration) -> None

        8. __init__(self: depthai.DynamicCalibrationControl, arg0: depthai.DynamicCalibrationControl.Commands.ResetData) -> None

        9. __init__(self: depthai.DynamicCalibrationControl, arg0: depthai.DynamicCalibrationControl.Commands.SetPerformanceMode) -> None
        """
    @overload
    def __init__(self, arg0: DynamicCalibrationControl.Commands.StartCalibration) -> None:
        """__init__(*args, **kwargs)
        Overloaded function.

        1. __init__(self: depthai.DynamicCalibrationControl) -> None

        2. __init__(self: depthai.DynamicCalibrationControl, arg0: depthai.DynamicCalibrationControl.Commands.Calibrate) -> None

        3. __init__(self: depthai.DynamicCalibrationControl, arg0: depthai.DynamicCalibrationControl.Commands.CalibrationQuality) -> None

        4. __init__(self: depthai.DynamicCalibrationControl, arg0: depthai.DynamicCalibrationControl.Commands.StartCalibration) -> None

        5. __init__(self: depthai.DynamicCalibrationControl, arg0: depthai.DynamicCalibrationControl.Commands.StopCalibration) -> None

        6. __init__(self: depthai.DynamicCalibrationControl, arg0: depthai.DynamicCalibrationControl.Commands.LoadImage) -> None

        7. __init__(self: depthai.DynamicCalibrationControl, arg0: depthai.DynamicCalibrationControl.Commands.ApplyCalibration) -> None

        8. __init__(self: depthai.DynamicCalibrationControl, arg0: depthai.DynamicCalibrationControl.Commands.ResetData) -> None

        9. __init__(self: depthai.DynamicCalibrationControl, arg0: depthai.DynamicCalibrationControl.Commands.SetPerformanceMode) -> None
        """
    @overload
    def __init__(self, arg0: DynamicCalibrationControl.Commands.StopCalibration) -> None:
        """__init__(*args, **kwargs)
        Overloaded function.

        1. __init__(self: depthai.DynamicCalibrationControl) -> None

        2. __init__(self: depthai.DynamicCalibrationControl, arg0: depthai.DynamicCalibrationControl.Commands.Calibrate) -> None

        3. __init__(self: depthai.DynamicCalibrationControl, arg0: depthai.DynamicCalibrationControl.Commands.CalibrationQuality) -> None

        4. __init__(self: depthai.DynamicCalibrationControl, arg0: depthai.DynamicCalibrationControl.Commands.StartCalibration) -> None

        5. __init__(self: depthai.DynamicCalibrationControl, arg0: depthai.DynamicCalibrationControl.Commands.StopCalibration) -> None

        6. __init__(self: depthai.DynamicCalibrationControl, arg0: depthai.DynamicCalibrationControl.Commands.LoadImage) -> None

        7. __init__(self: depthai.DynamicCalibrationControl, arg0: depthai.DynamicCalibrationControl.Commands.ApplyCalibration) -> None

        8. __init__(self: depthai.DynamicCalibrationControl, arg0: depthai.DynamicCalibrationControl.Commands.ResetData) -> None

        9. __init__(self: depthai.DynamicCalibrationControl, arg0: depthai.DynamicCalibrationControl.Commands.SetPerformanceMode) -> None
        """
    @overload
    def __init__(self, arg0: DynamicCalibrationControl.Commands.LoadImage) -> None:
        """__init__(*args, **kwargs)
        Overloaded function.

        1. __init__(self: depthai.DynamicCalibrationControl) -> None

        2. __init__(self: depthai.DynamicCalibrationControl, arg0: depthai.DynamicCalibrationControl.Commands.Calibrate) -> None

        3. __init__(self: depthai.DynamicCalibrationControl, arg0: depthai.DynamicCalibrationControl.Commands.CalibrationQuality) -> None

        4. __init__(self: depthai.DynamicCalibrationControl, arg0: depthai.DynamicCalibrationControl.Commands.StartCalibration) -> None

        5. __init__(self: depthai.DynamicCalibrationControl, arg0: depthai.DynamicCalibrationControl.Commands.StopCalibration) -> None

        6. __init__(self: depthai.DynamicCalibrationControl, arg0: depthai.DynamicCalibrationControl.Commands.LoadImage) -> None

        7. __init__(self: depthai.DynamicCalibrationControl, arg0: depthai.DynamicCalibrationControl.Commands.ApplyCalibration) -> None

        8. __init__(self: depthai.DynamicCalibrationControl, arg0: depthai.DynamicCalibrationControl.Commands.ResetData) -> None

        9. __init__(self: depthai.DynamicCalibrationControl, arg0: depthai.DynamicCalibrationControl.Commands.SetPerformanceMode) -> None
        """
    @overload
    def __init__(self, arg0: DynamicCalibrationControl.Commands.ApplyCalibration) -> None:
        """__init__(*args, **kwargs)
        Overloaded function.

        1. __init__(self: depthai.DynamicCalibrationControl) -> None

        2. __init__(self: depthai.DynamicCalibrationControl, arg0: depthai.DynamicCalibrationControl.Commands.Calibrate) -> None

        3. __init__(self: depthai.DynamicCalibrationControl, arg0: depthai.DynamicCalibrationControl.Commands.CalibrationQuality) -> None

        4. __init__(self: depthai.DynamicCalibrationControl, arg0: depthai.DynamicCalibrationControl.Commands.StartCalibration) -> None

        5. __init__(self: depthai.DynamicCalibrationControl, arg0: depthai.DynamicCalibrationControl.Commands.StopCalibration) -> None

        6. __init__(self: depthai.DynamicCalibrationControl, arg0: depthai.DynamicCalibrationControl.Commands.LoadImage) -> None

        7. __init__(self: depthai.DynamicCalibrationControl, arg0: depthai.DynamicCalibrationControl.Commands.ApplyCalibration) -> None

        8. __init__(self: depthai.DynamicCalibrationControl, arg0: depthai.DynamicCalibrationControl.Commands.ResetData) -> None

        9. __init__(self: depthai.DynamicCalibrationControl, arg0: depthai.DynamicCalibrationControl.Commands.SetPerformanceMode) -> None
        """
    @overload
    def __init__(self, arg0: DynamicCalibrationControl.Commands.ResetData) -> None:
        """__init__(*args, **kwargs)
        Overloaded function.

        1. __init__(self: depthai.DynamicCalibrationControl) -> None

        2. __init__(self: depthai.DynamicCalibrationControl, arg0: depthai.DynamicCalibrationControl.Commands.Calibrate) -> None

        3. __init__(self: depthai.DynamicCalibrationControl, arg0: depthai.DynamicCalibrationControl.Commands.CalibrationQuality) -> None

        4. __init__(self: depthai.DynamicCalibrationControl, arg0: depthai.DynamicCalibrationControl.Commands.StartCalibration) -> None

        5. __init__(self: depthai.DynamicCalibrationControl, arg0: depthai.DynamicCalibrationControl.Commands.StopCalibration) -> None

        6. __init__(self: depthai.DynamicCalibrationControl, arg0: depthai.DynamicCalibrationControl.Commands.LoadImage) -> None

        7. __init__(self: depthai.DynamicCalibrationControl, arg0: depthai.DynamicCalibrationControl.Commands.ApplyCalibration) -> None

        8. __init__(self: depthai.DynamicCalibrationControl, arg0: depthai.DynamicCalibrationControl.Commands.ResetData) -> None

        9. __init__(self: depthai.DynamicCalibrationControl, arg0: depthai.DynamicCalibrationControl.Commands.SetPerformanceMode) -> None
        """
    @overload
    def __init__(self, arg0: DynamicCalibrationControl.Commands.SetPerformanceMode) -> None:
        """__init__(*args, **kwargs)
        Overloaded function.

        1. __init__(self: depthai.DynamicCalibrationControl) -> None

        2. __init__(self: depthai.DynamicCalibrationControl, arg0: depthai.DynamicCalibrationControl.Commands.Calibrate) -> None

        3. __init__(self: depthai.DynamicCalibrationControl, arg0: depthai.DynamicCalibrationControl.Commands.CalibrationQuality) -> None

        4. __init__(self: depthai.DynamicCalibrationControl, arg0: depthai.DynamicCalibrationControl.Commands.StartCalibration) -> None

        5. __init__(self: depthai.DynamicCalibrationControl, arg0: depthai.DynamicCalibrationControl.Commands.StopCalibration) -> None

        6. __init__(self: depthai.DynamicCalibrationControl, arg0: depthai.DynamicCalibrationControl.Commands.LoadImage) -> None

        7. __init__(self: depthai.DynamicCalibrationControl, arg0: depthai.DynamicCalibrationControl.Commands.ApplyCalibration) -> None

        8. __init__(self: depthai.DynamicCalibrationControl, arg0: depthai.DynamicCalibrationControl.Commands.ResetData) -> None

        9. __init__(self: depthai.DynamicCalibrationControl, arg0: depthai.DynamicCalibrationControl.Commands.SetPerformanceMode) -> None
        """
    @staticmethod
    def applyCalibration(calibration, flash: bool = ...) -> DynamicCalibrationControl:
        """applyCalibration(calibration: dai::CalibrationHandler, flash: bool = False) -> depthai.DynamicCalibrationControl

        Create a DynamicCalibrationControl with an ApplyCalibration command.
        """
    @staticmethod
    def calibrate(force: bool = ...) -> DynamicCalibrationControl:
        """calibrate(force: bool = False) -> depthai.DynamicCalibrationControl

        Create a DynamicCalibrationControl with a Calibrate command.
        """
    @staticmethod
    def calibrationQuality(force: bool = ...) -> DynamicCalibrationControl:
        """calibrationQuality(force: bool = False) -> depthai.DynamicCalibrationControl

        Create a DynamicCalibrationControl with a CalibrationQuality command.
        """
    @staticmethod
    def computeCalibrationMetrics(calibration) -> DynamicCalibrationControl:
        """computeCalibrationMetrics(calibration: dai::CalibrationHandler) -> depthai.DynamicCalibrationControl

        Comute metrics on the given calibration.
        """
    @staticmethod
    def loadImage() -> DynamicCalibrationControl:
        """loadImage() -> depthai.DynamicCalibrationControl

        Create a DynamicCalibrationControl with a LoadImage command.
        """
    @staticmethod
    def resetData() -> DynamicCalibrationControl:
        """resetData() -> depthai.DynamicCalibrationControl

        Create a DynamicCalibrationControl with a ResetData command.
        """
    @staticmethod
    def setPerformanceMode(mode: DynamicCalibrationControl.PerformanceMode = ...) -> DynamicCalibrationControl:
        """setPerformanceMode(mode: depthai.DynamicCalibrationControl.PerformanceMode = <PerformanceMode.DEFAULT: 0>) -> depthai.DynamicCalibrationControl

        Create a DynamicCalibrationControl with a SetPerformanceMode command.
        """
    @staticmethod
    def startCalibration(loadImagePeriod: float = ..., calibrationPeriod: float = ...) -> DynamicCalibrationControl:
        """startCalibration(loadImagePeriod: float = 0.5, calibrationPeriod: float = 5.0) -> depthai.DynamicCalibrationControl

        Create a DynamicCalibrationControl with a StartCalibration command.
        """
    @staticmethod
    def stopCalibration() -> DynamicCalibrationControl:
        """stopCalibration() -> depthai.DynamicCalibrationControl

        Create a DynamicCalibrationControl with a StopCalibration command.
        """

class DynamicCalibrationProperties:
    """Specify properties for Dynamic calibration."""
    def __init__(self, *args, **kwargs) -> None:
        """Initialize self.  See help(type(self)) for accurate signature."""

class DynamicCalibrationResult(Buffer):
    calibrationData: DynamicCalibrationResultData | None
    info: str
    @overload
    def __init__(self) -> None:
        """__init__(*args, **kwargs)
        Overloaded function.

        1. __init__(self: depthai.DynamicCalibrationResult) -> None

        2. __init__(self: depthai.DynamicCalibrationResult, data: depthai.DynamicCalibrationResultData, info: str) -> None

        3. __init__(self: depthai.DynamicCalibrationResult, info: str) -> None
        """
    @overload
    def __init__(self, data: DynamicCalibrationResultData, info: str) -> None:
        """__init__(*args, **kwargs)
        Overloaded function.

        1. __init__(self: depthai.DynamicCalibrationResult) -> None

        2. __init__(self: depthai.DynamicCalibrationResult, data: depthai.DynamicCalibrationResultData, info: str) -> None

        3. __init__(self: depthai.DynamicCalibrationResult, info: str) -> None
        """
    @overload
    def __init__(self, info: str) -> None:
        """__init__(*args, **kwargs)
        Overloaded function.

        1. __init__(self: depthai.DynamicCalibrationResult) -> None

        2. __init__(self: depthai.DynamicCalibrationResult, data: depthai.DynamicCalibrationResultData, info: str) -> None

        3. __init__(self: depthai.DynamicCalibrationResult, info: str) -> None
        """

class DynamicCalibrationResultData:
    calibrationDifference: CalibrationQualityData
    currentCalibration: Incomplete
    newCalibration: Incomplete
    def __init__(self) -> None:
        """__init__(self: depthai.DynamicCalibrationResultData) -> None"""

class EdgeDetectorConfig(Buffer):
    """EdgeDetectorConfig message. Carries sobel edge filter config."""
    def __init__(self) -> None:
        """__init__(self: depthai.EdgeDetectorConfig) -> None"""
    def getConfigData(self) -> EdgeDetectorConfigData:
        """getConfigData(self: depthai.EdgeDetectorConfig) -> depthai.EdgeDetectorConfigData

        Retrieve configuration data for EdgeDetector

        Returns:
            EdgeDetectorConfigData: sobel filter horizontal and vertical 3x3 kernels
        """
    def setSobelFilterKernels(self, horizontalKernel: list[list[int]], verticalKernel: list[list[int]]) -> None:
        """setSobelFilterKernels(self: depthai.EdgeDetectorConfig, horizontalKernel: list[list[int]], verticalKernel: list[list[int]]) -> None

        Set sobel filter horizontal and vertical 3x3 kernels

        Parameter ``horizontalKernel``:
            Used for horizontal gradient computation in 3x3 Sobel filter

        Parameter ``verticalKernel``:
            Used for vertical gradient computation in 3x3 Sobel filter
        """

class EdgeDetectorConfigData:
    sobelFilterHorizontalKernel: list[list[int]]
    sobelFilterVerticalKernel: list[list[int]]
    def __init__(self) -> None:
        """__init__(self: depthai.EdgeDetectorConfigData) -> None"""

class EdgeDetectorProperties:
    """Specify properties for EdgeDetector"""
    initialConfig: EdgeDetectorConfig
    numFramesPool: int
    outputFrameSize: int
    def __init__(self, *args, **kwargs) -> None:
        """Initialize self.  See help(type(self)) for accurate signature."""

class EepromData:
    """EepromData structure

    Contains the Calibration and Board data stored on device"""
    batchName: str
    batchTime: int
    boardConf: str
    boardCustom: str
    boardName: str
    boardOptions: int
    boardRev: str
    cameraData: dict[CameraBoardSocket, CameraInfo]
    deviceName: str
    hardwareConf: str
    housingExtrinsics: Extrinsics
    imuExtrinsics: Extrinsics
    miscellaneousData: Incomplete
    productName: str
    stereoEnableDistortionCorrection: bool
    stereoRectificationData: StereoRectification
    stereoUseSpecTranslation: bool
    version: int
    verticalCameraSocket: CameraBoardSocket
    def __init__(self) -> None:
        """__init__(self: depthai.EepromData) -> None"""

class EepromError(RuntimeError): ...

class EncodedFrame(Buffer):
    class FrameType:
        """

        Members:

          I

          P

          B

          Unknown"""
        __members__: ClassVar[dict] = ...  # read-only
        B: ClassVar[EncodedFrame.FrameType] = ...
        I: ClassVar[EncodedFrame.FrameType] = ...
        P: ClassVar[EncodedFrame.FrameType] = ...
        Unknown: ClassVar[EncodedFrame.FrameType] = ...
        __entries: ClassVar[dict] = ...
        def __init__(self, value: int) -> None:
            """__init__(self: depthai.EncodedFrame.FrameType, value: int) -> None"""
        def __eq__(self, other: object) -> bool:
            """__eq__(self: object, other: object) -> bool"""
        def __hash__(self) -> int:
            """__hash__(self: object) -> int"""
        def __index__(self) -> int:
            """__index__(self: depthai.EncodedFrame.FrameType) -> int"""
        def __int__(self) -> int:
            """__int__(self: depthai.EncodedFrame.FrameType) -> int"""
        def __ne__(self, other: object) -> bool:
            """__ne__(self: object, other: object) -> bool"""
        @property
        def name(self) -> str:
            """name(self: object) -> str

            name(self: object) -> str
            """
        @property
        def value(self) -> int:
            """(arg0: depthai.EncodedFrame.FrameType) -> int"""

    class Profile:
        """Members:

          JPEG

          AVC

          HEVC"""
        __members__: ClassVar[dict] = ...  # read-only
        AVC: ClassVar[EncodedFrame.Profile] = ...
        HEVC: ClassVar[EncodedFrame.Profile] = ...
        JPEG: ClassVar[EncodedFrame.Profile] = ...
        __entries: ClassVar[dict] = ...
        def __init__(self, value: int) -> None:
            """__init__(self: depthai.EncodedFrame.Profile, value: int) -> None"""
        def __eq__(self, other: object) -> bool:
            """__eq__(self: object, other: object) -> bool"""
        def __hash__(self) -> int:
            """__hash__(self: object) -> int"""
        def __index__(self) -> int:
            """__index__(self: depthai.EncodedFrame.Profile) -> int"""
        def __int__(self) -> int:
            """__int__(self: depthai.EncodedFrame.Profile) -> int"""
        def __ne__(self, other: object) -> bool:
            """__ne__(self: object, other: object) -> bool"""
        @property
        def name(self) -> str:
            """name(self: object) -> str

            name(self: object) -> str
            """
        @property
        def value(self) -> int:
            """(arg0: depthai.EncodedFrame.Profile) -> int"""
    def __init__(self) -> None:
        """__init__(self: depthai.EncodedFrame) -> None"""
    def getBitrate(self) -> int:
        """getBitrate(self: depthai.EncodedFrame) -> int

        Retrieves the encoding bitrate
        """
    def getColorTemperature(self) -> int:
        """getColorTemperature(self: depthai.EncodedFrame) -> int

        Retrieves white-balance color temperature of the light source, in kelvins
        """
    def getExposureTime(self) -> datetime.timedelta:
        """getExposureTime(self: depthai.EncodedFrame) -> datetime.timedelta

        Retrieves exposure time
        """
    def getFrameType(self) -> EncodedFrame.FrameType:
        """getFrameType(self: depthai.EncodedFrame) -> depthai.EncodedFrame.FrameType

        Retrieves frame type (H26x only)
        """
    def getHeight(self) -> int:
        """getHeight(self: depthai.EncodedFrame) -> int

        Retrieves image height in pixels
        """
    def getInstanceNum(self) -> int:
        """getInstanceNum(self: depthai.EncodedFrame) -> int

        Retrieves instance number
        """
    def getLensPosition(self) -> int:
        """getLensPosition(self: depthai.EncodedFrame) -> int

        Retrieves lens position, range 0..255. Returns -1 if not available
        """
    def getLensPositionRaw(self) -> float:
        """getLensPositionRaw(self: depthai.EncodedFrame) -> float

        Retrieves lens position, range 0.0f..1.0f. Returns -1 if not available
        """
    def getLossless(self) -> bool:
        """getLossless(self: depthai.EncodedFrame) -> bool

        Returns true if encoding is lossless (JPEG only)
        """
    def getProfile(self) -> EncodedFrame.Profile:
        """getProfile(self: depthai.EncodedFrame) -> depthai.EncodedFrame.Profile

        Retrieves the encoding profile (JPEG, AVC or HEVC)
        """
    def getQuality(self) -> int:
        """getQuality(self: depthai.EncodedFrame) -> int

        Retrieves the encoding quality
        """
    def getSensitivity(self) -> int:
        """getSensitivity(self: depthai.EncodedFrame) -> int

        Retrieves sensitivity, as an ISO value
        """
    def getSequenceNum(self) -> int:
        """getSequenceNum(self: depthai.EncodedFrame) -> int

        Retrieves image sequence number
        """
    def getTimestamp(self) -> datetime.timedelta:
        """getTimestamp(self: depthai.EncodedFrame) -> datetime.timedelta

        Retrieves timestamp related to dai::Clock::now()
        """
    def getTimestampDevice(self) -> datetime.timedelta:
        """getTimestampDevice(self: depthai.EncodedFrame) -> datetime.timedelta

        Retrieves timestamp directly captured from device's monotonic clock, not
        synchronized to host time. Used mostly for debugging
        """
    def getTransformation(self) -> ImgTransformation:
        """getTransformation(self: depthai.EncodedFrame) -> depthai.ImgTransformation"""
    def getWidth(self) -> int:
        """getWidth(self: depthai.EncodedFrame) -> int

        Retrieves image width in pixels
        """
    def setBitrate(self, arg0: int) -> EncodedFrame:
        """setBitrate(self: depthai.EncodedFrame, arg0: int) -> depthai.EncodedFrame

        Retrieves the encoding bitrate
        """
    def setFrameType(self, arg0: EncodedFrame.FrameType) -> EncodedFrame:
        """setFrameType(self: depthai.EncodedFrame, arg0: depthai.EncodedFrame.FrameType) -> depthai.EncodedFrame

        Retrieves frame type (H26x only)
        """
    def setHeight(self, height: int) -> EncodedFrame:
        """setHeight(self: depthai.EncodedFrame, height: int) -> depthai.EncodedFrame

        Specifies frame height

        Parameter ``height``:
            frame height
        """
    def setLossless(self, arg0: bool) -> EncodedFrame:
        """setLossless(self: depthai.EncodedFrame, arg0: bool) -> depthai.EncodedFrame

        Returns true if encoding is lossless (JPEG only)
        """
    def setProfile(self, arg0: EncodedFrame.Profile) -> EncodedFrame:
        """setProfile(self: depthai.EncodedFrame, arg0: depthai.EncodedFrame.Profile) -> depthai.EncodedFrame

        Retrieves the encoding profile (JPEG, AVC or HEVC)
        """
    def setQuality(self, arg0: int) -> EncodedFrame:
        """setQuality(self: depthai.EncodedFrame, arg0: int) -> depthai.EncodedFrame

        Retrieves the encoding quality
        """
    @overload
    def setSize(self, width: int, height: int) -> EncodedFrame:
        """setSize(*args, **kwargs)
        Overloaded function.

        1. setSize(self: depthai.EncodedFrame, width: int, height: int) -> depthai.EncodedFrame

        Specifies frame size

        Parameter ``height``:
            frame height

        Parameter ``width``:
            frame width

        2. setSize(self: depthai.EncodedFrame, sizer: tuple[int, int]) -> depthai.EncodedFrame

        Specifies frame size

        Parameter ``size``:
            frame size
        """
    @overload
    def setSize(self, sizer: tuple[int, int]) -> EncodedFrame:
        """setSize(*args, **kwargs)
        Overloaded function.

        1. setSize(self: depthai.EncodedFrame, width: int, height: int) -> depthai.EncodedFrame

        Specifies frame size

        Parameter ``height``:
            frame height

        Parameter ``width``:
            frame width

        2. setSize(self: depthai.EncodedFrame, sizer: tuple[int, int]) -> depthai.EncodedFrame

        Specifies frame size

        Parameter ``size``:
            frame size
        """
    def setTransformation(self, arg0: ImgTransformation) -> None:
        """setTransformation(self: depthai.EncodedFrame, arg0: depthai.ImgTransformation) -> None"""
    def setWidth(self, width: int) -> EncodedFrame:
        """setWidth(self: depthai.EncodedFrame, width: int) -> depthai.EncodedFrame

        Specifies frame width

        Parameter ``width``:
            frame width
        """

class EventsManager:
    @overload
    def __init__(self, uploadCachedOnStart: bool = ...) -> None:
        """__init__(*args, **kwargs)
        Overloaded function.

        1. __init__(self: depthai.EventsManager, uploadCachedOnStart: bool = False) -> None

        2. __init__(self: depthai.EventsManager, apiKey: str = '', uploadCachedOnStart: bool = False) -> None
        """
    @overload
    def __init__(self, apiKey: str = ..., uploadCachedOnStart: bool = ...) -> None:
        """__init__(*args, **kwargs)
        Overloaded function.

        1. __init__(self: depthai.EventsManager, uploadCachedOnStart: bool = False) -> None

        2. __init__(self: depthai.EventsManager, apiKey: str = '', uploadCachedOnStart: bool = False) -> None
        """
    def sendEvent(self, name: str, tags: list[str] = ..., extras: dict[str, str] = ..., associateFiles: list[str] = ...) -> str | None:
        """sendEvent(self: depthai.EventsManager, name: str, tags: list[str] = [], extras: dict[str, str] = {}, associateFiles: list[str] = []) -> Optional[str]

        Send an event to the events service

        Parameter ``name``:
            Name of the event

        Parameter ``tags``:
            List of tags to send

        Parameter ``extras``:
            Extra data to send

        Parameter ``associateFiles``:
            List of associate files with ids

        Returns:
            LocalID of the sent Event
        """
    @overload
    def sendSnap(self, name: str, fileGroup: FileGroup = ..., tags: list[str] = ..., extras: dict[str, str] = ..., successCallback: Callable[[SendSnapCallbackResult], None] = ..., failureCallback: Callable[[SendSnapCallbackResult], None] = ...) -> str | None:
        """sendSnap(*args, **kwargs)
        Overloaded function.

        1. sendSnap(self: depthai.EventsManager, name: str, fileGroup: depthai.FileGroup = None, tags: list[str] = [], extras: dict[str, str] = {}, successCallback: Callable[[depthai.SendSnapCallbackResult], None] = None, failureCallback: Callable[[depthai.SendSnapCallbackResult], None] = None) -> Optional[str]

        Send a snap to the events service. Snaps should be used for sending images and
        other files.

        Parameter ``name``:
            Name of the snap

        Parameter ``fileGroup``:
            FileGroup containing FileData objects to send

        Parameter ``tags``:
            List of tags to send

        Parameter ``extras``:
            Extra data to send

        Parameter ``successCallback``:
            Callback to be called when the snap is successfully uploaded to the hub

        Parameter ``failureCallback``:
            Callback to be called if the snap upload is unsuccessful

        Returns:
            LocalID of the sent Snap

        2. sendSnap(self: depthai.EventsManager, name: str, fileTag: Optional[str], imgFrame: depthai.ImgFrame, imgDetections: Optional[depthai.ImgDetections], tags: list[str] = [], extras: dict[str, str] = {}, successCallback: Callable[[depthai.SendSnapCallbackResult], None] = None, failureCallback: Callable[[depthai.SendSnapCallbackResult], None] = None) -> Optional[str]

        Send a snap to the events service. Snaps should be used for sending images and
        other files.

        Parameter ``name``:
            Name of the snap

        Parameter ``fileGroup``:
            FileGroup containing FileData objects to send

        Parameter ``tags``:
            List of tags to send

        Parameter ``extras``:
            Extra data to send

        Parameter ``successCallback``:
            Callback to be called when the snap is successfully uploaded to the hub

        Parameter ``failureCallback``:
            Callback to be called if the snap upload is unsuccessful

        Returns:
            LocalID of the sent Snap
        """
    @overload
    def sendSnap(self, name: str, fileTag: str | None, imgFrame: ImgFrame, imgDetections: ImgDetections | None, tags: list[str] = ..., extras: dict[str, str] = ..., successCallback: Callable[[SendSnapCallbackResult], None] = ..., failureCallback: Callable[[SendSnapCallbackResult], None] = ...) -> str | None:
        """sendSnap(*args, **kwargs)
        Overloaded function.

        1. sendSnap(self: depthai.EventsManager, name: str, fileGroup: depthai.FileGroup = None, tags: list[str] = [], extras: dict[str, str] = {}, successCallback: Callable[[depthai.SendSnapCallbackResult], None] = None, failureCallback: Callable[[depthai.SendSnapCallbackResult], None] = None) -> Optional[str]

        Send a snap to the events service. Snaps should be used for sending images and
        other files.

        Parameter ``name``:
            Name of the snap

        Parameter ``fileGroup``:
            FileGroup containing FileData objects to send

        Parameter ``tags``:
            List of tags to send

        Parameter ``extras``:
            Extra data to send

        Parameter ``successCallback``:
            Callback to be called when the snap is successfully uploaded to the hub

        Parameter ``failureCallback``:
            Callback to be called if the snap upload is unsuccessful

        Returns:
            LocalID of the sent Snap

        2. sendSnap(self: depthai.EventsManager, name: str, fileTag: Optional[str], imgFrame: depthai.ImgFrame, imgDetections: Optional[depthai.ImgDetections], tags: list[str] = [], extras: dict[str, str] = {}, successCallback: Callable[[depthai.SendSnapCallbackResult], None] = None, failureCallback: Callable[[depthai.SendSnapCallbackResult], None] = None) -> Optional[str]

        Send a snap to the events service. Snaps should be used for sending images and
        other files.

        Parameter ``name``:
            Name of the snap

        Parameter ``fileGroup``:
            FileGroup containing FileData objects to send

        Parameter ``tags``:
            List of tags to send

        Parameter ``extras``:
            Extra data to send

        Parameter ``successCallback``:
            Callback to be called when the snap is successfully uploaded to the hub

        Parameter ``failureCallback``:
            Callback to be called if the snap upload is unsuccessful

        Returns:
            LocalID of the sent Snap
        """
    def setCacheDir(self, cacheDir: str) -> None:
        """setCacheDir(self: depthai.EventsManager, cacheDir: str) -> None

        Set the cache directory for storing cached data. By default, the cache directory
        is set to /internal/private

        Parameter ``cacheDir``:
            Cache directory

        Returns:
            void
        """
    def setCacheIfCannotSend(self, cacheIfCannotUpload: bool) -> None:
        """setCacheIfCannotSend(self: depthai.EventsManager, cacheIfCannotUpload: bool) -> None

        Set whether to cache data if it cannot be sent. By default, cacheIfCannotSend is
        set to false

        Parameter ``cacheIfCannotSend``:
            bool

        Returns:
            void
        """
    def setLogResponse(self, logResponse: bool) -> None:
        """setLogResponse(self: depthai.EventsManager, logResponse: bool) -> None

        Set whether to log the responses from the server. By default, logResponse is set
        to false

        Parameter ``logResponse``:
            bool

        Returns:
            void
        """
    def setVerifySsl(self, verifySsl: bool) -> None:
        """setVerifySsl(self: depthai.EventsManager, verifySsl: bool) -> None

        Set whether to verify the SSL certificate. By default, verifySsl is set to false

        Parameter ``verifySsl``:
            bool

        Returns:
            void
        """
    def waitForPendingUploads(self, timeoutMs: int = ...) -> bool:
        """waitForPendingUploads(self: depthai.EventsManager, timeoutMs: int = 0) -> bool

        Wait for pending snaps/events to be processed by the background upload thread

        Parameter ``timeoutMs``:
            Timeout in milliseconds. 0 means wait until uploads are finished, connection
            is dropped, or manager is stopped

        Returns:
            true if the pending data was uploaded before timeout, false if not - either
            because of timeout, dropped connection, or shutdown
        """

class ExternalFrameSyncRole:
    """Which external frame sync role the device should have.

    AUTO_DETECT denotes that the decision will be made by device. It will choose
    between MASTER and SLAVE.

    Members:

      AUTO_DETECT

      MASTER

      SLAVE"""
    __members__: ClassVar[dict] = ...  # read-only
    AUTO_DETECT: ClassVar[ExternalFrameSyncRole] = ...
    MASTER: ClassVar[ExternalFrameSyncRole] = ...
    SLAVE: ClassVar[ExternalFrameSyncRole] = ...
    __entries: ClassVar[dict] = ...
    def __init__(self, value: int) -> None:
        """__init__(self: depthai.ExternalFrameSyncRole, value: int) -> None"""
    def __eq__(self, other: object) -> bool:
        """__eq__(self: object, other: object) -> bool"""
    def __hash__(self) -> int:
        """__hash__(self: object) -> int"""
    def __index__(self) -> int:
        """__index__(self: depthai.ExternalFrameSyncRole) -> int"""
    def __int__(self) -> int:
        """__int__(self: depthai.ExternalFrameSyncRole) -> int"""
    def __ne__(self, other: object) -> bool:
        """__ne__(self: object, other: object) -> bool"""
    @property
    def name(self) -> str:
        """name(self: object) -> str

        name(self: object) -> str
        """
    @property
    def value(self) -> int:
        """(arg0: depthai.ExternalFrameSyncRole) -> int"""

class Extrinsics:
    """Extrinsics structure"""
    rotationMatrix: list[list[float]]
    specTranslation: Point3f
    toCameraSocket: CameraBoardSocket
    translation: Point3f
    def __init__(self) -> None:
        """__init__(self: depthai.Extrinsics) -> None"""

class FeatureTrackerConfig(Buffer):
    """FeatureTrackerConfig message. Carries config for feature tracking algorithm"""

    class CornerDetector:
        """Corner detector configuration structure."""

        class Thresholds:
            """Threshold settings structure for corner detector."""
            decreaseFactor: float
            increaseFactor: float
            initialValue: float
            max: float
            min: float
            def __init__(self) -> None:
                """__init__(self: depthai.FeatureTrackerConfig.CornerDetector.Thresholds) -> None"""

        class Type:
            """

            Members:

              HARRIS

              SHI_THOMASI"""
            __members__: ClassVar[dict] = ...  # read-only
            HARRIS: ClassVar[FeatureTrackerConfig.CornerDetector.Type] = ...
            SHI_THOMASI: ClassVar[FeatureTrackerConfig.CornerDetector.Type] = ...
            __entries: ClassVar[dict] = ...
            def __init__(self, value: int) -> None:
                """__init__(self: depthai.FeatureTrackerConfig.CornerDetector.Type, value: int) -> None"""
            def __eq__(self, other: object) -> bool:
                """__eq__(self: object, other: object) -> bool"""
            def __hash__(self) -> int:
                """__hash__(self: object) -> int"""
            def __index__(self) -> int:
                """__index__(self: depthai.FeatureTrackerConfig.CornerDetector.Type) -> int"""
            def __int__(self) -> int:
                """__int__(self: depthai.FeatureTrackerConfig.CornerDetector.Type) -> int"""
            def __ne__(self, other: object) -> bool:
                """__ne__(self: object, other: object) -> bool"""
            @property
            def name(self) -> str:
                """name(self: object) -> str

                name(self: object) -> str
                """
            @property
            def value(self) -> int:
                """(arg0: depthai.FeatureTrackerConfig.CornerDetector.Type) -> int"""
        cellGridDimension: int
        enableSobel: bool
        enableSorting: bool
        numMaxFeatures: int
        numTargetFeatures: int
        thresholds: FeatureTrackerConfig.CornerDetector.Thresholds
        type: FeatureTrackerConfig.CornerDetector.Type
        def __init__(self) -> None:
            """__init__(self: depthai.FeatureTrackerConfig.CornerDetector) -> None"""

    class FeatureMaintainer:
        """FeatureMaintainer configuration structure."""
        enable: bool
        lostFeatureErrorThreshold: float
        minimumDistanceBetweenFeatures: float
        trackedFeatureThreshold: float
        def __init__(self) -> None:
            """__init__(self: depthai.FeatureTrackerConfig.FeatureMaintainer) -> None"""

    class MotionEstimator:
        """Used for feature reidentification between current and previous features."""

        class OpticalFlow:
            """Optical flow configuration structure."""
            epsilon: float
            maxIterations: int
            pyramidLevels: int
            searchWindowHeight: int
            searchWindowWidth: int
            def __init__(self) -> None:
                """__init__(self: depthai.FeatureTrackerConfig.MotionEstimator.OpticalFlow) -> None"""

        class Type:
            """

            Members:

              LUCAS_KANADE_OPTICAL_FLOW

              HW_MOTION_ESTIMATION"""
            __members__: ClassVar[dict] = ...  # read-only
            HW_MOTION_ESTIMATION: ClassVar[FeatureTrackerConfig.MotionEstimator.Type] = ...
            LUCAS_KANADE_OPTICAL_FLOW: ClassVar[FeatureTrackerConfig.MotionEstimator.Type] = ...
            __entries: ClassVar[dict] = ...
            def __init__(self, value: int) -> None:
                """__init__(self: depthai.FeatureTrackerConfig.MotionEstimator.Type, value: int) -> None"""
            def __eq__(self, other: object) -> bool:
                """__eq__(self: object, other: object) -> bool"""
            def __hash__(self) -> int:
                """__hash__(self: object) -> int"""
            def __index__(self) -> int:
                """__index__(self: depthai.FeatureTrackerConfig.MotionEstimator.Type) -> int"""
            def __int__(self) -> int:
                """__int__(self: depthai.FeatureTrackerConfig.MotionEstimator.Type) -> int"""
            def __ne__(self, other: object) -> bool:
                """__ne__(self: object, other: object) -> bool"""
            @property
            def name(self) -> str:
                """name(self: object) -> str

                name(self: object) -> str
                """
            @property
            def value(self) -> int:
                """(arg0: depthai.FeatureTrackerConfig.MotionEstimator.Type) -> int"""
        enable: bool
        opticalFlow: FeatureTrackerConfig.MotionEstimator.OpticalFlow
        type: FeatureTrackerConfig.MotionEstimator.Type
        def __init__(self) -> None:
            """__init__(self: depthai.FeatureTrackerConfig.MotionEstimator) -> None"""
    def __init__(self) -> None:
        """__init__(self: depthai.FeatureTrackerConfig) -> None"""
    @overload
    def setCornerDetector(self, cornerDetector: FeatureTrackerConfig.CornerDetector.Type) -> FeatureTrackerConfig:
        """setCornerDetector(*args, **kwargs)
        Overloaded function.

        1. setCornerDetector(self: depthai.FeatureTrackerConfig, cornerDetector: depthai.FeatureTrackerConfig.CornerDetector.Type) -> depthai.FeatureTrackerConfig

        Set corner detector algorithm type.

        Parameter ``cornerDetector``:
            Corner detector type, HARRIS or SHI_THOMASI

        2. setCornerDetector(self: depthai.FeatureTrackerConfig, config: depthai.FeatureTrackerConfig.CornerDetector) -> depthai.FeatureTrackerConfig

        Set corner detector full configuration.

        Parameter ``config``:
            Corner detector configuration
        """
    @overload
    def setCornerDetector(self, config: FeatureTrackerConfig.CornerDetector) -> FeatureTrackerConfig:
        """setCornerDetector(*args, **kwargs)
        Overloaded function.

        1. setCornerDetector(self: depthai.FeatureTrackerConfig, cornerDetector: depthai.FeatureTrackerConfig.CornerDetector.Type) -> depthai.FeatureTrackerConfig

        Set corner detector algorithm type.

        Parameter ``cornerDetector``:
            Corner detector type, HARRIS or SHI_THOMASI

        2. setCornerDetector(self: depthai.FeatureTrackerConfig, config: depthai.FeatureTrackerConfig.CornerDetector) -> depthai.FeatureTrackerConfig

        Set corner detector full configuration.

        Parameter ``config``:
            Corner detector configuration
        """
    @overload
    def setFeatureMaintainer(self, enable: bool) -> FeatureTrackerConfig:
        """setFeatureMaintainer(*args, **kwargs)
        Overloaded function.

        1. setFeatureMaintainer(self: depthai.FeatureTrackerConfig, enable: bool) -> depthai.FeatureTrackerConfig

        Enable or disable feature maintainer.

        Parameter ``enable``:

        2. setFeatureMaintainer(self: depthai.FeatureTrackerConfig, config: depthai.FeatureTrackerConfig.FeatureMaintainer) -> depthai.FeatureTrackerConfig

        Set feature maintainer full configuration.

        Parameter ``config``:
            feature maintainer configuration
        """
    @overload
    def setFeatureMaintainer(self, config: FeatureTrackerConfig.FeatureMaintainer) -> FeatureTrackerConfig:
        """setFeatureMaintainer(*args, **kwargs)
        Overloaded function.

        1. setFeatureMaintainer(self: depthai.FeatureTrackerConfig, enable: bool) -> depthai.FeatureTrackerConfig

        Enable or disable feature maintainer.

        Parameter ``enable``:

        2. setFeatureMaintainer(self: depthai.FeatureTrackerConfig, config: depthai.FeatureTrackerConfig.FeatureMaintainer) -> depthai.FeatureTrackerConfig

        Set feature maintainer full configuration.

        Parameter ``config``:
            feature maintainer configuration
        """
    def setHwMotionEstimation(self) -> FeatureTrackerConfig:
        """setHwMotionEstimation(self: depthai.FeatureTrackerConfig) -> depthai.FeatureTrackerConfig

        Set hardware accelerated motion estimation using block matching. Faster than
        optical flow (software implementation) but might not be as accurate.
        """
    @overload
    def setMotionEstimator(self, enable: bool) -> FeatureTrackerConfig:
        """setMotionEstimator(*args, **kwargs)
        Overloaded function.

        1. setMotionEstimator(self: depthai.FeatureTrackerConfig, enable: bool) -> depthai.FeatureTrackerConfig

        Enable or disable motion estimator.

        Parameter ``enable``:

        2. setMotionEstimator(self: depthai.FeatureTrackerConfig, config: depthai.FeatureTrackerConfig.MotionEstimator) -> depthai.FeatureTrackerConfig

        Set motion estimator full configuration.

        Parameter ``config``:
            Motion estimator configuration
        """
    @overload
    def setMotionEstimator(self, config: FeatureTrackerConfig.MotionEstimator) -> FeatureTrackerConfig:
        """setMotionEstimator(*args, **kwargs)
        Overloaded function.

        1. setMotionEstimator(self: depthai.FeatureTrackerConfig, enable: bool) -> depthai.FeatureTrackerConfig

        Enable or disable motion estimator.

        Parameter ``enable``:

        2. setMotionEstimator(self: depthai.FeatureTrackerConfig, config: depthai.FeatureTrackerConfig.MotionEstimator) -> depthai.FeatureTrackerConfig

        Set motion estimator full configuration.

        Parameter ``config``:
            Motion estimator configuration
        """
    def setNumTargetFeatures(self, numTargetFeatures: int) -> FeatureTrackerConfig:
        """setNumTargetFeatures(self: depthai.FeatureTrackerConfig, numTargetFeatures: int) -> depthai.FeatureTrackerConfig

        Set number of target features to detect.

        Parameter ``numTargetFeatures``:
            Number of features
        """
    @overload
    def setOpticalFlow(self) -> FeatureTrackerConfig:
        """setOpticalFlow(*args, **kwargs)
        Overloaded function.

        1. setOpticalFlow(self: depthai.FeatureTrackerConfig) -> depthai.FeatureTrackerConfig

        Set optical flow as motion estimation algorithm type.

        2. setOpticalFlow(self: depthai.FeatureTrackerConfig, config: depthai.FeatureTrackerConfig.MotionEstimator.OpticalFlow) -> depthai.FeatureTrackerConfig

        Set optical flow full configuration.

        Parameter ``config``:
            Optical flow configuration
        """
    @overload
    def setOpticalFlow(self, config: FeatureTrackerConfig.MotionEstimator.OpticalFlow) -> FeatureTrackerConfig:
        """setOpticalFlow(*args, **kwargs)
        Overloaded function.

        1. setOpticalFlow(self: depthai.FeatureTrackerConfig) -> depthai.FeatureTrackerConfig

        Set optical flow as motion estimation algorithm type.

        2. setOpticalFlow(self: depthai.FeatureTrackerConfig, config: depthai.FeatureTrackerConfig.MotionEstimator.OpticalFlow) -> depthai.FeatureTrackerConfig

        Set optical flow full configuration.

        Parameter ``config``:
            Optical flow configuration
        """

class FeatureTrackerProperties:
    """Specify properties for FeatureTracker"""
    initialConfig: FeatureTrackerConfig
    numMemorySlices: int
    numShaves: int
    def __init__(self, *args, **kwargs) -> None:
        """Initialize self.  See help(type(self)) for accurate signature."""

class FileGroup:
    def __init__(self) -> None:
        """__init__(self: depthai.FileGroup) -> None"""
    @overload
    def addFile(self, fileTag: str, data: str, mimeType: str) -> None:
        """addFile(*args, **kwargs)
        Overloaded function.

        1. addFile(self: depthai.FileGroup, fileTag: str, data: str, mimeType: str) -> None

        2. addFile(self: depthai.FileGroup, fileTag: str, filePath: os.PathLike) -> None

        3. addFile(self: depthai.FileGroup, fileTag: Optional[str], imgFrame: depthai.ImgFrame) -> None

        4. addFile(self: depthai.FileGroup, fileTag: Optional[str], encodedFrame: depthai.EncodedFrame) -> None

        5. addFile(self: depthai.FileGroup, fileTag: Optional[str], imgDetections: depthai.ImgDetections) -> None
        """
    @overload
    def addFile(self, fileTag: str, filePath: os.PathLike) -> None:
        """addFile(*args, **kwargs)
        Overloaded function.

        1. addFile(self: depthai.FileGroup, fileTag: str, data: str, mimeType: str) -> None

        2. addFile(self: depthai.FileGroup, fileTag: str, filePath: os.PathLike) -> None

        3. addFile(self: depthai.FileGroup, fileTag: Optional[str], imgFrame: depthai.ImgFrame) -> None

        4. addFile(self: depthai.FileGroup, fileTag: Optional[str], encodedFrame: depthai.EncodedFrame) -> None

        5. addFile(self: depthai.FileGroup, fileTag: Optional[str], imgDetections: depthai.ImgDetections) -> None
        """
    @overload
    def addFile(self, fileTag: str | None, imgFrame: ImgFrame) -> None:
        """addFile(*args, **kwargs)
        Overloaded function.

        1. addFile(self: depthai.FileGroup, fileTag: str, data: str, mimeType: str) -> None

        2. addFile(self: depthai.FileGroup, fileTag: str, filePath: os.PathLike) -> None

        3. addFile(self: depthai.FileGroup, fileTag: Optional[str], imgFrame: depthai.ImgFrame) -> None

        4. addFile(self: depthai.FileGroup, fileTag: Optional[str], encodedFrame: depthai.EncodedFrame) -> None

        5. addFile(self: depthai.FileGroup, fileTag: Optional[str], imgDetections: depthai.ImgDetections) -> None
        """
    @overload
    def addFile(self, fileTag: str | None, encodedFrame: EncodedFrame) -> None:
        """addFile(*args, **kwargs)
        Overloaded function.

        1. addFile(self: depthai.FileGroup, fileTag: str, data: str, mimeType: str) -> None

        2. addFile(self: depthai.FileGroup, fileTag: str, filePath: os.PathLike) -> None

        3. addFile(self: depthai.FileGroup, fileTag: Optional[str], imgFrame: depthai.ImgFrame) -> None

        4. addFile(self: depthai.FileGroup, fileTag: Optional[str], encodedFrame: depthai.EncodedFrame) -> None

        5. addFile(self: depthai.FileGroup, fileTag: Optional[str], imgDetections: depthai.ImgDetections) -> None
        """
    @overload
    def addFile(self, fileTag: str | None, imgDetections: ImgDetections) -> None:
        """addFile(*args, **kwargs)
        Overloaded function.

        1. addFile(self: depthai.FileGroup, fileTag: str, data: str, mimeType: str) -> None

        2. addFile(self: depthai.FileGroup, fileTag: str, filePath: os.PathLike) -> None

        3. addFile(self: depthai.FileGroup, fileTag: Optional[str], imgFrame: depthai.ImgFrame) -> None

        4. addFile(self: depthai.FileGroup, fileTag: Optional[str], encodedFrame: depthai.EncodedFrame) -> None

        5. addFile(self: depthai.FileGroup, fileTag: Optional[str], imgDetections: depthai.ImgDetections) -> None
        """
    @overload
    def addImageDetectionsPair(self, fileTag: str | None, imgFrame: ImgFrame, imgDetections: ImgDetections) -> None:
        """addImageDetectionsPair(*args, **kwargs)
        Overloaded function.

        1. addImageDetectionsPair(self: depthai.FileGroup, fileTag: Optional[str], imgFrame: depthai.ImgFrame, imgDetections: depthai.ImgDetections) -> None

        2. addImageDetectionsPair(self: depthai.FileGroup, fileTag: Optional[str], encodedFrame: depthai.EncodedFrame, imgDetections: depthai.ImgDetections) -> None
        """
    @overload
    def addImageDetectionsPair(self, fileTag: str | None, encodedFrame: EncodedFrame, imgDetections: ImgDetections) -> None:
        """addImageDetectionsPair(*args, **kwargs)
        Overloaded function.

        1. addImageDetectionsPair(self: depthai.FileGroup, fileTag: Optional[str], imgFrame: depthai.ImgFrame, imgDetections: depthai.ImgDetections) -> None

        2. addImageDetectionsPair(self: depthai.FileGroup, fileTag: Optional[str], encodedFrame: depthai.EncodedFrame, imgDetections: depthai.ImgDetections) -> None
        """

class FrameEvent:
    """

    Members:

      NONE

      READOUT_START

      READOUT_END"""
    __members__: ClassVar[dict] = ...  # read-only
    NONE: ClassVar[FrameEvent] = ...
    READOUT_END: ClassVar[FrameEvent] = ...
    READOUT_START: ClassVar[FrameEvent] = ...
    __entries: ClassVar[dict] = ...
    def __init__(self, value: int) -> None:
        """__init__(self: depthai.FrameEvent, value: int) -> None"""
    def __eq__(self, other: object) -> bool:
        """__eq__(self: object, other: object) -> bool"""
    def __hash__(self) -> int:
        """__hash__(self: object) -> int"""
    def __index__(self) -> int:
        """__index__(self: depthai.FrameEvent) -> int"""
    def __int__(self) -> int:
        """__int__(self: depthai.FrameEvent) -> int"""
    def __ne__(self, other: object) -> bool:
        """__ne__(self: object, other: object) -> bool"""
    @property
    def name(self) -> str:
        """name(self: object) -> str

        name(self: object) -> str
        """
    @property
    def value(self) -> int:
        """(arg0: depthai.FrameEvent) -> int"""

class GateControl(Buffer):
    fps: int
    numMessages: int
    open: bool
    @overload
    def __init__(self) -> None:
        """__init__(*args, **kwargs)
        Overloaded function.

        1. __init__(self: depthai.GateControl) -> None

        2. __init__(self: depthai.GateControl, open: bool, numMessages: int, fps: int) -> None
        """
    @overload
    def __init__(self, open: bool, numMessages: int, fps: int) -> None:
        """__init__(*args, **kwargs)
        Overloaded function.

        1. __init__(self: depthai.GateControl) -> None

        2. __init__(self: depthai.GateControl, open: bool, numMessages: int, fps: int) -> None
        """
    @staticmethod
    def closeGate() -> GateControl:
        """closeGate() -> depthai.GateControl"""
    def getDatatype(self) -> DatatypeEnum:
        """getDatatype(self: depthai.GateControl) -> depthai.DatatypeEnum"""
    @overload
    @staticmethod
    def openGate(numMessages: int, fps: int = ...) -> GateControl:
        """openGate(*args, **kwargs)
        Overloaded function.

        1. openGate(numMessages: int, fps: int = -1) -> depthai.GateControl

        2. openGate() -> depthai.GateControl
        """
    @overload
    @staticmethod
    def openGate() -> GateControl:
        """openGate(*args, **kwargs)
        Overloaded function.

        1. openGate(numMessages: int, fps: int = -1) -> depthai.GateControl

        2. openGate() -> depthai.GateControl
        """

class GateProperties:
    """Specify properties for Gate."""
    def __init__(self, *args, **kwargs) -> None:
        """Initialize self.  See help(type(self)) for accurate signature."""

class GlobalProperties:
    """Specify properties which apply for whole pipeline"""
    cameraSocketTuningBlobSize: dict[CameraBoardSocket, int]
    cameraSocketTuningBlobUri: dict[CameraBoardSocket, str]
    cameraTuningBlobSize: int | None
    cameraTuningBlobUri: str
    leonOsFrequencyHz: float
    leonRtFrequencyHz: float
    pipelineName: str | None
    pipelineVersion: str | None
    sippBufferSize: int
    sippDmaBufferSize: int
    xlinkChunkSize: int
    def __init__(self, *args, **kwargs) -> None:
        """Initialize self.  See help(type(self)) for accurate signature."""

class HousingCoordinateSystem:
    """Which Housing to use.

    AUTO denotes that the decision will be made by device

    Members:

      CAM_A

      CAM_B

      CAM_C

      CAM_D

      CAM_E

      CAM_F

      CAM_G

      CAM_H

      CAM_I

      CAM_J

      FRONT_CAM_A

      FRONT_CAM_B

      FRONT_CAM_C

      FRONT_CAM_D

      FRONT_CAM_E

      FRONT_CAM_F

      FRONT_CAM_G

      FRONT_CAM_H

      FRONT_CAM_I

      FRONT_CAM_J

      VESA_A

      VESA_B

      VESA_C

      VESA_D

      VESA_E

      VESA_F

      VESA_G

      VESA_H

      VESA_I

      VESA_J

      IMU"""
    __members__: ClassVar[dict] = ...  # read-only
    CAM_A: ClassVar[HousingCoordinateSystem] = ...
    CAM_B: ClassVar[HousingCoordinateSystem] = ...
    CAM_C: ClassVar[HousingCoordinateSystem] = ...
    CAM_D: ClassVar[HousingCoordinateSystem] = ...
    CAM_E: ClassVar[HousingCoordinateSystem] = ...
    CAM_F: ClassVar[HousingCoordinateSystem] = ...
    CAM_G: ClassVar[HousingCoordinateSystem] = ...
    CAM_H: ClassVar[HousingCoordinateSystem] = ...
    CAM_I: ClassVar[HousingCoordinateSystem] = ...
    CAM_J: ClassVar[HousingCoordinateSystem] = ...
    FRONT_CAM_A: ClassVar[HousingCoordinateSystem] = ...
    FRONT_CAM_B: ClassVar[HousingCoordinateSystem] = ...
    FRONT_CAM_C: ClassVar[HousingCoordinateSystem] = ...
    FRONT_CAM_D: ClassVar[HousingCoordinateSystem] = ...
    FRONT_CAM_E: ClassVar[HousingCoordinateSystem] = ...
    FRONT_CAM_F: ClassVar[HousingCoordinateSystem] = ...
    FRONT_CAM_G: ClassVar[HousingCoordinateSystem] = ...
    FRONT_CAM_H: ClassVar[HousingCoordinateSystem] = ...
    FRONT_CAM_I: ClassVar[HousingCoordinateSystem] = ...
    FRONT_CAM_J: ClassVar[HousingCoordinateSystem] = ...
    IMU: ClassVar[HousingCoordinateSystem] = ...
    VESA_A: ClassVar[HousingCoordinateSystem] = ...
    VESA_B: ClassVar[HousingCoordinateSystem] = ...
    VESA_C: ClassVar[HousingCoordinateSystem] = ...
    VESA_D: ClassVar[HousingCoordinateSystem] = ...
    VESA_E: ClassVar[HousingCoordinateSystem] = ...
    VESA_F: ClassVar[HousingCoordinateSystem] = ...
    VESA_G: ClassVar[HousingCoordinateSystem] = ...
    VESA_H: ClassVar[HousingCoordinateSystem] = ...
    VESA_I: ClassVar[HousingCoordinateSystem] = ...
    VESA_J: ClassVar[HousingCoordinateSystem] = ...
    __entries: ClassVar[dict] = ...
    def __init__(self, value: int) -> None:
        """__init__(self: depthai.HousingCoordinateSystem, value: int) -> None"""
    def __eq__(self, other: object) -> bool:
        """__eq__(self: object, other: object) -> bool"""
    def __hash__(self) -> int:
        """__hash__(self: object) -> int"""
    def __index__(self) -> int:
        """__index__(self: depthai.HousingCoordinateSystem) -> int"""
    def __int__(self) -> int:
        """__int__(self: depthai.HousingCoordinateSystem) -> int"""
    def __ne__(self, other: object) -> bool:
        """__ne__(self: object, other: object) -> bool"""
    @property
    def name(self) -> str:
        """name(self: object) -> str

        name(self: object) -> str
        """
    @property
    def value(self) -> int:
        """(arg0: depthai.HousingCoordinateSystem) -> int"""

class IMUData(Buffer):
    """IMUData message. Carries normalized detection results"""
    packets: list[IMUPacket]
    def __init__(self) -> None:
        """__init__(self: depthai.IMUData) -> None"""

class IMUPacket:
    """IMU output

    Contains combined output for all possible modes. Only the enabled outputs are
    populated."""
    acceleroMeter: IMUReportAccelerometer
    gyroscope: IMUReportGyroscope
    magneticField: IMUReportMagneticField
    rotationVector: IMUReportRotationVectorWAcc
    def __init__(self) -> None:
        """__init__(self: depthai.IMUPacket) -> None"""

class IMUProperties:
    batchReportThreshold: int
    enableFirmwareUpdate: bool | None
    imuSensors: list[IMUSensorConfig]
    maxBatchReports: int
    def __init__(self, *args, **kwargs) -> None:
        """Initialize self.  See help(type(self)) for accurate signature."""

class IMUReport:
    class Accuracy:
        """Members:

          UNRELIABLE

          LOW

          MEDIUM

          HIGH"""
        __members__: ClassVar[dict] = ...  # read-only
        HIGH: ClassVar[IMUReport.Accuracy] = ...
        LOW: ClassVar[IMUReport.Accuracy] = ...
        MEDIUM: ClassVar[IMUReport.Accuracy] = ...
        UNRELIABLE: ClassVar[IMUReport.Accuracy] = ...
        __entries: ClassVar[dict] = ...
        def __init__(self, value: int) -> None:
            """__init__(self: depthai.IMUReport.Accuracy, value: int) -> None"""
        def __eq__(self, other: object) -> bool:
            """__eq__(self: object, other: object) -> bool"""
        def __hash__(self) -> int:
            """__hash__(self: object) -> int"""
        def __index__(self) -> int:
            """__index__(self: depthai.IMUReport.Accuracy) -> int"""
        def __int__(self) -> int:
            """__int__(self: depthai.IMUReport.Accuracy) -> int"""
        def __ne__(self, other: object) -> bool:
            """__ne__(self: object, other: object) -> bool"""
        @property
        def name(self) -> str:
            """name(self: object) -> str

            name(self: object) -> str
            """
        @property
        def value(self) -> int:
            """(arg0: depthai.IMUReport.Accuracy) -> int"""
    accuracy: IMUReport.Accuracy
    sequence: int
    timestamp: Timestamp
    tsDevice: Timestamp
    def __init__(self) -> None:
        """__init__(self: depthai.IMUReport) -> None"""
    def getSequenceNum(self) -> int:
        """getSequenceNum(self: depthai.IMUReport) -> int

        Retrieves IMU report sequence number
        """
    def getTimestamp(self) -> datetime.timedelta:
        """getTimestamp(self: depthai.IMUReport) -> datetime.timedelta

        Retrieves timestamp related to dai::Clock::now()
        """
    def getTimestampDevice(self) -> datetime.timedelta:
        """getTimestampDevice(self: depthai.IMUReport) -> datetime.timedelta

        Retrieves timestamp directly captured from device's monotonic clock, not
        synchronized to host time. Used mostly for debugging
        """

class IMUReportAccelerometer(IMUReport):
    """Accelerometer

    Units are [m/s^2]"""
    x: float
    y: float
    z: float
    def __init__(self) -> None:
        """__init__(self: depthai.IMUReportAccelerometer) -> None"""

class IMUReportGyroscope(IMUReport):
    """Gyroscope

    Units are [rad/s]"""
    x: float
    y: float
    z: float
    def __init__(self) -> None:
        """__init__(self: depthai.IMUReportGyroscope) -> None"""

class IMUReportMagneticField(IMUReport):
    """Magnetic field

    Units are [uTesla]"""
    x: float
    y: float
    z: float
    def __init__(self) -> None:
        """__init__(self: depthai.IMUReportMagneticField) -> None"""

class IMUReportRotationVectorWAcc(IMUReport):
    """Rotation Vector with Accuracy

    Contains quaternion components: i,j,k,real"""
    i: float
    j: float
    k: float
    real: float
    rotationVectorAccuracy: float
    def __init__(self) -> None:
        """__init__(self: depthai.IMUReportRotationVectorWAcc) -> None"""

class IMUSensor:
    """Available IMU sensors. More details about each sensor can be found in the
    datasheet:

    https://www.ceva-dsp.com/wp-content/uploads/2019/10/BNO080_085-Datasheet.pdf

    Members:

      ACCELEROMETER_RAW : Section 2.1.1

    Acceleration of the device without any postprocessing, straight from the sensor.
    Units are [m/s^2]

      ACCELEROMETER : Section 2.1.1

    Acceleration of the device including gravity. Units are [m/s^2]

      LINEAR_ACCELERATION : Section 2.1.1

    Acceleration of the device with gravity removed. Units are [m/s^2]

      GRAVITY : Section 2.1.1

    Gravity. Units are [m/s^2]

      GYROSCOPE_RAW : Section 2.1.2

    The angular velocity of the device without any postprocessing, straight from the
    sensor. Units are [rad/s]

      GYROSCOPE_CALIBRATED : Section 2.1.2

    The angular velocity of the device. Units are [rad/s]

      GYROSCOPE_UNCALIBRATED : Section 2.1.2

    Angular velocity without bias compensation. Units are [rad/s]

      MAGNETOMETER_RAW : Section 2.1.3

    Magnetic field measurement without any postprocessing, straight from the sensor.
    Units are [uTesla]

      MAGNETOMETER_CALIBRATED : Section 2.1.3

    The fully calibrated magnetic field measurement. Units are [uTesla]

      MAGNETOMETER_UNCALIBRATED : Section 2.1.3

    The magnetic field measurement without hard-iron offset applied. Units are
    [uTesla]

      ROTATION_VECTOR : Section 2.2

    The rotation vector provides an orientation output that is expressed as a
    quaternion referenced to magnetic north and gravity. It is produced by fusing
    the outputs of the accelerometer, gyroscope and magnetometer. The rotation
    vector is the most accurate orientation estimate available. The magnetometer
    provides correction in yaw to reduce drift and the gyroscope enables the most
    responsive performance.

      GAME_ROTATION_VECTOR : Section 2.2

    The game rotation vector is an orientation output that is expressed as a
    quaternion with no specific reference for heading, while roll and pitch are
    referenced against gravity. It is produced by fusing the outputs of the
    accelerometer and the gyroscope (i.e. no magnetometer). The game rotation vector
    does not use the magnetometer to correct the gyroscopes drift in yaw. This is a
    deliberate omission (as specified by Google) to allow gaming applications to use
    a smoother representation of the orientation without the jumps that an
    instantaneous correction provided by a magnetic field update could provide. Long
    term the output will likely drift in yaw due to the characteristics of
    gyroscopes, but this is seen as preferable for this output versus a corrected
    output.

      GEOMAGNETIC_ROTATION_VECTOR : Section 2.2

    The geomagnetic rotation vector is an orientation output that is expressed as a
    quaternion referenced to magnetic north and gravity. It is produced by fusing
    the outputs of the accelerometer and magnetometer. The gyroscope is specifically
    excluded in order to produce a rotation vector output using less power than is
    required to produce the rotation vector of section 2.2.4. The consequences of
    removing the gyroscope are: Less responsive output since the highly dynamic
    outputs of the gyroscope are not used More errors in the presence of varying
    magnetic fields.

      ARVR_STABILIZED_ROTATION_VECTOR : Section 2.2

    Estimates of the magnetic field and the roll/pitch of the device can create a
    potential correction in the rotation vector produced. For applications
    (typically augmented or virtual reality applications) where a sudden jump can be
    disturbing, the output is adjusted to prevent these jumps in a manner that takes
    account of the velocity of the sensor system.

      ARVR_STABILIZED_GAME_ROTATION_VECTOR : Section 2.2

    While the magnetometer is removed from the calculation of the game rotation
    vector, the accelerometer itself can create a potential correction in the
    rotation vector produced (i.e. the estimate of gravity changes). For
    applications (typically augmented or virtual reality applications) where a
    sudden jump can be disturbing, the output is adjusted to prevent these jumps in
    a manner that takes account of the velocity of the sensor system. This process
    is called AR/VR stabilization."""
    __members__: ClassVar[dict] = ...  # read-only
    ACCELEROMETER: ClassVar[IMUSensor] = ...
    ACCELEROMETER_RAW: ClassVar[IMUSensor] = ...
    ARVR_STABILIZED_GAME_ROTATION_VECTOR: ClassVar[IMUSensor] = ...
    ARVR_STABILIZED_ROTATION_VECTOR: ClassVar[IMUSensor] = ...
    GAME_ROTATION_VECTOR: ClassVar[IMUSensor] = ...
    GEOMAGNETIC_ROTATION_VECTOR: ClassVar[IMUSensor] = ...
    GRAVITY: ClassVar[IMUSensor] = ...
    GYROSCOPE_CALIBRATED: ClassVar[IMUSensor] = ...
    GYROSCOPE_RAW: ClassVar[IMUSensor] = ...
    GYROSCOPE_UNCALIBRATED: ClassVar[IMUSensor] = ...
    LINEAR_ACCELERATION: ClassVar[IMUSensor] = ...
    MAGNETOMETER_CALIBRATED: ClassVar[IMUSensor] = ...
    MAGNETOMETER_RAW: ClassVar[IMUSensor] = ...
    MAGNETOMETER_UNCALIBRATED: ClassVar[IMUSensor] = ...
    ROTATION_VECTOR: ClassVar[IMUSensor] = ...
    __entries: ClassVar[dict] = ...
    def __init__(self, value: int) -> None:
        """__init__(self: depthai.IMUSensor, value: int) -> None"""
    def __eq__(self, other: object) -> bool:
        """__eq__(self: object, other: object) -> bool"""
    def __hash__(self) -> int:
        """__hash__(self: object) -> int"""
    def __index__(self) -> int:
        """__index__(self: depthai.IMUSensor) -> int"""
    def __int__(self) -> int:
        """__int__(self: depthai.IMUSensor) -> int"""
    def __ne__(self, other: object) -> bool:
        """__ne__(self: object, other: object) -> bool"""
    @property
    def name(self) -> str:
        """name(self: object) -> str

        name(self: object) -> str
        """
    @property
    def value(self) -> int:
        """(arg0: depthai.IMUSensor) -> int"""

class IMUSensorConfig:
    changeSensitivity: int
    reportRate: int
    sensitivityEnabled: bool
    sensitivityRelative: bool
    sensorId: IMUSensor
    def __init__(self) -> None:
        """__init__(self: depthai.IMUSensorConfig) -> None"""

class ImageAlignConfig(Buffer):
    """ImageAlignConfig configuration structure"""
    staticDepthPlane: int
    def __init__(self) -> None:
        """__init__(self: depthai.ImageAlignConfig) -> None"""

class ImageAlignProperties:
    """Specify properties for ImageAlign"""
    alignHeight: int
    alignWidth: int
    initialConfig: ImageAlignConfig
    interpolation: Interpolation
    numFramesPool: int
    numShaves: int
    outKeepAspectRatio: bool
    warpHwIds: list[int]
    def __init__(self, *args, **kwargs) -> None:
        """Initialize self.  See help(type(self)) for accurate signature."""

class ImageFiltersConfig(Buffer):
    filterIndices: list[int]
    filterParams: Incomplete
    def __init__(self) -> None:
        """__init__(self: depthai.ImageFiltersConfig) -> None"""
    def insertFilter(self, params) -> ImageFiltersConfig:
        """insertFilter(self: depthai.ImageFiltersConfig, params: Union[dai::filters::params::MedianFilter, dai::filters::params::SpatialFilter, dai::filters::params::SpeckleFilter, dai::filters::params::TemporalFilter]) -> depthai.ImageFiltersConfig

        Insert filter parameters describing how a new filter should be inserted

        Parameter ``params``:
            Parameters of the filter to be inserted
        """
    def setProfilePreset(self, arg0) -> None:
        """setProfilePreset(self: depthai.ImageFiltersConfig, arg0: dai::ImageFiltersPresetMode) -> None

        Set preset mode for ImageFiltersConfig.

        Parameter ``presetMode``:
            Preset mode for ImageFiltersConfig.
        """
    def updateFilterAtIndex(self, index: int, params) -> ImageFiltersConfig:
        """updateFilterAtIndex(self: depthai.ImageFiltersConfig, index: int, params: Union[dai::filters::params::MedianFilter, dai::filters::params::SpatialFilter, dai::filters::params::SpeckleFilter, dai::filters::params::TemporalFilter]) -> depthai.ImageFiltersConfig

        Insert filter parameters describing how a filter at index index should be
        updated

        Parameter ``index``:
            Index of the filter to be inserted

        Parameter ``params``:
            Parameters of the filter to be inserted
        """

class ImageFiltersPresetMode:
    """Members:

      TOF_LOW_RANGE

      TOF_MID_RANGE

      TOF_HIGH_RANGE"""
    __members__: ClassVar[dict] = ...  # read-only
    TOF_HIGH_RANGE: ClassVar[ImageFiltersPresetMode] = ...
    TOF_LOW_RANGE: ClassVar[ImageFiltersPresetMode] = ...
    TOF_MID_RANGE: ClassVar[ImageFiltersPresetMode] = ...
    __entries: ClassVar[dict] = ...
    def __init__(self, value: int) -> None:
        """__init__(self: depthai.ImageFiltersPresetMode, value: int) -> None"""
    def __eq__(self, other: object) -> bool:
        """__eq__(self: object, other: object) -> bool"""
    def __hash__(self) -> int:
        """__hash__(self: object) -> int"""
    def __index__(self) -> int:
        """__index__(self: depthai.ImageFiltersPresetMode) -> int"""
    def __int__(self) -> int:
        """__int__(self: depthai.ImageFiltersPresetMode) -> int"""
    def __ne__(self, other: object) -> bool:
        """__ne__(self: object, other: object) -> bool"""
    @property
    def name(self) -> str:
        """name(self: object) -> str

        name(self: object) -> str
        """
    @property
    def value(self) -> int:
        """(arg0: depthai.ImageFiltersPresetMode) -> int"""

class ImageFiltersProperties:
    def __init__(self, *args, **kwargs) -> None:
        """Initialize self.  See help(type(self)) for accurate signature."""

class ImageManipConfig(Buffer):
    """ImageManipConfig message. Specifies image manipulation options like:

    - Crop

    - Resize

    - Warp

    - ..."""

    class ResizeMode:
        """Members:

          NONE

          LETTERBOX

          CENTER_CROP

          STRETCH"""
        __members__: ClassVar[dict] = ...  # read-only
        CENTER_CROP: ClassVar[ImageManipConfig.ResizeMode] = ...
        LETTERBOX: ClassVar[ImageManipConfig.ResizeMode] = ...
        NONE: ClassVar[ImageManipConfig.ResizeMode] = ...
        STRETCH: ClassVar[ImageManipConfig.ResizeMode] = ...
        __entries: ClassVar[dict] = ...
        def __init__(self, value: int) -> None:
            """__init__(self: depthai.ImageManipConfig.ResizeMode, value: int) -> None"""
        def __eq__(self, other: object) -> bool:
            """__eq__(self: object, other: object) -> bool"""
        def __hash__(self) -> int:
            """__hash__(self: object) -> int"""
        def __index__(self) -> int:
            """__index__(self: depthai.ImageManipConfig.ResizeMode) -> int"""
        def __int__(self) -> int:
            """__int__(self: depthai.ImageManipConfig.ResizeMode) -> int"""
        def __ne__(self, other: object) -> bool:
            """__ne__(self: object, other: object) -> bool"""
        @property
        def name(self) -> str:
            """name(self: object) -> str

            name(self: object) -> str
            """
        @property
        def value(self) -> int:
            """(arg0: depthai.ImageManipConfig.ResizeMode) -> int"""
    def __init__(self) -> None:
        """__init__(self: depthai.ImageManipConfig) -> None"""
    @overload
    def addCrop(self, x: int, y: int, w: int, h: int) -> ImageManipConfig:
        """addCrop(*args, **kwargs)
        Overloaded function.

        1. addCrop(self: depthai.ImageManipConfig, x: int, y: int, w: int, h: int) -> depthai.ImageManipConfig

        Crops the image to the specified rectangle

        Parameter ``x``:
            X coordinate of the top-left corner

        Parameter ``y``:
            Y coordinate of the top-left corner

        Parameter ``w``:
            Width of the rectangle

        Parameter ``h``:
            Height of the rectangle

        2. addCrop(self: depthai.ImageManipConfig, rect: depthai.Rect, normalizedCoords: bool) -> depthai.ImageManipConfig

        Crops the image to the specified rectangle

        Parameter ``x``:
            X coordinate of the top-left corner

        Parameter ``y``:
            Y coordinate of the top-left corner

        Parameter ``w``:
            Width of the rectangle

        Parameter ``h``:
            Height of the rectangle
        """
    @overload
    def addCrop(self, rect: Rect, normalizedCoords: bool) -> ImageManipConfig:
        """addCrop(*args, **kwargs)
        Overloaded function.

        1. addCrop(self: depthai.ImageManipConfig, x: int, y: int, w: int, h: int) -> depthai.ImageManipConfig

        Crops the image to the specified rectangle

        Parameter ``x``:
            X coordinate of the top-left corner

        Parameter ``y``:
            Y coordinate of the top-left corner

        Parameter ``w``:
            Width of the rectangle

        Parameter ``h``:
            Height of the rectangle

        2. addCrop(self: depthai.ImageManipConfig, rect: depthai.Rect, normalizedCoords: bool) -> depthai.ImageManipConfig

        Crops the image to the specified rectangle

        Parameter ``x``:
            X coordinate of the top-left corner

        Parameter ``y``:
            Y coordinate of the top-left corner

        Parameter ``w``:
            Width of the rectangle

        Parameter ``h``:
            Height of the rectangle
        """
    def addCropRotatedRect(self, rect: RotatedRect, normalizedCoords: bool) -> ImageManipConfig:
        """addCropRotatedRect(self: depthai.ImageManipConfig, rect: depthai.RotatedRect, normalizedCoords: bool) -> depthai.ImageManipConfig

        Crops the image to the specified (rotated) rectangle

        Parameter ``rect``:
            RotatedRect to crop

        Parameter ``normalizedCoords``:
            If true, the coordinates are normalized to range [0, 1] where 1 maps to the
            width/height of the image
        """
    def addFlipHorizontal(self) -> ImageManipConfig:
        """addFlipHorizontal(self: depthai.ImageManipConfig) -> depthai.ImageManipConfig

        Flips the image horizontally
        """
    def addFlipVertical(self) -> ImageManipConfig:
        """addFlipVertical(self: depthai.ImageManipConfig) -> depthai.ImageManipConfig

        Flips the image vertically
        """
    @overload
    def addRotateDeg(self, angle: float) -> ImageManipConfig:
        """addRotateDeg(*args, **kwargs)
        Overloaded function.

        1. addRotateDeg(self: depthai.ImageManipConfig, angle: float) -> depthai.ImageManipConfig

        Rotates the image around its center by the specified angle in degrees

        Parameter ``angle``:
            Angle in radians

        2. addRotateDeg(self: depthai.ImageManipConfig, angle: float, center: depthai.Point2f) -> depthai.ImageManipConfig

        Rotates the image around its center by the specified angle in degrees

        Parameter ``angle``:
            Angle in radians
        """
    @overload
    def addRotateDeg(self, angle: float, center: Point2f) -> ImageManipConfig:
        """addRotateDeg(*args, **kwargs)
        Overloaded function.

        1. addRotateDeg(self: depthai.ImageManipConfig, angle: float) -> depthai.ImageManipConfig

        Rotates the image around its center by the specified angle in degrees

        Parameter ``angle``:
            Angle in radians

        2. addRotateDeg(self: depthai.ImageManipConfig, angle: float, center: depthai.Point2f) -> depthai.ImageManipConfig

        Rotates the image around its center by the specified angle in degrees

        Parameter ``angle``:
            Angle in radians
        """
    @overload
    def addScale(self, scale: float) -> ImageManipConfig:
        """addScale(*args, **kwargs)
        Overloaded function.

        1. addScale(self: depthai.ImageManipConfig, scale: float) -> depthai.ImageManipConfig

        Rescales the image using the specified factors

        Parameter ``scaleX``:
            Scale factor for the X axis

        Parameter ``scaleY``:
            Scale factor for the Y axis. If not specified, scaleY is set to the same
            value as scaleX

        2. addScale(self: depthai.ImageManipConfig, scaleX: float, scaleY: float) -> depthai.ImageManipConfig

        Rescales the image using the specified factors

        Parameter ``scaleX``:
            Scale factor for the X axis

        Parameter ``scaleY``:
            Scale factor for the Y axis. If not specified, scaleY is set to the same
            value as scaleX
        """
    @overload
    def addScale(self, scaleX: float, scaleY: float) -> ImageManipConfig:
        """addScale(*args, **kwargs)
        Overloaded function.

        1. addScale(self: depthai.ImageManipConfig, scale: float) -> depthai.ImageManipConfig

        Rescales the image using the specified factors

        Parameter ``scaleX``:
            Scale factor for the X axis

        Parameter ``scaleY``:
            Scale factor for the Y axis. If not specified, scaleY is set to the same
            value as scaleX

        2. addScale(self: depthai.ImageManipConfig, scaleX: float, scaleY: float) -> depthai.ImageManipConfig

        Rescales the image using the specified factors

        Parameter ``scaleX``:
            Scale factor for the X axis

        Parameter ``scaleY``:
            Scale factor for the Y axis. If not specified, scaleY is set to the same
            value as scaleX
        """
    def addTransformAffine(self, mat) -> ImageManipConfig:
        """addTransformAffine(self: depthai.ImageManipConfig, mat: Annotated[list[float], FixedSize(4)]) -> depthai.ImageManipConfig

        Applies an affine transformation to the image

        Parameter ``matrix``:
            an array containing a 2x2 matrix representing the affine transformation
        """
    def addTransformFourPoints(self, src, dst, normalizedCoords: bool) -> ImageManipConfig:
        """addTransformFourPoints(self: depthai.ImageManipConfig, src: Annotated[list[depthai.Point2f], FixedSize(4)], dst: Annotated[list[depthai.Point2f], FixedSize(4)], normalizedCoords: bool) -> depthai.ImageManipConfig

        Applies a perspective transformation to the image

        Parameter ``src``:
            Source points

        Parameter ``dst``:
            Destination points

        Parameter ``normalizedCoords``:
            If true, the coordinates are normalized to range [0, 1] where 1 maps to the
            width/height of the image
        """
    def addTransformPerspective(self, mat) -> ImageManipConfig:
        """addTransformPerspective(self: depthai.ImageManipConfig, mat: Annotated[list[float], FixedSize(9)]) -> depthai.ImageManipConfig

        Applies a perspective transformation to the image

        Parameter ``matrix``:
            an array containing a 3x3 matrix representing the perspective transformation
        """
    def clearOps(self) -> ImageManipConfig:
        """clearOps(self: depthai.ImageManipConfig) -> depthai.ImageManipConfig

        Removes all operations from the list (does not affect output configuration)
        """
    def getUndistort(self) -> bool:
        """getUndistort(self: depthai.ImageManipConfig) -> bool

        Gets the undistort flag

        Returns:
            True if undistort is enabled, false otherwise
        """
    @overload
    def setBackgroundColor(self, r: int, g: int, b: int) -> ImageManipConfig:
        """setBackgroundColor(*args, **kwargs)
        Overloaded function.

        1. setBackgroundColor(self: depthai.ImageManipConfig, r: int, g: int, b: int) -> depthai.ImageManipConfig

        Sets the rgb background color of the output image

        Parameter ``red``:
            Red component of the background color

        Parameter ``green``:
            Green component of the background color

        Parameter ``blue``:
            Blue component of the background color

        2. setBackgroundColor(self: depthai.ImageManipConfig, val: int) -> depthai.ImageManipConfig

        Sets the rgb background color of the output image

        Parameter ``red``:
            Red component of the background color

        Parameter ``green``:
            Green component of the background color

        Parameter ``blue``:
            Blue component of the background color
        """
    @overload
    def setBackgroundColor(self, val: int) -> ImageManipConfig:
        """setBackgroundColor(*args, **kwargs)
        Overloaded function.

        1. setBackgroundColor(self: depthai.ImageManipConfig, r: int, g: int, b: int) -> depthai.ImageManipConfig

        Sets the rgb background color of the output image

        Parameter ``red``:
            Red component of the background color

        Parameter ``green``:
            Green component of the background color

        Parameter ``blue``:
            Blue component of the background color

        2. setBackgroundColor(self: depthai.ImageManipConfig, val: int) -> depthai.ImageManipConfig

        Sets the rgb background color of the output image

        Parameter ``red``:
            Red component of the background color

        Parameter ``green``:
            Green component of the background color

        Parameter ``blue``:
            Blue component of the background color
        """
    @overload
    def setColormap(self, colormap: Colormap) -> ImageManipConfig:
        """setColormap(*args, **kwargs)
        Overloaded function.

        1. setColormap(self: depthai.ImageManipConfig, colormap: depthai.Colormap) -> depthai.ImageManipConfig

        Sets the colormap to be applied to a grayscale image

        Parameter ``colormap``:
            Colormap type to be applied

        2. setColormap(self: depthai.ImageManipConfig, colormap: depthai.Colormap) -> depthai.ImageManipConfig

        Sets the colormap to be applied to a grayscale image

        Parameter ``colormap``:
            Colormap type to be applied
        """
    @overload
    def setColormap(self, colormap: Colormap) -> ImageManipConfig:
        """setColormap(*args, **kwargs)
        Overloaded function.

        1. setColormap(self: depthai.ImageManipConfig, colormap: depthai.Colormap) -> depthai.ImageManipConfig

        Sets the colormap to be applied to a grayscale image

        Parameter ``colormap``:
            Colormap type to be applied

        2. setColormap(self: depthai.ImageManipConfig, colormap: depthai.Colormap) -> depthai.ImageManipConfig

        Sets the colormap to be applied to a grayscale image

        Parameter ``colormap``:
            Colormap type to be applied
        """
    def setFrameType(self, type: ImgFrame.Type) -> ImageManipConfig:
        """setFrameType(self: depthai.ImageManipConfig, type: depthai.ImgFrame.Type) -> depthai.ImageManipConfig

        Sets the frame type of the output image

        Parameter ``frameType``:
            Frame type of the output image
        """
    def setOutputCenter(self, c: bool) -> ImageManipConfig:
        """setOutputCenter(self: depthai.ImageManipConfig, c: bool) -> depthai.ImageManipConfig

        Centers the content in the output image without resizing

        Parameter ``c``:
            True to center the content, false otherwise
        """
    def setOutputSize(self, w: int, h: int, mode: ImageManipConfig.ResizeMode = ...) -> ImageManipConfig:
        """setOutputSize(self: depthai.ImageManipConfig, w: int, h: int, mode: depthai.ImageManipConfig.ResizeMode = <ResizeMode.STRETCH: 1>) -> depthai.ImageManipConfig

        Sets the output size of the image

        Parameter ``w``:
            Width of the output image

        Parameter ``h``:
            Height of the output image

        Parameter ``mode``:
            Resize mode. NONE - no resize, STRETCH - stretch to fit, LETTERBOX - keep
            aspect ratio and pad with background color, CENTER_CROP - keep aspect ratio
            and crop
        """
    def setReusePreviousImage(self, reuse: bool) -> ImageManipConfig:
        """setReusePreviousImage(self: depthai.ImageManipConfig, reuse: bool) -> depthai.ImageManipConfig

        Instruct ImageManip to not remove current image from its queue and use the same
        for next message.

        Parameter ``reuse``:
            True to enable reuse, false otherwise
        """
    def setSkipCurrentImage(self, skip: bool) -> ImageManipConfig:
        """setSkipCurrentImage(self: depthai.ImageManipConfig, skip: bool) -> depthai.ImageManipConfig

        Instructs ImageManip to skip current image and wait for next in queue.

        Parameter ``skip``:
            True to skip current image, false otherwise
        """
    def setUndistort(self, undistort: bool) -> ImageManipConfig:
        """setUndistort(self: depthai.ImageManipConfig, undistort: bool) -> depthai.ImageManipConfig

        Sets the undistort flag
        """

class ImgAnnotation:
    circles: VectorCircleAnnotation
    points: VectorPointsAnnotation
    texts: VectorTextAnnotation
    def __init__(self) -> None:
        """__init__(self: depthai.ImgAnnotation) -> None"""

class ImgAnnotations(Buffer):
    annotations: VectorImgAnnotation
    @overload
    def __init__(self) -> None:
        """__init__(*args, **kwargs)
        Overloaded function.

        1. __init__(self: depthai.ImgAnnotations) -> None

        Construct ImgAnnotations message.

        2. __init__(self: depthai.ImgAnnotations, arg0: depthai.VectorImgAnnotation) -> None
        """
    @overload
    def __init__(self, arg0: VectorImgAnnotation) -> None:
        """__init__(*args, **kwargs)
        Overloaded function.

        1. __init__(self: depthai.ImgAnnotations) -> None

        Construct ImgAnnotations message.

        2. __init__(self: depthai.ImgAnnotations, arg0: depthai.VectorImgAnnotation) -> None
        """
    def getSequenceNum(self) -> int:
        """getSequenceNum(self: depthai.ImgAnnotations) -> int

        Retrieves image sequence number
        """
    def getTimestamp(self) -> datetime.timedelta:
        """getTimestamp(self: depthai.ImgAnnotations) -> datetime.timedelta

        Retrieves timestamp related to dai::Clock::now()
        """
    def getTimestampDevice(self) -> datetime.timedelta:
        """getTimestampDevice(self: depthai.ImgAnnotations) -> datetime.timedelta

        Retrieves timestamp directly captured from device's monotonic clock, not
        synchronized to host time. Used mostly for debugging
        """
    def getTransformation(self) -> ImgTransformation | None:
        """getTransformation(self: depthai.ImgAnnotations) -> Optional[depthai.ImgTransformation]"""
    def setTransformation(self, arg0: ImgTransformation | None) -> None:
        """setTransformation(self: depthai.ImgAnnotations, arg0: Optional[depthai.ImgTransformation]) -> None"""

class ImgDetection:
    confidence: float
    label: int
    labelName: str
    xmax: float
    xmin: float
    ymax: float
    ymin: float
    @overload
    def __init__(self) -> None:
        """__init__(*args, **kwargs)
        Overloaded function.

        1. __init__(self: depthai.ImgDetection) -> None

        2. __init__(self: depthai.ImgDetection, boundingBox: depthai.RotatedRect, confidence: float, label: int) -> None

        3. __init__(self: depthai.ImgDetection, boundingBox: depthai.RotatedRect, labelName: str, confidence: float, label: int) -> None

        4. __init__(self: depthai.ImgDetection, boundingBox: depthai.RotatedRect, keypoints: depthai.KeypointsList, confidence: float, label: int) -> None

        5. __init__(self: depthai.ImgDetection, boundingBox: depthai.RotatedRect, keypoints: depthai.KeypointsList, labelName: str, confidence: float, label: int) -> None
        """
    @overload
    def __init__(self, boundingBox: RotatedRect, confidence: float, label: int) -> None:
        """__init__(*args, **kwargs)
        Overloaded function.

        1. __init__(self: depthai.ImgDetection) -> None

        2. __init__(self: depthai.ImgDetection, boundingBox: depthai.RotatedRect, confidence: float, label: int) -> None

        3. __init__(self: depthai.ImgDetection, boundingBox: depthai.RotatedRect, labelName: str, confidence: float, label: int) -> None

        4. __init__(self: depthai.ImgDetection, boundingBox: depthai.RotatedRect, keypoints: depthai.KeypointsList, confidence: float, label: int) -> None

        5. __init__(self: depthai.ImgDetection, boundingBox: depthai.RotatedRect, keypoints: depthai.KeypointsList, labelName: str, confidence: float, label: int) -> None
        """
    @overload
    def __init__(self, boundingBox: RotatedRect, labelName: str, confidence: float, label: int) -> None:
        """__init__(*args, **kwargs)
        Overloaded function.

        1. __init__(self: depthai.ImgDetection) -> None

        2. __init__(self: depthai.ImgDetection, boundingBox: depthai.RotatedRect, confidence: float, label: int) -> None

        3. __init__(self: depthai.ImgDetection, boundingBox: depthai.RotatedRect, labelName: str, confidence: float, label: int) -> None

        4. __init__(self: depthai.ImgDetection, boundingBox: depthai.RotatedRect, keypoints: depthai.KeypointsList, confidence: float, label: int) -> None

        5. __init__(self: depthai.ImgDetection, boundingBox: depthai.RotatedRect, keypoints: depthai.KeypointsList, labelName: str, confidence: float, label: int) -> None
        """
    @overload
    def __init__(self, boundingBox: RotatedRect, keypoints: KeypointsList, confidence: float, label: int) -> None:
        """__init__(*args, **kwargs)
        Overloaded function.

        1. __init__(self: depthai.ImgDetection) -> None

        2. __init__(self: depthai.ImgDetection, boundingBox: depthai.RotatedRect, confidence: float, label: int) -> None

        3. __init__(self: depthai.ImgDetection, boundingBox: depthai.RotatedRect, labelName: str, confidence: float, label: int) -> None

        4. __init__(self: depthai.ImgDetection, boundingBox: depthai.RotatedRect, keypoints: depthai.KeypointsList, confidence: float, label: int) -> None

        5. __init__(self: depthai.ImgDetection, boundingBox: depthai.RotatedRect, keypoints: depthai.KeypointsList, labelName: str, confidence: float, label: int) -> None
        """
    @overload
    def __init__(self, boundingBox: RotatedRect, keypoints: KeypointsList, labelName: str, confidence: float, label: int) -> None:
        """__init__(*args, **kwargs)
        Overloaded function.

        1. __init__(self: depthai.ImgDetection) -> None

        2. __init__(self: depthai.ImgDetection, boundingBox: depthai.RotatedRect, confidence: float, label: int) -> None

        3. __init__(self: depthai.ImgDetection, boundingBox: depthai.RotatedRect, labelName: str, confidence: float, label: int) -> None

        4. __init__(self: depthai.ImgDetection, boundingBox: depthai.RotatedRect, keypoints: depthai.KeypointsList, confidence: float, label: int) -> None

        5. __init__(self: depthai.ImgDetection, boundingBox: depthai.RotatedRect, keypoints: depthai.KeypointsList, labelName: str, confidence: float, label: int) -> None
        """
    def getAngle(self) -> float:
        """getAngle(self: depthai.ImgDetection) -> float"""
    def getBoundingBox(self) -> RotatedRect:
        """getBoundingBox(self: depthai.ImgDetection) -> depthai.RotatedRect"""
    def getCenterX(self) -> float:
        """getCenterX(self: depthai.ImgDetection) -> float"""
    def getCenterY(self) -> float:
        """getCenterY(self: depthai.ImgDetection) -> float"""
    def getEdges(self, *args, **kwargs):
        """getEdges(self: depthai.ImgDetection) -> list[Annotated[list[int], FixedSize(2)]]

        Returns a list of edges, each edge is a pair of indices, or empty list if no
        keypoints were set.
        """
    def getHeight(self) -> float:
        """getHeight(self: depthai.ImgDetection) -> float"""
    def getKeypoints(self) -> list[Keypoint]:
        """getKeypoints(self: depthai.ImgDetection) -> list[depthai.Keypoint]

        Returns a list of Keypoint objects, or empty list if no keypoints were set.
        """
    def getKeypoints2f(self) -> VectorPoint2f:
        """getKeypoints2f(self: depthai.ImgDetection) -> depthai.VectorPoint2f

        Returns a list of Point2f coordinates of the keypoints, or empty list if no
        keypoints were set.
        """
    def getKeypoints3f(self) -> list[Point3f]:
        """getKeypoints3f(self: depthai.ImgDetection) -> list[depthai.Point3f]

        Returns a list of Point3f coordinates of the keypoints, or empty list if no
        keypoints were set.
        """
    def getOuterBoundingBox(self, *args, **kwargs):
        """getOuterBoundingBox(self: depthai.ImgDetection) -> Annotated[list[float], FixedSize(4)]

        Returns the outer bounding box as [minx, miny, maxx, maxy].
        """
    def getWidth(self) -> float:
        """getWidth(self: depthai.ImgDetection) -> float"""
    def setBoundingBox(self, boundingBox: RotatedRect) -> None:
        """setBoundingBox(self: depthai.ImgDetection, boundingBox: depthai.RotatedRect) -> None"""
    def setEdges(self, edges) -> None:
        """setEdges(self: depthai.ImgDetection, edges: list[Annotated[list[int], FixedSize(2)]]) -> None"""
    @overload
    def setKeypoints(self, keypoints: KeypointsList) -> None:
        """setKeypoints(*args, **kwargs)
        Overloaded function.

        1. setKeypoints(self: depthai.ImgDetection, keypoints: depthai.KeypointsList) -> None

        2. setKeypoints(self: depthai.ImgDetection, keypoints: list[depthai.Keypoint]) -> None

        3. setKeypoints(self: depthai.ImgDetection, keypoints: list[depthai.Keypoint], edges: list[Annotated[list[int], FixedSize(2)]]) -> None

        4. setKeypoints(self: depthai.ImgDetection, keypoints: list[depthai.Point3f]) -> None

        5. setKeypoints(self: depthai.ImgDetection, keypoints: depthai.VectorPoint2f) -> None
        """
    @overload
    def setKeypoints(self, keypoints: list[Keypoint]) -> None:
        """setKeypoints(*args, **kwargs)
        Overloaded function.

        1. setKeypoints(self: depthai.ImgDetection, keypoints: depthai.KeypointsList) -> None

        2. setKeypoints(self: depthai.ImgDetection, keypoints: list[depthai.Keypoint]) -> None

        3. setKeypoints(self: depthai.ImgDetection, keypoints: list[depthai.Keypoint], edges: list[Annotated[list[int], FixedSize(2)]]) -> None

        4. setKeypoints(self: depthai.ImgDetection, keypoints: list[depthai.Point3f]) -> None

        5. setKeypoints(self: depthai.ImgDetection, keypoints: depthai.VectorPoint2f) -> None
        """
    @overload
    def setKeypoints(self, keypoints: list[Keypoint], edges) -> None:
        """setKeypoints(*args, **kwargs)
        Overloaded function.

        1. setKeypoints(self: depthai.ImgDetection, keypoints: depthai.KeypointsList) -> None

        2. setKeypoints(self: depthai.ImgDetection, keypoints: list[depthai.Keypoint]) -> None

        3. setKeypoints(self: depthai.ImgDetection, keypoints: list[depthai.Keypoint], edges: list[Annotated[list[int], FixedSize(2)]]) -> None

        4. setKeypoints(self: depthai.ImgDetection, keypoints: list[depthai.Point3f]) -> None

        5. setKeypoints(self: depthai.ImgDetection, keypoints: depthai.VectorPoint2f) -> None
        """
    @overload
    def setKeypoints(self, keypoints: list[Point3f]) -> None:
        """setKeypoints(*args, **kwargs)
        Overloaded function.

        1. setKeypoints(self: depthai.ImgDetection, keypoints: depthai.KeypointsList) -> None

        2. setKeypoints(self: depthai.ImgDetection, keypoints: list[depthai.Keypoint]) -> None

        3. setKeypoints(self: depthai.ImgDetection, keypoints: list[depthai.Keypoint], edges: list[Annotated[list[int], FixedSize(2)]]) -> None

        4. setKeypoints(self: depthai.ImgDetection, keypoints: list[depthai.Point3f]) -> None

        5. setKeypoints(self: depthai.ImgDetection, keypoints: depthai.VectorPoint2f) -> None
        """
    @overload
    def setKeypoints(self, keypoints: VectorPoint2f) -> None:
        """setKeypoints(*args, **kwargs)
        Overloaded function.

        1. setKeypoints(self: depthai.ImgDetection, keypoints: depthai.KeypointsList) -> None

        2. setKeypoints(self: depthai.ImgDetection, keypoints: list[depthai.Keypoint]) -> None

        3. setKeypoints(self: depthai.ImgDetection, keypoints: list[depthai.Keypoint], edges: list[Annotated[list[int], FixedSize(2)]]) -> None

        4. setKeypoints(self: depthai.ImgDetection, keypoints: list[depthai.Point3f]) -> None

        5. setKeypoints(self: depthai.ImgDetection, keypoints: depthai.VectorPoint2f) -> None
        """
    def setOuterBoundingBox(self, xmin: float, ymin: float, xmax: float, ymax: float) -> None:
        """setOuterBoundingBox(self: depthai.ImgDetection, xmin: float, ymin: float, xmax: float, ymax: float) -> None"""

class ImgDetections(Buffer):
    """ImgDetections message. Carries normalized detections and optional segmentation
    mask. The segmentation mask is stored as a single-channel INT8 2-d array, where
    the value represents the instance index in the list of detections. The value 255
    is treated as a background pixel (no instance)."""
    detections: list[ImgDetection]
    segmentationMaskHeight: int
    segmentationMaskWidth: int
    def __init__(self) -> None:
        """__init__(self: depthai.ImgDetections) -> None"""
    def getCvSegmentationMask(self) -> numpy.ndarray | None:
        """getCvSegmentationMask(self: depthai.ImgDetections) -> Optional[numpy.ndarray]

        Retrieves mask data as a cv::Mat copy with specified width and height. If mask
        data is not set, returns std::nullopt.

        Parameter ``allocator``:
            Allows callers to supply a custom cv::MatAllocator for zero-copy/custom
            memory management; nullptr uses OpenCV’s default.
        """
    def getCvSegmentationMaskByClass(self, semantic_class: int) -> numpy.ndarray | None:
        """getCvSegmentationMaskByClass(self: depthai.ImgDetections, semantic_class: int) -> Optional[numpy.ndarray]

        Retrieves data by the semantic class. If no mask data is not set, returns
        std::nullopt.

        Parameter ``semanticClass``:
            Semantic class index

        Parameter ``allocator``:
            Allows callers to supply a custom cv::MatAllocator for zero-copy/custom
            memory management; nullptr uses OpenCV’s default.
        """
    def getCvSegmentationMaskByIndex(self, index: int) -> numpy.ndarray | None:
        """getCvSegmentationMaskByIndex(self: depthai.ImgDetections, index: int) -> Optional[numpy.ndarray]

        Returns a binary mask where pixels belonging to the instance index are set to 1,
        others to 0. If mask data is not set, returns std::nullopt.

        Parameter ``index``:
            Instance index

        Parameter ``allocator``:
            Allows callers to supply a custom cv::MatAllocator for zero-copy/custom
            memory management; nullptr uses OpenCV’s default.
        """
    def getMaskData(self, *args, **kwargs):
        """getMaskData(self: depthai.ImgDetections) -> Optional[std::vector<unsigned char, std::allocator<unsigned char> >]

        Returns a copy of the segmentation mask data as a vector of bytes. If mask data
        is not set, returns std::nullopt.
        """
    def getSegmentationMask(self) -> ImgFrame | None:
        """getSegmentationMask(self: depthai.ImgDetections) -> Optional[depthai.ImgFrame]

        Returns the segmentation mask as an ImgFrame. If mask data is not set, returns
        std::nullopt.
        """
    def getSegmentationMaskHeight(self) -> int:
        """getSegmentationMaskHeight(self: depthai.ImgDetections) -> int

        Returns the height of the segmentation mask.
        """
    def getSegmentationMaskWidth(self) -> int:
        """getSegmentationMaskWidth(self: depthai.ImgDetections) -> int

        Returns the width of the segmentation mask.
        """
    def getSequenceNum(self) -> int:
        """getSequenceNum(self: depthai.ImgDetections) -> int

        Retrieves image sequence number
        """
    def getTimestamp(self) -> datetime.timedelta:
        """getTimestamp(self: depthai.ImgDetections) -> datetime.timedelta

        Retrieves timestamp related to dai::Clock::now()
        """
    def getTimestampDevice(self) -> datetime.timedelta:
        """getTimestampDevice(self: depthai.ImgDetections) -> datetime.timedelta

        Retrieves timestamp directly captured from device's monotonic clock, not
        synchronized to host time. Used mostly for debugging
        """
    def getTransformation(self) -> ImgTransformation | None:
        """getTransformation(self: depthai.ImgDetections) -> Optional[depthai.ImgTransformation]

        Retrieves image transformation data
        """
    def setCvSegmentationMask(self, mask: numpy.ndarray) -> None:
        """setCvSegmentationMask(self: depthai.ImgDetections, mask: numpy.ndarray) -> None

        Copies cv::Mat data to Segmentation Mask buffer

        Parameter ``frame``:
            Input cv::Mat frame from which to copy the data @note Throws if mask is not
            a single channel INT8 type.
        """
    def setSegmentationMask(self, frame: ImgFrame) -> None:
        """setSegmentationMask(self: depthai.ImgDetections, frame: depthai.ImgFrame) -> None

        Sets the segmentation mask from a vector of bytes. The size of the vector must
        be equal to width * height.
        """
    def setTransformation(self, transformation: ImgTransformation | None) -> None:
        """setTransformation(self: depthai.ImgDetections, transformation: Optional[depthai.ImgTransformation]) -> None

        Specifies image transformation data

        Parameter ``transformation``:
            transformation data
        """

class ImgFrame(Buffer):
    class Specs:
        bytesPP: int
        height: int
        p1Offset: int
        p2Offset: int
        p3Offset: int
        stride: int
        type: ImgFrame.Type
        width: int
        def __init__(self) -> None:
            """__init__(self: depthai.ImgFrame.Specs) -> None"""

    class Type:
        """Members:

          YUV422i

          YUV444p

          YUV420p

          YUV422p

          YUV400p

          RGBA8888

          RGB161616

          RGB888p

          BGR888p

          RGB888i

          BGR888i

          RGBF16F16F16p

          BGRF16F16F16p

          RGBF16F16F16i

          BGRF16F16F16i

          GRAY8

          GRAYF16

          LUT2

          LUT4

          LUT16

          RAW16

          RAW14

          RAW12

          RAW10

          RAW8

          PACK10

          PACK12

          YUV444i

          NV12

          NV21

          BITSTREAM

          HDR

          RAW32

          NONE"""
        __members__: ClassVar[dict] = ...  # read-only
        BGR888i: ClassVar[ImgFrame.Type] = ...
        BGR888p: ClassVar[ImgFrame.Type] = ...
        BGRF16F16F16i: ClassVar[ImgFrame.Type] = ...
        BGRF16F16F16p: ClassVar[ImgFrame.Type] = ...
        BITSTREAM: ClassVar[ImgFrame.Type] = ...
        GRAY8: ClassVar[ImgFrame.Type] = ...
        GRAYF16: ClassVar[ImgFrame.Type] = ...
        HDR: ClassVar[ImgFrame.Type] = ...
        LUT16: ClassVar[ImgFrame.Type] = ...
        LUT2: ClassVar[ImgFrame.Type] = ...
        LUT4: ClassVar[ImgFrame.Type] = ...
        NONE: ClassVar[ImgFrame.Type] = ...
        NV12: ClassVar[ImgFrame.Type] = ...
        NV21: ClassVar[ImgFrame.Type] = ...
        PACK10: ClassVar[ImgFrame.Type] = ...
        PACK12: ClassVar[ImgFrame.Type] = ...
        RAW10: ClassVar[ImgFrame.Type] = ...
        RAW12: ClassVar[ImgFrame.Type] = ...
        RAW14: ClassVar[ImgFrame.Type] = ...
        RAW16: ClassVar[ImgFrame.Type] = ...
        RAW32: ClassVar[ImgFrame.Type] = ...
        RAW8: ClassVar[ImgFrame.Type] = ...
        RGB161616: ClassVar[ImgFrame.Type] = ...
        RGB888i: ClassVar[ImgFrame.Type] = ...
        RGB888p: ClassVar[ImgFrame.Type] = ...
        RGBA8888: ClassVar[ImgFrame.Type] = ...
        RGBF16F16F16i: ClassVar[ImgFrame.Type] = ...
        RGBF16F16F16p: ClassVar[ImgFrame.Type] = ...
        YUV400p: ClassVar[ImgFrame.Type] = ...
        YUV420p: ClassVar[ImgFrame.Type] = ...
        YUV422i: ClassVar[ImgFrame.Type] = ...
        YUV422p: ClassVar[ImgFrame.Type] = ...
        YUV444i: ClassVar[ImgFrame.Type] = ...
        YUV444p: ClassVar[ImgFrame.Type] = ...
        __entries: ClassVar[dict] = ...
        def __init__(self, value: int) -> None:
            """__init__(self: depthai.ImgFrame.Type, value: int) -> None"""
        def __eq__(self, other: object) -> bool:
            """__eq__(self: object, other: object) -> bool"""
        def __hash__(self) -> int:
            """__hash__(self: object) -> int"""
        def __index__(self) -> int:
            """__index__(self: depthai.ImgFrame.Type) -> int"""
        def __int__(self) -> int:
            """__int__(self: depthai.ImgFrame.Type) -> int"""
        def __ne__(self, other: object) -> bool:
            """__ne__(self: object, other: object) -> bool"""
        @property
        def name(self) -> str:
            """name(self: object) -> str

            name(self: object) -> str
            """
        @property
        def value(self) -> int:
            """(arg0: depthai.ImgFrame.Type) -> int"""
    @overload
    def __init__(self) -> None:
        """__init__(*args, **kwargs)
        Overloaded function.

        1. __init__(self: depthai.ImgFrame) -> None

        2. __init__(self: depthai.ImgFrame, arg0: int) -> None
        """
    @overload
    def __init__(self, arg0: int) -> None:
        """__init__(*args, **kwargs)
        Overloaded function.

        1. __init__(self: depthai.ImgFrame) -> None

        2. __init__(self: depthai.ImgFrame, arg0: int) -> None
        """
    def getBytesPerPixel(self) -> float:
        """getBytesPerPixel(self: depthai.ImgFrame) -> float

        Retrieves image bytes per pixel
        """
    def getCategory(self) -> int:
        """getCategory(self: depthai.ImgFrame) -> int

        Retrieves image category
        """
    def getColorTemperature(self) -> int:
        """getColorTemperature(self: depthai.ImgFrame) -> int

        Retrieves white-balance color temperature of the light source, in kelvins
        """
    def getCvFrame(self) -> numpy.ndarray:
        """getCvFrame(self: depthai.ImgFrame) -> numpy.ndarray

        @note This API only available if OpenCV support is enabled

        Retrieves cv::Mat suitable for use in common opencv functions. ImgFrame is
        converted to color BGR interleaved or grayscale depending on type.

        A copy is always made

        Returns:
            cv::Mat for use in opencv functions
        """
    def getExposureTime(self) -> datetime.timedelta:
        """getExposureTime(self: depthai.ImgFrame) -> datetime.timedelta

        Retrieves exposure time
        """
    def getFrame(self) -> numpy.ndarray:
        """getFrame(self: depthai.ImgFrame) -> numpy.ndarray

        @note This API only available if OpenCV support is enabled

        Retrieves data as cv::Mat with specified width, height and type

        Parameter ``copy``:
            If false only a reference to data is made, otherwise a copy

        Returns:
            cv::Mat with corresponding to ImgFrame parameters
        """
    def getHeight(self) -> int:
        """getHeight(self: depthai.ImgFrame) -> int

        Retrieves image height in pixels
        """
    def getInstanceNum(self) -> int:
        """getInstanceNum(self: depthai.ImgFrame) -> int

        Retrieves instance number
        """
    def getLensPosition(self) -> int:
        """getLensPosition(self: depthai.ImgFrame) -> int

        Retrieves lens position, range 0..255. Returns -1 if not available
        """
    def getLensPositionRaw(self) -> float:
        """getLensPositionRaw(self: depthai.ImgFrame) -> float

        Retrieves lens position, range 0.0f..1.0f. Returns -1 if not available
        """
    def getPlaneHeight(self) -> int:
        """getPlaneHeight(self: depthai.ImgFrame) -> int

        Retrieves image plane height in lines
        """
    def getPlaneStride(self, planeIndex: int) -> int:
        """getPlaneStride(self: depthai.ImgFrame, planeIndex: int) -> int

        Retrieves image plane stride (offset to next plane) in bytes

        Parameter ``current``:
            plane index, 0 or 1
        """
    def getSensitivity(self) -> int:
        """getSensitivity(self: depthai.ImgFrame) -> int

        Retrieves sensitivity, as an ISO value
        """
    def getSequenceNum(self) -> int:
        """getSequenceNum(self: depthai.ImgFrame) -> int

        Retrieves image sequence number
        """
    def getSourceDFov(self) -> float:
        """getSourceDFov(self: depthai.ImgFrame) -> float

        @note Fov API works correctly only on rectilinear frames Get the source diagonal
        field of view in degrees

        Returns:
            field of view in degrees
        """
    def getSourceHFov(self) -> float:
        """getSourceHFov(self: depthai.ImgFrame) -> float

        @note Fov API works correctly only on rectilinear frames Get the source
        horizontal field of view

        Parameter ``degrees``:
            field of view in degrees
        """
    def getSourceHeight(self) -> int:
        """getSourceHeight(self: depthai.ImgFrame) -> int

        Retrieves source image height in pixels
        """
    def getSourceVFov(self) -> float:
        """getSourceVFov(self: depthai.ImgFrame) -> float

        @note Fov API works correctly only on rectilinear frames Get the source vertical
        field of view

        Parameter ``degrees``:
            field of view in degrees
        """
    def getSourceWidth(self) -> int:
        """getSourceWidth(self: depthai.ImgFrame) -> int

        Retrieves source image width in pixels
        """
    def getStride(self) -> int:
        """getStride(self: depthai.ImgFrame) -> int

        Retrieves image line stride in bytes
        """
    @overload
    def getTimestamp(self) -> datetime.timedelta:
        """getTimestamp(*args, **kwargs)
        Overloaded function.

        1. getTimestamp(self: depthai.ImgFrame) -> datetime.timedelta

        Retrieves timestamp related to dai::Clock::now()

        2. getTimestamp(self: depthai.ImgFrame, offset: depthai.CameraExposureOffset) -> datetime.timedelta

        Retrieves image timestamp (at the specified offset of exposure) related to
        dai::Clock::now()
        """
    @overload
    def getTimestamp(self, offset: CameraExposureOffset) -> datetime.timedelta:
        """getTimestamp(*args, **kwargs)
        Overloaded function.

        1. getTimestamp(self: depthai.ImgFrame) -> datetime.timedelta

        Retrieves timestamp related to dai::Clock::now()

        2. getTimestamp(self: depthai.ImgFrame, offset: depthai.CameraExposureOffset) -> datetime.timedelta

        Retrieves image timestamp (at the specified offset of exposure) related to
        dai::Clock::now()
        """
    def getTimestampDevice(self) -> datetime.timedelta:
        """getTimestampDevice(*args, **kwargs)
        Overloaded function.

        1. getTimestampDevice(self: depthai.ImgFrame) -> datetime.timedelta

        Retrieves timestamp directly captured from device's monotonic clock, not
        synchronized to host time. Used mostly for debugging

        2. getTimestampDevice(self: depthai.ImgFrame, offset: depthai.CameraExposureOffset) -> datetime.timedelta

        Retrieves image timestamp (at the specified offset of exposure) directly
        captured from device's monotonic clock, not synchronized to host time. Used when
        monotonicity is required.
        """
    def getTransformation(self) -> ImgTransformation:
        """getTransformation(self: depthai.ImgFrame) -> depthai.ImgTransformation

        Retrieves image transformation data
        """
    def getType(self) -> ImgFrame.Type:
        """getType(self: depthai.ImgFrame) -> depthai.ImgFrame.Type

        Retrieves image type
        """
    def getWidth(self) -> int:
        """getWidth(self: depthai.ImgFrame) -> int

        Retrieves image width in pixels
        """
    def setCategory(self, category: int) -> ImgFrame:
        """setCategory(self: depthai.ImgFrame, category: int) -> depthai.ImgFrame

        Parameter ``category``:
            Image category
        """
    def setCvFrame(self, array: numpy.ndarray, type: ImgFrame.Type) -> ImgFrame:
        """setCvFrame(self: depthai.ImgFrame, array: numpy.ndarray, type: depthai.ImgFrame.Type) -> depthai.ImgFrame

        @note This API only available if OpenCV support is enabled

        Copies cv::Mat data to the ImgFrame buffer and converts to a specific type.

        Parameter ``frame``:
            Input cv::Mat BGR frame or single channel frame from which to copy the data
            from.

        Parameter ``type``:
            Specifies the target image format for the internal buffer, including color
            space, layout, and bit depth.
        """
    def setFrame(self, array: numpy.ndarray) -> ImgFrame:
        """setFrame(self: depthai.ImgFrame, array: numpy.ndarray) -> depthai.ImgFrame

        @note This API only available if OpenCV support is enabled

        Copies cv::Mat data to ImgFrame buffer

        Parameter ``frame``:
            Input cv::Mat frame from which to copy the data
        """
    def setHeight(self, height: int) -> ImgFrame:
        """setHeight(self: depthai.ImgFrame, height: int) -> depthai.ImgFrame

        Specifies frame height

        Parameter ``height``:
            frame height
        """
    def setInstanceNum(self, instance: int) -> ImgFrame:
        """setInstanceNum(self: depthai.ImgFrame, instance: int) -> depthai.ImgFrame

        Instance number relates to the origin of the frame (which camera)

        Parameter ``instance``:
            Instance number
        """
    @overload
    def setSize(self, width: int, height: int) -> ImgFrame:
        """setSize(*args, **kwargs)
        Overloaded function.

        1. setSize(self: depthai.ImgFrame, width: int, height: int) -> depthai.ImgFrame

        Specifies frame size

        Parameter ``height``:
            frame height

        Parameter ``width``:
            frame width

        2. setSize(self: depthai.ImgFrame, sizer: tuple[int, int]) -> depthai.ImgFrame

        Specifies frame size

        Parameter ``size``:
            frame size
        """
    @overload
    def setSize(self, sizer: tuple[int, int]) -> ImgFrame:
        """setSize(*args, **kwargs)
        Overloaded function.

        1. setSize(self: depthai.ImgFrame, width: int, height: int) -> depthai.ImgFrame

        Specifies frame size

        Parameter ``height``:
            frame height

        Parameter ``width``:
            frame width

        2. setSize(self: depthai.ImgFrame, sizer: tuple[int, int]) -> depthai.ImgFrame

        Specifies frame size

        Parameter ``size``:
            frame size
        """
    def setStride(self, stride: int) -> ImgFrame:
        """setStride(self: depthai.ImgFrame, stride: int) -> depthai.ImgFrame

        Specifies frame stride

        Parameter ``stride``:
            frame stride
        """
    def setTimestamp(self, timestamp: datetime.timedelta) -> None:
        """setTimestamp(self: depthai.ImgFrame, timestamp: datetime.timedelta) -> None

        Sets image timestamp related to dai::Clock::now()
        """
    def setTimestampDevice(self, timestampDevice: datetime.timedelta) -> None:
        """setTimestampDevice(self: depthai.ImgFrame, timestampDevice: datetime.timedelta) -> None

        Sets image timestamp related to dai::Clock::now()
        """
    def setTransformation(self, transformation: ImgTransformation) -> ImgFrame:
        """setTransformation(self: depthai.ImgFrame, transformation: depthai.ImgTransformation) -> depthai.ImgFrame

        Specifies image transformation data

        Parameter ``transformation``:
            transformation data
        """
    def setType(self, type: ImgFrame.Type) -> ImgFrame:
        """setType(self: depthai.ImgFrame, type: depthai.ImgFrame.Type) -> depthai.ImgFrame

        Specifies frame type, RGB, BGR, ...

        Parameter ``type``:
            Type of image
        """
    def setWidth(self, width: int) -> ImgFrame:
        """setWidth(self: depthai.ImgFrame, width: int) -> depthai.ImgFrame

        Specifies frame width

        Parameter ``width``:
            frame width
        """
    def validateTransformations(self) -> bool:
        """validateTransformations(self: depthai.ImgFrame) -> bool

        Check that the image transformation match the image size

        Returns:
            true if the transformations are valid
        """

class ImgFrameCapability(Capability):
    enableUndistortion: bool | None
    fps: CapabilityRangeFloat
    resizeMode: ImgResizeMode
    size: CapabilityRangeUintPair
    type: ImgFrame.Type | None
    def __init__(self) -> None:
        """__init__(self: depthai.ImgFrameCapability) -> None"""

class ImgResizeMode:
    """

    Members:

      CROP

      STRETCH

      LETTERBOX"""
    __members__: ClassVar[dict] = ...  # read-only
    CROP: ClassVar[ImgResizeMode] = ...
    LETTERBOX: ClassVar[ImgResizeMode] = ...
    STRETCH: ClassVar[ImgResizeMode] = ...
    __entries: ClassVar[dict] = ...
    def __init__(self, value: int) -> None:
        """__init__(self: depthai.ImgResizeMode, value: int) -> None"""
    def __eq__(self, other: object) -> bool:
        """__eq__(self: object, other: object) -> bool"""
    def __hash__(self) -> int:
        """__hash__(self: object) -> int"""
    def __index__(self) -> int:
        """__index__(self: depthai.ImgResizeMode) -> int"""
    def __int__(self) -> int:
        """__int__(self: depthai.ImgResizeMode) -> int"""
    def __ne__(self, other: object) -> bool:
        """__ne__(self: object, other: object) -> bool"""
    @property
    def name(self) -> str:
        """name(self: object) -> str

        name(self: object) -> str
        """
    @property
    def value(self) -> int:
        """(arg0: depthai.ImgResizeMode) -> int"""

class ImgTransformation:
    """ImgTransformation struct. Holds information of how a ImgFrame or related message
    was transformed from their source. Useful for remapping from one ImgFrame to
    another."""
    @overload
    def __init__(self) -> None:
        """__init__(*args, **kwargs)
        Overloaded function.

        1. __init__(self: depthai.ImgTransformation) -> None

        2. __init__(self: depthai.ImgTransformation, width: int, height: int) -> None

        3. __init__(self: depthai.ImgTransformation, srcWidth: int, srcHeight: int, width: int, height: int) -> None

        4. __init__(self: depthai.ImgTransformation, width: int, height: int, sourceIntrinsicMatrix: Annotated[list[Annotated[list[float], FixedSize(3)]], FixedSize(3)]) -> None

        5. __init__(self: depthai.ImgTransformation, width: int, height: int, sourceIntrinsicMatrix: Annotated[list[Annotated[list[float], FixedSize(3)]], FixedSize(3)], distortionModel: depthai.CameraModel, distortionCoefficients: list[float]) -> None
        """
    @overload
    def __init__(self, width: int, height: int) -> None:
        """__init__(*args, **kwargs)
        Overloaded function.

        1. __init__(self: depthai.ImgTransformation) -> None

        2. __init__(self: depthai.ImgTransformation, width: int, height: int) -> None

        3. __init__(self: depthai.ImgTransformation, srcWidth: int, srcHeight: int, width: int, height: int) -> None

        4. __init__(self: depthai.ImgTransformation, width: int, height: int, sourceIntrinsicMatrix: Annotated[list[Annotated[list[float], FixedSize(3)]], FixedSize(3)]) -> None

        5. __init__(self: depthai.ImgTransformation, width: int, height: int, sourceIntrinsicMatrix: Annotated[list[Annotated[list[float], FixedSize(3)]], FixedSize(3)], distortionModel: depthai.CameraModel, distortionCoefficients: list[float]) -> None
        """
    @overload
    def __init__(self, srcWidth: int, srcHeight: int, width: int, height: int) -> None:
        """__init__(*args, **kwargs)
        Overloaded function.

        1. __init__(self: depthai.ImgTransformation) -> None

        2. __init__(self: depthai.ImgTransformation, width: int, height: int) -> None

        3. __init__(self: depthai.ImgTransformation, srcWidth: int, srcHeight: int, width: int, height: int) -> None

        4. __init__(self: depthai.ImgTransformation, width: int, height: int, sourceIntrinsicMatrix: Annotated[list[Annotated[list[float], FixedSize(3)]], FixedSize(3)]) -> None

        5. __init__(self: depthai.ImgTransformation, width: int, height: int, sourceIntrinsicMatrix: Annotated[list[Annotated[list[float], FixedSize(3)]], FixedSize(3)], distortionModel: depthai.CameraModel, distortionCoefficients: list[float]) -> None
        """
    @overload
    def __init__(self, width: int, height: int, sourceIntrinsicMatrix) -> None:
        """__init__(*args, **kwargs)
        Overloaded function.

        1. __init__(self: depthai.ImgTransformation) -> None

        2. __init__(self: depthai.ImgTransformation, width: int, height: int) -> None

        3. __init__(self: depthai.ImgTransformation, srcWidth: int, srcHeight: int, width: int, height: int) -> None

        4. __init__(self: depthai.ImgTransformation, width: int, height: int, sourceIntrinsicMatrix: Annotated[list[Annotated[list[float], FixedSize(3)]], FixedSize(3)]) -> None

        5. __init__(self: depthai.ImgTransformation, width: int, height: int, sourceIntrinsicMatrix: Annotated[list[Annotated[list[float], FixedSize(3)]], FixedSize(3)], distortionModel: depthai.CameraModel, distortionCoefficients: list[float]) -> None
        """
    @overload
    def __init__(self, width: int, height: int, sourceIntrinsicMatrix, distortionModel: CameraModel, distortionCoefficients: list[float]) -> None:
        """__init__(*args, **kwargs)
        Overloaded function.

        1. __init__(self: depthai.ImgTransformation) -> None

        2. __init__(self: depthai.ImgTransformation, width: int, height: int) -> None

        3. __init__(self: depthai.ImgTransformation, srcWidth: int, srcHeight: int, width: int, height: int) -> None

        4. __init__(self: depthai.ImgTransformation, width: int, height: int, sourceIntrinsicMatrix: Annotated[list[Annotated[list[float], FixedSize(3)]], FixedSize(3)]) -> None

        5. __init__(self: depthai.ImgTransformation, width: int, height: int, sourceIntrinsicMatrix: Annotated[list[Annotated[list[float], FixedSize(3)]], FixedSize(3)], distortionModel: depthai.CameraModel, distortionCoefficients: list[float]) -> None
        """
    def addCrop(self, x: int, y: int, width: int, height: int) -> ImgTransformation:
        """addCrop(self: depthai.ImgTransformation, x: int, y: int, width: int, height: int) -> depthai.ImgTransformation

        Add a crop transformation.

        Parameter ``x``:
            X coordinate of the top-left corner of the crop

        Parameter ``y``:
            Y coordinate of the top-left corner of the crop

        Parameter ``width``:
            Width of the crop

        Parameter ``height``:
            Height of the crop
        """
    def addFlipHorizontal(self) -> ImgTransformation:
        """addFlipHorizontal(self: depthai.ImgTransformation) -> depthai.ImgTransformation

        Add a horizontal flip transformation.
        """
    def addFlipVertical(self) -> ImgTransformation:
        """addFlipVertical(self: depthai.ImgTransformation) -> depthai.ImgTransformation

        Add a vertical flip transformation.
        """
    def addPadding(self, x: int, y: int, width: int, height: int) -> ImgTransformation:
        """addPadding(self: depthai.ImgTransformation, x: int, y: int, width: int, height: int) -> depthai.ImgTransformation

        Add a pad transformation. Works like crop, but in reverse.

        Parameter ``top``:
            Padding on the top

        Parameter ``bottom``:
            Padding on the bottom

        Parameter ``left``:
            Padding on the left

        Parameter ``right``:
            Padding on the right
        """
    def addRotation(self, angle: float, rotationPoint: Point2f) -> ImgTransformation:
        """addRotation(self: depthai.ImgTransformation, angle: float, rotationPoint: depthai.Point2f) -> depthai.ImgTransformation

        Add a rotation transformation.

        Parameter ``angle``:
            Angle in degrees

        Parameter ``rotationPoint``:
            Point around which to rotate
        """
    def addScale(self, scaleX: float, scaleY: float) -> ImgTransformation:
        """addScale(self: depthai.ImgTransformation, scaleX: float, scaleY: float) -> depthai.ImgTransformation

        Add a scale transformation.

        Parameter ``scaleX``:
            Scale factor in the horizontal direction

        Parameter ``scaleY``:
            Scale factor in the vertical direction
        """
    def addTransformation(self, matrix) -> ImgTransformation:
        """addTransformation(self: depthai.ImgTransformation, matrix: Annotated[list[Annotated[list[float], FixedSize(3)]], FixedSize(3)]) -> depthai.ImgTransformation

        Add a new transformation.

        Parameter ``matrix``:
            Transformation matrix
        """
    def getDFov(self, source: bool = ...) -> float:
        """getDFov(self: depthai.ImgTransformation, source: bool = False) -> float"""
    def getDistortionCoefficients(self) -> list[float]:
        """getDistortionCoefficients(self: depthai.ImgTransformation) -> list[float]

        Retrieve the distortion coefficients of the source sensor

        Returns:
            vector of distortion coefficients
        """
    def getDistortionModel(self) -> CameraModel:
        """getDistortionModel(self: depthai.ImgTransformation) -> depthai.CameraModel

        Retrieve the distortion model of the source sensor

        Returns:
            Distortion model
        """
    def getDstMaskPt(self, x: int, y: int) -> bool:
        """getDstMaskPt(self: depthai.ImgTransformation, x: int, y: int) -> bool

        Returns true if the point is inside the image region (not in the background
        region).
        """
    def getHFov(self, source: bool = ...) -> float:
        """getHFov(self: depthai.ImgTransformation, source: bool = False) -> float"""
    def getIntrinsicMatrix(self, *args, **kwargs):
        """getIntrinsicMatrix(self: depthai.ImgTransformation) -> Annotated[list[Annotated[list[float], FixedSize(3)]], FixedSize(3)]"""
    def getIntrinsicMatrixInv(self, *args, **kwargs):
        """getIntrinsicMatrixInv(self: depthai.ImgTransformation) -> Annotated[list[Annotated[list[float], FixedSize(3)]], FixedSize(3)]"""
    def getMatrix(self, *args, **kwargs):
        """getMatrix(self: depthai.ImgTransformation) -> Annotated[list[Annotated[list[float], FixedSize(3)]], FixedSize(3)]"""
    def getMatrixInv(self, *args, **kwargs):
        """getMatrixInv(self: depthai.ImgTransformation) -> Annotated[list[Annotated[list[float], FixedSize(3)]], FixedSize(3)]"""
    def getSize(self) -> tuple[int, int]:
        """getSize(self: depthai.ImgTransformation) -> tuple[int, int]

        Retrieve the size of the frame. Should be equal to the size of the corresponding
        ImgFrame message.

        Returns:
            Size of the frame
        """
    def getSourceIntrinsicMatrix(self, *args, **kwargs):
        """getSourceIntrinsicMatrix(self: depthai.ImgTransformation) -> Annotated[list[Annotated[list[float], FixedSize(3)]], FixedSize(3)]"""
    def getSourceIntrinsicMatrixInv(self, *args, **kwargs):
        """getSourceIntrinsicMatrixInv(self: depthai.ImgTransformation) -> Annotated[list[Annotated[list[float], FixedSize(3)]], FixedSize(3)]"""
    def getSourceSize(self) -> tuple[int, int]:
        """getSourceSize(self: depthai.ImgTransformation) -> tuple[int, int]

        Retrieve the size of the source frame from which this frame was derived.

        Returns:
            Size of the frame
        """
    def getSrcCrops(self) -> list[RotatedRect]:
        """getSrcCrops(self: depthai.ImgTransformation) -> list[depthai.RotatedRect]"""
    def getSrcMaskPt(self, x: int, y: int) -> bool:
        """getSrcMaskPt(self: depthai.ImgTransformation, x: int, y: int) -> bool

        Returns true if the point is inside the transformed region of interest
        (determined by crops used).
        """
    def getVFov(self, source: bool = ...) -> float:
        """getVFov(self: depthai.ImgTransformation, source: bool = False) -> float"""
    def invTransformPoint(self, point: Point2f) -> Point2f:
        """invTransformPoint(self: depthai.ImgTransformation, point: depthai.Point2f) -> depthai.Point2f

        Transform a point from the current frame to the source frame.

        Parameter ``point``:
            Point to transform

        Returns:
            Transformed point
        """
    def invTransformRect(self, rect: RotatedRect) -> RotatedRect:
        """invTransformRect(self: depthai.ImgTransformation, rect: depthai.RotatedRect) -> depthai.RotatedRect

        Transform a rotated rect from the current frame to the source frame.

        Parameter ``rect``:
            Rectangle to transform

        Returns:
            Transformed rectangle
        """
    def isAlignedTo(self, to: ImgTransformation) -> bool:
        """isAlignedTo(self: depthai.ImgTransformation, to: depthai.ImgTransformation) -> bool

        Check if the transformations are aligned

        Parameter ``to``:
            Transformation to compare with
        """
    def isValid(self) -> bool:
        """isValid(self: depthai.ImgTransformation) -> bool

        Check if the transformations are valid. The transformations are valid if the
        source frame size and the current frame size are set.
        """
    def remapPointFrom(self, to: ImgTransformation, point: Point2f) -> Point2f:
        """remapPointFrom(self: depthai.ImgTransformation, to: depthai.ImgTransformation, point: depthai.Point2f) -> depthai.Point2f

        Remap a point to this transformation from another. If the intrinsics are
        different (e.g. different camera), the function will also use the intrinsics to
        remap the point.

        Parameter ``from``:
            Transformation to remap from

        Parameter ``point``:
            Point to remap
        """
    def remapPointTo(self, to: ImgTransformation, point: Point2f) -> Point2f:
        """remapPointTo(self: depthai.ImgTransformation, to: depthai.ImgTransformation, point: depthai.Point2f) -> depthai.Point2f

        Remap a point from this transformation to another. If the intrinsics are
        different (e.g. different camera), the function will also use the intrinsics to
        remap the point.

        Parameter ``to``:
            Transformation to remap to

        Parameter ``point``:
            Point to remap
        """
    def remapRectFrom(self, to: ImgTransformation, rect: RotatedRect) -> RotatedRect:
        """remapRectFrom(self: depthai.ImgTransformation, to: depthai.ImgTransformation, rect: depthai.RotatedRect) -> depthai.RotatedRect

        Remap a rotated rect to this transformation from another. If the intrinsics are
        different (e.g. different camera), the function will also use the intrinsics to
        remap the rect.

        Parameter ``from``:
            Transformation to remap from

        Parameter ``point``:
            RotatedRect to remap
        """
    def remapRectTo(self, to: ImgTransformation, rect: RotatedRect) -> RotatedRect:
        """remapRectTo(self: depthai.ImgTransformation, to: depthai.ImgTransformation, rect: depthai.RotatedRect) -> depthai.RotatedRect

        Remap a rotated rect from this transformation to another. If the intrinsics are
        different (e.g. different camera), the function will also use the intrinsics to
        remap the rect.

        Parameter ``to``:
            Transformation to remap to

        Parameter ``rect``:
            RotatedRect to remap
        """
    def setDistortionCoefficients(self, coefficients: list[float]) -> ImgTransformation:
        """setDistortionCoefficients(self: depthai.ImgTransformation, coefficients: list[float]) -> depthai.ImgTransformation"""
    def setDistortionModel(self, model: CameraModel) -> ImgTransformation:
        """setDistortionModel(self: depthai.ImgTransformation, model: depthai.CameraModel) -> depthai.ImgTransformation"""
    def setIntrinsicMatrix(self, intrinsicMatrix) -> ImgTransformation:
        """setIntrinsicMatrix(self: depthai.ImgTransformation, intrinsicMatrix: Annotated[list[Annotated[list[float], FixedSize(3)]], FixedSize(3)]) -> depthai.ImgTransformation"""
    def transformPoint(self, point: Point2f) -> Point2f:
        """transformPoint(self: depthai.ImgTransformation, point: depthai.Point2f) -> depthai.Point2f

        Transform a point from the source frame to the current frame.

        Parameter ``point``:
            Point to transform

        Returns:
            Transformed point
        """
    def transformRect(self, rect: RotatedRect) -> RotatedRect:
        """transformRect(self: depthai.ImgTransformation, rect: depthai.RotatedRect) -> depthai.RotatedRect

        Transform a rotated rect from the source frame to the current frame.

        Parameter ``rect``:
            Rectangle to transform

        Returns:
            Transformed rectangle
        """

class InputQueue:
    def __init__(self, *args, **kwargs) -> None:
        """Initialize self.  See help(type(self)) for accurate signature."""
    def send(self, msg: ADatatype) -> None:
        """send(self: depthai.InputQueue, msg: depthai.ADatatype) -> None

        Send a message to the connected input

        Parameter ``msg:``:
            Message to send
        """

class Interpolation:
    """Interpolation type

    Members:

      BILINEAR

      BICUBIC

      NEAREST_NEIGHBOR

      BYPASS

      DEFAULT

      DEFAULT_DISPARITY_DEPTH"""
    __members__: ClassVar[dict] = ...  # read-only
    BICUBIC: ClassVar[Interpolation] = ...
    BILINEAR: ClassVar[Interpolation] = ...
    BYPASS: ClassVar[Interpolation] = ...
    DEFAULT: ClassVar[Interpolation] = ...
    DEFAULT_DISPARITY_DEPTH: ClassVar[Interpolation] = ...
    NEAREST_NEIGHBOR: ClassVar[Interpolation] = ...
    __entries: ClassVar[dict] = ...
    def __init__(self, value: int) -> None:
        """__init__(self: depthai.Interpolation, value: int) -> None"""
    def __eq__(self, other: object) -> bool:
        """__eq__(self: object, other: object) -> bool"""
    def __hash__(self) -> int:
        """__hash__(self: object) -> int"""
    def __index__(self) -> int:
        """__index__(self: depthai.Interpolation) -> int"""
    def __int__(self) -> int:
        """__int__(self: depthai.Interpolation) -> int"""
    def __ne__(self, other: object) -> bool:
        """__ne__(self: object, other: object) -> bool"""
    @property
    def name(self) -> str:
        """name(self: object) -> str

        name(self: object) -> str
        """
    @property
    def value(self) -> int:
        """(arg0: depthai.Interpolation) -> int"""

class Keypoint:
    confidence: float
    imageCoordinates: Point3f
    label: int
    labelName: str
    @overload
    def __init__(self) -> None:
        """__init__(*args, **kwargs)
        Overloaded function.

        1. __init__(self: depthai.Keypoint) -> None

        2. __init__(self: depthai.Keypoint, coordinates: depthai.Point3f, confidence: float = -1.0, label: int = 0, labelName: str = '') -> None

        3. __init__(self: depthai.Keypoint, coordinates: depthai.Point2f, confidence: float = -1.0, label: int = 0, labelName: str = '') -> None

        4. __init__(self: depthai.Keypoint, x: float, y: float, z: float, confidence: float = -1.0, label: int = 0, labelName: str = '') -> None
        """
    @overload
    def __init__(self, coordinates: Point3f, confidence: float = ..., label: int = ..., labelName: str = ...) -> None:
        """__init__(*args, **kwargs)
        Overloaded function.

        1. __init__(self: depthai.Keypoint) -> None

        2. __init__(self: depthai.Keypoint, coordinates: depthai.Point3f, confidence: float = -1.0, label: int = 0, labelName: str = '') -> None

        3. __init__(self: depthai.Keypoint, coordinates: depthai.Point2f, confidence: float = -1.0, label: int = 0, labelName: str = '') -> None

        4. __init__(self: depthai.Keypoint, x: float, y: float, z: float, confidence: float = -1.0, label: int = 0, labelName: str = '') -> None
        """
    @overload
    def __init__(self, coordinates: Point2f, confidence: float = ..., label: int = ..., labelName: str = ...) -> None:
        """__init__(*args, **kwargs)
        Overloaded function.

        1. __init__(self: depthai.Keypoint) -> None

        2. __init__(self: depthai.Keypoint, coordinates: depthai.Point3f, confidence: float = -1.0, label: int = 0, labelName: str = '') -> None

        3. __init__(self: depthai.Keypoint, coordinates: depthai.Point2f, confidence: float = -1.0, label: int = 0, labelName: str = '') -> None

        4. __init__(self: depthai.Keypoint, x: float, y: float, z: float, confidence: float = -1.0, label: int = 0, labelName: str = '') -> None
        """
    @overload
    def __init__(self, x: float, y: float, z: float, confidence: float = ..., label: int = ..., labelName: str = ...) -> None:
        """__init__(*args, **kwargs)
        Overloaded function.

        1. __init__(self: depthai.Keypoint) -> None

        2. __init__(self: depthai.Keypoint, coordinates: depthai.Point3f, confidence: float = -1.0, label: int = 0, labelName: str = '') -> None

        3. __init__(self: depthai.Keypoint, coordinates: depthai.Point2f, confidence: float = -1.0, label: int = 0, labelName: str = '') -> None

        4. __init__(self: depthai.Keypoint, x: float, y: float, z: float, confidence: float = -1.0, label: int = 0, labelName: str = '') -> None
        """

class KeypointsList:
    @overload
    def __init__(self) -> None:
        """__init__(*args, **kwargs)
        Overloaded function.

        1. __init__(self: depthai.KeypointsList) -> None

        2. __init__(self: depthai.KeypointsList, keypoints: list[depthai.Keypoint], edges: list[Annotated[list[int], FixedSize(2)]]) -> None

        3. __init__(self: depthai.KeypointsList, keypoints: list[depthai.Keypoint]) -> None
        """
    @overload
    def __init__(self, keypoints: list[Keypoint], edges) -> None:
        """__init__(*args, **kwargs)
        Overloaded function.

        1. __init__(self: depthai.KeypointsList) -> None

        2. __init__(self: depthai.KeypointsList, keypoints: list[depthai.Keypoint], edges: list[Annotated[list[int], FixedSize(2)]]) -> None

        3. __init__(self: depthai.KeypointsList, keypoints: list[depthai.Keypoint]) -> None
        """
    @overload
    def __init__(self, keypoints: list[Keypoint]) -> None:
        """__init__(*args, **kwargs)
        Overloaded function.

        1. __init__(self: depthai.KeypointsList) -> None

        2. __init__(self: depthai.KeypointsList, keypoints: list[depthai.Keypoint], edges: list[Annotated[list[int], FixedSize(2)]]) -> None

        3. __init__(self: depthai.KeypointsList, keypoints: list[depthai.Keypoint]) -> None
        """
    def getEdges(self, *args, **kwargs):
        """getEdges(self: depthai.KeypointsList) -> list[Annotated[list[int], FixedSize(2)]]

        Get the indices of the edges.

        Returns:
            Vector of edge indices.
        """
    def getKeypoints(self) -> list[Keypoint]:
        """getKeypoints(self: depthai.KeypointsList) -> list[depthai.Keypoint]

        Get keypoints.

        Returns:
            Vector of Keypoint objects.
        """
    def getPoints2f(self) -> VectorPoint2f:
        """getPoints2f(self: depthai.KeypointsList) -> depthai.VectorPoint2f

        Get only image 2D coordinates of the keypoints and drop the z axis values.

        Returns:
            Vector of Point2f coordinates.
        """
    def getPoints3f(self) -> list[Point3f]:
        """getPoints3f(self: depthai.KeypointsList) -> list[depthai.Point3f]

        Get only image coordinates of the keypoints.

        Returns:
            Vector of Point3f coordinates.
        """
    def setEdges(self, edges) -> None:
        """setEdges(self: depthai.KeypointsList, edges: list[Annotated[list[int], FixedSize(2)]]) -> None"""
    @overload
    def setKeypoints(self, keypoints: list[Keypoint]) -> None:
        """setKeypoints(*args, **kwargs)
        Overloaded function.

        1. setKeypoints(self: depthai.KeypointsList, keypoints: list[depthai.Keypoint]) -> None

        Sets the keypoints list.

        Parameter ``keypoints``:
            list of Keypoint objects to set. @note This will clear any existing
            keypoints and edges.

        2. setKeypoints(self: depthai.KeypointsList, keypoints: list[depthai.Point3f]) -> None

        Sets the keypoints list.

        Parameter ``keypoints``:
            list of Keypoint objects to set. @note This will clear any existing
            keypoints and edges.

        3. setKeypoints(self: depthai.KeypointsList, keypoints: depthai.VectorPoint2f) -> None

        Sets the keypoints list.

        Parameter ``keypoints``:
            list of Keypoint objects to set. @note This will clear any existing
            keypoints and edges.

        4. setKeypoints(self: depthai.KeypointsList, keypoints: list[depthai.Keypoint], edges: list[Annotated[list[int], FixedSize(2)]]) -> None

        Sets the keypoints list.

        Parameter ``keypoints``:
            list of Keypoint objects to set. @note This will clear any existing
            keypoints and edges.
        """
    @overload
    def setKeypoints(self, keypoints: list[Point3f]) -> None:
        """setKeypoints(*args, **kwargs)
        Overloaded function.

        1. setKeypoints(self: depthai.KeypointsList, keypoints: list[depthai.Keypoint]) -> None

        Sets the keypoints list.

        Parameter ``keypoints``:
            list of Keypoint objects to set. @note This will clear any existing
            keypoints and edges.

        2. setKeypoints(self: depthai.KeypointsList, keypoints: list[depthai.Point3f]) -> None

        Sets the keypoints list.

        Parameter ``keypoints``:
            list of Keypoint objects to set. @note This will clear any existing
            keypoints and edges.

        3. setKeypoints(self: depthai.KeypointsList, keypoints: depthai.VectorPoint2f) -> None

        Sets the keypoints list.

        Parameter ``keypoints``:
            list of Keypoint objects to set. @note This will clear any existing
            keypoints and edges.

        4. setKeypoints(self: depthai.KeypointsList, keypoints: list[depthai.Keypoint], edges: list[Annotated[list[int], FixedSize(2)]]) -> None

        Sets the keypoints list.

        Parameter ``keypoints``:
            list of Keypoint objects to set. @note This will clear any existing
            keypoints and edges.
        """
    @overload
    def setKeypoints(self, keypoints: VectorPoint2f) -> None:
        """setKeypoints(*args, **kwargs)
        Overloaded function.

        1. setKeypoints(self: depthai.KeypointsList, keypoints: list[depthai.Keypoint]) -> None

        Sets the keypoints list.

        Parameter ``keypoints``:
            list of Keypoint objects to set. @note This will clear any existing
            keypoints and edges.

        2. setKeypoints(self: depthai.KeypointsList, keypoints: list[depthai.Point3f]) -> None

        Sets the keypoints list.

        Parameter ``keypoints``:
            list of Keypoint objects to set. @note This will clear any existing
            keypoints and edges.

        3. setKeypoints(self: depthai.KeypointsList, keypoints: depthai.VectorPoint2f) -> None

        Sets the keypoints list.

        Parameter ``keypoints``:
            list of Keypoint objects to set. @note This will clear any existing
            keypoints and edges.

        4. setKeypoints(self: depthai.KeypointsList, keypoints: list[depthai.Keypoint], edges: list[Annotated[list[int], FixedSize(2)]]) -> None

        Sets the keypoints list.

        Parameter ``keypoints``:
            list of Keypoint objects to set. @note This will clear any existing
            keypoints and edges.
        """
    @overload
    def setKeypoints(self, keypoints: list[Keypoint], edges) -> None:
        """setKeypoints(*args, **kwargs)
        Overloaded function.

        1. setKeypoints(self: depthai.KeypointsList, keypoints: list[depthai.Keypoint]) -> None

        Sets the keypoints list.

        Parameter ``keypoints``:
            list of Keypoint objects to set. @note This will clear any existing
            keypoints and edges.

        2. setKeypoints(self: depthai.KeypointsList, keypoints: list[depthai.Point3f]) -> None

        Sets the keypoints list.

        Parameter ``keypoints``:
            list of Keypoint objects to set. @note This will clear any existing
            keypoints and edges.

        3. setKeypoints(self: depthai.KeypointsList, keypoints: depthai.VectorPoint2f) -> None

        Sets the keypoints list.

        Parameter ``keypoints``:
            list of Keypoint objects to set. @note This will clear any existing
            keypoints and edges.

        4. setKeypoints(self: depthai.KeypointsList, keypoints: list[depthai.Keypoint], edges: list[Annotated[list[int], FixedSize(2)]]) -> None

        Sets the keypoints list.

        Parameter ``keypoints``:
            list of Keypoint objects to set. @note This will clear any existing
            keypoints and edges.
        """

class LengthUnit:
    """Measurement unit for depth and calibration data.

    Members:

      METER

      CENTIMETER

      MILLIMETER

      INCH

      FOOT

      CUSTOM"""
    __members__: ClassVar[dict] = ...  # read-only
    CENTIMETER: ClassVar[LengthUnit] = ...
    CUSTOM: ClassVar[LengthUnit] = ...
    FOOT: ClassVar[LengthUnit] = ...
    INCH: ClassVar[LengthUnit] = ...
    METER: ClassVar[LengthUnit] = ...
    MILLIMETER: ClassVar[LengthUnit] = ...
    __entries: ClassVar[dict] = ...
    def __init__(self, value: int) -> None:
        """__init__(self: depthai.LengthUnit, value: int) -> None"""
    def __eq__(self, other: object) -> bool:
        """__eq__(self: object, other: object) -> bool"""
    def __hash__(self) -> int:
        """__hash__(self: object) -> int"""
    def __index__(self) -> int:
        """__index__(self: depthai.LengthUnit) -> int"""
    def __int__(self) -> int:
        """__int__(self: depthai.LengthUnit) -> int"""
    def __ne__(self, other: object) -> bool:
        """__ne__(self: object, other: object) -> bool"""
    @property
    def name(self) -> str:
        """name(self: object) -> str

        name(self: object) -> str
        """
    @property
    def value(self) -> int:
        """(arg0: depthai.LengthUnit) -> int"""

class LogLevel:
    """Members:

      TRACE

      DEBUG

      INFO

      WARN

      ERR

      CRITICAL

      OFF"""
    __members__: ClassVar[dict] = ...  # read-only
    CRITICAL: ClassVar[LogLevel] = ...
    DEBUG: ClassVar[LogLevel] = ...
    ERR: ClassVar[LogLevel] = ...
    INFO: ClassVar[LogLevel] = ...
    OFF: ClassVar[LogLevel] = ...
    TRACE: ClassVar[LogLevel] = ...
    WARN: ClassVar[LogLevel] = ...
    __entries: ClassVar[dict] = ...
    def __init__(self, value: int) -> None:
        """__init__(self: depthai.LogLevel, value: int) -> None"""
    def __eq__(self, other: object) -> bool:
        """__eq__(self: object, other: object) -> bool"""
    def __hash__(self) -> int:
        """__hash__(self: object) -> int"""
    def __index__(self) -> int:
        """__index__(self: depthai.LogLevel) -> int"""
    def __int__(self) -> int:
        """__int__(self: depthai.LogLevel) -> int"""
    def __ne__(self, other: object) -> bool:
        """__ne__(self: object, other: object) -> bool"""
    @property
    def name(self) -> str:
        """name(self: object) -> str

        name(self: object) -> str
        """
    @property
    def value(self) -> int:
        """(arg0: depthai.LogLevel) -> int"""

class LogMessage:
    colorRangeEnd: int
    colorRangeStart: int
    level: LogLevel
    nodeIdName: str
    payload: str
    time: Timestamp
    def __init__(self, *args, **kwargs) -> None:
        """Initialize self.  See help(type(self)) for accurate signature."""

class MapData(Buffer):
    """MapData message. Carries grid map data and minX/minY messages to help place it
    in 3D space."""
    map: ImgFrame
    minX: float
    minY: float
    def __init__(self) -> None:
        """__init__(self: depthai.MapData) -> None"""

class MemoryInfo:
    """MemoryInfo structure

    Free, remaining and total memory stats"""
    remaining: int
    total: int
    used: int
    def __init__(self) -> None:
        """__init__(self: depthai.MemoryInfo) -> None"""

class MessageDemuxProperties:
    """Specify properties for MessageDemux."""
    processor: ProcessorType
    def __init__(self, *args, **kwargs) -> None:
        """Initialize self.  See help(type(self)) for accurate signature."""

class MessageGroup(Buffer):
    """MessageGroup message. Carries multiple messages in one."""
    def __init__(self) -> None:
        """__init__(self: depthai.MessageGroup) -> None"""
    def getIntervalNs(self) -> int:
        """getIntervalNs(self: depthai.MessageGroup) -> int

        Retrieves interval between the first and the last message in the group.
        """
    def getMessageNames(self) -> list[str]:
        """getMessageNames(self: depthai.MessageGroup) -> list[str]

        Gets the names of messages in the group
        """
    def getNumMessages(self) -> int:
        """getNumMessages(self: depthai.MessageGroup) -> int"""
    def getSequenceNum(self) -> int:
        """getSequenceNum(self: depthai.MessageGroup) -> int

        Retrieves image sequence number
        """
    def getTimestamp(self) -> datetime.timedelta:
        """getTimestamp(self: depthai.MessageGroup) -> datetime.timedelta

        Retrieves timestamp related to dai::Clock::now()
        """
    def getTimestampDevice(self) -> datetime.timedelta:
        """getTimestampDevice(self: depthai.MessageGroup) -> datetime.timedelta

        Retrieves timestamp directly captured from device's monotonic clock, not
        synchronized to host time. Used mostly for debugging
        """
    def isSynced(self, thresholdNs: int) -> bool:
        """isSynced(self: depthai.MessageGroup, thresholdNs: int) -> bool

        True if all messages in the group are in the interval

        Parameter ``thresholdNs``:
            Maximal interval between messages
        """
    def setSequenceNum(self, sequenceNum: int) -> None:
        """setSequenceNum(self: depthai.MessageGroup, sequenceNum: int) -> None

        Sets image sequence number
        """
    def setTimestamp(self, timestamp: datetime.timedelta) -> None:
        """setTimestamp(self: depthai.MessageGroup, timestamp: datetime.timedelta) -> None

        Sets image timestamp related to dai::Clock::now()
        """
    def setTimestampDevice(self, timestampDevice: datetime.timedelta) -> None:
        """setTimestampDevice(self: depthai.MessageGroup, timestampDevice: datetime.timedelta) -> None

        Sets image timestamp related to dai::Clock::now()
        """
    def __getitem__(self, arg0: str) -> ADatatype:
        """__getitem__(self: depthai.MessageGroup, arg0: str) -> depthai.ADatatype"""
    def __iter__(self) -> Iterator[tuple[str, ADatatype]]:
        """__iter__(self: depthai.MessageGroup) -> Iterator[tuple[str, depthai.ADatatype]]"""
    def __setitem__(self, arg0: str, arg1: ADatatype) -> None:
        """__setitem__(self: depthai.MessageGroup, arg0: str, arg1: depthai.ADatatype) -> None"""

class MessageQueue:
    """Thread safe queue to send messages between nodes"""
    QueueException: ClassVar[type[MessageQueue.QueueException]] = ...
    @overload
    def __init__(self, name: str) -> None:
        """__init__(*args, **kwargs)
        Overloaded function.

        1. __init__(self: depthai.MessageQueue, name: str) -> None

        2. __init__(self: depthai.MessageQueue, name: str = '', maxSize: int = 16, blocking: bool = True) -> None
        """
    @overload
    def __init__(self, name: str = ..., maxSize: int = ..., blocking: bool = ...) -> None:
        """__init__(*args, **kwargs)
        Overloaded function.

        1. __init__(self: depthai.MessageQueue, name: str) -> None

        2. __init__(self: depthai.MessageQueue, name: str = '', maxSize: int = 16, blocking: bool = True) -> None
        """
    def addCallback(self, callback: Callable) -> int:
        """addCallback(self: depthai.MessageQueue, callback: Callable) -> int

        Adds a callback on message received

        Parameter ``callback``:
            Callback function with queue name and message pointer

        Returns:
            Callback id
        """
    def close(self) -> None:
        """close(self: depthai.MessageQueue) -> None

        Closes the queue and unblocks any waiting consumers or producers
        """
    def front(self) -> ADatatype:
        """front(self: depthai.MessageQueue) -> depthai.ADatatype

        Gets first message in the queue.

        Returns:
            Message of type T or nullptr if no message available
        """
    @overload
    def get(self) -> ADatatype:
        """get(*args, **kwargs)
        Overloaded function.

        1. get(self: depthai.MessageQueue) -> depthai.ADatatype

        Block until a message is available.

        Returns:
            Message of type T or nullptr if no message available

        2. get(self: depthai.MessageQueue, timeout: datetime.timedelta) -> depthai.ADatatype

        Block until a message is available.

        Returns:
            Message of type T or nullptr if no message available
        """
    @overload
    def get(self, timeout: datetime.timedelta) -> ADatatype:
        """get(*args, **kwargs)
        Overloaded function.

        1. get(self: depthai.MessageQueue) -> depthai.ADatatype

        Block until a message is available.

        Returns:
            Message of type T or nullptr if no message available

        2. get(self: depthai.MessageQueue, timeout: datetime.timedelta) -> depthai.ADatatype

        Block until a message is available.

        Returns:
            Message of type T or nullptr if no message available
        """
    def getAll(self) -> list[ADatatype]:
        """getAll(self: depthai.MessageQueue) -> list[depthai.ADatatype]

        Block until at least one message in the queue. Then return all messages from the
        queue.

        Returns:
            Vector of messages which can either be of type T or nullptr
        """
    def getBlocking(self) -> bool:
        """getBlocking(self: depthai.MessageQueue) -> bool

        Gets current queue behavior when full (maxSize)

        Returns:
            True if blocking, false otherwise
        """
    def getMaxSize(self) -> int:
        """getMaxSize(self: depthai.MessageQueue) -> int

        Gets queue maximum size

        Returns:
            Maximum queue size
        """
    @overload
    def getName(self) -> str:
        """getName(*args, **kwargs)
        Overloaded function.

        1. getName(self: depthai.MessageQueue) -> str

        Get name of the queue

        2. getName(self: depthai.MessageQueue) -> str

        Get name of the queue
        """
    @overload
    def getName(self) -> str:
        """getName(*args, **kwargs)
        Overloaded function.

        1. getName(self: depthai.MessageQueue) -> str

        Get name of the queue

        2. getName(self: depthai.MessageQueue) -> str

        Get name of the queue
        """
    def getSize(self) -> int:
        """getSize(self: depthai.MessageQueue) -> int

        Gets queue current size

        Returns:
            Current queue size
        """
    def has(self) -> bool:
        """has(self: depthai.MessageQueue) -> bool

        Check whether front of the queue has message of type T

        Returns:
            True if queue isn't empty and the first element is of type T, false
            otherwise
        """
    def isClosed(self) -> bool:
        """isClosed(self: depthai.MessageQueue) -> bool

        Check whether queue is closed
        """
    def isFull(self) -> int:
        """isFull(self: depthai.MessageQueue) -> int

        Gets whether queue is full

        Returns:
            True if queue is full, false otherwise
        """
    def removeCallback(self, callbackId: int) -> bool:
        """removeCallback(self: depthai.MessageQueue, callbackId: int) -> bool

        Removes a callback

        Parameter ``callbackId``:
            Id of callback to be removed

        Returns:
            True if callback was removed, false otherwise
        """
    @overload
    def send(self, msg: ADatatype) -> None:
        """send(*args, **kwargs)
        Overloaded function.

        1. send(self: depthai.MessageQueue, msg: depthai.ADatatype) -> None

        Adds a message to the queue, which will be picked up and sent to the device. Can
        either block if 'blocking' behavior is true or overwrite oldest

        Parameter ``msg``:
            Message to add to the queue

        2. send(self: depthai.MessageQueue, msg: depthai.ADatatype, timeout: datetime.timedelta) -> bool

        Adds a message to the queue, which will be picked up and sent to the device. Can
        either block if 'blocking' behavior is true or overwrite oldest

        Parameter ``msg``:
            Message to add to the queue
        """
    @overload
    def send(self, msg: ADatatype, timeout: datetime.timedelta) -> bool:
        """send(*args, **kwargs)
        Overloaded function.

        1. send(self: depthai.MessageQueue, msg: depthai.ADatatype) -> None

        Adds a message to the queue, which will be picked up and sent to the device. Can
        either block if 'blocking' behavior is true or overwrite oldest

        Parameter ``msg``:
            Message to add to the queue

        2. send(self: depthai.MessageQueue, msg: depthai.ADatatype, timeout: datetime.timedelta) -> bool

        Adds a message to the queue, which will be picked up and sent to the device. Can
        either block if 'blocking' behavior is true or overwrite oldest

        Parameter ``msg``:
            Message to add to the queue
        """
    def setBlocking(self, blocking: bool) -> None:
        """setBlocking(self: depthai.MessageQueue, blocking: bool) -> None

        Sets queue behavior when full (maxSize)

        Parameter ``blocking``:
            Specifies if block or overwrite the oldest message in the queue
        """
    def setMaxSize(self, maxSize: int) -> None:
        """setMaxSize(self: depthai.MessageQueue, maxSize: int) -> None

        Sets queue maximum size

        Parameter ``maxSize``:
            Specifies maximum number of messages in the queue @note If maxSize is
            smaller than size, queue will not be truncated immediately, only after
            messages are popped
        """
    def setName(self, name: str) -> None:
        """setName(self: depthai.MessageQueue, name: str) -> None

        Set the name of the queue
        """
    def tryGet(self) -> ADatatype:
        """tryGet(self: depthai.MessageQueue) -> depthai.ADatatype

        Try to retrieve message T from queue. If message isn't of type T it returns
        nullptr

        Returns:
            Message of type T or nullptr if no message available
        """
    def tryGetAll(self) -> list[ADatatype]:
        """tryGetAll(self: depthai.MessageQueue) -> list[depthai.ADatatype]

        Try to retrieve all messages in the queue.

        Returns:
            Vector of messages which can either be of type T or nullptr
        """
    def trySend(self, msg: ADatatype) -> bool:
        """trySend(self: depthai.MessageQueue, msg: depthai.ADatatype) -> bool

        Tries sending a message

        Parameter ``msg``:
            message to send
        """

class ModelType:
    """Neural network model type

    Members:

      BLOB

      SUPERBLOB

      DLC

      NNARCHIVE

      OTHER"""
    __members__: ClassVar[dict] = ...  # read-only
    BLOB: ClassVar[ModelType] = ...
    DLC: ClassVar[ModelType] = ...
    NNARCHIVE: ClassVar[ModelType] = ...
    OTHER: ClassVar[ModelType] = ...
    SUPERBLOB: ClassVar[ModelType] = ...
    __entries: ClassVar[dict] = ...
    def __init__(self, value: int) -> None:
        """__init__(self: depthai.ModelType, value: int) -> None"""
    def __eq__(self, other: object) -> bool:
        """__eq__(self: object, other: object) -> bool"""
    def __hash__(self) -> int:
        """__hash__(self: object) -> int"""
    def __index__(self) -> int:
        """__index__(self: depthai.ModelType) -> int"""
    def __int__(self) -> int:
        """__int__(self: depthai.ModelType) -> int"""
    def __ne__(self, other: object) -> bool:
        """__ne__(self: object, other: object) -> bool"""
    @property
    def name(self) -> str:
        """name(self: object) -> str

        name(self: object) -> str
        """
    @property
    def value(self) -> int:
        """(arg0: depthai.ModelType) -> int"""

class MonoCameraProperties:
    """Specify properties for MonoCamera such as camera ID, ..."""

    class SensorResolution:
        """Select the camera sensor resolution: 1280×720, 1280×800, 640×400, 640×480,
        1920×1200, ...

        Members:

          THE_720_P

          THE_800_P

          THE_400_P

          THE_480_P

          THE_1200_P

          THE_4000X3000

          THE_4224X3136"""
        __members__: ClassVar[dict] = ...  # read-only
        THE_1200_P: ClassVar[MonoCameraProperties.SensorResolution] = ...
        THE_4000X3000: ClassVar[MonoCameraProperties.SensorResolution] = ...
        THE_400_P: ClassVar[MonoCameraProperties.SensorResolution] = ...
        THE_4224X3136: ClassVar[MonoCameraProperties.SensorResolution] = ...
        THE_480_P: ClassVar[MonoCameraProperties.SensorResolution] = ...
        THE_720_P: ClassVar[MonoCameraProperties.SensorResolution] = ...
        THE_800_P: ClassVar[MonoCameraProperties.SensorResolution] = ...
        __entries: ClassVar[dict] = ...
        def __init__(self, value: int) -> None:
            """__init__(self: depthai.MonoCameraProperties.SensorResolution, value: int) -> None"""
        def __eq__(self, other: object) -> bool:
            """__eq__(self: object, other: object) -> bool"""
        def __hash__(self) -> int:
            """__hash__(self: object) -> int"""
        def __index__(self) -> int:
            """__index__(self: depthai.MonoCameraProperties.SensorResolution) -> int"""
        def __int__(self) -> int:
            """__int__(self: depthai.MonoCameraProperties.SensorResolution) -> int"""
        def __ne__(self, other: object) -> bool:
            """__ne__(self: object, other: object) -> bool"""
        @property
        def name(self) -> str:
            """name(self: object) -> str

            name(self: object) -> str
            """
        @property
        def value(self) -> int:
            """(arg0: depthai.MonoCameraProperties.SensorResolution) -> int"""
    boardSocket: CameraBoardSocket
    eventFilter: list[FrameEvent]
    fps: float
    initialControl: CameraControl
    isp3aFps: int
    numFramesPool: int
    numFramesPoolRaw: int
    resolution: MonoCameraProperties.SensorResolution
    def __init__(self, *args, **kwargs) -> None:
        """Initialize self.  See help(type(self)) for accurate signature."""

class NNArchive:
    @overload
    def __init__(self, archivePath: os.PathLike, compression: NNArchiveEntry.Compression = ...) -> None:
        """__init__(*args, **kwargs)
        Overloaded function.

        1. __init__(self: depthai.NNArchive, archivePath: os.PathLike, compression: depthai.NNArchiveEntry.Compression = <Compression.???: 0>) -> None

        Construct a new NNArchive object - a container holding a model and its
        configuration

        Parameter ``archivePath:``:
            Path to the archive file

        Parameter ``options:``:
            Archive options such as compression, number of shaves, etc. See
            NNArchiveOptions.

        2. __init__(self: depthai.NNArchive, archivePath: os.PathLike, options: depthai.NNArchiveOptions = <depthai.NNArchiveOptions object at 0xff8e22e17ab0>) -> None

        Construct a new NNArchive object - a container holding a model and its
        configuration

        Parameter ``archivePath:``:
            Path to the archive file

        Parameter ``options:``:
            Archive options such as compression, number of shaves, etc. See
            NNArchiveOptions.
        """
    @overload
    def __init__(self, archivePath: os.PathLike, options: NNArchiveOptions = ...) -> None:
        """__init__(*args, **kwargs)
        Overloaded function.

        1. __init__(self: depthai.NNArchive, archivePath: os.PathLike, compression: depthai.NNArchiveEntry.Compression = <Compression.???: 0>) -> None

        Construct a new NNArchive object - a container holding a model and its
        configuration

        Parameter ``archivePath:``:
            Path to the archive file

        Parameter ``options:``:
            Archive options such as compression, number of shaves, etc. See
            NNArchiveOptions.

        2. __init__(self: depthai.NNArchive, archivePath: os.PathLike, options: depthai.NNArchiveOptions = <depthai.NNArchiveOptions object at 0xff8e22e17ab0>) -> None

        Construct a new NNArchive object - a container holding a model and its
        configuration

        Parameter ``archivePath:``:
            Path to the archive file

        Parameter ``options:``:
            Archive options such as compression, number of shaves, etc. See
            NNArchiveOptions.
        """
    def getBlob(self) -> OpenVINO.Blob | None:
        """getBlob(self: depthai.NNArchive) -> Optional[depthai.OpenVINO.Blob]

        Return a SuperVINO::Blob from the archive if getModelType() returns BLOB,
        nothing otherwise

        Returns:
            std::optional<OpenVINO::Blob>: Model blob
        """
    def getConfig(self) -> nn_archive.v1.Config:
        """getConfig(self: depthai.NNArchive) -> Union[depthai.nn_archive.v1.Config]

        Get NNArchive config.

        Template parameter ``T:``:
            Type of config to get

        Returns:
            const T&: Config
        """
    def getConfigV1(self) -> nn_archive.v1.Config:
        """getConfigV1(self: depthai.NNArchive) -> depthai.nn_archive.v1.Config

        Get NNArchive config.

        Template parameter ``T:``:
            Type of config to get

        Returns:
            const T&: Config
        """
    def getHeadConfig(self, index: int = ...) -> nn_archive.v1.Head:
        """getHeadConfig(self: depthai.NNArchive, index: int = 0) -> depthai.nn_archive.v1.Head

        Get specific head configuration from NNArchive.

        Parameter ``headIndex:``:
            Index of the head to retrieve

        Returns:
            dai::nn_archive::v1::Head: Head configuration
        """
    def getInputHeight(self, index: int = ...) -> int | None:
        """getInputHeight(self: depthai.NNArchive, index: int = 0) -> Optional[int]

        Get inputHeight of the model

        Parameter ``index:``:
            Index of input

        Returns:
            int: inputHeight
        """
    def getInputSize(self, index: int = ...) -> tuple[int, int] | None:
        """getInputSize(self: depthai.NNArchive, index: int = 0) -> Optional[tuple[int, int]]

        Get inputSize of the model

        Parameter ``index:``:
            Index of input @note this function is only valid for models with NCHW and
            NHWC input formats

        Returns:
            std::vector<std::pair<int, int>>: inputSize
        """
    def getInputWidth(self, index: int = ...) -> int | None:
        """getInputWidth(self: depthai.NNArchive, index: int = 0) -> Optional[int]

        Get inputWidth of the model

        Parameter ``index:``:
            Index of input

        Returns:
            int: inputWidth
        """
    def getModelType(self) -> ModelType:
        """getModelType(self: depthai.NNArchive) -> depthai.ModelType

        Get type of model contained in NNArchive

        Returns:
            model::ModelType: type of model in archive
        """
    def getOtherModelFormat(self, *args, **kwargs):
        """getOtherModelFormat(self: depthai.NNArchive) -> Optional[std::vector<unsigned char, std::allocator<unsigned char> >]

        Return a model from the archive if getModelType() returns OTHER or DLC, nothing
        otherwise

        Returns:
            std::optional<std::vector<uint8_t>>: Model
        """
    def getSuperBlob(self) -> OpenVINO.SuperBlob | None:
        """getSuperBlob(self: depthai.NNArchive) -> Optional[depthai.OpenVINO.SuperBlob]

        Return a SuperVINO::Blob from the archive if getModelType() returns BLOB,
        nothing otherwise

        Returns:
            std::optional<OpenVINO::Blob>: Model blob
        """
    def getSupportedPlatforms(self) -> list[Platform]:
        """getSupportedPlatforms(self: depthai.NNArchive) -> list[depthai.Platform]

        Get supported platforms

        Returns:
            std::vector<dai::Platform>: Supported platforms
        """

class NNArchiveConfigVersion:
    """

    Members:

      V1"""
    __members__: ClassVar[dict] = ...  # read-only
    V1: ClassVar[NNArchiveConfigVersion] = ...
    __entries: ClassVar[dict] = ...
    def __init__(self, value: int) -> None:
        """__init__(self: depthai.NNArchiveConfigVersion, value: int) -> None"""
    def __eq__(self, other: object) -> bool:
        """__eq__(self: object, other: object) -> bool"""
    def __hash__(self) -> int:
        """__hash__(self: object) -> int"""
    def __index__(self) -> int:
        """__index__(self: depthai.NNArchiveConfigVersion) -> int"""
    def __int__(self) -> int:
        """__int__(self: depthai.NNArchiveConfigVersion) -> int"""
    def __ne__(self, other: object) -> bool:
        """__ne__(self: object, other: object) -> bool"""
    @property
    def name(self) -> str:
        """name(self: object) -> str

        name(self: object) -> str
        """
    @property
    def value(self) -> int:
        """(arg0: depthai.NNArchiveConfigVersion) -> int"""

class NNArchiveEntry:
    class Compression:
        """Members:

          AUTO

          RAW_FS

          TAR

          TAR_GZ

          TAR_XZ"""
        __members__: ClassVar[dict] = ...  # read-only
        AUTO: ClassVar[NNArchiveEntry.Compression] = ...
        RAW_FS: ClassVar[NNArchiveEntry.Compression] = ...
        TAR: ClassVar[NNArchiveEntry.Compression] = ...
        TAR_GZ: ClassVar[NNArchiveEntry.Compression] = ...
        TAR_XZ: ClassVar[NNArchiveEntry.Compression] = ...
        __entries: ClassVar[dict] = ...
        def __init__(self, value: int) -> None:
            """__init__(self: depthai.NNArchiveEntry.Compression, value: int) -> None"""
        def __eq__(self, other: object) -> bool:
            """__eq__(self: object, other: object) -> bool"""
        def __hash__(self) -> int:
            """__hash__(self: object) -> int"""
        def __index__(self) -> int:
            """__index__(self: depthai.NNArchiveEntry.Compression) -> int"""
        def __int__(self) -> int:
            """__int__(self: depthai.NNArchiveEntry.Compression) -> int"""
        def __ne__(self, other: object) -> bool:
            """__ne__(self: object, other: object) -> bool"""
        @property
        def name(self) -> str:
            """name(self: object) -> str

            name(self: object) -> str
            """
        @property
        def value(self) -> int:
            """(arg0: depthai.NNArchiveEntry.Compression) -> int"""

    class Seek:
        """Members:

          SET

          CUR

          END"""
        __members__: ClassVar[dict] = ...  # read-only
        CUR: ClassVar[NNArchiveEntry.Seek] = ...
        END: ClassVar[NNArchiveEntry.Seek] = ...
        SET: ClassVar[NNArchiveEntry.Seek] = ...
        __entries: ClassVar[dict] = ...
        def __init__(self, value: int) -> None:
            """__init__(self: depthai.NNArchiveEntry.Seek, value: int) -> None"""
        def __eq__(self, other: object) -> bool:
            """__eq__(self: object, other: object) -> bool"""
        def __hash__(self) -> int:
            """__hash__(self: object) -> int"""
        def __index__(self) -> int:
            """__index__(self: depthai.NNArchiveEntry.Seek) -> int"""
        def __int__(self) -> int:
            """__int__(self: depthai.NNArchiveEntry.Seek) -> int"""
        def __ne__(self, other: object) -> bool:
            """__ne__(self: object, other: object) -> bool"""
        @property
        def name(self) -> str:
            """name(self: object) -> str

            name(self: object) -> str
            """
        @property
        def value(self) -> int:
            """(arg0: depthai.NNArchiveEntry.Seek) -> int"""
    def __init__(self, *args, **kwargs) -> None:
        """Initialize self.  See help(type(self)) for accurate signature."""

class NNArchiveOptions:
    compression: NNArchiveEntry.Compression
    def __init__(self) -> None:
        """__init__(self: depthai.NNArchiveOptions) -> None"""

class NNArchiveVersionedConfig:
    @overload
    def __init__(self, path: os.PathLike, compression: NNArchiveEntry.Compression = ...) -> None:
        """__init__(*args, **kwargs)
        Overloaded function.

        1. __init__(self: depthai.NNArchiveVersionedConfig, path: os.PathLike, compression: depthai.NNArchiveEntry.Compression = <Compression.???: 0>) -> None

        @data Should point to a whole compressed NNArchive read to memory if compression
        is not set to RAW_FS. If compression is set to RAW_FS, then this should point to
        just the config.json file read to memory.

        2. __init__(self: depthai.NNArchiveVersionedConfig, data: std::vector<unsigned char, std::allocator<unsigned char> >, compression: depthai.NNArchiveEntry.Compression = <Compression.???: 0>) -> None

        @data Should point to a whole compressed NNArchive read to memory if compression
        is not set to RAW_FS. If compression is set to RAW_FS, then this should point to
        just the config.json file read to memory.

        3. __init__(self: depthai.NNArchiveVersionedConfig, arg0: Callable[[], int], arg1: Callable[[], std::vector<unsigned char, std::allocator<unsigned char> >], arg2: Callable[[int, depthai.NNArchiveEntry.Seek], int], arg3: Callable[[int], int], arg4: Callable[[], int], arg5: depthai.NNArchiveEntry.Compression) -> None
        """
    @overload
    def __init__(self, data, std, compression: NNArchiveEntry.Compression = ...) -> None:
        """__init__(*args, **kwargs)
        Overloaded function.

        1. __init__(self: depthai.NNArchiveVersionedConfig, path: os.PathLike, compression: depthai.NNArchiveEntry.Compression = <Compression.???: 0>) -> None

        @data Should point to a whole compressed NNArchive read to memory if compression
        is not set to RAW_FS. If compression is set to RAW_FS, then this should point to
        just the config.json file read to memory.

        2. __init__(self: depthai.NNArchiveVersionedConfig, data: std::vector<unsigned char, std::allocator<unsigned char> >, compression: depthai.NNArchiveEntry.Compression = <Compression.???: 0>) -> None

        @data Should point to a whole compressed NNArchive read to memory if compression
        is not set to RAW_FS. If compression is set to RAW_FS, then this should point to
        just the config.json file read to memory.

        3. __init__(self: depthai.NNArchiveVersionedConfig, arg0: Callable[[], int], arg1: Callable[[], std::vector<unsigned char, std::allocator<unsigned char> >], arg2: Callable[[int, depthai.NNArchiveEntry.Seek], int], arg3: Callable[[int], int], arg4: Callable[[], int], arg5: depthai.NNArchiveEntry.Compression) -> None
        """
    @overload
    def __init__(self, arg0: Callable[[], int], arg1, arg2: Callable[[int, NNArchiveEntry.Seek], int], arg3: Callable[[int], int], arg4: Callable[[], int], arg5: NNArchiveEntry.Compression) -> None:
        """__init__(*args, **kwargs)
        Overloaded function.

        1. __init__(self: depthai.NNArchiveVersionedConfig, path: os.PathLike, compression: depthai.NNArchiveEntry.Compression = <Compression.???: 0>) -> None

        @data Should point to a whole compressed NNArchive read to memory if compression
        is not set to RAW_FS. If compression is set to RAW_FS, then this should point to
        just the config.json file read to memory.

        2. __init__(self: depthai.NNArchiveVersionedConfig, data: std::vector<unsigned char, std::allocator<unsigned char> >, compression: depthai.NNArchiveEntry.Compression = <Compression.???: 0>) -> None

        @data Should point to a whole compressed NNArchive read to memory if compression
        is not set to RAW_FS. If compression is set to RAW_FS, then this should point to
        just the config.json file read to memory.

        3. __init__(self: depthai.NNArchiveVersionedConfig, arg0: Callable[[], int], arg1: Callable[[], std::vector<unsigned char, std::allocator<unsigned char> >], arg2: Callable[[int, depthai.NNArchiveEntry.Seek], int], arg3: Callable[[int], int], arg4: Callable[[], int], arg5: depthai.NNArchiveEntry.Compression) -> None
        """
    def getConfig(self) -> nn_archive.v1.Config:
        """getConfig(self: depthai.NNArchiveVersionedConfig) -> Union[depthai.nn_archive.v1.Config]

        Get stored config cast to a specific version.

        Template parameter ``T:``:
            Config type to cast to.
        """
    def getConfigV1(self) -> nn_archive.v1.Config:
        """getConfigV1(self: depthai.NNArchiveVersionedConfig) -> depthai.nn_archive.v1.Config

        Get stored config cast to a specific version.

        Template parameter ``T:``:
            Config type to cast to.
        """
    def getVersion(self) -> NNArchiveConfigVersion:
        """getVersion(self: depthai.NNArchiveVersionedConfig) -> depthai.NNArchiveConfigVersion

        Get version of the underlying config.
        """

class NNData(Buffer):
    """NNData message. Carries tensors and their metadata"""
    @overload
    def __init__(self) -> None:
        """__init__(*args, **kwargs)
        Overloaded function.

        1. __init__(self: depthai.NNData) -> None

        Construct NNData message.

        2. __init__(self: depthai.NNData, size: int) -> None
        """
    @overload
    def __init__(self, size: int) -> None:
        """__init__(*args, **kwargs)
        Overloaded function.

        1. __init__(self: depthai.NNData) -> None

        Construct NNData message.

        2. __init__(self: depthai.NNData, size: int) -> None
        """
    @overload
    def addTensor(self, name: str, tensor: list[int], storageOrder: TensorInfo.StorageOrder) -> NNData:
        """addTensor(*args, **kwargs)
        Overloaded function.

        1. addTensor(self: depthai.NNData, name: str, tensor: list[int], storageOrder: depthai.TensorInfo.StorageOrder) -> depthai.NNData

        Add a tensor to this NNData object. The provided array is stored as a 1xN tensor
        where N is the length of the array.

        Parameter ``name:``:
            Name of the tensor

        Parameter ``data:``:
            array

        Returns:
            NNData&: reference to this object

        2. addTensor(self: depthai.NNData, name: str, tensor: list[float], storageOrder: depthai.TensorInfo.StorageOrder) -> depthai.NNData

        Add a tensor to this NNData object. The provided array is stored as a 1xN tensor
        where N is the length of the array.

        Parameter ``name:``:
            Name of the tensor

        Parameter ``data:``:
            array

        Returns:
            NNData&: reference to this object

        3. addTensor(self: depthai.NNData, name: str, tensor: list[float], storageOrder: depthai.TensorInfo.StorageOrder) -> depthai.NNData

        Add a tensor to this NNData object. The provided array is stored as a 1xN tensor
        where N is the length of the array.

        Parameter ``name:``:
            Name of the tensor

        Parameter ``data:``:
            array

        Returns:
            NNData&: reference to this object

        4. addTensor(self: depthai.NNData, name: str, tensor: numpy.ndarray[numpy.int32], storageOrder: depthai.TensorInfo.StorageOrder) -> depthai.NNData

        5. addTensor(self: depthai.NNData, name: str, tensor: numpy.ndarray[numpy.float32], storageOrder: depthai.TensorInfo.StorageOrder) -> depthai.NNData

        6. addTensor(self: depthai.NNData, name: str, tensor: numpy.ndarray[numpy.float64], storageOrder: depthai.TensorInfo.StorageOrder) -> depthai.NNData

        7. addTensor(self: depthai.NNData, name: str, tensor: object, dataType: depthai.TensorInfo.DataType) -> None

        Add a tensor to this NNData object. The provided array is stored as a 1xN tensor
        where N is the length of the array.

        Parameter ``name:``:
            Name of the tensor

        Parameter ``data:``:
            array

        Returns:
            NNData&: reference to this object

        8. addTensor(self: depthai.NNData, name: str, tensor: object) -> None
        """
    @overload
    def addTensor(self, name: str, tensor: list[float], storageOrder: TensorInfo.StorageOrder) -> NNData:
        """addTensor(*args, **kwargs)
        Overloaded function.

        1. addTensor(self: depthai.NNData, name: str, tensor: list[int], storageOrder: depthai.TensorInfo.StorageOrder) -> depthai.NNData

        Add a tensor to this NNData object. The provided array is stored as a 1xN tensor
        where N is the length of the array.

        Parameter ``name:``:
            Name of the tensor

        Parameter ``data:``:
            array

        Returns:
            NNData&: reference to this object

        2. addTensor(self: depthai.NNData, name: str, tensor: list[float], storageOrder: depthai.TensorInfo.StorageOrder) -> depthai.NNData

        Add a tensor to this NNData object. The provided array is stored as a 1xN tensor
        where N is the length of the array.

        Parameter ``name:``:
            Name of the tensor

        Parameter ``data:``:
            array

        Returns:
            NNData&: reference to this object

        3. addTensor(self: depthai.NNData, name: str, tensor: list[float], storageOrder: depthai.TensorInfo.StorageOrder) -> depthai.NNData

        Add a tensor to this NNData object. The provided array is stored as a 1xN tensor
        where N is the length of the array.

        Parameter ``name:``:
            Name of the tensor

        Parameter ``data:``:
            array

        Returns:
            NNData&: reference to this object

        4. addTensor(self: depthai.NNData, name: str, tensor: numpy.ndarray[numpy.int32], storageOrder: depthai.TensorInfo.StorageOrder) -> depthai.NNData

        5. addTensor(self: depthai.NNData, name: str, tensor: numpy.ndarray[numpy.float32], storageOrder: depthai.TensorInfo.StorageOrder) -> depthai.NNData

        6. addTensor(self: depthai.NNData, name: str, tensor: numpy.ndarray[numpy.float64], storageOrder: depthai.TensorInfo.StorageOrder) -> depthai.NNData

        7. addTensor(self: depthai.NNData, name: str, tensor: object, dataType: depthai.TensorInfo.DataType) -> None

        Add a tensor to this NNData object. The provided array is stored as a 1xN tensor
        where N is the length of the array.

        Parameter ``name:``:
            Name of the tensor

        Parameter ``data:``:
            array

        Returns:
            NNData&: reference to this object

        8. addTensor(self: depthai.NNData, name: str, tensor: object) -> None
        """
    @overload
    def addTensor(self, name: str, tensor: list[float], storageOrder: TensorInfo.StorageOrder) -> NNData:
        """addTensor(*args, **kwargs)
        Overloaded function.

        1. addTensor(self: depthai.NNData, name: str, tensor: list[int], storageOrder: depthai.TensorInfo.StorageOrder) -> depthai.NNData

        Add a tensor to this NNData object. The provided array is stored as a 1xN tensor
        where N is the length of the array.

        Parameter ``name:``:
            Name of the tensor

        Parameter ``data:``:
            array

        Returns:
            NNData&: reference to this object

        2. addTensor(self: depthai.NNData, name: str, tensor: list[float], storageOrder: depthai.TensorInfo.StorageOrder) -> depthai.NNData

        Add a tensor to this NNData object. The provided array is stored as a 1xN tensor
        where N is the length of the array.

        Parameter ``name:``:
            Name of the tensor

        Parameter ``data:``:
            array

        Returns:
            NNData&: reference to this object

        3. addTensor(self: depthai.NNData, name: str, tensor: list[float], storageOrder: depthai.TensorInfo.StorageOrder) -> depthai.NNData

        Add a tensor to this NNData object. The provided array is stored as a 1xN tensor
        where N is the length of the array.

        Parameter ``name:``:
            Name of the tensor

        Parameter ``data:``:
            array

        Returns:
            NNData&: reference to this object

        4. addTensor(self: depthai.NNData, name: str, tensor: numpy.ndarray[numpy.int32], storageOrder: depthai.TensorInfo.StorageOrder) -> depthai.NNData

        5. addTensor(self: depthai.NNData, name: str, tensor: numpy.ndarray[numpy.float32], storageOrder: depthai.TensorInfo.StorageOrder) -> depthai.NNData

        6. addTensor(self: depthai.NNData, name: str, tensor: numpy.ndarray[numpy.float64], storageOrder: depthai.TensorInfo.StorageOrder) -> depthai.NNData

        7. addTensor(self: depthai.NNData, name: str, tensor: object, dataType: depthai.TensorInfo.DataType) -> None

        Add a tensor to this NNData object. The provided array is stored as a 1xN tensor
        where N is the length of the array.

        Parameter ``name:``:
            Name of the tensor

        Parameter ``data:``:
            array

        Returns:
            NNData&: reference to this object

        8. addTensor(self: depthai.NNData, name: str, tensor: object) -> None
        """
    @overload
    def addTensor(self, name: str, tensor: numpy.ndarray[numpy.int32], storageOrder: TensorInfo.StorageOrder) -> NNData:
        """addTensor(*args, **kwargs)
        Overloaded function.

        1. addTensor(self: depthai.NNData, name: str, tensor: list[int], storageOrder: depthai.TensorInfo.StorageOrder) -> depthai.NNData

        Add a tensor to this NNData object. The provided array is stored as a 1xN tensor
        where N is the length of the array.

        Parameter ``name:``:
            Name of the tensor

        Parameter ``data:``:
            array

        Returns:
            NNData&: reference to this object

        2. addTensor(self: depthai.NNData, name: str, tensor: list[float], storageOrder: depthai.TensorInfo.StorageOrder) -> depthai.NNData

        Add a tensor to this NNData object. The provided array is stored as a 1xN tensor
        where N is the length of the array.

        Parameter ``name:``:
            Name of the tensor

        Parameter ``data:``:
            array

        Returns:
            NNData&: reference to this object

        3. addTensor(self: depthai.NNData, name: str, tensor: list[float], storageOrder: depthai.TensorInfo.StorageOrder) -> depthai.NNData

        Add a tensor to this NNData object. The provided array is stored as a 1xN tensor
        where N is the length of the array.

        Parameter ``name:``:
            Name of the tensor

        Parameter ``data:``:
            array

        Returns:
            NNData&: reference to this object

        4. addTensor(self: depthai.NNData, name: str, tensor: numpy.ndarray[numpy.int32], storageOrder: depthai.TensorInfo.StorageOrder) -> depthai.NNData

        5. addTensor(self: depthai.NNData, name: str, tensor: numpy.ndarray[numpy.float32], storageOrder: depthai.TensorInfo.StorageOrder) -> depthai.NNData

        6. addTensor(self: depthai.NNData, name: str, tensor: numpy.ndarray[numpy.float64], storageOrder: depthai.TensorInfo.StorageOrder) -> depthai.NNData

        7. addTensor(self: depthai.NNData, name: str, tensor: object, dataType: depthai.TensorInfo.DataType) -> None

        Add a tensor to this NNData object. The provided array is stored as a 1xN tensor
        where N is the length of the array.

        Parameter ``name:``:
            Name of the tensor

        Parameter ``data:``:
            array

        Returns:
            NNData&: reference to this object

        8. addTensor(self: depthai.NNData, name: str, tensor: object) -> None
        """
    @overload
    def addTensor(self, name: str, tensor: numpy.ndarray[numpy.float32], storageOrder: TensorInfo.StorageOrder) -> NNData:
        """addTensor(*args, **kwargs)
        Overloaded function.

        1. addTensor(self: depthai.NNData, name: str, tensor: list[int], storageOrder: depthai.TensorInfo.StorageOrder) -> depthai.NNData

        Add a tensor to this NNData object. The provided array is stored as a 1xN tensor
        where N is the length of the array.

        Parameter ``name:``:
            Name of the tensor

        Parameter ``data:``:
            array

        Returns:
            NNData&: reference to this object

        2. addTensor(self: depthai.NNData, name: str, tensor: list[float], storageOrder: depthai.TensorInfo.StorageOrder) -> depthai.NNData

        Add a tensor to this NNData object. The provided array is stored as a 1xN tensor
        where N is the length of the array.

        Parameter ``name:``:
            Name of the tensor

        Parameter ``data:``:
            array

        Returns:
            NNData&: reference to this object

        3. addTensor(self: depthai.NNData, name: str, tensor: list[float], storageOrder: depthai.TensorInfo.StorageOrder) -> depthai.NNData

        Add a tensor to this NNData object. The provided array is stored as a 1xN tensor
        where N is the length of the array.

        Parameter ``name:``:
            Name of the tensor

        Parameter ``data:``:
            array

        Returns:
            NNData&: reference to this object

        4. addTensor(self: depthai.NNData, name: str, tensor: numpy.ndarray[numpy.int32], storageOrder: depthai.TensorInfo.StorageOrder) -> depthai.NNData

        5. addTensor(self: depthai.NNData, name: str, tensor: numpy.ndarray[numpy.float32], storageOrder: depthai.TensorInfo.StorageOrder) -> depthai.NNData

        6. addTensor(self: depthai.NNData, name: str, tensor: numpy.ndarray[numpy.float64], storageOrder: depthai.TensorInfo.StorageOrder) -> depthai.NNData

        7. addTensor(self: depthai.NNData, name: str, tensor: object, dataType: depthai.TensorInfo.DataType) -> None

        Add a tensor to this NNData object. The provided array is stored as a 1xN tensor
        where N is the length of the array.

        Parameter ``name:``:
            Name of the tensor

        Parameter ``data:``:
            array

        Returns:
            NNData&: reference to this object

        8. addTensor(self: depthai.NNData, name: str, tensor: object) -> None
        """
    @overload
    def addTensor(self, name: str, tensor: numpy.ndarray[numpy.float64], storageOrder: TensorInfo.StorageOrder) -> NNData:
        """addTensor(*args, **kwargs)
        Overloaded function.

        1. addTensor(self: depthai.NNData, name: str, tensor: list[int], storageOrder: depthai.TensorInfo.StorageOrder) -> depthai.NNData

        Add a tensor to this NNData object. The provided array is stored as a 1xN tensor
        where N is the length of the array.

        Parameter ``name:``:
            Name of the tensor

        Parameter ``data:``:
            array

        Returns:
            NNData&: reference to this object

        2. addTensor(self: depthai.NNData, name: str, tensor: list[float], storageOrder: depthai.TensorInfo.StorageOrder) -> depthai.NNData

        Add a tensor to this NNData object. The provided array is stored as a 1xN tensor
        where N is the length of the array.

        Parameter ``name:``:
            Name of the tensor

        Parameter ``data:``:
            array

        Returns:
            NNData&: reference to this object

        3. addTensor(self: depthai.NNData, name: str, tensor: list[float], storageOrder: depthai.TensorInfo.StorageOrder) -> depthai.NNData

        Add a tensor to this NNData object. The provided array is stored as a 1xN tensor
        where N is the length of the array.

        Parameter ``name:``:
            Name of the tensor

        Parameter ``data:``:
            array

        Returns:
            NNData&: reference to this object

        4. addTensor(self: depthai.NNData, name: str, tensor: numpy.ndarray[numpy.int32], storageOrder: depthai.TensorInfo.StorageOrder) -> depthai.NNData

        5. addTensor(self: depthai.NNData, name: str, tensor: numpy.ndarray[numpy.float32], storageOrder: depthai.TensorInfo.StorageOrder) -> depthai.NNData

        6. addTensor(self: depthai.NNData, name: str, tensor: numpy.ndarray[numpy.float64], storageOrder: depthai.TensorInfo.StorageOrder) -> depthai.NNData

        7. addTensor(self: depthai.NNData, name: str, tensor: object, dataType: depthai.TensorInfo.DataType) -> None

        Add a tensor to this NNData object. The provided array is stored as a 1xN tensor
        where N is the length of the array.

        Parameter ``name:``:
            Name of the tensor

        Parameter ``data:``:
            array

        Returns:
            NNData&: reference to this object

        8. addTensor(self: depthai.NNData, name: str, tensor: object) -> None
        """
    @overload
    def addTensor(self, name: str, tensor: object, dataType: TensorInfo.DataType) -> None:
        """addTensor(*args, **kwargs)
        Overloaded function.

        1. addTensor(self: depthai.NNData, name: str, tensor: list[int], storageOrder: depthai.TensorInfo.StorageOrder) -> depthai.NNData

        Add a tensor to this NNData object. The provided array is stored as a 1xN tensor
        where N is the length of the array.

        Parameter ``name:``:
            Name of the tensor

        Parameter ``data:``:
            array

        Returns:
            NNData&: reference to this object

        2. addTensor(self: depthai.NNData, name: str, tensor: list[float], storageOrder: depthai.TensorInfo.StorageOrder) -> depthai.NNData

        Add a tensor to this NNData object. The provided array is stored as a 1xN tensor
        where N is the length of the array.

        Parameter ``name:``:
            Name of the tensor

        Parameter ``data:``:
            array

        Returns:
            NNData&: reference to this object

        3. addTensor(self: depthai.NNData, name: str, tensor: list[float], storageOrder: depthai.TensorInfo.StorageOrder) -> depthai.NNData

        Add a tensor to this NNData object. The provided array is stored as a 1xN tensor
        where N is the length of the array.

        Parameter ``name:``:
            Name of the tensor

        Parameter ``data:``:
            array

        Returns:
            NNData&: reference to this object

        4. addTensor(self: depthai.NNData, name: str, tensor: numpy.ndarray[numpy.int32], storageOrder: depthai.TensorInfo.StorageOrder) -> depthai.NNData

        5. addTensor(self: depthai.NNData, name: str, tensor: numpy.ndarray[numpy.float32], storageOrder: depthai.TensorInfo.StorageOrder) -> depthai.NNData

        6. addTensor(self: depthai.NNData, name: str, tensor: numpy.ndarray[numpy.float64], storageOrder: depthai.TensorInfo.StorageOrder) -> depthai.NNData

        7. addTensor(self: depthai.NNData, name: str, tensor: object, dataType: depthai.TensorInfo.DataType) -> None

        Add a tensor to this NNData object. The provided array is stored as a 1xN tensor
        where N is the length of the array.

        Parameter ``name:``:
            Name of the tensor

        Parameter ``data:``:
            array

        Returns:
            NNData&: reference to this object

        8. addTensor(self: depthai.NNData, name: str, tensor: object) -> None
        """
    @overload
    def addTensor(self, name: str, tensor: object) -> None:
        """addTensor(*args, **kwargs)
        Overloaded function.

        1. addTensor(self: depthai.NNData, name: str, tensor: list[int], storageOrder: depthai.TensorInfo.StorageOrder) -> depthai.NNData

        Add a tensor to this NNData object. The provided array is stored as a 1xN tensor
        where N is the length of the array.

        Parameter ``name:``:
            Name of the tensor

        Parameter ``data:``:
            array

        Returns:
            NNData&: reference to this object

        2. addTensor(self: depthai.NNData, name: str, tensor: list[float], storageOrder: depthai.TensorInfo.StorageOrder) -> depthai.NNData

        Add a tensor to this NNData object. The provided array is stored as a 1xN tensor
        where N is the length of the array.

        Parameter ``name:``:
            Name of the tensor

        Parameter ``data:``:
            array

        Returns:
            NNData&: reference to this object

        3. addTensor(self: depthai.NNData, name: str, tensor: list[float], storageOrder: depthai.TensorInfo.StorageOrder) -> depthai.NNData

        Add a tensor to this NNData object. The provided array is stored as a 1xN tensor
        where N is the length of the array.

        Parameter ``name:``:
            Name of the tensor

        Parameter ``data:``:
            array

        Returns:
            NNData&: reference to this object

        4. addTensor(self: depthai.NNData, name: str, tensor: numpy.ndarray[numpy.int32], storageOrder: depthai.TensorInfo.StorageOrder) -> depthai.NNData

        5. addTensor(self: depthai.NNData, name: str, tensor: numpy.ndarray[numpy.float32], storageOrder: depthai.TensorInfo.StorageOrder) -> depthai.NNData

        6. addTensor(self: depthai.NNData, name: str, tensor: numpy.ndarray[numpy.float64], storageOrder: depthai.TensorInfo.StorageOrder) -> depthai.NNData

        7. addTensor(self: depthai.NNData, name: str, tensor: object, dataType: depthai.TensorInfo.DataType) -> None

        Add a tensor to this NNData object. The provided array is stored as a 1xN tensor
        where N is the length of the array.

        Parameter ``name:``:
            Name of the tensor

        Parameter ``data:``:
            array

        Returns:
            NNData&: reference to this object

        8. addTensor(self: depthai.NNData, name: str, tensor: object) -> None
        """
    def getAllLayerNames(self) -> list[str]:
        """getAllLayerNames(self: depthai.NNData) -> list[str]

        Returns:
            Names of all layers added
        """
    def getAllLayers(self) -> list[TensorInfo]:
        """getAllLayers(self: depthai.NNData) -> list[depthai.TensorInfo]

        Returns:
            All layers and their information
        """
    @overload
    def getFirstTensor(self, dequantize: bool = ...) -> object:
        """getFirstTensor(*args, **kwargs)
        Overloaded function.

        1. getFirstTensor(self: depthai.NNData, dequantize: bool = False) -> object

        Convenience function to retrieve values from the first tensor

        Returns:
            xt::xarray<_Ty> tensor

        2. getFirstTensor(self: depthai.NNData, storageOrder: depthai.TensorInfo.StorageOrder, dequantize: bool = False) -> object

        Convenience function to retrieve values from the first tensor

        Returns:
            xt::xarray<_Ty> tensor
        """
    @overload
    def getFirstTensor(self, storageOrder: TensorInfo.StorageOrder, dequantize: bool = ...) -> object:
        """getFirstTensor(*args, **kwargs)
        Overloaded function.

        1. getFirstTensor(self: depthai.NNData, dequantize: bool = False) -> object

        Convenience function to retrieve values from the first tensor

        Returns:
            xt::xarray<_Ty> tensor

        2. getFirstTensor(self: depthai.NNData, storageOrder: depthai.TensorInfo.StorageOrder, dequantize: bool = False) -> object

        Convenience function to retrieve values from the first tensor

        Returns:
            xt::xarray<_Ty> tensor
        """
    def getLayerDatatype(self, name: str, datatype: TensorInfo.DataType) -> bool:
        """getLayerDatatype(self: depthai.NNData, name: str, datatype: depthai.TensorInfo.DataType) -> bool

        Retrieve datatype of a layers tensor

        Parameter ``name``:
            Name of the layer

        Parameter ``datatype``:
            Datatype of layers tensor

        Returns:
            True if layer exists, false otherwise
        """
    def getSequenceNum(self) -> int:
        """getSequenceNum(self: depthai.NNData) -> int

        Retrieves image sequence number
        """
    @overload
    def getTensor(self, name: str, dequantize: bool = ...) -> object:
        """getTensor(*args, **kwargs)
        Overloaded function.

        1. getTensor(self: depthai.NNData, name: str, dequantize: bool = False) -> object

        Convenience function to retrieve values from a tensor

        Returns:
            xt::xarray<_Ty> tensor

        2. getTensor(self: depthai.NNData, name: str, storageOrder: depthai.TensorInfo.StorageOrder, dequantize: bool = False) -> object

        Convenience function to retrieve values from a tensor

        Returns:
            xt::xarray<_Ty> tensor
        """
    @overload
    def getTensor(self, name: str, storageOrder: TensorInfo.StorageOrder, dequantize: bool = ...) -> object:
        """getTensor(*args, **kwargs)
        Overloaded function.

        1. getTensor(self: depthai.NNData, name: str, dequantize: bool = False) -> object

        Convenience function to retrieve values from a tensor

        Returns:
            xt::xarray<_Ty> tensor

        2. getTensor(self: depthai.NNData, name: str, storageOrder: depthai.TensorInfo.StorageOrder, dequantize: bool = False) -> object

        Convenience function to retrieve values from a tensor

        Returns:
            xt::xarray<_Ty> tensor
        """
    def getTensorDatatype(self, name: str) -> TensorInfo.DataType:
        """getTensorDatatype(self: depthai.NNData, name: str) -> depthai.TensorInfo.DataType

        Get the datatype of a given tensor

        Returns:
            TensorInfo::DataType tensor datatype
        """
    def getTensorInfo(self, name: str) -> TensorInfo | None:
        """getTensorInfo(self: depthai.NNData, name: str) -> Optional[depthai.TensorInfo]

        Retrieve tensor information

        Parameter ``name``:
            Name of the tensor

        Returns:
            Tensor information
        """
    def getTimestamp(self) -> datetime.timedelta:
        """getTimestamp(self: depthai.NNData) -> datetime.timedelta

        Retrieves timestamp related to dai::Clock::now()
        """
    def getTimestampDevice(self) -> datetime.timedelta:
        """getTimestampDevice(self: depthai.NNData) -> datetime.timedelta

        Retrieves timestamp directly captured from device's monotonic clock, not
        synchronized to host time. Used mostly for debugging
        """
    def getTransformation(self) -> ImgTransformation | None:
        """getTransformation(self: depthai.NNData) -> Optional[depthai.ImgTransformation]"""
    def hasLayer(self, name: str) -> bool:
        """hasLayer(self: depthai.NNData, name: str) -> bool

        Checks if given layer exists

        Parameter ``name``:
            Name of the layer

        Returns:
            True if layer exists, false otherwise
        """
    def setSequenceNum(self, sequenceNum: int) -> None:
        """setSequenceNum(self: depthai.NNData, sequenceNum: int) -> None

        Sets image sequence number
        """
    def setTimestamp(self, timestamp: datetime.timedelta) -> None:
        """setTimestamp(self: depthai.NNData, timestamp: datetime.timedelta) -> None

        Sets image timestamp related to dai::Clock::now()
        """
    def setTimestampDevice(self, timestampDevice: datetime.timedelta) -> None:
        """setTimestampDevice(self: depthai.NNData, timestampDevice: datetime.timedelta) -> None

        Sets image timestamp related to dai::Clock::now()
        """
    def setTransformation(self, transformation: ImgTransformation | None) -> None:
        """setTransformation(self: depthai.NNData, transformation: Optional[depthai.ImgTransformation]) -> None

        Specifies image transformation data

        Parameter ``transformation``:
            transformation data
        """

class NNModelDescription:
    compressionLevel: str
    model: str
    modelPrecisionType: str
    optimizationLevel: str
    platform: str
    snpeVersion: str
    @overload
    def __init__(self) -> None:
        """__init__(*args, **kwargs)
        Overloaded function.

        1. __init__(self: depthai.NNModelDescription) -> None

        2. __init__(self: depthai.NNModelDescription, model: str, platform: str = '', optimizationLevel: str = '', compressionLevel: str = '', snpeVersion: str = '', modelPrecisionType: str = '') -> None

        3. __init__(self: depthai.NNModelDescription, model: str) -> None
        """
    @overload
    def __init__(self, model: str, platform: str = ..., optimizationLevel: str = ..., compressionLevel: str = ..., snpeVersion: str = ..., modelPrecisionType: str = ...) -> None:
        """__init__(*args, **kwargs)
        Overloaded function.

        1. __init__(self: depthai.NNModelDescription) -> None

        2. __init__(self: depthai.NNModelDescription, model: str, platform: str = '', optimizationLevel: str = '', compressionLevel: str = '', snpeVersion: str = '', modelPrecisionType: str = '') -> None

        3. __init__(self: depthai.NNModelDescription, model: str) -> None
        """
    @overload
    def __init__(self, model: str) -> None:
        """__init__(*args, **kwargs)
        Overloaded function.

        1. __init__(self: depthai.NNModelDescription) -> None

        2. __init__(self: depthai.NNModelDescription, model: str, platform: str = '', optimizationLevel: str = '', compressionLevel: str = '', snpeVersion: str = '', modelPrecisionType: str = '') -> None

        3. __init__(self: depthai.NNModelDescription, model: str) -> None
        """
    def check(self) -> bool:
        """check(self: depthai.NNModelDescription) -> bool

        Check if the model description is valid (contains all required fields)

        Returns:
            bool: True if the model description is valid, false otherwise
        """
    @staticmethod
    def fromYamlFile(yamlPath: os.PathLike, modelsPath: os.PathLike = ...) -> NNModelDescription:
        """fromYamlFile(yamlPath: os.PathLike, modelsPath: os.PathLike = '') -> depthai.NNModelDescription

        Initialize NNModelDescription from yaml file If modelName is a relative path
        (e.g. ./yolo.yaml), it is used as is. If modelName is a full path (e.g.
        /home/user/models/yolo.yaml), it is used as is. If modelName is a model name
        (e.g. yolo) or a model yaml file (e.g. yolo.yaml), the function will use
        modelsPath if provided or the DEPTHAI_ZOO_MODELS_PATH environment variable and
        use a path made by combining the modelsPath and the model name to the yaml file.
        For instance, yolo -> ./depthai_models/yolo.yaml (if modelsPath or
        DEPTHAI_ZOO_MODELS_PATH are ./depthai_models)

        Parameter ``modelName:``:
            model name or yaml file path (string is implicitly converted to Path)

        Parameter ``modelsPath:``:
            Path to the models folder, use environment variable DEPTHAI_ZOO_MODELS_PATH
            if not provided

        Returns:
            NNModelDescription
        """
    def saveToYamlFile(self, yamlPath: os.PathLike) -> None:
        """saveToYamlFile(self: depthai.NNModelDescription, yamlPath: os.PathLike) -> None

        Save NNModelDescription to yaml file

        Parameter ``yamlPath:``:
            Path to yaml file
        """
    def toString(self) -> str:
        """toString(self: depthai.NNModelDescription) -> str

        Convert NNModelDescription to string for printing purposes. This can be used for
        debugging.

        Returns:
            std::string: String representation
        """

class NeuralDepthConfig(Buffer):
    """NeuralDepthConfig message."""

    class PostProcessing:
        confidenceThreshold: int
        edgeThreshold: int
        temporalFilter: filters.params.TemporalFilter
        def __init__(self) -> None:
            """__init__(self: depthai.NeuralDepthConfig.PostProcessing) -> None"""
    postProcessing: NeuralDepthConfig.PostProcessing
    def __init__(self) -> None:
        """__init__(self: depthai.NeuralDepthConfig) -> None"""
    def getConfidenceThreshold(self) -> int:
        """getConfidenceThreshold(self: depthai.NeuralDepthConfig) -> int

        Get confidence threshold for disparity calculation
        """
    def getCustomDepthUnitMultiplier(self) -> float:
        """getCustomDepthUnitMultiplier(self: depthai.NeuralDepthConfig) -> float

        Get custom depth unit multiplier relative to 1 meter.
        """
    def getDepthUnit(self) -> LengthUnit:
        """getDepthUnit(self: depthai.NeuralDepthConfig) -> depthai.LengthUnit

        Get depth unit of depth map.
        """
    def getEdgeThreshold(self) -> int:
        """getEdgeThreshold(self: depthai.NeuralDepthConfig) -> int

        Get edge threshold for disparity calculation
        """
    def setConfidenceThreshold(self, confidenceThreshold: int) -> NeuralDepthConfig:
        """setConfidenceThreshold(self: depthai.NeuralDepthConfig, confidenceThreshold: int) -> depthai.NeuralDepthConfig

        Confidence threshold for disparity calculation

        Parameter ``confThr``:
            Confidence threshold value 0..255
        """
    def setCustomDepthUnitMultiplier(self, customDepthUnitMultiplier: float) -> NeuralDepthConfig:
        """setCustomDepthUnitMultiplier(self: depthai.NeuralDepthConfig, customDepthUnitMultiplier: float) -> depthai.NeuralDepthConfig

        Set custom depth unit multiplier relative to 1 meter.
        """
    def setDepthUnit(self, depthUnit: LengthUnit) -> NeuralDepthConfig:
        """setDepthUnit(self: depthai.NeuralDepthConfig, depthUnit: depthai.LengthUnit) -> depthai.NeuralDepthConfig

        Set depth unit of depth map.
        """
    def setEdgeThreshold(self, edgeThreshold: int) -> NeuralDepthConfig:
        """setEdgeThreshold(self: depthai.NeuralDepthConfig, edgeThreshold: int) -> depthai.NeuralDepthConfig

        Set edge threshold for disparity calculation

        Parameter ``edgeThr``:
            Edge threshold value 0..255
        """

class NeuralDepthProperties:
    """Specify properties for NeuralDepth"""
    def __init__(self, *args, **kwargs) -> None:
        """Initialize self.  See help(type(self)) for accurate signature."""

class NeuralNetworkProperties:
    """Specify properties for NeuralNetwork such as blob path, ..."""
    blobSize: int | None
    blobUri: str
    numFrames: int
    numNCEPerThread: int
    numThreads: int
    def __init__(self, *args, **kwargs) -> None:
        """Initialize self.  See help(type(self)) for accurate signature."""

class Node:
    """Abstract Node"""

    class Connection:
        """Connection between an Input and Output"""
        inputGroup: str
        inputId: int
        inputName: str
        outputGroup: str
        outputId: int
        outputName: str
        def __init__(self, *args, **kwargs) -> None:
            """Initialize self.  See help(type(self)) for accurate signature."""

    class DatatypeHierarchy:
        datatype: DatatypeEnum
        descendants: bool
        def __init__(self, arg0: DatatypeEnum, arg1: bool) -> None:
            """__init__(self: depthai.Node.DatatypeHierarchy, arg0: depthai.DatatypeEnum, arg1: bool) -> None"""

    class Id:
        """Node identificator. Unique for every node on a single Pipeline"""
        def __init__(self, *args, **kwargs) -> None:
            """Initialize self.  See help(type(self)) for accurate signature."""

    class Input(MessageQueue):
        class Type:
            """Members:

              SReceiver

              MReceiver"""
            __members__: ClassVar[dict] = ...  # read-only
            MReceiver: ClassVar[Node.Input.Type] = ...
            SReceiver: ClassVar[Node.Input.Type] = ...
            __entries: ClassVar[dict] = ...
            def __init__(self, value: int) -> None:
                """__init__(self: depthai.Node.Input.Type, value: int) -> None"""
            def __eq__(self, other: object) -> bool:
                """__eq__(self: object, other: object) -> bool"""
            def __hash__(self) -> int:
                """__hash__(self: object) -> int"""
            def __index__(self) -> int:
                """__index__(self: depthai.Node.Input.Type) -> int"""
            def __int__(self) -> int:
                """__int__(self: depthai.Node.Input.Type) -> int"""
            def __ne__(self, other: object) -> bool:
                """__ne__(self: object, other: object) -> bool"""
            @property
            def name(self) -> str:
                """name(self: object) -> str

                name(self: object) -> str
                """
            @property
            def value(self) -> int:
                """(arg0: depthai.Node.Input.Type) -> int"""
        possibleDatatypes: list[Node.DatatypeHierarchy]
        def __init__(self, parent: Node, name: str = ..., group: str = ..., blocking: bool = ..., queueSize: int = ..., types: list[Node.DatatypeHierarchy] = ..., waitForMessage: bool = ...) -> None:
            """__init__(self: depthai.Node.Input, parent: depthai.Node, name: str = '', group: str = '', blocking: bool = True, queueSize: int = 3, types: list[depthai.Node.DatatypeHierarchy] = [<depthai.Node.DatatypeHierarchy object at 0xff8e121578b0>], waitForMessage: bool = False) -> None"""
        def createInputQueue(self, maxSize: int = ..., blocking: bool = ...) -> InputQueue:
            """createInputQueue(self: depthai.Node.Input, maxSize: int = 16, blocking: bool = False) -> depthai.InputQueue

            Create an shared pointer to an input queue that can be used to send messages to
            this input from onhost

            Parameter ``maxSize:``:
                Maximum size of the input queue

            Parameter ``blocking:``:
                Whether the input queue should block when full

            Returns:
                std::shared_ptr<InputQueue>: shared pointer to an input queue
            """
        @overload
        def getParent(self) -> Node:
            """getParent(*args, **kwargs)
            Overloaded function.

            1. getParent(self: depthai.Node.Input) -> depthai.Node

            Get the parent node

            2. getParent(self: depthai.Node.Input) -> depthai.Node

            Get the parent node
            """
        @overload
        def getParent(self) -> Node:
            """getParent(*args, **kwargs)
            Overloaded function.

            1. getParent(self: depthai.Node.Input) -> depthai.Node

            Get the parent node

            2. getParent(self: depthai.Node.Input) -> depthai.Node

            Get the parent node
            """
        def getPossibleDatatypes(self) -> list[Node.DatatypeHierarchy]:
            """getPossibleDatatypes(self: depthai.Node.Input) -> list[depthai.Node.DatatypeHierarchy]

            Get possible datatypes that can be received
            """
        def getReusePreviousMessage(self) -> bool:
            """getReusePreviousMessage(self: depthai.Node.Input) -> bool

            Equivalent to getWaitForMessage but with inverted logic.
            """
        def getWaitForMessage(self) -> bool:
            """getWaitForMessage(self: depthai.Node.Input) -> bool

            Get behavior whether to wait for this input when a Node processes certain data
            or not

            Returns:
                Whether to wait for message to arrive to this input or not
            """
        def getXLinkBridge(self) -> node.internal.XLinkInBridge:
            """getXLinkBridge(self: depthai.Node.Input) -> depthai.node.internal.XLinkInBridge

            Get XLink bridge associated with this input (only valid for device inputs after
            pipeline build)

            Returns:
                std::shared_ptr<dai::node::internal::XLinkInBridge>: pointer to the XLink
                bridge or nullptr if not applicable
            """
        @overload
        def setPossibleDatatypes(self, types: list[Node.DatatypeHierarchy]) -> None:
            """setPossibleDatatypes(*args, **kwargs)
            Overloaded function.

            1. setPossibleDatatypes(self: depthai.Node.Input, types: list[depthai.Node.DatatypeHierarchy]) -> None

            Set possible datatypes that can be received

            2. setPossibleDatatypes(self: depthai.Node.Input, types: list[tuple[depthai.DatatypeEnum, bool]]) -> None

            Set possible datatypes that can be received
            """
        @overload
        def setPossibleDatatypes(self, types: list[tuple[DatatypeEnum, bool]]) -> None:
            """setPossibleDatatypes(*args, **kwargs)
            Overloaded function.

            1. setPossibleDatatypes(self: depthai.Node.Input, types: list[depthai.Node.DatatypeHierarchy]) -> None

            Set possible datatypes that can be received

            2. setPossibleDatatypes(self: depthai.Node.Input, types: list[tuple[depthai.DatatypeEnum, bool]]) -> None

            Set possible datatypes that can be received
            """
        def setReusePreviousMessage(self, reusePreviousMessage: bool) -> None:
            """setReusePreviousMessage(self: depthai.Node.Input, reusePreviousMessage: bool) -> None

            Equivalent to setWaitForMessage but with inverted logic.
            """
        def setWaitForMessage(self, waitForMessage: bool) -> None:
            """setWaitForMessage(self: depthai.Node.Input, waitForMessage: bool) -> None

            Overrides default wait for message behavior. Applicable for nodes with multiple
            inputs. Specifies behavior whether to wait for this input when a Node processes
            certain data or not.

            Parameter ``waitForMessage``:
                Whether to wait for message to arrive to this input or not
            """

    class InputMap:
        def __init__(self, *args, **kwargs) -> None:
            """Initialize self.  See help(type(self)) for accurate signature."""
        def items(self) -> Iterator[tuple[tuple[str, str], Node.Input]]:
            """items(self: depthai.Node.InputMap) -> Iterator[tuple[tuple[str, str], depthai.Node.Input]]"""
        def __bool__(self) -> bool:
            """__bool__(self: depthai.Node.InputMap) -> bool

            Check whether the map is nonempty
            """
        @overload
        def __contains__(self, arg0: str) -> bool:
            """__contains__(*args, **kwargs)
            Overloaded function.

            1. __contains__(self: depthai.Node.InputMap, arg0: str) -> bool

            2. __contains__(self: depthai.Node.InputMap, arg0: tuple[str, str]) -> bool
            """
        @overload
        def __contains__(self, arg0: tuple[str, str]) -> bool:
            """__contains__(*args, **kwargs)
            Overloaded function.

            1. __contains__(self: depthai.Node.InputMap, arg0: str) -> bool

            2. __contains__(self: depthai.Node.InputMap, arg0: tuple[str, str]) -> bool
            """
        @overload
        def __delitem__(self, arg0: str) -> None:
            """__delitem__(*args, **kwargs)
            Overloaded function.

            1. __delitem__(self: depthai.Node.InputMap, arg0: str) -> None

            2. __delitem__(self: depthai.Node.InputMap, arg0: tuple[str, str]) -> None
            """
        @overload
        def __delitem__(self, arg0: tuple[str, str]) -> None:
            """__delitem__(*args, **kwargs)
            Overloaded function.

            1. __delitem__(self: depthai.Node.InputMap, arg0: str) -> None

            2. __delitem__(self: depthai.Node.InputMap, arg0: tuple[str, str]) -> None
            """
        @overload
        def __getitem__(self, arg0: str) -> Node.Input:
            """__getitem__(*args, **kwargs)
            Overloaded function.

            1. __getitem__(self: depthai.Node.InputMap, arg0: str) -> depthai.Node.Input

            2. __getitem__(self: depthai.Node.InputMap, arg0: tuple[str, str]) -> depthai.Node.Input
            """
        @overload
        def __getitem__(self, arg0: tuple[str, str]) -> Node.Input:
            """__getitem__(*args, **kwargs)
            Overloaded function.

            1. __getitem__(self: depthai.Node.InputMap, arg0: str) -> depthai.Node.Input

            2. __getitem__(self: depthai.Node.InputMap, arg0: tuple[str, str]) -> depthai.Node.Input
            """
        def __iter__(self) -> Iterator[tuple[str, str]]:
            """__iter__(self: depthai.Node.InputMap) -> Iterator[tuple[str, str]]"""
        def __len__(self) -> int:
            """__len__(self: dict[tuple[str, str], depthai.Node.Input]) -> int"""
        def __setitem__(self, arg0: tuple[str, str], arg1: Node.Input) -> None:
            """__setitem__(self: depthai.Node.InputMap, arg0: tuple[str, str], arg1: depthai.Node.Input) -> None"""

    class Output:
        class Type:
            """Members:

              MSender

              SSender"""
            __members__: ClassVar[dict] = ...  # read-only
            MSender: ClassVar[Node.Output.Type] = ...
            SSender: ClassVar[Node.Output.Type] = ...
            __entries: ClassVar[dict] = ...
            def __init__(self, value: int) -> None:
                """__init__(self: depthai.Node.Output.Type, value: int) -> None"""
            def __eq__(self, other: object) -> bool:
                """__eq__(self: object, other: object) -> bool"""
            def __hash__(self) -> int:
                """__hash__(self: object) -> int"""
            def __index__(self) -> int:
                """__index__(self: depthai.Node.Output.Type) -> int"""
            def __int__(self) -> int:
                """__int__(self: depthai.Node.Output.Type) -> int"""
            def __ne__(self, other: object) -> bool:
                """__ne__(self: object, other: object) -> bool"""
            @property
            def name(self) -> str:
                """name(self: object) -> str

                name(self: object) -> str
                """
            @property
            def value(self) -> int:
                """(arg0: depthai.Node.Output.Type) -> int"""
        def __init__(self, parent: Node, name: str = ..., group: str = ..., possibleDatatypes: list[Node.DatatypeHierarchy] = ...) -> None:
            """__init__(self: depthai.Node.Output, parent: depthai.Node, name: str = '', group: str = '', possibleDatatypes: list[depthai.Node.DatatypeHierarchy] = [<depthai.Node.DatatypeHierarchy object at 0xff8e0c6e92b0>]) -> None"""
        def canConnect(self, input: Node.Input) -> bool:
            """canConnect(self: depthai.Node.Output, input: depthai.Node.Input) -> bool

            Check if connection is possible

            Parameter ``in``:
                Input to connect to

            Returns:
                True if connection is possible, false otherwise
            """
        def createOutputQueue(self, maxSize: int = ..., blocking: bool = ...) -> MessageQueue:
            """createOutputQueue(self: depthai.Node.Output, maxSize: int = 16, blocking: bool = False) -> depthai.MessageQueue

            Construct and return a shared pointer to an output message queue

            Parameter ``maxSize:``:
                Maximum size of the output queue

            Parameter ``blocking:``:
                Whether the output queue should block when full

            Returns:
                std::shared_ptr<dai::MessageQueue>: shared pointer to an output queue
            """
        def getName(self) -> str:
            """getName(self: depthai.Node.Output) -> str

            Get name of the output
            """
        @overload
        def getParent(self) -> Node:
            """getParent(*args, **kwargs)
            Overloaded function.

            1. getParent(self: depthai.Node.Output) -> depthai.Node

            2. getParent(self: depthai.Node.Output) -> depthai.Node
            """
        @overload
        def getParent(self) -> Node:
            """getParent(*args, **kwargs)
            Overloaded function.

            1. getParent(self: depthai.Node.Output) -> depthai.Node

            2. getParent(self: depthai.Node.Output) -> depthai.Node
            """
        def getPossibleDatatypes(self) -> list[Node.DatatypeHierarchy]:
            """getPossibleDatatypes(self: depthai.Node.Output) -> list[depthai.Node.DatatypeHierarchy]

            Get possible datatypes that can be sent
            """
        def getXLinkBridge(self) -> node.internal.XLinkOutBridge:
            """getXLinkBridge(self: depthai.Node.Output) -> depthai.node.internal.XLinkOutBridge

            Get XLink bridge associated with this output (only valid for device outputs
            after pipeline build)

            Returns:
                std::shared_ptr<dai::node::internal::XLinkOutBridge>: pointer to the XLink
                bridge or nullptr if not applicable
            """
        def isSamePipeline(self, input: Node.Input) -> bool:
            """isSamePipeline(self: depthai.Node.Output, input: depthai.Node.Input) -> bool

            Check if this output and given input are on the same pipeline.

            See also:
                canConnect for checking if connection is possible

            Returns:
                True if output and input are on the same pipeline
            """
        def link(self, input: Node.Input) -> None:
            """link(self: depthai.Node.Output, input: depthai.Node.Input) -> None"""
        def send(self, msg: ADatatype) -> None:
            """send(self: depthai.Node.Output, msg: depthai.ADatatype) -> None

            Sends a Message to all connected inputs

            Parameter ``msg``:
                Message to send to all connected inputs
            """
        @overload
        def setPossibleDatatypes(self, types: list[Node.DatatypeHierarchy]) -> None:
            """setPossibleDatatypes(*args, **kwargs)
            Overloaded function.

            1. setPossibleDatatypes(self: depthai.Node.Output, types: list[depthai.Node.DatatypeHierarchy]) -> None

            Set possible datatypes that can be sent

            2. setPossibleDatatypes(self: depthai.Node.Output, types: list[tuple[depthai.DatatypeEnum, bool]]) -> None

            Set possible datatypes that can be sent
            """
        @overload
        def setPossibleDatatypes(self, types: list[tuple[DatatypeEnum, bool]]) -> None:
            """setPossibleDatatypes(*args, **kwargs)
            Overloaded function.

            1. setPossibleDatatypes(self: depthai.Node.Output, types: list[depthai.Node.DatatypeHierarchy]) -> None

            Set possible datatypes that can be sent

            2. setPossibleDatatypes(self: depthai.Node.Output, types: list[tuple[depthai.DatatypeEnum, bool]]) -> None

            Set possible datatypes that can be sent
            """
        def trySend(self, msg: ADatatype) -> bool:
            """trySend(self: depthai.Node.Output, msg: depthai.ADatatype) -> bool

            Try sending a message to all connected inputs

            Parameter ``msg``:
                Message to send to all connected inputs

            Returns:
                True if ALL connected inputs got the message, false otherwise
            """
        def unlink(self, input: Node.Input) -> None:
            """unlink(self: depthai.Node.Output, input: depthai.Node.Input) -> None"""

    class OutputMap:
        def __init__(self, *args, **kwargs) -> None:
            """Initialize self.  See help(type(self)) for accurate signature."""
        def items(self) -> Iterator[tuple[tuple[str, str], Node.Output]]:
            """items(self: depthai.Node.OutputMap) -> Iterator[tuple[tuple[str, str], depthai.Node.Output]]"""
        def __bool__(self) -> bool:
            """__bool__(self: depthai.Node.OutputMap) -> bool

            Check whether the map is nonempty
            """
        @overload
        def __contains__(self, arg0: str) -> bool:
            """__contains__(*args, **kwargs)
            Overloaded function.

            1. __contains__(self: depthai.Node.OutputMap, arg0: str) -> bool

            2. __contains__(self: depthai.Node.OutputMap, arg0: tuple[str, str]) -> bool
            """
        @overload
        def __contains__(self, arg0: tuple[str, str]) -> bool:
            """__contains__(*args, **kwargs)
            Overloaded function.

            1. __contains__(self: depthai.Node.OutputMap, arg0: str) -> bool

            2. __contains__(self: depthai.Node.OutputMap, arg0: tuple[str, str]) -> bool
            """
        @overload
        def __delitem__(self, arg0: str) -> None:
            """__delitem__(*args, **kwargs)
            Overloaded function.

            1. __delitem__(self: depthai.Node.OutputMap, arg0: str) -> None

            2. __delitem__(self: depthai.Node.OutputMap, arg0: tuple[str, str]) -> None
            """
        @overload
        def __delitem__(self, arg0: tuple[str, str]) -> None:
            """__delitem__(*args, **kwargs)
            Overloaded function.

            1. __delitem__(self: depthai.Node.OutputMap, arg0: str) -> None

            2. __delitem__(self: depthai.Node.OutputMap, arg0: tuple[str, str]) -> None
            """
        @overload
        def __getitem__(self, arg0: str) -> Node.Output:
            """__getitem__(*args, **kwargs)
            Overloaded function.

            1. __getitem__(self: depthai.Node.OutputMap, arg0: str) -> depthai.Node.Output

            2. __getitem__(self: depthai.Node.OutputMap, arg0: tuple[str, str]) -> depthai.Node.Output
            """
        @overload
        def __getitem__(self, arg0: tuple[str, str]) -> Node.Output:
            """__getitem__(*args, **kwargs)
            Overloaded function.

            1. __getitem__(self: depthai.Node.OutputMap, arg0: str) -> depthai.Node.Output

            2. __getitem__(self: depthai.Node.OutputMap, arg0: tuple[str, str]) -> depthai.Node.Output
            """
        def __iter__(self) -> Iterator[tuple[str, str]]:
            """__iter__(self: depthai.Node.OutputMap) -> Iterator[tuple[str, str]]"""
        def __len__(self) -> int:
            """__len__(self: dict[tuple[str, str], depthai.Node.Output]) -> int"""
        def __setitem__(self, arg0: tuple[str, str], arg1: Node.Output) -> None:
            """__setitem__(self: depthai.Node.OutputMap, arg0: tuple[str, str], arg1: depthai.Node.Output) -> None"""
    def __init__(self, *args, **kwargs) -> None:
        """Initialize self.  See help(type(self)) for accurate signature."""
    def add(self, node: Node) -> None:
        """add(self: depthai.Node, node: depthai.Node) -> None

        Add existing node to nodeMap
        """
    @overload
    def getAssetManager(self) -> AssetManager:
        """getAssetManager(*args, **kwargs)
        Overloaded function.

        1. getAssetManager(self: depthai.Node) -> depthai.AssetManager

        Get node AssetManager as a const reference

        2. getAssetManager(self: depthai.Node) -> depthai.AssetManager

        Get node AssetManager as a const reference
        """
    @overload
    def getAssetManager(self) -> AssetManager:
        """getAssetManager(*args, **kwargs)
        Overloaded function.

        1. getAssetManager(self: depthai.Node) -> depthai.AssetManager

        Get node AssetManager as a const reference

        2. getAssetManager(self: depthai.Node) -> depthai.AssetManager

        Get node AssetManager as a const reference
        """
    def getInputMapRefs(self) -> list[Node.InputMap]:
        """getInputMapRefs(self: depthai.Node) -> list[depthai.Node.InputMap]

        Retrieves reference to node inputs
        """
    @overload
    def getInputRefs(self) -> list[Node.Input]:
        """getInputRefs(*args, **kwargs)
        Overloaded function.

        1. getInputRefs(self: depthai.Node) -> list[depthai.Node.Input]

        Retrieves reference to node inputs

        2. getInputRefs(self: depthai.Node) -> list[depthai.Node.Input]

        Retrieves reference to node inputs
        """
    @overload
    def getInputRefs(self) -> list[Node.Input]:
        """getInputRefs(*args, **kwargs)
        Overloaded function.

        1. getInputRefs(self: depthai.Node) -> list[depthai.Node.Input]

        Retrieves reference to node inputs

        2. getInputRefs(self: depthai.Node) -> list[depthai.Node.Input]

        Retrieves reference to node inputs
        """
    def getInputs(self) -> list[Node.Input]:
        """getInputs(self: depthai.Node) -> list[depthai.Node.Input]

        Retrieves all nodes inputs
        """
    def getName(self) -> str:
        """getName(self: depthai.Node) -> str

        Retrieves nodes name
        """
    def getOutputMapRefs(self) -> list[Node.OutputMap]:
        """getOutputMapRefs(self: depthai.Node) -> list[depthai.Node.OutputMap]

        Retrieves reference to node outputs
        """
    @overload
    def getOutputRefs(self) -> list[Node.Output]:
        """getOutputRefs(*args, **kwargs)
        Overloaded function.

        1. getOutputRefs(self: depthai.Node) -> list[depthai.Node.Output]

        Retrieves reference to node outputs

        2. getOutputRefs(self: depthai.Node) -> list[depthai.Node.Output]

        Retrieves reference to node outputs
        """
    @overload
    def getOutputRefs(self) -> list[Node.Output]:
        """getOutputRefs(*args, **kwargs)
        Overloaded function.

        1. getOutputRefs(self: depthai.Node) -> list[depthai.Node.Output]

        Retrieves reference to node outputs

        2. getOutputRefs(self: depthai.Node) -> list[depthai.Node.Output]

        Retrieves reference to node outputs
        """
    def getOutputs(self) -> list[Node.Output]:
        """getOutputs(self: depthai.Node) -> list[depthai.Node.Output]

        Retrieves all nodes outputs
        """
    @overload
    def getParentPipeline(self) -> Pipeline:
        """getParentPipeline(*args, **kwargs)
        Overloaded function.

        1. getParentPipeline(self: depthai.Node) -> depthai.Pipeline

        2. getParentPipeline(self: depthai.Node) -> depthai.Pipeline
        """
    @overload
    def getParentPipeline(self) -> Pipeline:
        """getParentPipeline(*args, **kwargs)
        Overloaded function.

        1. getParentPipeline(self: depthai.Node) -> depthai.Pipeline

        2. getParentPipeline(self: depthai.Node) -> depthai.Pipeline
        """
    def stopPipeline(self) -> None:
        """stopPipeline(self: depthai.Node) -> None"""
    @property
    def id(self) -> int:
        """Id of node. Assigned after being placed on the pipeline
        (self: depthai.Node) -> int
        """

class NodeGroup(Node):
    def __init__(self, *args, **kwargs) -> None:
        """Initialize self.  See help(type(self)) for accurate signature."""

class NodeState:
    class DurationEvent:
        durationUs: int
        startEvent: PipelineEvent
        def __init__(self) -> None:
            """__init__(self: depthai.NodeState.DurationEvent) -> None"""

    class DurationStats:
        averageMicrosRecent: int
        maxMicros: int
        maxMicrosRecent: int
        medianMicrosRecent: int
        minMicros: int
        minMicrosRecent: int
        stdDevMicrosRecent: int
        def __init__(self) -> None:
            """__init__(self: depthai.NodeState.DurationStats) -> None"""

    class InputQueueState:
        class State:
            """

            Members:

              IDLE

              WAITING

              BLOCKED"""
            __members__: ClassVar[dict] = ...  # read-only
            BLOCKED: ClassVar[NodeState.InputQueueState.State] = ...
            IDLE: ClassVar[NodeState.InputQueueState.State] = ...
            WAITING: ClassVar[NodeState.InputQueueState.State] = ...
            __entries: ClassVar[dict] = ...
            def __init__(self, value: int) -> None:
                """__init__(self: depthai.NodeState.InputQueueState.State, value: int) -> None"""
            def __eq__(self, other: object) -> bool:
                """__eq__(self: object, other: object) -> bool"""
            def __hash__(self) -> int:
                """__hash__(self: object) -> int"""
            def __index__(self) -> int:
                """__index__(self: depthai.NodeState.InputQueueState.State) -> int"""
            def __int__(self) -> int:
                """__int__(self: depthai.NodeState.InputQueueState.State) -> int"""
            def __ne__(self, other: object) -> bool:
                """__ne__(self: object, other: object) -> bool"""
            @property
            def name(self) -> str:
                """name(self: object) -> str

                name(self: object) -> str
                """
            @property
            def value(self) -> int:
                """(arg0: depthai.NodeState.InputQueueState.State) -> int"""
        numQueued: int
        queueStats: NodeState.QueueStats
        state: NodeState.InputQueueState.State
        timing: NodeState.Timing
        def __init__(self) -> None:
            """__init__(self: depthai.NodeState.InputQueueState) -> None"""
        def isValid(self) -> bool:
            """isValid(self: depthai.NodeState.InputQueueState) -> bool"""

    class OutputQueueState:
        class State:
            """

            Members:

              IDLE

              SENDING"""
            __members__: ClassVar[dict] = ...  # read-only
            IDLE: ClassVar[NodeState.OutputQueueState.State] = ...
            SENDING: ClassVar[NodeState.OutputQueueState.State] = ...
            __entries: ClassVar[dict] = ...
            def __init__(self, value: int) -> None:
                """__init__(self: depthai.NodeState.OutputQueueState.State, value: int) -> None"""
            def __eq__(self, other: object) -> bool:
                """__eq__(self: object, other: object) -> bool"""
            def __hash__(self) -> int:
                """__hash__(self: object) -> int"""
            def __index__(self) -> int:
                """__index__(self: depthai.NodeState.OutputQueueState.State) -> int"""
            def __int__(self) -> int:
                """__int__(self: depthai.NodeState.OutputQueueState.State) -> int"""
            def __ne__(self, other: object) -> bool:
                """__ne__(self: object, other: object) -> bool"""
            @property
            def name(self) -> str:
                """name(self: object) -> str

                name(self: object) -> str
                """
            @property
            def value(self) -> int:
                """(arg0: depthai.NodeState.OutputQueueState.State) -> int"""
        state: NodeState.OutputQueueState.State
        timing: NodeState.Timing
        def __init__(self) -> None:
            """__init__(self: depthai.NodeState.OutputQueueState) -> None"""
        def isValid(self) -> bool:
            """isValid(self: depthai.NodeState.OutputQueueState) -> bool"""

    class QueueStats:
        maxQueued: int
        maxQueuedRecent: int
        medianQueuedRecent: int
        minQueuedRecent: int
        def __init__(self) -> None:
            """__init__(self: depthai.NodeState.QueueStats) -> None"""

    class State:
        """

        Members:

          IDLE

          GETTING_INPUTS

          PROCESSING

          SENDING_OUTPUTS"""
        __members__: ClassVar[dict] = ...  # read-only
        GETTING_INPUTS: ClassVar[NodeState.State] = ...
        IDLE: ClassVar[NodeState.State] = ...
        PROCESSING: ClassVar[NodeState.State] = ...
        SENDING_OUTPUTS: ClassVar[NodeState.State] = ...
        __entries: ClassVar[dict] = ...
        def __init__(self, value: int) -> None:
            """__init__(self: depthai.NodeState.State, value: int) -> None"""
        def __eq__(self, other: object) -> bool:
            """__eq__(self: object, other: object) -> bool"""
        def __hash__(self) -> int:
            """__hash__(self: object) -> int"""
        def __index__(self) -> int:
            """__index__(self: depthai.NodeState.State) -> int"""
        def __int__(self) -> int:
            """__int__(self: depthai.NodeState.State) -> int"""
        def __ne__(self, other: object) -> bool:
            """__ne__(self: object, other: object) -> bool"""
        @property
        def name(self) -> str:
            """name(self: object) -> str

            name(self: object) -> str
            """
        @property
        def value(self) -> int:
            """(arg0: depthai.NodeState.State) -> int"""

    class Timing:
        durationStats: NodeState.DurationStats
        fps: float
        def __init__(self) -> None:
            """__init__(self: depthai.NodeState.Timing) -> None"""
        def isValid(self) -> bool:
            """isValid(self: depthai.NodeState.Timing) -> bool"""
    events: list[NodeState.DurationEvent]
    inputStates: dict[str, NodeState.InputQueueState]
    inputsGetTiming: NodeState.Timing
    mainLoopTiming: NodeState.Timing
    otherTimings: dict[str, NodeState.Timing]
    outputStates: dict[str, NodeState.OutputQueueState]
    outputsSendTiming: NodeState.Timing
    state: NodeState.State
    def __init__(self) -> None:
        """__init__(self: depthai.NodeState) -> None"""

class NodeStateApi:
    def __init__(self, *args, **kwargs) -> None:
        """Initialize self.  See help(type(self)) for accurate signature."""
    def detailed(self) -> NodeState:
        """detailed(self: depthai.NodeStateApi) -> depthai.NodeState"""
    @overload
    def inputs(self) -> dict[str, NodeState.InputQueueState]:
        """inputs(*args, **kwargs)
        Overloaded function.

        1. inputs(self: depthai.NodeStateApi) -> dict[str, depthai.NodeState.InputQueueState]

        2. inputs(self: depthai.NodeStateApi, inputNames: list[str]) -> dict[str, depthai.NodeState.InputQueueState]

        3. inputs(self: depthai.NodeStateApi, inputName: str) -> depthai.NodeState.InputQueueState
        """
    @overload
    def inputs(self, inputNames: list[str]) -> dict[str, NodeState.InputQueueState]:
        """inputs(*args, **kwargs)
        Overloaded function.

        1. inputs(self: depthai.NodeStateApi) -> dict[str, depthai.NodeState.InputQueueState]

        2. inputs(self: depthai.NodeStateApi, inputNames: list[str]) -> dict[str, depthai.NodeState.InputQueueState]

        3. inputs(self: depthai.NodeStateApi, inputName: str) -> depthai.NodeState.InputQueueState
        """
    @overload
    def inputs(self, inputName: str) -> NodeState.InputQueueState:
        """inputs(*args, **kwargs)
        Overloaded function.

        1. inputs(self: depthai.NodeStateApi) -> dict[str, depthai.NodeState.InputQueueState]

        2. inputs(self: depthai.NodeStateApi, inputNames: list[str]) -> dict[str, depthai.NodeState.InputQueueState]

        3. inputs(self: depthai.NodeStateApi, inputName: str) -> depthai.NodeState.InputQueueState
        """
    @overload
    def otherTimings(self) -> dict[str, NodeState.Timing]:
        """otherTimings(*args, **kwargs)
        Overloaded function.

        1. otherTimings(self: depthai.NodeStateApi) -> dict[str, depthai.NodeState.Timing]

        2. otherTimings(self: depthai.NodeStateApi, timingNames: list[str]) -> dict[str, depthai.NodeState.Timing]

        3. otherTimings(self: depthai.NodeStateApi, timingName: str) -> depthai.NodeState.Timing
        """
    @overload
    def otherTimings(self, timingNames: list[str]) -> dict[str, NodeState.Timing]:
        """otherTimings(*args, **kwargs)
        Overloaded function.

        1. otherTimings(self: depthai.NodeStateApi) -> dict[str, depthai.NodeState.Timing]

        2. otherTimings(self: depthai.NodeStateApi, timingNames: list[str]) -> dict[str, depthai.NodeState.Timing]

        3. otherTimings(self: depthai.NodeStateApi, timingName: str) -> depthai.NodeState.Timing
        """
    @overload
    def otherTimings(self, timingName: str) -> NodeState.Timing:
        """otherTimings(*args, **kwargs)
        Overloaded function.

        1. otherTimings(self: depthai.NodeStateApi) -> dict[str, depthai.NodeState.Timing]

        2. otherTimings(self: depthai.NodeStateApi, timingNames: list[str]) -> dict[str, depthai.NodeState.Timing]

        3. otherTimings(self: depthai.NodeStateApi, timingName: str) -> depthai.NodeState.Timing
        """
    @overload
    def outputs(self) -> dict[str, NodeState.OutputQueueState]:
        """outputs(*args, **kwargs)
        Overloaded function.

        1. outputs(self: depthai.NodeStateApi) -> dict[str, depthai.NodeState.OutputQueueState]

        2. outputs(self: depthai.NodeStateApi, outputNames: list[str]) -> dict[str, depthai.NodeState.OutputQueueState]

        3. outputs(self: depthai.NodeStateApi, outputName: str) -> depthai.NodeState.OutputQueueState
        """
    @overload
    def outputs(self, outputNames: list[str]) -> dict[str, NodeState.OutputQueueState]:
        """outputs(*args, **kwargs)
        Overloaded function.

        1. outputs(self: depthai.NodeStateApi) -> dict[str, depthai.NodeState.OutputQueueState]

        2. outputs(self: depthai.NodeStateApi, outputNames: list[str]) -> dict[str, depthai.NodeState.OutputQueueState]

        3. outputs(self: depthai.NodeStateApi, outputName: str) -> depthai.NodeState.OutputQueueState
        """
    @overload
    def outputs(self, outputName: str) -> NodeState.OutputQueueState:
        """outputs(*args, **kwargs)
        Overloaded function.

        1. outputs(self: depthai.NodeStateApi) -> dict[str, depthai.NodeState.OutputQueueState]

        2. outputs(self: depthai.NodeStateApi, outputNames: list[str]) -> dict[str, depthai.NodeState.OutputQueueState]

        3. outputs(self: depthai.NodeStateApi, outputName: str) -> depthai.NodeState.OutputQueueState
        """
    def summary(self) -> NodeState:
        """summary(self: depthai.NodeStateApi) -> depthai.NodeState"""

class NodesStateApi:
    """pipeline.getState().nodes({nodeId1}).summary() ->
    std::unordered_map<std::string, TimingStats>;
    pipeline.getState().nodes({nodeId1}).detailed() ->
    std::unordered_map<std::string, NodeState>;
    pipeline.getState().nodes(nodeId1).detailed() -> NodeState;
    pipeline.getState().nodes({nodeId1}).outputs() ->
    std::unordered_map<std::string, TimingStats>;
    pipeline.getState().nodes({nodeId1}).outputs({outputName1}) ->
    std::unordered_map<std::string, TimingStats>;
    pipeline.getState().nodes({nodeId1}).outputs(outputName) -> TimingStats;
    pipeline.getState().nodes({nodeId1}).events();
    pipeline.getState().nodes({nodeId1}).inputs() -> std::unordered_map<std::string,
    QueueState>; pipeline.getState().nodes({nodeId1}).inputs({inputName1}) ->
    std::unordered_map<std::string, QueueState>;
    pipeline.getState().nodes({nodeId1}).inputs(inputName) -> QueueState;
    pipeline.getState().nodes({nodeId1}).otherStats() ->
    std::unordered_map<std::string, TimingStats>;
    pipeline.getState().nodes({nodeId1}).otherStats({statName1}) ->
    std::unordered_map<std::string, TimingStats>;
    pipeline.getState().nodes({nodeId1}).outputs(statName) -> TimingStats;"""
    def __init__(self, *args, **kwargs) -> None:
        """Initialize self.  See help(type(self)) for accurate signature."""
    def detailed(self) -> PipelineState:
        """detailed(self: depthai.NodesStateApi) -> depthai.PipelineState"""
    def inputs(self) -> dict[int, dict[str, NodeState.InputQueueState]]:
        """inputs(self: depthai.NodesStateApi) -> dict[int, dict[str, depthai.NodeState.InputQueueState]]"""
    def otherTimings(self) -> dict[int, dict[str, NodeState.Timing]]:
        """otherTimings(self: depthai.NodesStateApi) -> dict[int, dict[str, depthai.NodeState.Timing]]"""
    def outputs(self) -> dict[int, dict[str, NodeState.OutputQueueState]]:
        """outputs(self: depthai.NodesStateApi) -> dict[int, dict[str, depthai.NodeState.OutputQueueState]]"""
    def summary(self) -> PipelineState:
        """summary(self: depthai.NodesStateApi) -> depthai.PipelineState"""

class ObjectTrackerProperties:
    """Specify properties for ObjectTracker"""
    detectionLabelsToTrack: list[int]
    maxObjectsToTrack: int
    trackerIdAssignmentPolicy: TrackerIdAssignmentPolicy
    trackerThreshold: float
    trackerType: TrackerType
    def __init__(self, *args, **kwargs) -> None:
        """Initialize self.  See help(type(self)) for accurate signature."""

class OpenVINO:
    """Support for basic OpenVINO related actions like version identification of neural
    network blobs,..."""

    class Blob:
        """OpenVINO Blob"""
        data: Incomplete
        device: OpenVINO.Device
        networkInputs: dict[str, TensorInfo]
        networkOutputs: dict[str, TensorInfo]
        numShaves: int
        numSlices: int
        stageCount: int
        version: OpenVINO.Version
        @overload
        def __init__(self, arg0, std) -> None:
            """__init__(*args, **kwargs)
            Overloaded function.

            1. __init__(self: depthai.OpenVINO.Blob, arg0: std::vector<unsigned char, std::allocator<unsigned char> >) -> None

            Construct a new Blob from data in memory

            Parameter ``data``:
                In memory blob

            2. __init__(self: depthai.OpenVINO.Blob, arg0: os.PathLike) -> None

            Construct a new Blob by loading from a filesystem path

            Parameter ``path``:
                Filesystem path to the blob
            """
        @overload
        def __init__(self, arg0: os.PathLike) -> None:
            """__init__(*args, **kwargs)
            Overloaded function.

            1. __init__(self: depthai.OpenVINO.Blob, arg0: std::vector<unsigned char, std::allocator<unsigned char> >) -> None

            Construct a new Blob from data in memory

            Parameter ``data``:
                In memory blob

            2. __init__(self: depthai.OpenVINO.Blob, arg0: os.PathLike) -> None

            Construct a new Blob by loading from a filesystem path

            Parameter ``path``:
                Filesystem path to the blob
            """

    class Device:
        """

        Members:

          VPU

          VPUX"""
        __members__: ClassVar[dict] = ...  # read-only
        VPU: ClassVar[OpenVINO.Device] = ...
        VPUX: ClassVar[OpenVINO.Device] = ...
        __entries: ClassVar[dict] = ...
        def __init__(self, value: int) -> None:
            """__init__(self: depthai.OpenVINO.Device, value: int) -> None"""
        def __eq__(self, other: object) -> bool:
            """__eq__(self: object, other: object) -> bool"""
        def __hash__(self) -> int:
            """__hash__(self: object) -> int"""
        def __index__(self) -> int:
            """__index__(self: depthai.OpenVINO.Device) -> int"""
        def __int__(self) -> int:
            """__int__(self: depthai.OpenVINO.Device) -> int"""
        def __ne__(self, other: object) -> bool:
            """__ne__(self: object, other: object) -> bool"""
        @property
        def name(self) -> str:
            """name(self: object) -> str

            name(self: object) -> str
            """
        @property
        def value(self) -> int:
            """(arg0: depthai.OpenVINO.Device) -> int"""

    class SuperBlob:
        """A superblob is an efficient way of storing generated blobs for all different
        number of shaves."""
        NUMBER_OF_PATCHES: ClassVar[int] = ...  # read-only
        @overload
        def __init__(self, superblobBytes, std) -> None:
            """__init__(*args, **kwargs)
            Overloaded function.

            1. __init__(self: depthai.OpenVINO.SuperBlob, superblobBytes: std::vector<unsigned char, std::allocator<unsigned char> >) -> None

            Construct a new SuperBlob object

            Parameter ``data:``:
                In memory superblob data

            2. __init__(self: depthai.OpenVINO.SuperBlob, pathToSuperBlobFile: str) -> None

            Construct a new SuperBlob object

            Parameter ``data:``:
                In memory superblob data
            """
        @overload
        def __init__(self, pathToSuperBlobFile: str) -> None:
            """__init__(*args, **kwargs)
            Overloaded function.

            1. __init__(self: depthai.OpenVINO.SuperBlob, superblobBytes: std::vector<unsigned char, std::allocator<unsigned char> >) -> None

            Construct a new SuperBlob object

            Parameter ``data:``:
                In memory superblob data

            2. __init__(self: depthai.OpenVINO.SuperBlob, pathToSuperBlobFile: str) -> None

            Construct a new SuperBlob object

            Parameter ``data:``:
                In memory superblob data
            """
        def getBlobWithNumShaves(self, numShaves: int) -> OpenVINO.Blob:
            """getBlobWithNumShaves(self: depthai.OpenVINO.SuperBlob, numShaves: int) -> depthai.OpenVINO.Blob

            Generate a blob with a specific number of shaves

            Parameter ``numShaves:``:
                Number of shaves to generate the blob for. Must be between 1 and
                NUMBER_OF_PATCHES.

            Returns:
                dai::OpenVINO::Blob: Blob compiled for the specified number of shaves
            """

    class Version:
        """OpenVINO Version supported version information

        Members:

          VERSION_2020_3

          VERSION_2020_4

          VERSION_2021_1

          VERSION_2021_2

          VERSION_2021_3

          VERSION_2021_4

          VERSION_2022_1

          VERSION_UNIVERSAL"""
        __members__: ClassVar[dict] = ...  # read-only
        VERSION_2020_3: ClassVar[OpenVINO.Version] = ...
        VERSION_2020_4: ClassVar[OpenVINO.Version] = ...
        VERSION_2021_1: ClassVar[OpenVINO.Version] = ...
        VERSION_2021_2: ClassVar[OpenVINO.Version] = ...
        VERSION_2021_3: ClassVar[OpenVINO.Version] = ...
        VERSION_2021_4: ClassVar[OpenVINO.Version] = ...
        VERSION_2022_1: ClassVar[OpenVINO.Version] = ...
        VERSION_UNIVERSAL: ClassVar[OpenVINO.Version] = ...
        __entries: ClassVar[dict] = ...
        def __init__(self, value: int) -> None:
            """__init__(self: depthai.OpenVINO.Version, value: int) -> None"""
        def __eq__(self, other: object) -> bool:
            """__eq__(self: object, other: object) -> bool"""
        def __hash__(self) -> int:
            """__hash__(self: object) -> int"""
        def __index__(self) -> int:
            """__index__(self: depthai.OpenVINO.Version) -> int"""
        def __int__(self) -> int:
            """__int__(self: depthai.OpenVINO.Version) -> int"""
        def __ne__(self, other: object) -> bool:
            """__ne__(self: object, other: object) -> bool"""
        @property
        def name(self) -> str:
            """name(self: object) -> str

            name(self: object) -> str
            """
        @property
        def value(self) -> int:
            """(arg0: depthai.OpenVINO.Version) -> int"""
    DEFAULT_VERSION: ClassVar[OpenVINO.Version] = ...
    VERSION_2020_3: ClassVar[OpenVINO.Version] = ...
    VERSION_2020_4: ClassVar[OpenVINO.Version] = ...
    VERSION_2021_1: ClassVar[OpenVINO.Version] = ...
    VERSION_2021_2: ClassVar[OpenVINO.Version] = ...
    VERSION_2021_3: ClassVar[OpenVINO.Version] = ...
    VERSION_2021_4: ClassVar[OpenVINO.Version] = ...
    VERSION_2022_1: ClassVar[OpenVINO.Version] = ...
    VERSION_UNIVERSAL: ClassVar[OpenVINO.Version] = ...
    def __init__(self, *args, **kwargs) -> None:
        """Initialize self.  See help(type(self)) for accurate signature."""
    @staticmethod
    def areVersionsBlobCompatible(v1: OpenVINO.Version, v2: OpenVINO.Version) -> bool:
        """areVersionsBlobCompatible(v1: depthai.OpenVINO.Version, v2: depthai.OpenVINO.Version) -> bool

        Checks whether two blob versions are compatible
        """
    @staticmethod
    def getBlobLatestSupportedVersion(*args, **kwargs):
        """getBlobLatestSupportedVersion(majorVersion: int, majorVersion: int) -> depthai.OpenVINO.Version

        Returns latest potentially supported version by a given blob version.

        Parameter ``majorVersion``:
            Major version from OpenVINO blob

        Parameter ``minorVersion``:
            Minor version from OpenVINO blob

        Returns:
            Latest potentially supported version
        """
    @staticmethod
    def getBlobSupportedVersions(*args, **kwargs):
        """getBlobSupportedVersions(majorVersion: int, majorVersion: int) -> list[depthai.OpenVINO.Version]

        Returns a list of potentially supported versions for a specified blob major and
        minor versions.

        Parameter ``majorVersion``:
            Major version from OpenVINO blob

        Parameter ``minorVersion``:
            Minor version from OpenVINO blob

        Returns:
            Vector of potentially supported versions
        """
    @staticmethod
    def getVersionName(version: OpenVINO.Version) -> str:
        """getVersionName(version: depthai.OpenVINO.Version) -> str

        Returns string representation of a given version

        Parameter ``version``:
            OpenVINO version

        Returns:
            Name of a given version
        """
    @staticmethod
    def getVersions() -> list[OpenVINO.Version]:
        """getVersions() -> list[depthai.OpenVINO.Version]

        Returns:
            Supported versions
        """
    @staticmethod
    def parseVersionName(versionString: str) -> OpenVINO.Version:
        """parseVersionName(versionString: str) -> depthai.OpenVINO.Version

        Creates Version from string representation. Throws if not possible.

        Parameter ``versionString``:
            Version as string

        Returns:
            Version object if successful
        """

class Pipeline:
    @overload
    def __init__(self, createImplicitDevice: bool = ...) -> None:
        """__init__(*args, **kwargs)
        Overloaded function.

        1. __init__(self: depthai.Pipeline, createImplicitDevice: bool = True) -> None

        Creates a pipeline

        Parameter ``createImplicitDevice``:
            If true, creates a default device (default = true)

        2. __init__(self: depthai.Pipeline, defaultDevice: depthai.Device) -> None

        Creates a pipeline

        Parameter ``createImplicitDevice``:
            If true, creates a default device (default = true)
        """
    @overload
    def __init__(self, defaultDevice: Device) -> None:
        """__init__(*args, **kwargs)
        Overloaded function.

        1. __init__(self: depthai.Pipeline, createImplicitDevice: bool = True) -> None

        Creates a pipeline

        Parameter ``createImplicitDevice``:
            If true, creates a default device (default = true)

        2. __init__(self: depthai.Pipeline, defaultDevice: depthai.Device) -> None

        Creates a pipeline

        Parameter ``createImplicitDevice``:
            If true, creates a default device (default = true)
        """
    def add(self, arg0: Node) -> Node:
        """add(self: depthai.Pipeline, arg0: depthai.Node) -> depthai.Node"""
    def build(self) -> None:
        """build(self: depthai.Pipeline) -> None"""
    def create(self, arg0: Type[T], *args, **kwargs) -> T:
        """create(self: depthai.Pipeline, arg0: object, *args, **kwargs) -> depthai.Node"""
    def enableHolisticRecord(self, recordConfig: RecordConfig) -> None:
        """enableHolisticRecord(self: depthai.Pipeline, recordConfig: depthai.RecordConfig) -> None

        Record and Replay
        """
    def enableHolisticReplay(self, recordingPath: str) -> None:
        """enableHolisticReplay(self: depthai.Pipeline, recordingPath: str) -> None"""
    def enablePipelineDebugging(self, enable: bool = ...) -> None:
        """enablePipelineDebugging(self: depthai.Pipeline, enable: bool = True) -> None

        Pipeline debugging
        """
    def getAllNodes(self) -> list[Node]:
        """getAllNodes(self: depthai.Pipeline) -> list[depthai.Node]

        Get a vector of all nodes
        """
    @overload
    def getAssetManager(self) -> AssetManager:
        """getAssetManager(*args, **kwargs)
        Overloaded function.

        1. getAssetManager(self: depthai.Pipeline) -> depthai.AssetManager

        Get pipelines AssetManager as reference

        2. getAssetManager(self: depthai.Pipeline) -> depthai.AssetManager

        Get pipelines AssetManager as reference
        """
    @overload
    def getAssetManager(self) -> AssetManager:
        """getAssetManager(*args, **kwargs)
        Overloaded function.

        1. getAssetManager(self: depthai.Pipeline) -> depthai.AssetManager

        Get pipelines AssetManager as reference

        2. getAssetManager(self: depthai.Pipeline) -> depthai.AssetManager

        Get pipelines AssetManager as reference
        """
    def getBoardConfig(self) -> BoardConfig:
        """getBoardConfig(self: depthai.Pipeline) -> depthai.BoardConfig

        Gets board configuration
        """
    def getCalibrationData(self) -> CalibrationHandler:
        """getCalibrationData(self: depthai.Pipeline) -> depthai.CalibrationHandler

        gets the calibration data which is set through pipeline

        Returns:
            the calibrationHandler with calib data in the pipeline
        """
    @overload
    def getDefaultDevice(self) -> Device:
        """getDefaultDevice(*args, **kwargs)
        Overloaded function.

        1. getDefaultDevice(self: depthai.Pipeline) -> depthai.Device

        2. getDefaultDevice(self: depthai.Pipeline) -> depthai.Device
        """
    @overload
    def getDefaultDevice(self) -> Device:
        """getDefaultDevice(*args, **kwargs)
        Overloaded function.

        1. getDefaultDevice(self: depthai.Pipeline) -> depthai.Device

        2. getDefaultDevice(self: depthai.Pipeline) -> depthai.Device
        """
    def getDeviceConfig(self) -> Device.Config:
        """getDeviceConfig(self: depthai.Pipeline) -> depthai.Device.Config

        Get device configuration needed for this pipeline
        """
    def getGlobalProperties(self) -> GlobalProperties:
        """getGlobalProperties(self: depthai.Pipeline) -> depthai.GlobalProperties

        Returns:
            Global properties of current pipeline
        """
    def getNode(self, arg0: int) -> Node:
        """getNode(self: depthai.Pipeline, arg0: int) -> depthai.Node

        Get node with id if it exists, nullptr otherwise
        """
    def getPipelineState(self) -> PipelineStateApi:
        """getPipelineState(self: depthai.Pipeline) -> depthai.PipelineStateApi"""
    def isBuilt(self) -> bool:
        """isBuilt(self: depthai.Pipeline) -> bool"""
    def isRunning(self) -> bool:
        """isRunning(self: depthai.Pipeline) -> bool"""
    def processTasks(self, waitForTasks: bool = ..., timeoutSeconds: float = ...) -> None:
        """processTasks(self: depthai.Pipeline, waitForTasks: bool = False, timeoutSeconds: float = -1.0) -> None"""
    def remove(self, node: Node) -> None:
        """remove(self: depthai.Pipeline, node: depthai.Node) -> None

        Removes a node from pipeline
        """
    def run(self) -> None:
        """run(self: depthai.Pipeline) -> None"""
    def serializeToJson(self, arg0: bool) -> json:
        """serializeToJson(self: depthai.Pipeline, arg0: bool) -> json

        Returns whole pipeline represented as JSON
        """
    def setBoardConfig(self, arg0: BoardConfig) -> None:
        """setBoardConfig(self: depthai.Pipeline, arg0: depthai.BoardConfig) -> None

        Sets board configuration
        """
    def setCalibrationData(self, calibrationDataHandler: CalibrationHandler) -> None:
        """setCalibrationData(self: depthai.Pipeline, calibrationDataHandler: depthai.CalibrationHandler) -> None

        Sets the calibration in pipeline which overrides the calibration data in eeprom

        Parameter ``calibrationDataHandler``:
            CalibrationHandler object which is loaded with calibration information.
        """
    @overload
    def setCameraTuningBlobPath(self, path: os.PathLike) -> None:
        """setCameraTuningBlobPath(*args, **kwargs)
        Overloaded function.

        1. setCameraTuningBlobPath(self: depthai.Pipeline, path: os.PathLike) -> None

        Set a camera IQ (Image Quality) tuning blob, used for all cameras

        2. setCameraTuningBlobPath(self: depthai.Pipeline, socket: depthai.CameraBoardSocket, path: os.PathLike) -> None

        Set a camera IQ (Image Quality) tuning blob, used for specific board socket
        """
    @overload
    def setCameraTuningBlobPath(self, socket: CameraBoardSocket, path: os.PathLike) -> None:
        """setCameraTuningBlobPath(*args, **kwargs)
        Overloaded function.

        1. setCameraTuningBlobPath(self: depthai.Pipeline, path: os.PathLike) -> None

        Set a camera IQ (Image Quality) tuning blob, used for all cameras

        2. setCameraTuningBlobPath(self: depthai.Pipeline, socket: depthai.CameraBoardSocket, path: os.PathLike) -> None

        Set a camera IQ (Image Quality) tuning blob, used for specific board socket
        """
    def setSippBufferSize(self, sizeBytes: int) -> None:
        """setSippBufferSize(self: depthai.Pipeline, sizeBytes: int) -> None

        SIPP (Signal Image Processing Pipeline) internal memory pool. SIPP is a
        framework used to schedule HW filters, e.g. ISP, Warp, Median filter etc.
        Changing the size of this pool is meant for advanced use cases, pushing the
        limits of the HW. By default memory is allocated in high speed CMX memory.
        Setting to 0 will allocate in DDR 256 kilobytes. Units are bytes.
        """
    def setSippDmaBufferSize(self, sizeBytes: int) -> None:
        """setSippDmaBufferSize(self: depthai.Pipeline, sizeBytes: int) -> None

        SIPP (Signal Image Processing Pipeline) internal DMA memory pool. SIPP is a
        framework used to schedule HW filters, e.g. ISP, Warp, Median filter etc.
        Changing the size of this pool is meant for advanced use cases, pushing the
        limits of the HW. Memory is allocated in high speed CMX memory Units are bytes.
        """
    def setXLinkChunkSize(self, sizeBytes: int) -> None:
        """setXLinkChunkSize(self: depthai.Pipeline, sizeBytes: int) -> None

        Set chunk size for splitting device-sent XLink packets, in bytes. A larger value
        could increase performance, with 0 disabling chunking. A negative value won't
        modify the device defaults - configured per protocol, currently 64*1024 for both
        USB and Ethernet.
        """
    def start(self) -> None:
        """start(self: depthai.Pipeline) -> None"""
    def stop(self) -> None:
        """stop(self: depthai.Pipeline) -> None"""
    def wait(self) -> None:
        """wait(self: depthai.Pipeline) -> None"""
    def __enter__(self) -> Pipeline:
        """__enter__(self: depthai.Pipeline) -> depthai.Pipeline"""
    def __exit__(self, arg0: object, arg1: object, arg2: object) -> None:
        """__exit__(self: depthai.Pipeline, arg0: object, arg1: object, arg2: object) -> None"""

class PipelineEvent(Buffer):
    """Pipeline event message."""

    class Interval:
        """

        Members:

          NONE

          START

          END"""
        __members__: ClassVar[dict] = ...  # read-only
        END: ClassVar[PipelineEvent.Interval] = ...
        NONE: ClassVar[PipelineEvent.Interval] = ...
        START: ClassVar[PipelineEvent.Interval] = ...
        __entries: ClassVar[dict] = ...
        def __init__(self, value: int) -> None:
            """__init__(self: depthai.PipelineEvent.Interval, value: int) -> None"""
        def __eq__(self, other: object) -> bool:
            """__eq__(self: object, other: object) -> bool"""
        def __hash__(self) -> int:
            """__hash__(self: object) -> int"""
        def __index__(self) -> int:
            """__index__(self: depthai.PipelineEvent.Interval) -> int"""
        def __int__(self) -> int:
            """__int__(self: depthai.PipelineEvent.Interval) -> int"""
        def __ne__(self, other: object) -> bool:
            """__ne__(self: object, other: object) -> bool"""
        @property
        def name(self) -> str:
            """name(self: object) -> str

            name(self: object) -> str
            """
        @property
        def value(self) -> int:
            """(arg0: depthai.PipelineEvent.Interval) -> int"""

    class Status:
        """

        Members:

          SUCCESS

          BLOCKED

          CANCELLED"""
        __members__: ClassVar[dict] = ...  # read-only
        BLOCKED: ClassVar[PipelineEvent.Status] = ...
        CANCELLED: ClassVar[PipelineEvent.Status] = ...
        SUCCESS: ClassVar[PipelineEvent.Status] = ...
        __entries: ClassVar[dict] = ...
        def __init__(self, value: int) -> None:
            """__init__(self: depthai.PipelineEvent.Status, value: int) -> None"""
        def __eq__(self, other: object) -> bool:
            """__eq__(self: object, other: object) -> bool"""
        def __hash__(self) -> int:
            """__hash__(self: object) -> int"""
        def __index__(self) -> int:
            """__index__(self: depthai.PipelineEvent.Status) -> int"""
        def __int__(self) -> int:
            """__int__(self: depthai.PipelineEvent.Status) -> int"""
        def __ne__(self, other: object) -> bool:
            """__ne__(self: object, other: object) -> bool"""
        @property
        def name(self) -> str:
            """name(self: object) -> str

            name(self: object) -> str
            """
        @property
        def value(self) -> int:
            """(arg0: depthai.PipelineEvent.Status) -> int"""

    class Type:
        """

        Members:

          CUSTOM

          LOOP

          INPUT

          OUTPUT

          INPUT_BLOCK

          OUTPUT_BLOCK"""
        __members__: ClassVar[dict] = ...  # read-only
        CUSTOM: ClassVar[PipelineEvent.Type] = ...
        INPUT: ClassVar[PipelineEvent.Type] = ...
        INPUT_BLOCK: ClassVar[PipelineEvent.Type] = ...
        LOOP: ClassVar[PipelineEvent.Type] = ...
        OUTPUT: ClassVar[PipelineEvent.Type] = ...
        OUTPUT_BLOCK: ClassVar[PipelineEvent.Type] = ...
        __entries: ClassVar[dict] = ...
        def __init__(self, value: int) -> None:
            """__init__(self: depthai.PipelineEvent.Type, value: int) -> None"""
        def __eq__(self, other: object) -> bool:
            """__eq__(self: object, other: object) -> bool"""
        def __hash__(self) -> int:
            """__hash__(self: object) -> int"""
        def __index__(self) -> int:
            """__index__(self: depthai.PipelineEvent.Type) -> int"""
        def __int__(self) -> int:
            """__int__(self: depthai.PipelineEvent.Type) -> int"""
        def __ne__(self, other: object) -> bool:
            """__ne__(self: object, other: object) -> bool"""
        @property
        def name(self) -> str:
            """name(self: object) -> str

            name(self: object) -> str
            """
        @property
        def value(self) -> int:
            """(arg0: depthai.PipelineEvent.Type) -> int"""
    interval: PipelineEvent.Interval
    nodeId: int
    queueSize: int | None
    source: str
    status: PipelineEvent.Status
    type: PipelineEvent.Type
    def __init__(self) -> None:
        """__init__(self: depthai.PipelineEvent) -> None"""
    def getSequenceNum(self) -> int:
        """getSequenceNum(self: depthai.PipelineEvent) -> int

        Retrieves image sequence number
        """
    def getTimestamp(self) -> datetime.timedelta:
        """getTimestamp(self: depthai.PipelineEvent) -> datetime.timedelta

        Retrieves timestamp related to dai::Clock::now()
        """
    def getTimestampDevice(self) -> datetime.timedelta:
        """getTimestampDevice(self: depthai.PipelineEvent) -> datetime.timedelta

        Retrieves timestamp directly captured from device's monotonic clock, not
        synchronized to host time. Used mostly for debugging
        """
    def setSequenceNum(self, sequenceNum: int) -> None:
        """setSequenceNum(self: depthai.PipelineEvent, sequenceNum: int) -> None

        Sets image sequence number
        """
    def setTimestamp(self, timestamp: datetime.timedelta) -> None:
        """setTimestamp(self: depthai.PipelineEvent, timestamp: datetime.timedelta) -> None

        Sets image timestamp related to dai::Clock::now()
        """
    def setTimestampDevice(self, timestampDevice: datetime.timedelta) -> None:
        """setTimestampDevice(self: depthai.PipelineEvent, timestampDevice: datetime.timedelta) -> None

        Sets image timestamp related to dai::Clock::now()
        """

class PipelineState(Buffer):
    """Pipeline event message."""
    nodeStates: dict[int, NodeState]
    def __init__(self) -> None:
        """__init__(self: depthai.PipelineState) -> None"""
    def getSequenceNum(self) -> int:
        """getSequenceNum(self: depthai.PipelineState) -> int

        Retrieves image sequence number
        """
    def getTimestamp(self) -> datetime.timedelta:
        """getTimestamp(self: depthai.PipelineState) -> datetime.timedelta

        Retrieves timestamp related to dai::Clock::now()
        """
    def getTimestampDevice(self) -> datetime.timedelta:
        """getTimestampDevice(self: depthai.PipelineState) -> datetime.timedelta

        Retrieves timestamp directly captured from device's monotonic clock, not
        synchronized to host time. Used mostly for debugging
        """
    def setSequenceNum(self, sequenceNum: int) -> None:
        """setSequenceNum(self: depthai.PipelineState, sequenceNum: int) -> None

        Sets image sequence number
        """
    def setTimestamp(self, timestamp: datetime.timedelta) -> None:
        """setTimestamp(self: depthai.PipelineState, timestamp: datetime.timedelta) -> None

        Sets image timestamp related to dai::Clock::now()
        """
    def setTimestampDevice(self, timestampDevice: datetime.timedelta) -> None:
        """setTimestampDevice(self: depthai.PipelineState, timestampDevice: datetime.timedelta) -> None

        Sets image timestamp related to dai::Clock::now()
        """

class PipelineStateApi:
    def __init__(self, *args, **kwargs) -> None:
        """Initialize self.  See help(type(self)) for accurate signature."""
    @overload
    def nodes(self) -> NodesStateApi:
        """nodes(*args, **kwargs)
        Overloaded function.

        1. nodes(self: depthai.PipelineStateApi) -> depthai.NodesStateApi

        2. nodes(self: depthai.PipelineStateApi, nodeIds: list[int]) -> depthai.NodesStateApi

        3. nodes(self: depthai.PipelineStateApi, nodeId: int) -> depthai.NodeStateApi
        """
    @overload
    def nodes(self, nodeIds: list[int]) -> NodesStateApi:
        """nodes(*args, **kwargs)
        Overloaded function.

        1. nodes(self: depthai.PipelineStateApi) -> depthai.NodesStateApi

        2. nodes(self: depthai.PipelineStateApi, nodeIds: list[int]) -> depthai.NodesStateApi

        3. nodes(self: depthai.PipelineStateApi, nodeId: int) -> depthai.NodeStateApi
        """
    @overload
    def nodes(self, nodeId: int) -> NodeStateApi:
        """nodes(*args, **kwargs)
        Overloaded function.

        1. nodes(self: depthai.PipelineStateApi) -> depthai.NodesStateApi

        2. nodes(self: depthai.PipelineStateApi, nodeIds: list[int]) -> depthai.NodesStateApi

        3. nodes(self: depthai.PipelineStateApi, nodeId: int) -> depthai.NodeStateApi
        """

class Platform:
    """Hardware platform type

    Members:

      RVC2 : 

      RVC3 : 

      RVC4 : """
    __members__: ClassVar[dict] = ...  # read-only
    RVC2: ClassVar[Platform] = ...
    RVC3: ClassVar[Platform] = ...
    RVC4: ClassVar[Platform] = ...
    __entries: ClassVar[dict] = ...
    def __init__(self, value: int) -> None:
        """__init__(self: depthai.Platform, value: int) -> None"""
    def __eq__(self, other: object) -> bool:
        """__eq__(self: object, other: object) -> bool"""
    def __hash__(self) -> int:
        """__hash__(self: object) -> int"""
    def __index__(self) -> int:
        """__index__(self: depthai.Platform) -> int"""
    def __int__(self) -> int:
        """__int__(self: depthai.Platform) -> int"""
    def __ne__(self, other: object) -> bool:
        """__ne__(self: object, other: object) -> bool"""
    @property
    def name(self) -> str:
        """name(self: object) -> str

        name(self: object) -> str
        """
    @property
    def value(self) -> int:
        """(arg0: depthai.Platform) -> int"""

class Point2f:
    """Point2f structure

    x and y coordinates that define a 2D point."""
    x: float
    y: float
    @overload
    def __init__(self) -> None:
        """__init__(*args, **kwargs)
        Overloaded function.

        1. __init__(self: depthai.Point2f) -> None

        2. __init__(self: depthai.Point2f, x: float, y: float) -> None

        3. __init__(self: depthai.Point2f, x: float, y: float, normalized: bool) -> None
        """
    @overload
    def __init__(self, x: float, y: float) -> None:
        """__init__(*args, **kwargs)
        Overloaded function.

        1. __init__(self: depthai.Point2f) -> None

        2. __init__(self: depthai.Point2f, x: float, y: float) -> None

        3. __init__(self: depthai.Point2f, x: float, y: float, normalized: bool) -> None
        """
    @overload
    def __init__(self, x: float, y: float, normalized: bool) -> None:
        """__init__(*args, **kwargs)
        Overloaded function.

        1. __init__(self: depthai.Point2f) -> None

        2. __init__(self: depthai.Point2f, x: float, y: float) -> None

        3. __init__(self: depthai.Point2f, x: float, y: float, normalized: bool) -> None
        """
    def isNormalized(self) -> bool:
        """isNormalized(self: depthai.Point2f) -> bool"""

class Point3d:
    """Point3d structure

    x,y,z coordinates that define a 3D point."""
    x: float
    y: float
    z: float
    @overload
    def __init__(self) -> None:
        """__init__(*args, **kwargs)
        Overloaded function.

        1. __init__(self: depthai.Point3d) -> None

        2. __init__(self: depthai.Point3d, x: float, y: float, z: float) -> None
        """
    @overload
    def __init__(self, x: float, y: float, z: float) -> None:
        """__init__(*args, **kwargs)
        Overloaded function.

        1. __init__(self: depthai.Point3d) -> None

        2. __init__(self: depthai.Point3d, x: float, y: float, z: float) -> None
        """

class Point3f:
    """Point3f structure

    x,y,z coordinates that define a 3D point."""
    x: float
    y: float
    z: float
    @overload
    def __init__(self) -> None:
        """__init__(*args, **kwargs)
        Overloaded function.

        1. __init__(self: depthai.Point3f) -> None

        2. __init__(self: depthai.Point3f, x: float, y: float, z: float) -> None
        """
    @overload
    def __init__(self, x: float, y: float, z: float) -> None:
        """__init__(*args, **kwargs)
        Overloaded function.

        1. __init__(self: depthai.Point3f) -> None

        2. __init__(self: depthai.Point3f, x: float, y: float, z: float) -> None
        """

class Point3fRGBA:
    """Point3fRGBA structure

    x,y,z coordinates and RGB color values that define a 3D point with color and
    alpha."""
    a: int
    b: int
    g: int
    r: int
    x: float
    y: float
    z: float
    @overload
    def __init__(self) -> None:
        """__init__(*args, **kwargs)
        Overloaded function.

        1. __init__(self: depthai.Point3fRGBA) -> None

        2. __init__(self: depthai.Point3fRGBA, x: float, y: float, z: float, r: int, g: int, b: int, a: int = 255) -> None
        """
    @overload
    def __init__(self, x: float, y: float, z: float, r: int, g: int, b: int, a: int = ...) -> None:
        """__init__(*args, **kwargs)
        Overloaded function.

        1. __init__(self: depthai.Point3fRGBA) -> None

        2. __init__(self: depthai.Point3fRGBA, x: float, y: float, z: float, r: int, g: int, b: int, a: int = 255) -> None
        """

class PointCloudConfig(Buffer):
    """PointCloudConfig message. Carries ROI (region of interest) and threshold for
    depth calculation"""
    def __init__(self) -> None:
        """__init__(self: depthai.PointCloudConfig) -> None"""
    def getSparse(self) -> bool:
        """getSparse(self: depthai.PointCloudConfig) -> bool

        Retrieve sparse point cloud calculation status.

        Returns:
            true if sparse point cloud calculation is enabled, false otherwise
        """
    def getTransformationMatrix(self, *args, **kwargs):
        """getTransformationMatrix(self: depthai.PointCloudConfig) -> Annotated[list[Annotated[list[float], FixedSize(4)]], FixedSize(4)]

        Retrieve transformation matrix for point cloud calculation.

        Returns:
            4x4 transformation matrix
        """
    def setSparse(self, arg0: bool) -> PointCloudConfig:
        """setSparse(self: depthai.PointCloudConfig, arg0: bool) -> depthai.PointCloudConfig

        Enable or disable sparse point cloud calculation.

        Parameter ``enable``:
        """
    @overload
    def setTransformationMatrix(self, arg0) -> PointCloudConfig:
        """setTransformationMatrix(*args, **kwargs)
        Overloaded function.

        1. setTransformationMatrix(self: depthai.PointCloudConfig, arg0: Annotated[list[Annotated[list[float], FixedSize(3)]], FixedSize(3)]) -> depthai.PointCloudConfig

        Set 4x4 transformation matrix for point cloud calculation. Default is an
        identity matrix.

        Parameter ``transformationMatrix``:

        2. setTransformationMatrix(self: depthai.PointCloudConfig, arg0: Annotated[list[Annotated[list[float], FixedSize(4)]], FixedSize(4)]) -> depthai.PointCloudConfig

        Set 4x4 transformation matrix for point cloud calculation. Default is an
        identity matrix.

        Parameter ``transformationMatrix``:
        """
    @overload
    def setTransformationMatrix(self, arg0) -> PointCloudConfig:
        """setTransformationMatrix(*args, **kwargs)
        Overloaded function.

        1. setTransformationMatrix(self: depthai.PointCloudConfig, arg0: Annotated[list[Annotated[list[float], FixedSize(3)]], FixedSize(3)]) -> depthai.PointCloudConfig

        Set 4x4 transformation matrix for point cloud calculation. Default is an
        identity matrix.

        Parameter ``transformationMatrix``:

        2. setTransformationMatrix(self: depthai.PointCloudConfig, arg0: Annotated[list[Annotated[list[float], FixedSize(4)]], FixedSize(4)]) -> depthai.PointCloudConfig

        Set 4x4 transformation matrix for point cloud calculation. Default is an
        identity matrix.

        Parameter ``transformationMatrix``:
        """

class PointCloudData(Buffer):
    """PointCloudData message. Carries point cloud data."""
    def __init__(self) -> None:
        """__init__(self: depthai.PointCloudData) -> None"""
    def getHeight(self) -> int:
        """getHeight(self: depthai.PointCloudData) -> int

        Retrieves the height in pixels - in case of a sparse point cloud, this
        represents the hight of the frame which was used to generate the point cloud
        """
    def getInstanceNum(self) -> int:
        """getInstanceNum(self: depthai.PointCloudData) -> int

        Retrieves instance number
        """
    def getMaxX(self) -> float:
        """getMaxX(self: depthai.PointCloudData) -> float

        Retrieves maximal x coordinate in depth units (millimeter by default)
        """
    def getMaxY(self) -> float:
        """getMaxY(self: depthai.PointCloudData) -> float

        Retrieves maximal y coordinate in depth units (millimeter by default)
        """
    def getMaxZ(self) -> float:
        """getMaxZ(self: depthai.PointCloudData) -> float

        Retrieves maximal z coordinate in depth units (millimeter by default)
        """
    def getMinX(self) -> float:
        """getMinX(self: depthai.PointCloudData) -> float

        Retrieves minimal x coordinate in depth units (millimeter by default)
        """
    def getMinY(self) -> float:
        """getMinY(self: depthai.PointCloudData) -> float

        Retrieves minimal y coordinate in depth units (millimeter by default)
        """
    def getMinZ(self) -> float:
        """getMinZ(self: depthai.PointCloudData) -> float

        Retrieves minimal z coordinate in depth units (millimeter by default)
        """
    def getPoints(self) -> numpy.ndarray[numpy.float32]:
        """getPoints(self: object) -> numpy.ndarray[numpy.float32]"""
    def getPointsRGB(self) -> tuple:
        """getPointsRGB(self: object) -> tuple"""
    def getSequenceNum(self) -> int:
        """getSequenceNum(self: depthai.PointCloudData) -> int

        Retrieves image sequence number
        """
    def getTimestamp(self) -> datetime.timedelta:
        """getTimestamp(self: depthai.PointCloudData) -> datetime.timedelta

        Retrieves timestamp related to dai::Clock::now()
        """
    def getTimestampDevice(self) -> datetime.timedelta:
        """getTimestampDevice(self: depthai.PointCloudData) -> datetime.timedelta

        Retrieves timestamp directly captured from device's monotonic clock, not
        synchronized to host time. Used mostly for debugging
        """
    def getWidth(self) -> int:
        """getWidth(self: depthai.PointCloudData) -> int

        Retrieves the height in pixels - in case of a sparse point cloud, this
        represents the hight of the frame which was used to generate the point cloud
        """
    def isColor(self) -> bool:
        """isColor(self: depthai.PointCloudData) -> bool

        Retrieves whether point cloud is color
        """
    def isSparse(self) -> bool:
        """isSparse(self: depthai.PointCloudData) -> bool

        Retrieves whether point cloud is sparse
        """
    def setHeight(self, arg0: int) -> PointCloudData:
        """setHeight(self: depthai.PointCloudData, arg0: int) -> depthai.PointCloudData

        Specifies frame height

        Parameter ``height``:
            frame height
        """
    def setInstanceNum(self, arg0: int) -> PointCloudData:
        """setInstanceNum(self: depthai.PointCloudData, arg0: int) -> depthai.PointCloudData

        Specifies instance number

        Parameter ``instanceNum``:
            instance number
        """
    def setMaxX(self, arg0: float) -> PointCloudData:
        """setMaxX(self: depthai.PointCloudData, arg0: float) -> depthai.PointCloudData

        Specifies maximal x coordinate in depth units (millimeter by default)

        Parameter ``val``:
            maximal x coordinate in depth units (millimeter by default)
        """
    def setMaxY(self, arg0: float) -> PointCloudData:
        """setMaxY(self: depthai.PointCloudData, arg0: float) -> depthai.PointCloudData

        Specifies maximal y coordinate in depth units (millimeter by default)

        Parameter ``val``:
            maximal y coordinate in depth units (millimeter by default)
        """
    def setMaxZ(self, arg0: float) -> PointCloudData:
        """setMaxZ(self: depthai.PointCloudData, arg0: float) -> depthai.PointCloudData

        Specifies maximal z coordinate in depth units (millimeter by default)

        Parameter ``val``:
            maximal z coordinate in depth units (millimeter by default)
        """
    def setMinX(self, arg0: float) -> PointCloudData:
        """setMinX(self: depthai.PointCloudData, arg0: float) -> depthai.PointCloudData

        Specifies minimal x coordinate in depth units (millimeter by default)

        Parameter ``val``:
            minimal x coordinate in depth units (millimeter by default)
        """
    def setMinY(self, arg0: float) -> PointCloudData:
        """setMinY(self: depthai.PointCloudData, arg0: float) -> depthai.PointCloudData

        Specifies minimal y coordinate in depth units (millimeter by default)

        Parameter ``val``:
            minimal y coordinate in depth units (millimeter by default)
        """
    def setMinZ(self, arg0: float) -> PointCloudData:
        """setMinZ(self: depthai.PointCloudData, arg0: float) -> depthai.PointCloudData

        Specifies minimal z coordinate in depth units (millimeter by default)

        Parameter ``val``:
            minimal z coordinate in depth units (millimeter by default)
        """
    def setPoints(self, arg0: numpy.ndarray[numpy.float32]) -> None:
        """setPoints(self: object, arg0: numpy.ndarray[numpy.float32]) -> None"""
    def setPointsRGB(self, arg0: numpy.ndarray[numpy.float32], arg1: numpy.ndarray[numpy.uint8]) -> None:
        """setPointsRGB(self: object, arg0: numpy.ndarray[numpy.float32], arg1: numpy.ndarray[numpy.uint8]) -> None"""
    @overload
    def setSize(self, width: int, height: int) -> PointCloudData:
        """setSize(*args, **kwargs)
        Overloaded function.

        1. setSize(self: depthai.PointCloudData, width: int, height: int) -> depthai.PointCloudData

        Specifies frame size

        Parameter ``height``:
            frame height

        Parameter ``width``:
            frame width

        2. setSize(self: depthai.PointCloudData, size: tuple[int, int]) -> depthai.PointCloudData

        Specifies frame size

        Parameter ``size``:
            frame size
        """
    @overload
    def setSize(self, size: tuple[int, int]) -> PointCloudData:
        """setSize(*args, **kwargs)
        Overloaded function.

        1. setSize(self: depthai.PointCloudData, width: int, height: int) -> depthai.PointCloudData

        Specifies frame size

        Parameter ``height``:
            frame height

        Parameter ``width``:
            frame width

        2. setSize(self: depthai.PointCloudData, size: tuple[int, int]) -> depthai.PointCloudData

        Specifies frame size

        Parameter ``size``:
            frame size
        """
    def setWidth(self, arg0: int) -> PointCloudData:
        """setWidth(self: depthai.PointCloudData, arg0: int) -> depthai.PointCloudData

        Specifies frame width

        Parameter ``width``:
            frame width
        """

class PointCloudProperties:
    """Specify properties for PointCloud"""
    initialConfig: PointCloudConfig
    numFramesPool: int
    def __init__(self, *args, **kwargs) -> None:
        """Initialize self.  See help(type(self)) for accurate signature."""

class PointsAnnotation:
    fillColor: Color
    outlineColor: Color
    outlineColors: VectorColor
    points: VectorPoint2f
    thickness: float
    type: PointsAnnotationType
    def __init__(self) -> None:
        """__init__(self: depthai.PointsAnnotation) -> None"""

class PointsAnnotationType:
    """

    Members:

      UNKNOWN

      POINTS

      LINE_LOOP

      LINE_STRIP

      LINE_LIST"""
    __members__: ClassVar[dict] = ...  # read-only
    LINE_LIST: ClassVar[PointsAnnotationType] = ...
    LINE_LOOP: ClassVar[PointsAnnotationType] = ...
    LINE_STRIP: ClassVar[PointsAnnotationType] = ...
    POINTS: ClassVar[PointsAnnotationType] = ...
    UNKNOWN: ClassVar[PointsAnnotationType] = ...
    __entries: ClassVar[dict] = ...
    def __init__(self, value: int) -> None:
        """__init__(self: depthai.PointsAnnotationType, value: int) -> None"""
    def __eq__(self, other: object) -> bool:
        """__eq__(self: object, other: object) -> bool"""
    def __hash__(self) -> int:
        """__hash__(self: object) -> int"""
    def __index__(self) -> int:
        """__index__(self: depthai.PointsAnnotationType) -> int"""
    def __int__(self) -> int:
        """__int__(self: depthai.PointsAnnotationType) -> int"""
    def __ne__(self, other: object) -> bool:
        """__ne__(self: object, other: object) -> bool"""
    @property
    def name(self) -> str:
        """name(self: object) -> str

        name(self: object) -> str
        """
    @property
    def value(self) -> int:
        """(arg0: depthai.PointsAnnotationType) -> int"""

class ProcessorType:
    """Members:

      LEON_CSS

      LEON_MSS

      CPU

      DSP"""
    __members__: ClassVar[dict] = ...  # read-only
    CPU: ClassVar[ProcessorType] = ...
    DSP: ClassVar[ProcessorType] = ...
    LEON_CSS: ClassVar[ProcessorType] = ...
    LEON_MSS: ClassVar[ProcessorType] = ...
    __entries: ClassVar[dict] = ...
    def __init__(self, value: int) -> None:
        """__init__(self: depthai.ProcessorType, value: int) -> None"""
    def __eq__(self, other: object) -> bool:
        """__eq__(self: object, other: object) -> bool"""
    def __hash__(self) -> int:
        """__hash__(self: object) -> int"""
    def __index__(self) -> int:
        """__index__(self: depthai.ProcessorType) -> int"""
    def __int__(self) -> int:
        """__int__(self: depthai.ProcessorType) -> int"""
    def __ne__(self, other: object) -> bool:
        """__ne__(self: object, other: object) -> bool"""
    @property
    def name(self) -> str:
        """name(self: object) -> str

        name(self: object) -> str
        """
    @property
    def value(self) -> int:
        """(arg0: depthai.ProcessorType) -> int"""

class ProfilingData:
    numBytesRead: int
    numBytesWritten: int
    def __init__(self, *args, **kwargs) -> None:
        """Initialize self.  See help(type(self)) for accurate signature."""

class Properties:
    """Base Properties structure"""
    def __init__(self, *args, **kwargs) -> None:
        """Initialize self.  See help(type(self)) for accurate signature."""

class Quaterniond:
    """Quaterniond structure

    qx,qy,qz,qw coordinates that define a 3D point orientation."""
    qw: float
    qx: float
    qy: float
    qz: float
    @overload
    def __init__(self) -> None:
        """__init__(*args, **kwargs)
        Overloaded function.

        1. __init__(self: depthai.Quaterniond) -> None

        2. __init__(self: depthai.Quaterniond, qx: float, qy: float, qz: float, qw: float) -> None
        """
    @overload
    def __init__(self, qx: float, qy: float, qz: float, qw: float) -> None:
        """__init__(*args, **kwargs)
        Overloaded function.

        1. __init__(self: depthai.Quaterniond) -> None

        2. __init__(self: depthai.Quaterniond, qx: float, qy: float, qz: float, qw: float) -> None
        """

class RGBDData(Buffer):
    """RGBD message. Carries RGB and Depth frames. Frames can be either of type
    ImgFrame or EncodedFrame."""
    def __init__(self) -> None:
        """__init__(self: depthai.RGBDData) -> None"""
    def getDepthFrame(self) -> ImgFrame | EncodedFrame | None:
        """getDepthFrame(self: depthai.RGBDData) -> Optional[Union[depthai.ImgFrame, depthai.EncodedFrame]]"""
    def getRGBFrame(self) -> ImgFrame | EncodedFrame | None:
        """getRGBFrame(self: depthai.RGBDData) -> Optional[Union[depthai.ImgFrame, depthai.EncodedFrame]]"""
    def setDepthFrame(self, frame: ImgFrame | EncodedFrame) -> None:
        """setDepthFrame(self: depthai.RGBDData, frame: Union[depthai.ImgFrame, depthai.EncodedFrame]) -> None"""
    def setRGBFrame(self, frame: ImgFrame | EncodedFrame) -> None:
        """setRGBFrame(self: depthai.RGBDData, frame: Union[depthai.ImgFrame, depthai.EncodedFrame]) -> None"""

class RecordConfig:
    """Configuration for recording and replaying messages"""

    class VideoEncoding:
        bitrate: int
        enabled: bool
        lossless: bool
        profile: VideoEncoderProperties.Profile
        quality: int
        def __init__(self) -> None:
            """__init__(self: depthai.RecordConfig.VideoEncoding) -> None"""
    compressionLevel: Incomplete
    outputDir: os.PathLike
    videoEncoding: RecordConfig.VideoEncoding
    def __init__(self) -> None:
        """__init__(self: depthai.RecordConfig) -> None"""

class Rect:
    """Rect structure

    x,y coordinates together with width and height that define a rectangle. Can be
    either normalized [0,1] or absolute representation."""
    height: float
    width: float
    x: float
    y: float
    @overload
    def __init__(self) -> None:
        """__init__(*args, **kwargs)
        Overloaded function.

        1. __init__(self: depthai.Rect) -> None

        2. __init__(self: depthai.Rect, x: float, y: float, width: float, height: float) -> None

        3. __init__(self: depthai.Rect, x: float, y: float, width: float, height: float, normalized: bool) -> None

        Construct Rectangle from (x, y) coordinates of the top-left corner, width and
        height.

        4. __init__(self: depthai.Rect, top_left: depthai.Point2f, size: depthai.Size2f) -> None

        Copy constructor

        5. __init__(self: depthai.Rect, top_left: depthai.Point2f, size: depthai.Size2f, normalized: bool) -> None

        Construct Rectangle from top left point and size

        6. __init__(self: depthai.Rect, point_1: depthai.Point2f, point_2: depthai.Point2f) -> None

        Construct Rectangle from top left point, size and normalization flag. @note: if
        normalized is true, the coordinates are in [0,1] range

        7. __init__(self: depthai.Rect, point_1: depthai.Point2f, point_2: depthai.Point2f, normalized: bool) -> None

        Construct Rectangle between any two given points. Constructor will determine
        top-left and bottom-right points.

        Parameter ``pt1``:
            First point

        Parameter ``pt2``:
            Second point
        """
    @overload
    def __init__(self, x: float, y: float, width: float, height: float) -> None:
        """__init__(*args, **kwargs)
        Overloaded function.

        1. __init__(self: depthai.Rect) -> None

        2. __init__(self: depthai.Rect, x: float, y: float, width: float, height: float) -> None

        3. __init__(self: depthai.Rect, x: float, y: float, width: float, height: float, normalized: bool) -> None

        Construct Rectangle from (x, y) coordinates of the top-left corner, width and
        height.

        4. __init__(self: depthai.Rect, top_left: depthai.Point2f, size: depthai.Size2f) -> None

        Copy constructor

        5. __init__(self: depthai.Rect, top_left: depthai.Point2f, size: depthai.Size2f, normalized: bool) -> None

        Construct Rectangle from top left point and size

        6. __init__(self: depthai.Rect, point_1: depthai.Point2f, point_2: depthai.Point2f) -> None

        Construct Rectangle from top left point, size and normalization flag. @note: if
        normalized is true, the coordinates are in [0,1] range

        7. __init__(self: depthai.Rect, point_1: depthai.Point2f, point_2: depthai.Point2f, normalized: bool) -> None

        Construct Rectangle between any two given points. Constructor will determine
        top-left and bottom-right points.

        Parameter ``pt1``:
            First point

        Parameter ``pt2``:
            Second point
        """
    @overload
    def __init__(self, x: float, y: float, width: float, height: float, normalized: bool) -> None:
        """__init__(*args, **kwargs)
        Overloaded function.

        1. __init__(self: depthai.Rect) -> None

        2. __init__(self: depthai.Rect, x: float, y: float, width: float, height: float) -> None

        3. __init__(self: depthai.Rect, x: float, y: float, width: float, height: float, normalized: bool) -> None

        Construct Rectangle from (x, y) coordinates of the top-left corner, width and
        height.

        4. __init__(self: depthai.Rect, top_left: depthai.Point2f, size: depthai.Size2f) -> None

        Copy constructor

        5. __init__(self: depthai.Rect, top_left: depthai.Point2f, size: depthai.Size2f, normalized: bool) -> None

        Construct Rectangle from top left point and size

        6. __init__(self: depthai.Rect, point_1: depthai.Point2f, point_2: depthai.Point2f) -> None

        Construct Rectangle from top left point, size and normalization flag. @note: if
        normalized is true, the coordinates are in [0,1] range

        7. __init__(self: depthai.Rect, point_1: depthai.Point2f, point_2: depthai.Point2f, normalized: bool) -> None

        Construct Rectangle between any two given points. Constructor will determine
        top-left and bottom-right points.

        Parameter ``pt1``:
            First point

        Parameter ``pt2``:
            Second point
        """
    @overload
    def __init__(self, top_left: Point2f, size: Size2f) -> None:
        """__init__(*args, **kwargs)
        Overloaded function.

        1. __init__(self: depthai.Rect) -> None

        2. __init__(self: depthai.Rect, x: float, y: float, width: float, height: float) -> None

        3. __init__(self: depthai.Rect, x: float, y: float, width: float, height: float, normalized: bool) -> None

        Construct Rectangle from (x, y) coordinates of the top-left corner, width and
        height.

        4. __init__(self: depthai.Rect, top_left: depthai.Point2f, size: depthai.Size2f) -> None

        Copy constructor

        5. __init__(self: depthai.Rect, top_left: depthai.Point2f, size: depthai.Size2f, normalized: bool) -> None

        Construct Rectangle from top left point and size

        6. __init__(self: depthai.Rect, point_1: depthai.Point2f, point_2: depthai.Point2f) -> None

        Construct Rectangle from top left point, size and normalization flag. @note: if
        normalized is true, the coordinates are in [0,1] range

        7. __init__(self: depthai.Rect, point_1: depthai.Point2f, point_2: depthai.Point2f, normalized: bool) -> None

        Construct Rectangle between any two given points. Constructor will determine
        top-left and bottom-right points.

        Parameter ``pt1``:
            First point

        Parameter ``pt2``:
            Second point
        """
    @overload
    def __init__(self, top_left: Point2f, size: Size2f, normalized: bool) -> None:
        """__init__(*args, **kwargs)
        Overloaded function.

        1. __init__(self: depthai.Rect) -> None

        2. __init__(self: depthai.Rect, x: float, y: float, width: float, height: float) -> None

        3. __init__(self: depthai.Rect, x: float, y: float, width: float, height: float, normalized: bool) -> None

        Construct Rectangle from (x, y) coordinates of the top-left corner, width and
        height.

        4. __init__(self: depthai.Rect, top_left: depthai.Point2f, size: depthai.Size2f) -> None

        Copy constructor

        5. __init__(self: depthai.Rect, top_left: depthai.Point2f, size: depthai.Size2f, normalized: bool) -> None

        Construct Rectangle from top left point and size

        6. __init__(self: depthai.Rect, point_1: depthai.Point2f, point_2: depthai.Point2f) -> None

        Construct Rectangle from top left point, size and normalization flag. @note: if
        normalized is true, the coordinates are in [0,1] range

        7. __init__(self: depthai.Rect, point_1: depthai.Point2f, point_2: depthai.Point2f, normalized: bool) -> None

        Construct Rectangle between any two given points. Constructor will determine
        top-left and bottom-right points.

        Parameter ``pt1``:
            First point

        Parameter ``pt2``:
            Second point
        """
    @overload
    def __init__(self, point_1: Point2f, point_2: Point2f) -> None:
        """__init__(*args, **kwargs)
        Overloaded function.

        1. __init__(self: depthai.Rect) -> None

        2. __init__(self: depthai.Rect, x: float, y: float, width: float, height: float) -> None

        3. __init__(self: depthai.Rect, x: float, y: float, width: float, height: float, normalized: bool) -> None

        Construct Rectangle from (x, y) coordinates of the top-left corner, width and
        height.

        4. __init__(self: depthai.Rect, top_left: depthai.Point2f, size: depthai.Size2f) -> None

        Copy constructor

        5. __init__(self: depthai.Rect, top_left: depthai.Point2f, size: depthai.Size2f, normalized: bool) -> None

        Construct Rectangle from top left point and size

        6. __init__(self: depthai.Rect, point_1: depthai.Point2f, point_2: depthai.Point2f) -> None

        Construct Rectangle from top left point, size and normalization flag. @note: if
        normalized is true, the coordinates are in [0,1] range

        7. __init__(self: depthai.Rect, point_1: depthai.Point2f, point_2: depthai.Point2f, normalized: bool) -> None

        Construct Rectangle between any two given points. Constructor will determine
        top-left and bottom-right points.

        Parameter ``pt1``:
            First point

        Parameter ``pt2``:
            Second point
        """
    @overload
    def __init__(self, point_1: Point2f, point_2: Point2f, normalized: bool) -> None:
        """__init__(*args, **kwargs)
        Overloaded function.

        1. __init__(self: depthai.Rect) -> None

        2. __init__(self: depthai.Rect, x: float, y: float, width: float, height: float) -> None

        3. __init__(self: depthai.Rect, x: float, y: float, width: float, height: float, normalized: bool) -> None

        Construct Rectangle from (x, y) coordinates of the top-left corner, width and
        height.

        4. __init__(self: depthai.Rect, top_left: depthai.Point2f, size: depthai.Size2f) -> None

        Copy constructor

        5. __init__(self: depthai.Rect, top_left: depthai.Point2f, size: depthai.Size2f, normalized: bool) -> None

        Construct Rectangle from top left point and size

        6. __init__(self: depthai.Rect, point_1: depthai.Point2f, point_2: depthai.Point2f) -> None

        Construct Rectangle from top left point, size and normalization flag. @note: if
        normalized is true, the coordinates are in [0,1] range

        7. __init__(self: depthai.Rect, point_1: depthai.Point2f, point_2: depthai.Point2f, normalized: bool) -> None

        Construct Rectangle between any two given points. Constructor will determine
        top-left and bottom-right points.

        Parameter ``pt1``:
            First point

        Parameter ``pt2``:
            Second point
        """
    def area(self) -> float:
        """area(self: depthai.Rect) -> float

        Area (width*height) of the rectangle
        """
    def bottomRight(self) -> Point2f:
        """bottomRight(self: depthai.Rect) -> depthai.Point2f

        The bottom-right corner
        """
    def contains(self, point: Point2f) -> bool:
        """contains(self: depthai.Rect, point: depthai.Point2f) -> bool

        Checks whether the rectangle contains the point.
        """
    def denormalize(self, width: int, height: int) -> Rect:
        """denormalize(self: depthai.Rect, width: int, height: int) -> depthai.Rect

        Denormalize rectangle.

        Parameter ``destWidth``:
            Destination frame width.

        Parameter ``destHeight``:
            Destination frame height.
        """
    def empty(self) -> bool:
        """empty(self: depthai.Rect) -> bool

        True if rectangle is empty.
        """
    def isNormalized(self) -> bool:
        """isNormalized(self: depthai.Rect) -> bool

        Whether rectangle is normalized (coordinates in [0,1] range) or not.
        """
    def normalize(self, width: int, height: int) -> Rect:
        """normalize(self: depthai.Rect, width: int, height: int) -> depthai.Rect

        Normalize rectangle.

        Parameter ``srcWidth``:
            Source frame width.

        Parameter ``srcHeight``:
            Source frame height.
        """
    def size(self) -> Size2f:
        """size(self: depthai.Rect) -> depthai.Size2f

        Size (width, height) of the rectangle
        """
    def topLeft(self) -> Point2f:
        """topLeft(self: depthai.Rect) -> depthai.Point2f

        The top-left corner.
        """

class RectificationProperties:
    outputHeight: int | None
    outputWidth: int | None
    def __init__(self, *args, **kwargs) -> None:
        """Initialize self.  See help(type(self)) for accurate signature."""

class RemoteConnection:
    def __init__(self, address: str = ..., webSocketPort: int = ..., serveFrontend: bool = ..., httpPort: int = ...) -> None:
        """__init__(self: depthai.RemoteConnection, address: str = '0.0.0.0', webSocketPort: int = 8765, serveFrontend: bool = True, httpPort: int = 8082) -> None

        Constructs a RemoteConnection instance.

        Parameter ``address``:
            The address to bind the connection to.

        Parameter ``webSocketPort``:
            The port for WebSocket communication.

        Parameter ``serveFrontend``:
            Whether to serve a frontend UI.

        Parameter ``httpPort``:
            The port for HTTP communication.
        """
    @overload
    def addTopic(self, topicName: str, output: Node.Output, group: str = ..., useVisualizationIfAvailable: bool = ...) -> None:
        """addTopic(*args, **kwargs)
        Overloaded function.

        1. addTopic(self: depthai.RemoteConnection, topicName: str, output: depthai.Node.Output, group: str = '', useVisualizationIfAvailable: bool = True) -> None

        Adds a topic to the remote connection.

        Parameter ``topicName``:
            The name of the topic.

        Parameter ``output``:
            The output to link to the topic.

        Parameter ``group``:
            An optional group name for the topic.

        Parameter ``useVisualizationIfAvailable``:
            Whether to enable visualization on the message if available or send message
            as is.

        2. addTopic(self: depthai.RemoteConnection, topicName: str, group: str = '', maxSize: int = 16, blocking: bool = False, useVisualizationIfAvailable: bool = True) -> depthai.MessageQueue

        Adds a topic to the remote connection.

        Parameter ``topicName``:
            The name of the topic.

        Parameter ``output``:
            The output to link to the topic.

        Parameter ``group``:
            An optional group name for the topic.

        Parameter ``useVisualizationIfAvailable``:
            Whether to enable visualization on the message if available or send message
            as is.
        """
    @overload
    def addTopic(self, topicName: str, group: str = ..., maxSize: int = ..., blocking: bool = ..., useVisualizationIfAvailable: bool = ...) -> MessageQueue:
        """addTopic(*args, **kwargs)
        Overloaded function.

        1. addTopic(self: depthai.RemoteConnection, topicName: str, output: depthai.Node.Output, group: str = '', useVisualizationIfAvailable: bool = True) -> None

        Adds a topic to the remote connection.

        Parameter ``topicName``:
            The name of the topic.

        Parameter ``output``:
            The output to link to the topic.

        Parameter ``group``:
            An optional group name for the topic.

        Parameter ``useVisualizationIfAvailable``:
            Whether to enable visualization on the message if available or send message
            as is.

        2. addTopic(self: depthai.RemoteConnection, topicName: str, group: str = '', maxSize: int = 16, blocking: bool = False, useVisualizationIfAvailable: bool = True) -> depthai.MessageQueue

        Adds a topic to the remote connection.

        Parameter ``topicName``:
            The name of the topic.

        Parameter ``output``:
            The output to link to the topic.

        Parameter ``group``:
            An optional group name for the topic.

        Parameter ``useVisualizationIfAvailable``:
            Whether to enable visualization on the message if available or send message
            as is.
        """
    def registerBinaryService(self, serviceName: str, callback: object) -> None:
        """registerBinaryService(self: depthai.RemoteConnection, serviceName: str, callback: object) -> None

        Registers a binary service with a callback function.

        Parameter ``serviceName``:
            The name of the service.

        Parameter ``callback``:
            The callback function to handle requests.
        """
    def registerPipeline(self, pipeline: Pipeline) -> None:
        """registerPipeline(self: depthai.RemoteConnection, pipeline: depthai.Pipeline) -> None

        Registers a pipeline with the remote connection.

        Parameter ``pipeline``:
            The pipeline to register.
        """
    def registerService(self, serviceName: str, callback: Callable[[json], json]) -> None:
        """registerService(self: depthai.RemoteConnection, serviceName: str, callback: Callable[[json], json]) -> None

        Registers a service with a callback function.

        Parameter ``serviceName``:
            The name of the service.

        Parameter ``callback``:
            The callback function to handle requests.
        """
    def removeTopic(self, topicName: str) -> bool:
        """removeTopic(self: depthai.RemoteConnection, topicName: str) -> bool

        Removes a topic from the remote connection.

        Parameter ``topicName``:
            The name of the topic to remove. @note After removing a topic any messages
            sent to it will cause an exception to be called on the sender, since this
            closes the queue.

        Returns:
            True if the topic was successfully removed, false otherwise.
        """
    def waitKey(self, delay: int) -> int:
        """waitKey(self: depthai.RemoteConnection, delay: int) -> int

        Waits for a key event.

        Parameter ``delayMs``:
            The delay in milliseconds to wait for a key press.

        Returns:
            The key code of the pressed key.
        """

class RotatedRect:
    """RotatedRect structure"""
    angle: float
    center: Point2f
    size: Size2f
    @overload
    def __init__(self) -> None:
        """__init__(*args, **kwargs)
        Overloaded function.

        1. __init__(self: depthai.RotatedRect) -> None

        2. __init__(self: depthai.RotatedRect, center: depthai.Point2f, size: depthai.Size2f, angle: float) -> None

        3. __init__(self: depthai.RotatedRect, rect: depthai.Rect, angle: float = 0.0) -> None
        """
    @overload
    def __init__(self, center: Point2f, size: Size2f, angle: float) -> None:
        """__init__(*args, **kwargs)
        Overloaded function.

        1. __init__(self: depthai.RotatedRect) -> None

        2. __init__(self: depthai.RotatedRect, center: depthai.Point2f, size: depthai.Size2f, angle: float) -> None

        3. __init__(self: depthai.RotatedRect, rect: depthai.Rect, angle: float = 0.0) -> None
        """
    @overload
    def __init__(self, rect: Rect, angle: float = ...) -> None:
        """__init__(*args, **kwargs)
        Overloaded function.

        1. __init__(self: depthai.RotatedRect) -> None

        2. __init__(self: depthai.RotatedRect, center: depthai.Point2f, size: depthai.Size2f, angle: float) -> None

        3. __init__(self: depthai.RotatedRect, rect: depthai.Rect, angle: float = 0.0) -> None
        """
    def denormalize(self, width: int, height: int, force: bool = ...) -> RotatedRect:
        """denormalize(self: depthai.RotatedRect, width: int, height: int, force: bool = False) -> depthai.RotatedRect

        Denormalize the rotated rectangle. The denormalized rectangle will have center
        and size coordinates in range [0, width] and [0, height]

        Returns:
            Denormalized rotated rectangle
        """
    def getOuterCXCYWH(self) -> tuple[Point2f, Size2f]:
        """getOuterCXCYWH(self: depthai.RotatedRect) -> tuple[depthai.Point2f, depthai.Size2f]

        Returns the outer non-rotated rectangle in the YOLO (xcenter, ycenter, width,
        height) format.

        Returns:
            (center point, size)
        """
    def getOuterRect(self, *args, **kwargs):
        """getOuterRect(self: depthai.RotatedRect) -> Annotated[list[float], FixedSize(4)]

        Returns the outer non-rotated rectangle

        Returns:
            [minx, miny, maxx, maxy]
        """
    def getOuterXYWH(self) -> tuple[Point2f, Size2f]:
        """getOuterXYWH(self: depthai.RotatedRect) -> tuple[depthai.Point2f, depthai.Size2f]

        Returns the outer non-rotated rectangle in the COCO (xmin, ymin, width, height)
        format.

        Returns:
            (top-left point, size)
        """
    def getPoints(self, *args, **kwargs):
        """getPoints(self: depthai.RotatedRect) -> Annotated[list[depthai.Point2f], FixedSize(4)]

        Get the 4 corner points of the rotated rectangle

        Returns:
            4 corner points
        """
    def isNormalized(self) -> bool:
        """isNormalized(self: depthai.RotatedRect) -> bool"""
    def normalize(self, width: int, height: int) -> RotatedRect:
        """normalize(self: depthai.RotatedRect, width: int, height: int) -> depthai.RotatedRect

        Normalize the rotated rectangle. The normalized rectangle will have center and
        size coordinates in range [0,1]

        Returns:
            Normalized rotated rectangle
        """

class SPIInProperties:
    """Properties for SPIIn node"""
    busId: int
    maxDataSize: int
    numFrames: int
    streamName: str
    def __init__(self, *args, **kwargs) -> None:
        """Initialize self.  See help(type(self)) for accurate signature."""

class SPIOutProperties:
    """Specify properties for SPIOut node"""
    busId: int
    streamName: str
    def __init__(self, *args, **kwargs) -> None:
        """Initialize self.  See help(type(self)) for accurate signature."""

class ScriptProperties:
    """Specify ScriptProperties options such as script uri, script name, ..."""
    processor: ProcessorType
    scriptName: str
    scriptUri: str
    def __init__(self, *args, **kwargs) -> None:
        """Initialize self.  See help(type(self)) for accurate signature."""

class SegmentationMask(Buffer):
    """SegmentationMask message.

    Segmentation mask of an image is stored as a single-channel UINT8 array, where
    each value represents a class or instance index. The value 255 is treated as
    background pixels (no class/instance)."""
    @overload
    def __init__(self) -> None:
        """__init__(*args, **kwargs)
        Overloaded function.

        1. __init__(self: depthai.SegmentationMask) -> None

        2. __init__(self: depthai.SegmentationMask, mask: std::vector<unsigned char, std::allocator<unsigned char> >, width: int, height: int) -> None
        """
    @overload
    def __init__(self, mask, std, width: int, height: int) -> None:
        """__init__(*args, **kwargs)
        Overloaded function.

        1. __init__(self: depthai.SegmentationMask) -> None

        2. __init__(self: depthai.SegmentationMask, mask: std::vector<unsigned char, std::allocator<unsigned char> >, width: int, height: int) -> None
        """
    def getArea(self, index: int) -> int | None:
        """getArea(self: depthai.SegmentationMask, index: int) -> Optional[int]

        Returns the area (number of pixels) of the specified instance/class index in the
        segmentation mask.

        Parameter ``index``:
            Instance/Class index @note If index is not present in the mask, returns
            std::nullopt.
        """
    def getBoundingBoxes(self, index: int, calculateRotation: bool = ...) -> list[RotatedRect]:
        """getBoundingBoxes(self: depthai.SegmentationMask, index: int, calculateRotation: bool = False) -> list[depthai.RotatedRect]

        Returns a bounding box for each continuous region with the specified index.

        Parameter ``index``:
            class index

        Parameter ``calculateRotation``:
            If true, returns rotated bounding boxes, otherwise returns the outer, axis-
            aligned bounding boxes.
        """
    def getCentroid(self, index: int) -> Point2f | None:
        """getCentroid(self: depthai.SegmentationMask, index: int) -> Optional[depthai.Point2f]

        Returns the normalized centroid (x,y) coordinates of the specified
        instance/class index in the segmentation mask.

        Parameter ``index``:
            Instance/Class index @note If index is not present in the mask, returns
            std::nullopt.
        """
    def getContour(self, index: int) -> list[VectorPoint2f]:
        """getContour(self: depthai.SegmentationMask, index: int) -> list[depthai.VectorPoint2f]

        Calls the opencv findContours function and filters the results based on the
        provided index. Returns filtered contour as a vector of vectors of non-
        normalized points. If mask data is not set, returns an empty vector.

        Parameter ``index``:
            class index
        """
    def getCvMask(self) -> numpy.ndarray:
        """getCvMask(self: depthai.SegmentationMask) -> numpy.ndarray

        Retrieves mask data as a cv::Mat copy with specified width and height. If mask
        data is not set, returns an empty matrix.

        Parameter ``allocator``:
            Allows callers to supply a custom cv::MatAllocator for zero-copy/custom
            memory management; nullptr uses OpenCV’s default.
        """
    def getCvMaskByIndex(self, index: int) -> numpy.ndarray:
        """getCvMaskByIndex(self: depthai.SegmentationMask, index: int) -> numpy.ndarray

        Returns a binary mask where pixels belonging to the instance index are set to 1,
        others to 0. If mask data is not set, returns an empty matrix.

        Parameter ``index``:
            Instance index

        Parameter ``allocator``:
            Allows callers to supply a custom cv::MatAllocator for zero-copy/custom
            memory management; nullptr uses OpenCV’s default.
        """
    def getFrame(self) -> ImgFrame:
        """getFrame(self: depthai.SegmentationMask) -> depthai.ImgFrame

        Returns the segmentation mask as an ImgFrame. If mask data is not set, returns
        an empty frame with only metadata set.
        """
    def getHeight(self) -> int:
        """getHeight(self: depthai.SegmentationMask) -> int

        Returns the height of the segmentation mask.
        """
    def getLabels(self) -> list[str]:
        """getLabels(self: depthai.SegmentationMask) -> list[str]

        Returns all class labels associated with the segmentation mask. If no labels are
        set, returns an empty vector.
        """
    def getMaskByIndex(self, index: int) -> numpy.ndarray[numpy.uint8]:
        """getMaskByIndex(self: depthai.SegmentationMask, index: int) -> numpy.ndarray[numpy.uint8]

        Returns a binary mask where pixels belonging to the specified instance/class
        index are set to 1, others to 0. If mask data is not set, returns an empty
        vector.
        """
    def getMaskByLabel(self, label: str) -> numpy.ndarray[numpy.uint8]:
        """getMaskByLabel(self: depthai.SegmentationMask, label: str) -> numpy.ndarray[numpy.uint8]

        Returns a binary mask where pixels belonging to the specified class label are
        set to 1, others to 0. If labels are not set or label not found, returns an
        empty vector.
        """
    def getMaskData(self) -> numpy.ndarray[numpy.uint8]:
        """getMaskData(self: depthai.SegmentationMask) -> numpy.ndarray[numpy.uint8]

        Returns a copy of the segmentation mask data as a vector of bytes. If mask data
        is not set, returns an empty vector.
        """
    def getSequenceNum(self) -> int:
        """getSequenceNum(self: depthai.SegmentationMask) -> int

        Retrieves image sequence number
        """
    def getTimestamp(self) -> datetime.timedelta:
        """getTimestamp(self: depthai.SegmentationMask) -> datetime.timedelta

        Retrieves timestamp related to dai::Clock::now()
        """
    def getTimestampDevice(self) -> datetime.timedelta:
        """getTimestampDevice(self: depthai.SegmentationMask) -> datetime.timedelta

        Retrieves timestamp directly captured from device's monotonic clock, not
        synchronized to host time. Used mostly for debugging
        """
    def getTransformation(self) -> ImgTransformation | None:
        """getTransformation(self: depthai.SegmentationMask) -> Optional[depthai.ImgTransformation]

        Retrieves image transformation data
        """
    def getUniqueIndices(self) -> list:
        """getUniqueIndices(self: depthai.SegmentationMask) -> list"""
    def getWidth(self) -> int:
        """getWidth(self: depthai.SegmentationMask) -> int

        Returns the width of the segmentation mask.
        """
    def hasValidMask(self) -> bool:
        """hasValidMask(self: depthai.SegmentationMask) -> bool

        Returns true if the mask data is not empty and has valid size (width * height).
        """
    def setCvMask(self, mask: numpy.ndarray) -> None:
        """setCvMask(self: depthai.SegmentationMask, mask: numpy.ndarray) -> None

        Copies cv::Mat data to Segmentation Mask buffer

        Parameter ``frame``:
            Input cv::Mat frame from which to copy the data @note Throws if mask is not
            a single channel INT8 type.
        """
    def setLabels(self, labels: list[str]) -> None:
        """setLabels(self: depthai.SegmentationMask, labels: list[str]) -> None

        Sets the class labels associated with the segmentation mask. The label at index
        `i` in the `labels` vector corresponds to the value `i` in the segmentation mask
        data array.

        Parameter ``labels``:
            Vector of class labels
        """
    @overload
    def setMask(self, mask: list[int], width: int, height: int) -> None:
        """setMask(*args, **kwargs)
        Overloaded function.

        1. setMask(self: depthai.SegmentationMask, mask: list[int], width: int, height: int) -> None

        Sets the segmentation mask from a vector of bytes. The size of the vector must
        be equal to width * height.

        2. setMask(self: depthai.SegmentationMask, frame: depthai.ImgFrame) -> None

        Sets the segmentation mask from an ImgFrame.

        Parameter ``frame``:
            Frame must be of type GRAY8
        """
    @overload
    def setMask(self, frame: ImgFrame) -> None:
        """setMask(*args, **kwargs)
        Overloaded function.

        1. setMask(self: depthai.SegmentationMask, mask: list[int], width: int, height: int) -> None

        Sets the segmentation mask from a vector of bytes. The size of the vector must
        be equal to width * height.

        2. setMask(self: depthai.SegmentationMask, frame: depthai.ImgFrame) -> None

        Sets the segmentation mask from an ImgFrame.

        Parameter ``frame``:
            Frame must be of type GRAY8
        """
    def setSequenceNum(self, sequenceNum: int) -> None:
        """setSequenceNum(self: depthai.SegmentationMask, sequenceNum: int) -> None

        Sets image sequence number
        """
    def setTimestamp(self, timestamp: datetime.timedelta) -> None:
        """setTimestamp(self: depthai.SegmentationMask, timestamp: datetime.timedelta) -> None

        Sets image timestamp related to dai::Clock::now()
        """
    def setTimestampDevice(self, timestampDevice: datetime.timedelta) -> None:
        """setTimestampDevice(self: depthai.SegmentationMask, timestampDevice: datetime.timedelta) -> None

        Sets image timestamp related to dai::Clock::now()
        """
    def setTransformation(self, transformation: ImgTransformation) -> None:
        """setTransformation(self: depthai.SegmentationMask, transformation: depthai.ImgTransformation) -> None

        Specifies image transformation data

        Parameter ``transformation``:
            transformation data
        """

class SegmentationParserConfig(Buffer):
    confidenceThreshold: float
    stepSize: int
    def __init__(self) -> None:
        """__init__(self: depthai.SegmentationParserConfig) -> None"""
    def getConfidenceThreshold(self) -> float:
        """getConfidenceThreshold(self: depthai.SegmentationParserConfig) -> float

        Get confidence threshold
        """
    def getStepSize(self) -> int:
        """getStepSize(self: depthai.SegmentationParserConfig) -> int

        Gets the step size for segmentation parsing.
        """
    def setConfidenceThreshold(self, threshold: float) -> None:
        """setConfidenceThreshold(self: depthai.SegmentationParserConfig, threshold: float) -> None

        Add a confidence threshold to the argmax operation over the segmentation tensor.
        Pixels with confidence values below this threshold will be assigned the
        background class (255).

        Parameter ``threshold``:
            Confidence threshold for segmentation parsing @note Default is -1.0f, which
            means no thresholding is applied. @note Only applicable if output classes
            are not in a single layer (eg. classesInOneLayer = false).
        """
    def setStepSize(self, stepSize: int) -> None:
        """setStepSize(self: depthai.SegmentationParserConfig, stepSize: int) -> None

        Sets the step size for segmentation parsing. A step size of 1 means every pixel
        is processed, a step size of 2 means every second pixel is processed, and so on.
        This can be used to speed up processing at the cost of lower resolution masks.

        Parameter ``stepSize``:
            Step size for segmentation parsing
        """

class SegmentationParserProperties:
    """Specify properties for SegmentationParser

    @property labels Vector of class labels associated with the segmentation mask.
    The label at index $i$ in the `labels` vector corresponds to the value $i$-th
    channel in the segmentation mask data array.

    Parameter ``networkOutputName``:
        Name of the output tensor from the neural network to parse. If empty, the
        first output will be used.

    Parameter ``classesInOneLayer``:
        If true, assumes that the segmentation classes are already encoded in a
        single layer as integer values. If false, an argmax operation is performed
        across multiple channels.

    Parameter ``backgroundClass``:
        If true, the first class (index 0) is considered as background."""
    backgroundClass: bool
    classesInOneLayer: bool
    labels: list[str]
    networkOutputName: str
    def __init__(self, *args, **kwargs) -> None:
        """Initialize self.  See help(type(self)) for accurate signature."""

class SendSnapCallbackResult:
    def __init__(self) -> None:
        """__init__(self: depthai.SendSnapCallbackResult) -> None"""
    @property
    def snapHubID(self) -> str | None:
        """(self: depthai.SendSnapCallbackResult) -> Optional[str]"""
    @property
    def snapLocalID(self) -> str:
        """(self: depthai.SendSnapCallbackResult) -> str"""
    @property
    def snapName(self) -> str:
        """(self: depthai.SendSnapCallbackResult) -> str"""
    @property
    def snapPayload(self) -> str:
        """(self: depthai.SendSnapCallbackResult) -> str"""
    @property
    def snapTimestamp(self) -> int:
        """(self: depthai.SendSnapCallbackResult) -> int"""
    @property
    def uploadStatus(self) -> SendSnapCallbackStatus:
        """(self: depthai.SendSnapCallbackResult) -> depthai.SendSnapCallbackStatus"""

class SendSnapCallbackStatus:
    """

    Members:

      SUCCESS

      FILE_BATCH_PREPARATION_FAILED

      GROUP_CONTAINS_REJECTED_FILES

      FILE_UPLOAD_FAILED

      SEND_EVENT_FAILED

      EVENT_REJECTED"""
    __members__: ClassVar[dict] = ...  # read-only
    EVENT_REJECTED: ClassVar[SendSnapCallbackStatus] = ...
    FILE_BATCH_PREPARATION_FAILED: ClassVar[SendSnapCallbackStatus] = ...
    FILE_UPLOAD_FAILED: ClassVar[SendSnapCallbackStatus] = ...
    GROUP_CONTAINS_REJECTED_FILES: ClassVar[SendSnapCallbackStatus] = ...
    SEND_EVENT_FAILED: ClassVar[SendSnapCallbackStatus] = ...
    SUCCESS: ClassVar[SendSnapCallbackStatus] = ...
    __entries: ClassVar[dict] = ...
    def __init__(self, value: int) -> None:
        """__init__(self: depthai.SendSnapCallbackStatus, value: int) -> None"""
    def __eq__(self, other: object) -> bool:
        """__eq__(self: object, other: object) -> bool"""
    def __hash__(self) -> int:
        """__hash__(self: object) -> int"""
    def __index__(self) -> int:
        """__index__(self: depthai.SendSnapCallbackStatus) -> int"""
    def __int__(self) -> int:
        """__int__(self: depthai.SendSnapCallbackStatus) -> int"""
    def __ne__(self, other: object) -> bool:
        """__ne__(self: object, other: object) -> bool"""
    @property
    def name(self) -> str:
        """name(self: object) -> str

        name(self: object) -> str
        """
    @property
    def value(self) -> int:
        """(arg0: depthai.SendSnapCallbackStatus) -> int"""

class SerializationType:
    """Members:

      LIBNOP

      JSON

      JSON_MSGPACK"""
    __members__: ClassVar[dict] = ...  # read-only
    JSON: ClassVar[SerializationType] = ...
    JSON_MSGPACK: ClassVar[SerializationType] = ...
    LIBNOP: ClassVar[SerializationType] = ...
    __entries: ClassVar[dict] = ...
    def __init__(self, value: int) -> None:
        """__init__(self: depthai.SerializationType, value: int) -> None"""
    def __eq__(self, other: object) -> bool:
        """__eq__(self: object, other: object) -> bool"""
    def __hash__(self) -> int:
        """__hash__(self: object) -> int"""
    def __index__(self) -> int:
        """__index__(self: depthai.SerializationType) -> int"""
    def __int__(self) -> int:
        """__int__(self: depthai.SerializationType) -> int"""
    def __ne__(self, other: object) -> bool:
        """__ne__(self: object, other: object) -> bool"""
    @property
    def name(self) -> str:
        """name(self: object) -> str

        name(self: object) -> str
        """
    @property
    def value(self) -> int:
        """(arg0: depthai.SerializationType) -> int"""

class Size2f:
    """Size2f structure

    width, height values define the size of the shape/frame"""
    height: float
    width: float
    @overload
    def __init__(self) -> None:
        """__init__(*args, **kwargs)
        Overloaded function.

        1. __init__(self: depthai.Size2f) -> None

        2. __init__(self: depthai.Size2f, width: float, height: float) -> None

        3. __init__(self: depthai.Size2f, width: float, height: float, normalized: bool) -> None
        """
    @overload
    def __init__(self, width: float, height: float) -> None:
        """__init__(*args, **kwargs)
        Overloaded function.

        1. __init__(self: depthai.Size2f) -> None

        2. __init__(self: depthai.Size2f, width: float, height: float) -> None

        3. __init__(self: depthai.Size2f, width: float, height: float, normalized: bool) -> None
        """
    @overload
    def __init__(self, width: float, height: float, normalized: bool) -> None:
        """__init__(*args, **kwargs)
        Overloaded function.

        1. __init__(self: depthai.Size2f) -> None

        2. __init__(self: depthai.Size2f, width: float, height: float) -> None

        3. __init__(self: depthai.Size2f, width: float, height: float, normalized: bool) -> None
        """
    def isNormalized(self) -> bool:
        """isNormalized(self: depthai.Size2f) -> bool"""

class SlugComponents:
    modelRef: str
    modelSlug: str
    modelVariantSlug: str
    teamName: str
    @overload
    def __init__(self) -> None:
        """__init__(*args, **kwargs)
        Overloaded function.

        1. __init__(self: depthai.SlugComponents) -> None

        2. __init__(self: depthai.SlugComponents, teamName: str = '', modelSlug: str = '', modelVariantSlug: str = '', modelRef: str = '') -> None
        """
    @overload
    def __init__(self, teamName: str = ..., modelSlug: str = ..., modelVariantSlug: str = ..., modelRef: str = ...) -> None:
        """__init__(*args, **kwargs)
        Overloaded function.

        1. __init__(self: depthai.SlugComponents) -> None

        2. __init__(self: depthai.SlugComponents, teamName: str = '', modelSlug: str = '', modelVariantSlug: str = '', modelRef: str = '') -> None
        """
    def merge(self) -> str:
        """merge(self: depthai.SlugComponents) -> str"""
    @staticmethod
    def split(slug: str) -> SlugComponents:
        """split(slug: str) -> depthai.SlugComponents"""

class SpatialDetectionNetworkProperties:
    """Specify properties for SpatialDetectionNetwork"""
    calculationAlgorithm: SpatialLocationCalculatorAlgorithm
    depthThresholds: SpatialLocationCalculatorConfigThresholds
    detectedBBScaleFactor: float
    stepSize: int
    def __init__(self, *args, **kwargs) -> None:
        """Initialize self.  See help(type(self)) for accurate signature."""

class SpatialImgDetection:
    """SpatialImgDetection structure

    Contains image detection results together with spatial location data."""
    boundingBox: RotatedRect | None
    boundingBoxMapping: SpatialLocationCalculatorConfigData
    confidence: float
    label: int
    labelName: str
    spatialCoordinates: Point3f
    xmax: float
    xmin: float
    ymax: float
    ymin: float
    @overload
    def __init__(self) -> None:
        """__init__(*args, **kwargs)
        Overloaded function.

        1. __init__(self: depthai.SpatialImgDetection) -> None

        2. __init__(self: depthai.SpatialImgDetection, boundingBox: depthai.RotatedRect, spatialCoordinates: depthai.Point3f, confidence: float, label: int) -> None

        3. __init__(self: depthai.SpatialImgDetection, boundingBox: depthai.RotatedRect, spatialCoordinates: depthai.Point3f, labelName: str, confidence: float, label: int) -> None

        4. __init__(self: depthai.SpatialImgDetection, boundingBox: depthai.RotatedRect, spatialCoordinates: depthai.Point3f, keypoints: depthai.SpatialKeypointsList, confidence: float, label: int) -> None

        5. __init__(self: depthai.SpatialImgDetection, boundingBox: depthai.RotatedRect, spatialCoordinates: depthai.Point3f, keypoints: depthai.SpatialKeypointsList, labelName: str, confidence: float, label: int) -> None
        """
    @overload
    def __init__(self, boundingBox: RotatedRect, spatialCoordinates: Point3f, confidence: float, label: int) -> None:
        """__init__(*args, **kwargs)
        Overloaded function.

        1. __init__(self: depthai.SpatialImgDetection) -> None

        2. __init__(self: depthai.SpatialImgDetection, boundingBox: depthai.RotatedRect, spatialCoordinates: depthai.Point3f, confidence: float, label: int) -> None

        3. __init__(self: depthai.SpatialImgDetection, boundingBox: depthai.RotatedRect, spatialCoordinates: depthai.Point3f, labelName: str, confidence: float, label: int) -> None

        4. __init__(self: depthai.SpatialImgDetection, boundingBox: depthai.RotatedRect, spatialCoordinates: depthai.Point3f, keypoints: depthai.SpatialKeypointsList, confidence: float, label: int) -> None

        5. __init__(self: depthai.SpatialImgDetection, boundingBox: depthai.RotatedRect, spatialCoordinates: depthai.Point3f, keypoints: depthai.SpatialKeypointsList, labelName: str, confidence: float, label: int) -> None
        """
    @overload
    def __init__(self, boundingBox: RotatedRect, spatialCoordinates: Point3f, labelName: str, confidence: float, label: int) -> None:
        """__init__(*args, **kwargs)
        Overloaded function.

        1. __init__(self: depthai.SpatialImgDetection) -> None

        2. __init__(self: depthai.SpatialImgDetection, boundingBox: depthai.RotatedRect, spatialCoordinates: depthai.Point3f, confidence: float, label: int) -> None

        3. __init__(self: depthai.SpatialImgDetection, boundingBox: depthai.RotatedRect, spatialCoordinates: depthai.Point3f, labelName: str, confidence: float, label: int) -> None

        4. __init__(self: depthai.SpatialImgDetection, boundingBox: depthai.RotatedRect, spatialCoordinates: depthai.Point3f, keypoints: depthai.SpatialKeypointsList, confidence: float, label: int) -> None

        5. __init__(self: depthai.SpatialImgDetection, boundingBox: depthai.RotatedRect, spatialCoordinates: depthai.Point3f, keypoints: depthai.SpatialKeypointsList, labelName: str, confidence: float, label: int) -> None
        """
    @overload
    def __init__(self, boundingBox: RotatedRect, spatialCoordinates: Point3f, keypoints: SpatialKeypointsList, confidence: float, label: int) -> None:
        """__init__(*args, **kwargs)
        Overloaded function.

        1. __init__(self: depthai.SpatialImgDetection) -> None

        2. __init__(self: depthai.SpatialImgDetection, boundingBox: depthai.RotatedRect, spatialCoordinates: depthai.Point3f, confidence: float, label: int) -> None

        3. __init__(self: depthai.SpatialImgDetection, boundingBox: depthai.RotatedRect, spatialCoordinates: depthai.Point3f, labelName: str, confidence: float, label: int) -> None

        4. __init__(self: depthai.SpatialImgDetection, boundingBox: depthai.RotatedRect, spatialCoordinates: depthai.Point3f, keypoints: depthai.SpatialKeypointsList, confidence: float, label: int) -> None

        5. __init__(self: depthai.SpatialImgDetection, boundingBox: depthai.RotatedRect, spatialCoordinates: depthai.Point3f, keypoints: depthai.SpatialKeypointsList, labelName: str, confidence: float, label: int) -> None
        """
    @overload
    def __init__(self, boundingBox: RotatedRect, spatialCoordinates: Point3f, keypoints: SpatialKeypointsList, labelName: str, confidence: float, label: int) -> None:
        """__init__(*args, **kwargs)
        Overloaded function.

        1. __init__(self: depthai.SpatialImgDetection) -> None

        2. __init__(self: depthai.SpatialImgDetection, boundingBox: depthai.RotatedRect, spatialCoordinates: depthai.Point3f, confidence: float, label: int) -> None

        3. __init__(self: depthai.SpatialImgDetection, boundingBox: depthai.RotatedRect, spatialCoordinates: depthai.Point3f, labelName: str, confidence: float, label: int) -> None

        4. __init__(self: depthai.SpatialImgDetection, boundingBox: depthai.RotatedRect, spatialCoordinates: depthai.Point3f, keypoints: depthai.SpatialKeypointsList, confidence: float, label: int) -> None

        5. __init__(self: depthai.SpatialImgDetection, boundingBox: depthai.RotatedRect, spatialCoordinates: depthai.Point3f, keypoints: depthai.SpatialKeypointsList, labelName: str, confidence: float, label: int) -> None
        """
    def getAngle(self) -> float:
        """getAngle(self: depthai.SpatialImgDetection) -> float

        Returns the angle of the bounding box.
        """
    def getBoundingBox(self) -> RotatedRect:
        """getBoundingBox(self: depthai.SpatialImgDetection) -> depthai.RotatedRect

        Returns bounding box if it was set, else it constructs a new one from the legacy
        xmin, ymin, xmax, ymax values.
        """
    def getCenterX(self) -> float:
        """getCenterX(self: depthai.SpatialImgDetection) -> float

        Returns the X coordinate of the center of the bounding box.
        """
    def getCenterY(self) -> float:
        """getCenterY(self: depthai.SpatialImgDetection) -> float

        Returns the Y coordinate of the center of the bounding box.
        """
    def getEdges(self, *args, **kwargs):
        """getEdges(self: depthai.SpatialImgDetection) -> list[Annotated[list[int], FixedSize(2)]]

        Returns a list of edges, each edge is a pair of indices, or empty list if no
        keypoints were set.
        """
    def getHeight(self) -> float:
        """getHeight(self: depthai.SpatialImgDetection) -> float

        Returns the height of the (rotated) bounding box.
        """
    def getImgDetection(self) -> ImgDetection:
        """getImgDetection(self: depthai.SpatialImgDetection) -> depthai.ImgDetection

        Converts SpatialImgDetection to ImgDetection by dropping spatial data.

        Returns:
            dai::ImgDetection object.
        """
    def getKeypointSpatialCoordinates(self) -> list[Point3f]:
        """getKeypointSpatialCoordinates(self: depthai.SpatialImgDetection) -> list[depthai.Point3f]

        Returns a list of spatial coordinates for each keypoint, or empty list if no
        keypoints were set.
        """
    def getKeypoints(self) -> list[SpatialKeypoint]:
        """getKeypoints(self: depthai.SpatialImgDetection) -> list[depthai.SpatialKeypoint]

        Returns a list of Keypoint objects, or empty list if no keypoints were set.
        """
    def getWidth(self) -> float:
        """getWidth(self: depthai.SpatialImgDetection) -> float

        Returns the width of the (rotated) bounding box.
        """
    def setBoundingBox(self, boundingBox: RotatedRect) -> None:
        """setBoundingBox(self: depthai.SpatialImgDetection, boundingBox: depthai.RotatedRect) -> None

        Sets the bounding box and the legacy coordinates of the detection.
        """
    def setEdges(self, edges) -> None:
        """setEdges(self: depthai.SpatialImgDetection, edges: list[Annotated[list[int], FixedSize(2)]]) -> None

        Sets edges for the keypoints, throws if no keypoints were set beforehand.
        """
    @overload
    def setKeypoints(self, keypoints: SpatialKeypointsList) -> None:
        """setKeypoints(*args, **kwargs)
        Overloaded function.

        1. setKeypoints(self: depthai.SpatialImgDetection, keypoints: depthai.SpatialKeypointsList) -> None

        Sets the keypoints of the detection.

        Parameter ``keypoints``:
            list of Keypoint objects to set. @note This will clear any existing
            keypoints and edges.

        2. setKeypoints(self: depthai.SpatialImgDetection, keypoints: list[depthai.SpatialKeypoint]) -> None

        Sets the keypoints of the detection.

        Parameter ``keypoints``:
            list of Keypoint objects to set. @note This will clear any existing
            keypoints and edges.

        3. setKeypoints(self: depthai.SpatialImgDetection, keypoints: list[depthai.SpatialKeypoint], edges: list[Annotated[list[int], FixedSize(2)]]) -> None

        Sets the keypoints of the detection.

        Parameter ``keypoints``:
            list of Keypoint objects to set. @note This will clear any existing
            keypoints and edges.

        4. setKeypoints(self: depthai.SpatialImgDetection, keypoints: list[depthai.Point3f]) -> None

        Sets the keypoints of the detection.

        Parameter ``keypoints``:
            list of Keypoint objects to set. @note This will clear any existing
            keypoints and edges.
        """
    @overload
    def setKeypoints(self, keypoints: list[SpatialKeypoint]) -> None:
        """setKeypoints(*args, **kwargs)
        Overloaded function.

        1. setKeypoints(self: depthai.SpatialImgDetection, keypoints: depthai.SpatialKeypointsList) -> None

        Sets the keypoints of the detection.

        Parameter ``keypoints``:
            list of Keypoint objects to set. @note This will clear any existing
            keypoints and edges.

        2. setKeypoints(self: depthai.SpatialImgDetection, keypoints: list[depthai.SpatialKeypoint]) -> None

        Sets the keypoints of the detection.

        Parameter ``keypoints``:
            list of Keypoint objects to set. @note This will clear any existing
            keypoints and edges.

        3. setKeypoints(self: depthai.SpatialImgDetection, keypoints: list[depthai.SpatialKeypoint], edges: list[Annotated[list[int], FixedSize(2)]]) -> None

        Sets the keypoints of the detection.

        Parameter ``keypoints``:
            list of Keypoint objects to set. @note This will clear any existing
            keypoints and edges.

        4. setKeypoints(self: depthai.SpatialImgDetection, keypoints: list[depthai.Point3f]) -> None

        Sets the keypoints of the detection.

        Parameter ``keypoints``:
            list of Keypoint objects to set. @note This will clear any existing
            keypoints and edges.
        """
    @overload
    def setKeypoints(self, keypoints: list[SpatialKeypoint], edges) -> None:
        """setKeypoints(*args, **kwargs)
        Overloaded function.

        1. setKeypoints(self: depthai.SpatialImgDetection, keypoints: depthai.SpatialKeypointsList) -> None

        Sets the keypoints of the detection.

        Parameter ``keypoints``:
            list of Keypoint objects to set. @note This will clear any existing
            keypoints and edges.

        2. setKeypoints(self: depthai.SpatialImgDetection, keypoints: list[depthai.SpatialKeypoint]) -> None

        Sets the keypoints of the detection.

        Parameter ``keypoints``:
            list of Keypoint objects to set. @note This will clear any existing
            keypoints and edges.

        3. setKeypoints(self: depthai.SpatialImgDetection, keypoints: list[depthai.SpatialKeypoint], edges: list[Annotated[list[int], FixedSize(2)]]) -> None

        Sets the keypoints of the detection.

        Parameter ``keypoints``:
            list of Keypoint objects to set. @note This will clear any existing
            keypoints and edges.

        4. setKeypoints(self: depthai.SpatialImgDetection, keypoints: list[depthai.Point3f]) -> None

        Sets the keypoints of the detection.

        Parameter ``keypoints``:
            list of Keypoint objects to set. @note This will clear any existing
            keypoints and edges.
        """
    @overload
    def setKeypoints(self, keypoints: list[Point3f]) -> None:
        """setKeypoints(*args, **kwargs)
        Overloaded function.

        1. setKeypoints(self: depthai.SpatialImgDetection, keypoints: depthai.SpatialKeypointsList) -> None

        Sets the keypoints of the detection.

        Parameter ``keypoints``:
            list of Keypoint objects to set. @note This will clear any existing
            keypoints and edges.

        2. setKeypoints(self: depthai.SpatialImgDetection, keypoints: list[depthai.SpatialKeypoint]) -> None

        Sets the keypoints of the detection.

        Parameter ``keypoints``:
            list of Keypoint objects to set. @note This will clear any existing
            keypoints and edges.

        3. setKeypoints(self: depthai.SpatialImgDetection, keypoints: list[depthai.SpatialKeypoint], edges: list[Annotated[list[int], FixedSize(2)]]) -> None

        Sets the keypoints of the detection.

        Parameter ``keypoints``:
            list of Keypoint objects to set. @note This will clear any existing
            keypoints and edges.

        4. setKeypoints(self: depthai.SpatialImgDetection, keypoints: list[depthai.Point3f]) -> None

        Sets the keypoints of the detection.

        Parameter ``keypoints``:
            list of Keypoint objects to set. @note This will clear any existing
            keypoints and edges.
        """
    def setOuterBoundingBox(self, xmin: float, ymin: float, xmax: float, ymax: float) -> None:
        """setOuterBoundingBox(self: depthai.SpatialImgDetection, xmin: float, ymin: float, xmax: float, ymax: float) -> None

        Sets the bounding box and the legacy coordinates of the detection from the top-
        left and bottom-right points.
        """
    def setSpatialCoordinate(self, spatialCoordinates: Point3f) -> None:
        """setSpatialCoordinate(self: depthai.SpatialImgDetection, spatialCoordinates: depthai.Point3f) -> None

        Sets spatial coordinates for the detection.

        Parameter ``spatialCoordinates``:
            list of Point3f objects to set. @note The size of spatialCoordinates.
        """

class SpatialImgDetections(Buffer):
    """SpatialImgDetections message. Carries detection results together with spatial
    location data"""
    detections: list[SpatialImgDetection]
    def __init__(self) -> None:
        """__init__(self: depthai.SpatialImgDetections) -> None"""
    def getCvSegmentationMask(self) -> numpy.ndarray | None:
        """getCvSegmentationMask(self: depthai.SpatialImgDetections) -> Optional[numpy.ndarray]

        Retrieves mask data as a cv::Mat copy with specified width and height. If mask
        data is not set, returns std::nullopt.

        Parameter ``allocator``:
            Allows callers to supply a custom cv::MatAllocator for zero-copy/custom
            memory management; nullptr uses OpenCV’s default.
        """
    def getCvSegmentationMaskByClass(self, semantic_class: int) -> numpy.ndarray | None:
        """getCvSegmentationMaskByClass(self: depthai.SpatialImgDetections, semantic_class: int) -> Optional[numpy.ndarray]

        Retrieves data by the semantic class. If no mask data is not set, returns
        std::nullopt.

        Parameter ``semanticClass``:
            Semantic class index

        Parameter ``allocator``:
            Allows callers to supply a custom cv::MatAllocator for zero-copy/custom
            memory management; nullptr uses OpenCV’s default.
        """
    def getCvSegmentationMaskByIndex(self, index: int) -> numpy.ndarray | None:
        """getCvSegmentationMaskByIndex(self: depthai.SpatialImgDetections, index: int) -> Optional[numpy.ndarray]

        Returns a binary mask where pixels belonging to the instance index are set to 1,
        others to 0. If mask data is not set, returns std::nullopt.

        Parameter ``index``:
            Instance index

        Parameter ``allocator``:
            Allows callers to supply a custom cv::MatAllocator for zero-copy/custom
            memory management; nullptr uses OpenCV’s default.
        """
    def getMaskData(self, *args, **kwargs):
        """getMaskData(self: depthai.SpatialImgDetections) -> Optional[std::vector<unsigned char, std::allocator<unsigned char> >]

        Returns a copy of the segmentation mask data as a vector of bytes. If mask data
        is not set, returns std::nullopt.
        """
    def getSegmentationMask(self) -> ImgFrame | None:
        """getSegmentationMask(self: depthai.SpatialImgDetections) -> Optional[depthai.ImgFrame]

        Returns the segmentation mask as an ImgFrame. If mask data is not set, returns
        std::nullopt.
        """
    def getSegmentationMaskHeight(self) -> int:
        """getSegmentationMaskHeight(self: depthai.SpatialImgDetections) -> int

        Returns the height of the segmentation mask.
        """
    def getSegmentationMaskWidth(self) -> int:
        """getSegmentationMaskWidth(self: depthai.SpatialImgDetections) -> int

        Returns the width of the segmentation mask.
        """
    def getSequenceNum(self) -> int:
        """getSequenceNum(self: depthai.SpatialImgDetections) -> int

        Retrieves image sequence number
        """
    def getTimestamp(self) -> datetime.timedelta:
        """getTimestamp(self: depthai.SpatialImgDetections) -> datetime.timedelta

        Retrieves timestamp related to dai::Clock::now()
        """
    def getTimestampDevice(self) -> datetime.timedelta:
        """getTimestampDevice(self: depthai.SpatialImgDetections) -> datetime.timedelta

        Retrieves timestamp directly captured from device's monotonic clock, not
        synchronized to host time. Used mostly for debugging
        """
    def getTransformation(self) -> ImgTransformation | None:
        """getTransformation(self: depthai.SpatialImgDetections) -> Optional[depthai.ImgTransformation]"""
    def setCvSegmentationMask(self, mask: numpy.ndarray) -> None:
        """setCvSegmentationMask(self: depthai.SpatialImgDetections, mask: numpy.ndarray) -> None

        Copies cv::Mat data to Segmentation Mask buffer

        Parameter ``frame``:
            Input cv::Mat frame from which to copy the data @note Throws if mask is not
            a single channel INT8 type.
        """
    def setSegmentationMask(self, frame: ImgFrame) -> None:
        """setSegmentationMask(self: depthai.SpatialImgDetections, frame: depthai.ImgFrame) -> None

        Sets the segmentation mask from a vector of bytes. The size of the vector must
        be equal to width * height.
        """
    def setTransformation(self, arg0: ImgTransformation | None) -> None:
        """setTransformation(self: depthai.SpatialImgDetections, arg0: Optional[depthai.ImgTransformation]) -> None"""

class SpatialKeypoint:
    confidence: float
    imageCoordinates: Point3f
    label: int
    labelName: str
    spatialCoordinates: Point3f
    @overload
    def __init__(self) -> None:
        """__init__(*args, **kwargs)
        Overloaded function.

        1. __init__(self: depthai.SpatialKeypoint) -> None

        2. __init__(self: depthai.SpatialKeypoint, imageCoordinates: depthai.Point3f, spatialCoordinates: depthai.Point3f = <depthai.Point3f object at 0xff8e0c738470>, confidence: float = -1.0, label: int = 0, labelName: str = '') -> None

        3. __init__(self: depthai.SpatialKeypoint, imageCoordinates: depthai.Point2f, spatialCoordinates: depthai.Point3f = <depthai.Point3f object at 0xff8e0c723430>, confidence: float = 0.0, label: int = 0, labelName: str = '') -> None

        4. __init__(self: depthai.SpatialKeypoint, x: float, y: float, z: float, spatialX: float, spatialY: float, spatialZ: float, confidence: float = 0.0, label: int = 0, labelName: str = '') -> None
        """
    @overload
    def __init__(self, imageCoordinates: Point3f, spatialCoordinates: Point3f = ..., confidence: float = ..., label: int = ..., labelName: str = ...) -> None:
        """__init__(*args, **kwargs)
        Overloaded function.

        1. __init__(self: depthai.SpatialKeypoint) -> None

        2. __init__(self: depthai.SpatialKeypoint, imageCoordinates: depthai.Point3f, spatialCoordinates: depthai.Point3f = <depthai.Point3f object at 0xff8e0c738470>, confidence: float = -1.0, label: int = 0, labelName: str = '') -> None

        3. __init__(self: depthai.SpatialKeypoint, imageCoordinates: depthai.Point2f, spatialCoordinates: depthai.Point3f = <depthai.Point3f object at 0xff8e0c723430>, confidence: float = 0.0, label: int = 0, labelName: str = '') -> None

        4. __init__(self: depthai.SpatialKeypoint, x: float, y: float, z: float, spatialX: float, spatialY: float, spatialZ: float, confidence: float = 0.0, label: int = 0, labelName: str = '') -> None
        """
    @overload
    def __init__(self, imageCoordinates: Point2f, spatialCoordinates: Point3f = ..., confidence: float = ..., label: int = ..., labelName: str = ...) -> None:
        """__init__(*args, **kwargs)
        Overloaded function.

        1. __init__(self: depthai.SpatialKeypoint) -> None

        2. __init__(self: depthai.SpatialKeypoint, imageCoordinates: depthai.Point3f, spatialCoordinates: depthai.Point3f = <depthai.Point3f object at 0xff8e0c738470>, confidence: float = -1.0, label: int = 0, labelName: str = '') -> None

        3. __init__(self: depthai.SpatialKeypoint, imageCoordinates: depthai.Point2f, spatialCoordinates: depthai.Point3f = <depthai.Point3f object at 0xff8e0c723430>, confidence: float = 0.0, label: int = 0, labelName: str = '') -> None

        4. __init__(self: depthai.SpatialKeypoint, x: float, y: float, z: float, spatialX: float, spatialY: float, spatialZ: float, confidence: float = 0.0, label: int = 0, labelName: str = '') -> None
        """
    @overload
    def __init__(self, x: float, y: float, z: float, spatialX: float, spatialY: float, spatialZ: float, confidence: float = ..., label: int = ..., labelName: str = ...) -> None:
        """__init__(*args, **kwargs)
        Overloaded function.

        1. __init__(self: depthai.SpatialKeypoint) -> None

        2. __init__(self: depthai.SpatialKeypoint, imageCoordinates: depthai.Point3f, spatialCoordinates: depthai.Point3f = <depthai.Point3f object at 0xff8e0c738470>, confidence: float = -1.0, label: int = 0, labelName: str = '') -> None

        3. __init__(self: depthai.SpatialKeypoint, imageCoordinates: depthai.Point2f, spatialCoordinates: depthai.Point3f = <depthai.Point3f object at 0xff8e0c723430>, confidence: float = 0.0, label: int = 0, labelName: str = '') -> None

        4. __init__(self: depthai.SpatialKeypoint, x: float, y: float, z: float, spatialX: float, spatialY: float, spatialZ: float, confidence: float = 0.0, label: int = 0, labelName: str = '') -> None
        """

class SpatialKeypointsList:
    @overload
    def __init__(self) -> None:
        """__init__(*args, **kwargs)
        Overloaded function.

        1. __init__(self: depthai.SpatialKeypointsList) -> None

        2. __init__(self: depthai.SpatialKeypointsList, keypoints: list[depthai.SpatialKeypoint], edges: list[Annotated[list[int], FixedSize(2)]]) -> None

        3. __init__(self: depthai.SpatialKeypointsList, keypoints: list[depthai.SpatialKeypoint]) -> None
        """
    @overload
    def __init__(self, keypoints: list[SpatialKeypoint], edges) -> None:
        """__init__(*args, **kwargs)
        Overloaded function.

        1. __init__(self: depthai.SpatialKeypointsList) -> None

        2. __init__(self: depthai.SpatialKeypointsList, keypoints: list[depthai.SpatialKeypoint], edges: list[Annotated[list[int], FixedSize(2)]]) -> None

        3. __init__(self: depthai.SpatialKeypointsList, keypoints: list[depthai.SpatialKeypoint]) -> None
        """
    @overload
    def __init__(self, keypoints: list[SpatialKeypoint]) -> None:
        """__init__(*args, **kwargs)
        Overloaded function.

        1. __init__(self: depthai.SpatialKeypointsList) -> None

        2. __init__(self: depthai.SpatialKeypointsList, keypoints: list[depthai.SpatialKeypoint], edges: list[Annotated[list[int], FixedSize(2)]]) -> None

        3. __init__(self: depthai.SpatialKeypointsList, keypoints: list[depthai.SpatialKeypoint]) -> None
        """
    def getEdges(self, *args, **kwargs):
        """getEdges(self: depthai.SpatialKeypointsList) -> list[Annotated[list[int], FixedSize(2)]]

        Get the indices of the edges.

        Returns:
            Vector of edge indices.
        """
    def getKeypoints(self) -> list[SpatialKeypoint]:
        """getKeypoints(self: depthai.SpatialKeypointsList) -> list[depthai.SpatialKeypoint]

        Get keypoints.

        Returns:
            Vector of Keypoint objects.
        """
    def getPoints2f(self) -> VectorPoint2f:
        """getPoints2f(self: depthai.SpatialKeypointsList) -> depthai.VectorPoint2f

        Get only image 2D coordinates of the keypoints and drop the z axis values.

        Returns:
            Vector of Point2f coordinates.
        """
    def getPoints3f(self) -> list[Point3f]:
        """getPoints3f(self: depthai.SpatialKeypointsList) -> list[depthai.Point3f]

        Get only image coordinates of the keypoints.

        Returns:
            Vector of Point3f coordinates.
        """
    def getSpatialCoordinates(self) -> list[Point3f]:
        """getSpatialCoordinates(self: depthai.SpatialKeypointsList) -> list[depthai.Point3f]

        Get spatial coordinates of the keypoints.

        Returns:
            Vector of Point3f spatial coordinates.
        """
    def setEdges(self, edges) -> None:
        """setEdges(self: depthai.SpatialKeypointsList, edges: list[Annotated[list[int], FixedSize(2)]]) -> None"""
    @overload
    def setKeypoints(self, keypoints: list[SpatialKeypoint]) -> None:
        """setKeypoints(*args, **kwargs)
        Overloaded function.

        1. setKeypoints(self: depthai.SpatialKeypointsList, keypoints: list[depthai.SpatialKeypoint]) -> None

        Sets the keypoints list.

        Parameter ``keypoints``:
            list of Keypoint objects to set. @note This will clear any existing
            keypoints and edges.

        2. setKeypoints(self: depthai.SpatialKeypointsList, keypoints: list[depthai.Point3f]) -> None

        Sets the keypoints list.

        Parameter ``keypoints``:
            list of Keypoint objects to set. @note This will clear any existing
            keypoints and edges.

        3. setKeypoints(self: depthai.SpatialKeypointsList, keypoints: list[depthai.SpatialKeypoint], edges: list[Annotated[list[int], FixedSize(2)]]) -> None

        Sets the keypoints list.

        Parameter ``keypoints``:
            list of Keypoint objects to set. @note This will clear any existing
            keypoints and edges.
        """
    @overload
    def setKeypoints(self, keypoints: list[Point3f]) -> None:
        """setKeypoints(*args, **kwargs)
        Overloaded function.

        1. setKeypoints(self: depthai.SpatialKeypointsList, keypoints: list[depthai.SpatialKeypoint]) -> None

        Sets the keypoints list.

        Parameter ``keypoints``:
            list of Keypoint objects to set. @note This will clear any existing
            keypoints and edges.

        2. setKeypoints(self: depthai.SpatialKeypointsList, keypoints: list[depthai.Point3f]) -> None

        Sets the keypoints list.

        Parameter ``keypoints``:
            list of Keypoint objects to set. @note This will clear any existing
            keypoints and edges.

        3. setKeypoints(self: depthai.SpatialKeypointsList, keypoints: list[depthai.SpatialKeypoint], edges: list[Annotated[list[int], FixedSize(2)]]) -> None

        Sets the keypoints list.

        Parameter ``keypoints``:
            list of Keypoint objects to set. @note This will clear any existing
            keypoints and edges.
        """
    @overload
    def setKeypoints(self, keypoints: list[SpatialKeypoint], edges) -> None:
        """setKeypoints(*args, **kwargs)
        Overloaded function.

        1. setKeypoints(self: depthai.SpatialKeypointsList, keypoints: list[depthai.SpatialKeypoint]) -> None

        Sets the keypoints list.

        Parameter ``keypoints``:
            list of Keypoint objects to set. @note This will clear any existing
            keypoints and edges.

        2. setKeypoints(self: depthai.SpatialKeypointsList, keypoints: list[depthai.Point3f]) -> None

        Sets the keypoints list.

        Parameter ``keypoints``:
            list of Keypoint objects to set. @note This will clear any existing
            keypoints and edges.

        3. setKeypoints(self: depthai.SpatialKeypointsList, keypoints: list[depthai.SpatialKeypoint], edges: list[Annotated[list[int], FixedSize(2)]]) -> None

        Sets the keypoints list.

        Parameter ``keypoints``:
            list of Keypoint objects to set. @note This will clear any existing
            keypoints and edges.
        """
    def setSpatialCoordinates(self, spatialCoordinates: list[Point3f]) -> None:
        """setSpatialCoordinates(self: depthai.SpatialKeypointsList, spatialCoordinates: list[depthai.Point3f]) -> None

        Sets the keypoints from a vector of 3D spatial points.

        Parameter ``spatialCoordinates``:
            vector of Point3f objects to set as spatial coordinates. @note The size of
            spatialCoordinates must match the number of keypoints.
        """

class SpatialLocationCalculatorAlgorithm:
    """SpatialLocationCalculatorAlgorithm configuration modes

    Contains calculation method used to obtain spatial locations.

    Members:

      AVERAGE

      MEAN

      MIN

      MAX

      MODE

      MEDIAN"""
    __members__: ClassVar[dict] = ...  # read-only
    AVERAGE: ClassVar[SpatialLocationCalculatorAlgorithm] = ...
    MAX: ClassVar[SpatialLocationCalculatorAlgorithm] = ...
    MEAN: ClassVar[SpatialLocationCalculatorAlgorithm] = ...
    MEDIAN: ClassVar[SpatialLocationCalculatorAlgorithm] = ...
    MIN: ClassVar[SpatialLocationCalculatorAlgorithm] = ...
    MODE: ClassVar[SpatialLocationCalculatorAlgorithm] = ...
    __entries: ClassVar[dict] = ...
    def __init__(self, value: int) -> None:
        """__init__(self: depthai.SpatialLocationCalculatorAlgorithm, value: int) -> None"""
    def __eq__(self, other: object) -> bool:
        """__eq__(self: object, other: object) -> bool"""
    def __hash__(self) -> int:
        """__hash__(self: object) -> int"""
    def __index__(self) -> int:
        """__index__(self: depthai.SpatialLocationCalculatorAlgorithm) -> int"""
    def __int__(self) -> int:
        """__int__(self: depthai.SpatialLocationCalculatorAlgorithm) -> int"""
    def __ne__(self, other: object) -> bool:
        """__ne__(self: object, other: object) -> bool"""
    @property
    def name(self) -> str:
        """name(self: object) -> str

        name(self: object) -> str
        """
    @property
    def value(self) -> int:
        """(arg0: depthai.SpatialLocationCalculatorAlgorithm) -> int"""

class SpatialLocationCalculatorConfig(Buffer):
    """Configuration for SpatialLocationCalculator.

    Holds global parameters and optional per-ROI entries used to compute 3D spatial
    locations from a depth map.

    Global parameters (defaults): - Lower depth threshold [mm]: 0 - Upper depth
    threshold [mm]: 65535 - Calculation algorithm: MEDIAN - Step size: AUTO -
    Keypoint radius [px]: 10 - Calculate spatial keypoints: true - Use segmentation
    for ImgDetections: true - Segmentation passthrough: true - Bounding box scale
    factor: 1.0

    An optional list of per-ROI configurations is available via `config`. ROI
    settings override the corresponding global values where specified."""
    calculateSpatialKeypoints: bool
    globalLowerThreshold: int
    globalStepSize: int
    globalUpperThreshold: int
    useSegmentation: bool
    def __init__(self) -> None:
        """__init__(self: depthai.SpatialLocationCalculatorConfig) -> None"""
    def addROI(self, ROI: SpatialLocationCalculatorConfigData) -> None:
        """addROI(self: depthai.SpatialLocationCalculatorConfig, ROI: depthai.SpatialLocationCalculatorConfigData) -> None

        Add a new region of interest (ROI) to configuration data.

        Parameter ``roi``:
            Configuration parameters for ROI
        """
    def getBoundingBoxScaleFactor(self) -> float:
        """getBoundingBoxScaleFactor(self: depthai.SpatialLocationCalculatorConfig) -> float

        Retrieve scale factor for bounding boxes used in spatial calculations.
        """
    def getCalculateSpatialKeypoints(self) -> bool:
        """getCalculateSpatialKeypoints(self: depthai.SpatialLocationCalculatorConfig) -> bool

        Retrieve whether keypoints are used for spatial location calculation. $..
        warning::

        Only applicable to ImgDetections with keypoints.
        """
    def getCalculationAlgorithm(self) -> SpatialLocationCalculatorAlgorithm:
        """getCalculationAlgorithm(self: depthai.SpatialLocationCalculatorConfig) -> depthai.SpatialLocationCalculatorAlgorithm"""
    def getConfigData(self) -> list[SpatialLocationCalculatorConfigData]:
        """getConfigData(self: depthai.SpatialLocationCalculatorConfig) -> list[depthai.SpatialLocationCalculatorConfigData]

        Retrieve configuration data for SpatialLocationCalculator

        Returns:
            Vector of configuration parameters for ROIs (region of interests)
        """
    def getDepthThresholds(self) -> tuple[int, int]:
        """getDepthThresholds(self: depthai.SpatialLocationCalculatorConfig) -> tuple[int, int]"""
    def getKeypointRadius(self) -> int:
        """getKeypointRadius(self: depthai.SpatialLocationCalculatorConfig) -> int

        Retrieve radius around keypoints used to calculate spatial coordinates.
        """
    def getSegmentationPassthrough(self) -> bool:
        """getSegmentationPassthrough(self: depthai.SpatialLocationCalculatorConfig) -> bool

        Retrieve whether segmentation is passed through along with spatial detections.
        $.. warning::

        Only applicable to ImgDetections with segmentation masks.
        """
    def getStepSize(self) -> int:
        """getStepSize(self: depthai.SpatialLocationCalculatorConfig) -> int"""
    def getUseSegmentation(self) -> bool:
        """getUseSegmentation(self: depthai.SpatialLocationCalculatorConfig) -> bool

        Retrieve whether segmentation is used for spatial location calculation. $..
        warning::

        Only applicable to ImgDetections with segmentation masks.
        """
    def setBoundingBoxScaleFactor(self, arg0: float) -> None:
        """setBoundingBoxScaleFactor(self: depthai.SpatialLocationCalculatorConfig, arg0: float) -> None

        Set scale factor for bounding boxes used in spatial calculations.

        Parameter ``scaleFactor``:
            Scale factor must be in the interval (0,1].
        """
    def setCalculateSpatialKeypoints(self, arg0: bool) -> None:
        """setCalculateSpatialKeypoints(self: depthai.SpatialLocationCalculatorConfig, arg0: bool) -> None

        If false, spatial coordinates of keypoints will not be calculated.

        Parameter ``calculateSpatialKeypoints``:
            $.. warning::

        Only applicable to ImgDetections with keypoints.
        """
    def setCalculationAlgorithm(self, arg0: SpatialLocationCalculatorAlgorithm) -> None:
        """setCalculationAlgorithm(self: depthai.SpatialLocationCalculatorConfig, arg0: depthai.SpatialLocationCalculatorAlgorithm) -> None

        Set spatial location calculation algorithm. Possible values:

        - MEDIAN: Median of all depth values in the ROI - AVERAGE: Average of all depth
        values in the ROI - MIN: Minimum depth value in the ROI - MAX: Maximum depth
        value in the ROI - MODE: Most frequent depth value in the ROI
        """
    def setDepthThresholds(self, arg0: int, arg1: int) -> None:
        """setDepthThresholds(self: depthai.SpatialLocationCalculatorConfig, arg0: int, arg1: int) -> None

        Set the lower and upper depth value thresholds to be used in the spatial
        calculations.

        Parameter ``lowerThreshold``:
            Lower threshold in depth units (millimeter by default).

        Parameter ``upperThreshold``:
            Upper threshold in depth units (millimeter by default).
        """
    def setKeypointRadius(self, arg0: int) -> None:
        """setKeypointRadius(self: depthai.SpatialLocationCalculatorConfig, arg0: int) -> None

        Set radius around keypoints to calculate spatial coordinates.

        Parameter ``radius``:
            Radius in pixels. $.. warning::

        Only applicable to Keypoints or ImgDetections with keypoints.
        """
    def setROIs(self, ROIs: list[SpatialLocationCalculatorConfigData]) -> None:
        """setROIs(self: depthai.SpatialLocationCalculatorConfig, ROIs: list[depthai.SpatialLocationCalculatorConfigData]) -> None

        Specify additional regions of interest (ROI) to calculate their spatial
        coordinates. Results of ROI coordinates are available on
        SpatialLocationCalculatorData output.

        Parameter ``ROIs``:
            Vector of configuration parameters for ROIs (region of interests)
        """
    def setSegmentationPassthrough(self, arg0: bool) -> None:
        """setSegmentationPassthrough(self: depthai.SpatialLocationCalculatorConfig, arg0: bool) -> None

        Specify whether to passthrough segmentation mask along with spatial detections.

        Parameter ``passthroughSegmentation``:
            $.. warning::

        Only applicable to ImgDetections with segmentation masks.
        """
    def setStepSize(self, arg0: int) -> None:
        """setStepSize(self: depthai.SpatialLocationCalculatorConfig, arg0: int) -> None

        Set step size for spatial location calculation. Step size 1 means that every
        pixel is taken into calculation, size 2 means every second etc. for AVERAGE,
        MIN, MAX step size is 1; for MODE/MEDIAN it's 2.
        """
    def setUseSegmentation(self, arg0: bool) -> None:
        """setUseSegmentation(self: depthai.SpatialLocationCalculatorConfig, arg0: bool) -> None

        Specify whether to consider only segmented pixels within a detection bounding
        box for spatial calculations.

        Parameter ``useSegmentation``:
            $.. warning::

        Only applicable to ImgDetections with segmentation masks.
        """

class SpatialLocationCalculatorConfigData:
    """SpatialLocation configuration data structure"""
    calculationAlgorithm: SpatialLocationCalculatorAlgorithm
    depthThresholds: SpatialLocationCalculatorConfigThresholds
    roi: Rect
    def __init__(self) -> None:
        """__init__(self: depthai.SpatialLocationCalculatorConfigData) -> None"""

class SpatialLocationCalculatorConfigThresholds:
    """SpatialLocation configuration thresholds structure

    Contains configuration data for lower and upper threshold in depth units
    (millimeter by default) for ROI. Values outside of threshold range will be
    ignored when calculating spatial coordinates from depth map."""
    lowerThreshold: int
    upperThreshold: int
    def __init__(self) -> None:
        """__init__(self: depthai.SpatialLocationCalculatorConfigThresholds) -> None"""

class SpatialLocationCalculatorData(Buffer):
    """SpatialLocationCalculatorData message. Carries spatial information (X,Y,Z) and
    their configuration parameters"""
    spatialLocations: list[SpatialLocations]
    def __init__(self) -> None:
        """__init__(self: depthai.SpatialLocationCalculatorData) -> None"""
    def getSequenceNum(self) -> int:
        """getSequenceNum(self: depthai.SpatialLocationCalculatorData) -> int

        Retrieves image sequence number
        """
    def getSpatialLocations(self) -> list[SpatialLocations]:
        """getSpatialLocations(self: depthai.SpatialLocationCalculatorData) -> list[depthai.SpatialLocations]

        Retrieve configuration data for SpatialLocationCalculatorData.

        Returns:
            Vector of spatial location data, carrying spatial information (X,Y,Z)
        """
    def getTimestamp(self) -> datetime.timedelta:
        """getTimestamp(self: depthai.SpatialLocationCalculatorData) -> datetime.timedelta

        Retrieves timestamp related to dai::Clock::now()
        """
    def getTimestampDevice(self) -> datetime.timedelta:
        """getTimestampDevice(self: depthai.SpatialLocationCalculatorData) -> datetime.timedelta

        Retrieves timestamp directly captured from device's monotonic clock, not
        synchronized to host time. Used mostly for debugging
        """

class SpatialLocationCalculatorProperties:
    """Specify properties for SpatialLocationCalculator"""
    roiConfig: SpatialLocationCalculatorConfig
    def __init__(self, *args, **kwargs) -> None:
        """Initialize self.  See help(type(self)) for accurate signature."""

class SpatialLocations:
    """SpatialLocations structure

    Contains configuration data, average depth for the calculated ROI on depth map.
    Together with spatial coordinates: x,y,z relative to the center of depth map.
    Units are in depth units (millimeter by default)."""
    config: SpatialLocationCalculatorConfigData
    depthAverage: float
    depthAveragePixelCount: int
    depthMax: int
    depthMedian: float
    depthMin: int
    depthMode: float
    spatialCoordinates: Point3f
    def __init__(self) -> None:
        """__init__(self: depthai.SpatialLocations) -> None"""

class StereoDepthConfig(Buffer):
    """StereoDepthConfig message."""

    class AlgorithmControl:
        class DepthAlign:
            """Align the disparity/depth to the perspective of a rectified output, or center it

            Members:

              RECTIFIED_RIGHT : 

              RECTIFIED_LEFT : 

              CENTER : """
            __members__: ClassVar[dict] = ...  # read-only
            CENTER: ClassVar[StereoDepthConfig.AlgorithmControl.DepthAlign] = ...
            RECTIFIED_LEFT: ClassVar[StereoDepthConfig.AlgorithmControl.DepthAlign] = ...
            RECTIFIED_RIGHT: ClassVar[StereoDepthConfig.AlgorithmControl.DepthAlign] = ...
            __entries: ClassVar[dict] = ...
            def __init__(self, value: int) -> None:
                """__init__(self: depthai.StereoDepthConfig.AlgorithmControl.DepthAlign, value: int) -> None"""
            def __eq__(self, other: object) -> bool:
                """__eq__(self: object, other: object) -> bool"""
            def __hash__(self) -> int:
                """__hash__(self: object) -> int"""
            def __index__(self) -> int:
                """__index__(self: depthai.StereoDepthConfig.AlgorithmControl.DepthAlign) -> int"""
            def __int__(self) -> int:
                """__int__(self: depthai.StereoDepthConfig.AlgorithmControl.DepthAlign) -> int"""
            def __ne__(self, other: object) -> bool:
                """__ne__(self: object, other: object) -> bool"""
            @property
            def name(self) -> str:
                """name(self: object) -> str

                name(self: object) -> str
                """
            @property
            def value(self) -> int:
                """(arg0: depthai.StereoDepthConfig.AlgorithmControl.DepthAlign) -> int"""

        class DepthUnit:
            """Measurement unit for depth and calibration data.

            Members:

              METER

              CENTIMETER

              MILLIMETER

              INCH

              FOOT

              CUSTOM"""
            __members__: ClassVar[dict] = ...  # read-only
            CENTIMETER: ClassVar[LengthUnit] = ...
            CUSTOM: ClassVar[LengthUnit] = ...
            FOOT: ClassVar[LengthUnit] = ...
            INCH: ClassVar[LengthUnit] = ...
            METER: ClassVar[LengthUnit] = ...
            MILLIMETER: ClassVar[LengthUnit] = ...
            __entries: ClassVar[dict] = ...
            def __init__(self, value: int) -> None:
                """__init__(self: depthai.LengthUnit, value: int) -> None"""
            def __eq__(self, other: object) -> bool:
                """__eq__(self: object, other: object) -> bool"""
            def __hash__(self) -> int:
                """__hash__(self: object) -> int"""
            def __index__(self) -> int:
                """__index__(self: depthai.LengthUnit) -> int"""
            def __int__(self) -> int:
                """__int__(self: depthai.LengthUnit) -> int"""
            def __ne__(self, other: object) -> bool:
                """__ne__(self: object, other: object) -> bool"""
            @property
            def name(self) -> str:
                """name(self: object) -> str

                name(self: object) -> str
                """
            @property
            def value(self) -> int:
                """(arg0: depthai.LengthUnit) -> int"""
        centerAlignmentShiftFactor: float | None
        customDepthUnitMultiplier: float
        depthAlign: StereoDepthConfig.AlgorithmControl.DepthAlign
        depthUnit: LengthUnit
        disparityShift: int
        enableExtended: bool
        enableLeftRightCheck: bool
        enableSubpixel: bool
        enableSwLeftRightCheck: bool
        leftRightCheckThreshold: int
        numInvalidateEdgePixels: int
        subpixelFractionalBits: int
        def __init__(self) -> None:
            """__init__(self: depthai.StereoDepthConfig.AlgorithmControl) -> None"""

    class CensusTransform:
        """The basic cost function used by the Stereo Accelerator for matching the left and
        right images is the Census Transform. It works on a block of pixels and computes
        a bit vector which represents the structure of the image in that block. There
        are two types of Census Transform based on how the middle pixel is used: Classic
        Approach and Modified Census. The comparisons that are made between pixels can
        be or not thresholded. In some cases a mask can be applied to filter out only
        specific bits from the entire bit stream. All these approaches are: Classic
        Approach: Uses middle pixel to compare against all its neighbors over a defined
        window. Each comparison results in a new bit, that is 0 if central pixel is
        smaller, or 1 if is it bigger than its neighbor. Modified Census Transform: same
        as classic Census Transform, but instead of comparing central pixel with its
        neighbors, the window mean will be compared with each pixel over the window.
        Thresholding Census Transform: same as classic Census Transform, but it is not
        enough that a neighbor pixel to be bigger than the central pixel, it must be
        significant bigger (based on a threshold). Census Transform with Mask: same as

        classic Census Transform, but in this case not all of the pixel from the support
        window are part of the binary descriptor. We use a ma sk “M” to define which
        pixels are part of the binary descriptor (1), and which pixels should be skipped
        (0)."""

        class KernelSize:
            """Census transform kernel size possible values.

            Members:

              AUTO : 

              KERNEL_5x5 : 

              KERNEL_7x7 : 

              KERNEL_7x9 : """
            __members__: ClassVar[dict] = ...  # read-only
            AUTO: ClassVar[StereoDepthConfig.CensusTransform.KernelSize] = ...
            KERNEL_5x5: ClassVar[StereoDepthConfig.CensusTransform.KernelSize] = ...
            KERNEL_7x7: ClassVar[StereoDepthConfig.CensusTransform.KernelSize] = ...
            KERNEL_7x9: ClassVar[StereoDepthConfig.CensusTransform.KernelSize] = ...
            __entries: ClassVar[dict] = ...
            def __init__(self, value: int) -> None:
                """__init__(self: depthai.StereoDepthConfig.CensusTransform.KernelSize, value: int) -> None"""
            def __eq__(self, other: object) -> bool:
                """__eq__(self: object, other: object) -> bool"""
            def __hash__(self) -> int:
                """__hash__(self: object) -> int"""
            def __index__(self) -> int:
                """__index__(self: depthai.StereoDepthConfig.CensusTransform.KernelSize) -> int"""
            def __int__(self) -> int:
                """__int__(self: depthai.StereoDepthConfig.CensusTransform.KernelSize) -> int"""
            def __ne__(self, other: object) -> bool:
                """__ne__(self: object, other: object) -> bool"""
            @property
            def name(self) -> str:
                """name(self: object) -> str

                name(self: object) -> str
                """
            @property
            def value(self) -> int:
                """(arg0: depthai.StereoDepthConfig.CensusTransform.KernelSize) -> int"""
        enableMeanMode: bool
        kernelMask: int
        kernelSize: StereoDepthConfig.CensusTransform.KernelSize
        noiseThresholdOffset: int
        noiseThresholdScale: int
        threshold: int
        def __init__(self) -> None:
            """__init__(self: depthai.StereoDepthConfig.CensusTransform) -> None"""

    class ConfidenceMetrics:
        flatnessConfidenceThreshold: int
        flatnessConfidenceWeight: int
        flatnessOverride: bool
        motionVectorConfidenceThreshold: int
        motionVectorConfidenceWeight: int
        occlusionConfidenceWeight: int
        def __init__(self) -> None:
            """__init__(self: depthai.StereoDepthConfig.ConfidenceMetrics) -> None"""

    class CostAggregation:
        """Cost Aggregation is based on Semi Global Block Matching (SGBM). This algorithm
        uses a semi global technique to aggregate the cost map. Ultimately the idea is
        to build inertia into the stereo algorithm. If a pixel has very little texture
        information, then odds are the correct disparity for this pixel is close to that
        of the previous pixel considered. This means that we get improved results in
        areas with low texture."""

        class P1Config:
            """Structure for adaptive P1 penalty configuration."""
            defaultValue: int
            edgeThreshold: int
            edgeValue: int
            enableAdaptive: bool
            smoothThreshold: int
            smoothValue: int
            def __init__(self) -> None:
                """__init__(self: depthai.StereoDepthConfig.CostAggregation.P1Config) -> None"""

        class P2Config:
            """Structure for adaptive P2 penalty configuration."""
            defaultValue: int
            edgeValue: int
            enableAdaptive: bool
            smoothValue: int
            def __init__(self) -> None:
                """__init__(self: depthai.StereoDepthConfig.CostAggregation.P2Config) -> None"""
        divisionFactor: int
        horizontalPenaltyCostP1: int
        horizontalPenaltyCostP2: int
        p1Config: StereoDepthConfig.CostAggregation.P1Config
        p2Config: StereoDepthConfig.CostAggregation.P2Config
        verticalPenaltyCostP1: int
        verticalPenaltyCostP2: int
        def __init__(self) -> None:
            """__init__(self: depthai.StereoDepthConfig.CostAggregation) -> None"""

    class CostMatching:
        """The matching cost is way of measuring the similarity of image locations in
        stereo correspondence algorithm. Based on the configuration parameters and based
        on the descriptor type, a linear equation is applied to computing the cost for
        each candidate disparity at each pixel."""

        class DisparityWidth:
            """Disparity search range: 64 or 96 pixels are supported by the HW.

            Members:

              DISPARITY_64 : 

              DISPARITY_96 : """
            __members__: ClassVar[dict] = ...  # read-only
            DISPARITY_64: ClassVar[StereoDepthConfig.CostMatching.DisparityWidth] = ...
            DISPARITY_96: ClassVar[StereoDepthConfig.CostMatching.DisparityWidth] = ...
            __entries: ClassVar[dict] = ...
            def __init__(self, value: int) -> None:
                """__init__(self: depthai.StereoDepthConfig.CostMatching.DisparityWidth, value: int) -> None"""
            def __eq__(self, other: object) -> bool:
                """__eq__(self: object, other: object) -> bool"""
            def __hash__(self) -> int:
                """__hash__(self: object) -> int"""
            def __index__(self) -> int:
                """__index__(self: depthai.StereoDepthConfig.CostMatching.DisparityWidth) -> int"""
            def __int__(self) -> int:
                """__int__(self: depthai.StereoDepthConfig.CostMatching.DisparityWidth) -> int"""
            def __ne__(self, other: object) -> bool:
                """__ne__(self: object, other: object) -> bool"""
            @property
            def name(self) -> str:
                """name(self: object) -> str

                name(self: object) -> str
                """
            @property
            def value(self) -> int:
                """(arg0: depthai.StereoDepthConfig.CostMatching.DisparityWidth) -> int"""

        class LinearEquationParameters:
            """The linear equation applied for computing the cost is: COMB_COST = α*AD +
            β*(CTC<<3). CLAMP(COMB_COST >> 5, threshold). Where AD is the Absolute
            Difference between 2 pixels values. CTC is the Census Transform Cost between 2
            pixels, based on Hamming distance (xor). The α and β parameters are subject to
            fine tuning by the user."""
            alpha: int
            beta: int
            threshold: int
            def __init__(self) -> None:
                """__init__(self: depthai.StereoDepthConfig.CostMatching.LinearEquationParameters) -> None"""
        confidenceThreshold: int
        disparityWidth: StereoDepthConfig.CostMatching.DisparityWidth
        enableCompanding: bool
        enableSwConfidenceThresholding: bool
        invalidDisparityValue: int
        linearEquationParameters: StereoDepthConfig.CostMatching.LinearEquationParameters
        def __init__(self) -> None:
            """__init__(self: depthai.StereoDepthConfig.CostMatching) -> None"""

    class PostProcessing:
        """Post-processing filters, all the filters are applied in disparity domain."""

        class AdaptiveMedianFilter:
            confidenceThreshold: int
            enable: bool
            def __init__(self) -> None:
                """__init__(self: depthai.StereoDepthConfig.PostProcessing.AdaptiveMedianFilter) -> None"""

        class BrightnessFilter:
            """Brightness filtering. If input frame pixel is too dark or too bright, disparity
            will be invalidated. The idea is that for too dark/too bright pixels we have low
            confidence, since that area was under/over exposed and details were lost."""
            maxBrightness: int
            minBrightness: int
            def __init__(self) -> None:
                """__init__(self: depthai.StereoDepthConfig.PostProcessing.BrightnessFilter) -> None"""

        class DecimationFilter:
            """Decimation filter. Reduces the depth scene complexity. The filter runs on kernel
            sizes [2x2] to [8x8] pixels."""

            class DecimationMode:
                """Decimation algorithm type.

                Members:

                  PIXEL_SKIPPING : 

                  NON_ZERO_MEDIAN : 

                  NON_ZERO_MEAN : """
                __members__: ClassVar[dict] = ...  # read-only
                NON_ZERO_MEAN: ClassVar[StereoDepthConfig.PostProcessing.DecimationFilter.DecimationMode] = ...
                NON_ZERO_MEDIAN: ClassVar[StereoDepthConfig.PostProcessing.DecimationFilter.DecimationMode] = ...
                PIXEL_SKIPPING: ClassVar[StereoDepthConfig.PostProcessing.DecimationFilter.DecimationMode] = ...
                __entries: ClassVar[dict] = ...
                def __init__(self, value: int) -> None:
                    """__init__(self: depthai.StereoDepthConfig.PostProcessing.DecimationFilter.DecimationMode, value: int) -> None"""
                def __eq__(self, other: object) -> bool:
                    """__eq__(self: object, other: object) -> bool"""
                def __hash__(self) -> int:
                    """__hash__(self: object) -> int"""
                def __index__(self) -> int:
                    """__index__(self: depthai.StereoDepthConfig.PostProcessing.DecimationFilter.DecimationMode) -> int"""
                def __int__(self) -> int:
                    """__int__(self: depthai.StereoDepthConfig.PostProcessing.DecimationFilter.DecimationMode) -> int"""
                def __ne__(self, other: object) -> bool:
                    """__ne__(self: object, other: object) -> bool"""
                @property
                def name(self) -> str:
                    """name(self: object) -> str

                    name(self: object) -> str
                    """
                @property
                def value(self) -> int:
                    """(arg0: depthai.StereoDepthConfig.PostProcessing.DecimationFilter.DecimationMode) -> int"""
            decimationFactor: int
            decimationMode: StereoDepthConfig.PostProcessing.DecimationFilter.DecimationMode
            def __init__(self) -> None:
                """__init__(self: depthai.StereoDepthConfig.PostProcessing.DecimationFilter) -> None"""

        class Filter:
            """

            Members:

              NONE : 

              DECIMATION : 

              SPECKLE : 

              MEDIAN : 

              SPATIAL : 

              TEMPORAL : """
            __members__: ClassVar[dict] = ...  # read-only
            DECIMATION: ClassVar[StereoDepthConfig.PostProcessing.Filter] = ...
            MEDIAN: ClassVar[StereoDepthConfig.PostProcessing.Filter] = ...
            NONE: ClassVar[StereoDepthConfig.PostProcessing.Filter] = ...
            SPATIAL: ClassVar[StereoDepthConfig.PostProcessing.Filter] = ...
            SPECKLE: ClassVar[StereoDepthConfig.PostProcessing.Filter] = ...
            TEMPORAL: ClassVar[StereoDepthConfig.PostProcessing.Filter] = ...
            __entries: ClassVar[dict] = ...
            def __init__(self, value: int) -> None:
                """__init__(self: depthai.StereoDepthConfig.PostProcessing.Filter, value: int) -> None"""
            def __eq__(self, other: object) -> bool:
                """__eq__(self: object, other: object) -> bool"""
            def __hash__(self) -> int:
                """__hash__(self: object) -> int"""
            def __index__(self) -> int:
                """__index__(self: depthai.StereoDepthConfig.PostProcessing.Filter) -> int"""
            def __int__(self) -> int:
                """__int__(self: depthai.StereoDepthConfig.PostProcessing.Filter) -> int"""
            def __ne__(self, other: object) -> bool:
                """__ne__(self: object, other: object) -> bool"""
            @property
            def name(self) -> str:
                """name(self: object) -> str

                name(self: object) -> str
                """
            @property
            def value(self) -> int:
                """(arg0: depthai.StereoDepthConfig.PostProcessing.Filter) -> int"""

        class HoleFilling:
            enable: bool
            fillConfidenceThreshold: int
            highConfidenceThreshold: int
            invalidateDisparities: bool
            minValidDisparity: int
            def __init__(self) -> None:
                """__init__(self: depthai.StereoDepthConfig.PostProcessing.HoleFilling) -> None"""

        class ThresholdFilter:
            """Threshold filtering. Filters out distances outside of a given interval."""
            maxRange: int
            minRange: int
            def __init__(self) -> None:
                """__init__(self: depthai.StereoDepthConfig.PostProcessing.ThresholdFilter) -> None"""
        MedianFilter: ClassVar[type[filters.params.MedianFilter]] = ...
        SpatialFilter: ClassVar[type[filters.params.SpatialFilter]] = ...
        SpeckleFilter: ClassVar[type[filters.params.SpeckleFilter]] = ...
        TemporalFilter: ClassVar[type[filters.params.TemporalFilter]] = ...
        adaptiveMedianFilter: StereoDepthConfig.PostProcessing.AdaptiveMedianFilter
        bilateralSigmaValue: int
        brightnessFilter: StereoDepthConfig.PostProcessing.BrightnessFilter
        decimationFilter: StereoDepthConfig.PostProcessing.DecimationFilter
        filteringOrder: Incomplete
        holeFilling: StereoDepthConfig.PostProcessing.HoleFilling
        median: filters.params.MedianFilter
        spatialFilter: filters.params.SpatialFilter
        speckleFilter: filters.params.SpeckleFilter
        temporalFilter: filters.params.TemporalFilter
        thresholdFilter: StereoDepthConfig.PostProcessing.ThresholdFilter
        def __init__(self) -> None:
            """__init__(self: depthai.StereoDepthConfig.PostProcessing) -> None"""
    MedianFilter: ClassVar[type[filters.params.MedianFilter]] = ...
    algorithmControl: StereoDepthConfig.AlgorithmControl
    censusTransform: StereoDepthConfig.CensusTransform
    confidenceMetrics: StereoDepthConfig.ConfidenceMetrics
    costAggregation: StereoDepthConfig.CostAggregation
    costMatching: StereoDepthConfig.CostMatching
    postProcessing: StereoDepthConfig.PostProcessing
    @overload
    def __init__(self) -> None:
        """__init__(*args, **kwargs)
        Overloaded function.

        1. __init__(self: depthai.StereoDepthConfig) -> None

        2. __init__(self: depthai.StereoDepthConfig) -> None
        """
    @overload
    def __init__(self) -> None:
        """__init__(*args, **kwargs)
        Overloaded function.

        1. __init__(self: depthai.StereoDepthConfig) -> None

        2. __init__(self: depthai.StereoDepthConfig) -> None
        """
    def getBilateralFilterSigma(self) -> int:
        """getBilateralFilterSigma(self: depthai.StereoDepthConfig) -> int

        Get sigma value for 5x5 bilateral filter
        """
    def getConfidenceThreshold(self) -> int:
        """getConfidenceThreshold(self: depthai.StereoDepthConfig) -> int

        Get confidence threshold for disparity calculation
        """
    def getCustomDepthUnitMultiplier(self) -> float:
        """getCustomDepthUnitMultiplier(self: depthai.StereoDepthConfig) -> float

        Get custom depth unit multiplier relative to 1 meter.
        """
    def getDepthUnit(self) -> LengthUnit:
        """getDepthUnit(self: depthai.StereoDepthConfig) -> depthai.LengthUnit

        Get depth unit of depth map.
        """
    def getExtendedDisparity(self) -> bool:
        """getExtendedDisparity(self: depthai.StereoDepthConfig) -> bool

        Get extended disparity setting
        """
    def getFiltersComputeBackend(self) -> ProcessorType:
        """getFiltersComputeBackend(self: depthai.StereoDepthConfig) -> depthai.ProcessorType

        Get filters compute backend
        """
    def getLeftRightCheck(self) -> bool:
        """getLeftRightCheck(self: depthai.StereoDepthConfig) -> bool

        Get left-right check setting
        """
    def getLeftRightCheckThreshold(self) -> int:
        """getLeftRightCheckThreshold(self: depthai.StereoDepthConfig) -> int

        Get threshold for left-right check combine
        """
    def getMaxDisparity(self) -> float:
        """getMaxDisparity(self: depthai.StereoDepthConfig) -> float

        Useful for normalization of the disparity map.

        Returns:
            Maximum disparity value that the node can return
        """
    def getMedianFilter(self) -> filters.params.MedianFilter:
        """getMedianFilter(self: depthai.StereoDepthConfig) -> depthai.filters.params.MedianFilter

        Get median filter setting
        """
    def getSubpixel(self) -> bool:
        """getSubpixel(self: depthai.StereoDepthConfig) -> bool

        Get subpixel setting
        """
    def getSubpixelFractionalBits(self) -> int:
        """getSubpixelFractionalBits(self: depthai.StereoDepthConfig) -> int

        Get number of fractional bits for subpixel mode
        """
    def setBilateralFilterSigma(self, sigma: int) -> StereoDepthConfig:
        """setBilateralFilterSigma(self: depthai.StereoDepthConfig, sigma: int) -> depthai.StereoDepthConfig

        A larger value of the parameter means that farther colors within the pixel
        neighborhood will be mixed together, resulting in larger areas of semi-equal
        color.

        Parameter ``sigma``:
            Set sigma value for 5x5 bilateral filter. 0..65535
        """
    def setConfidenceThreshold(self, confThr: int) -> StereoDepthConfig:
        """setConfidenceThreshold(self: depthai.StereoDepthConfig, confThr: int) -> depthai.StereoDepthConfig

        Confidence threshold for disparity calculation

        Parameter ``confThr``:
            Confidence threshold value 0..255
        """
    def setCustomDepthUnitMultiplier(self, arg0: float) -> StereoDepthConfig:
        """setCustomDepthUnitMultiplier(self: depthai.StereoDepthConfig, arg0: float) -> depthai.StereoDepthConfig

        Set custom depth unit multiplier relative to 1 meter.
        """
    def setDepthAlign(self, align: StereoDepthConfig.AlgorithmControl.DepthAlign) -> StereoDepthConfig:
        """setDepthAlign(self: depthai.StereoDepthConfig, align: depthai.StereoDepthConfig.AlgorithmControl.DepthAlign) -> depthai.StereoDepthConfig

        Parameter ``align``:
            Set the disparity/depth alignment: centered (between the 'left' and 'right'
            inputs), or from the perspective of a rectified output stream
        """
    def setDepthUnit(self, arg0: LengthUnit) -> StereoDepthConfig:
        """setDepthUnit(self: depthai.StereoDepthConfig, arg0: depthai.LengthUnit) -> depthai.StereoDepthConfig

        Set depth unit of depth map.

        Meter, centimeter, millimeter, inch, foot or custom unit is available.
        """
    def setDisparityShift(self, arg0: int) -> StereoDepthConfig:
        """setDisparityShift(self: depthai.StereoDepthConfig, arg0: int) -> depthai.StereoDepthConfig

        Shift input frame by a number of pixels to increase minimum depth. For example
        shifting by 48 will change effective disparity search range from (0,95] to
        [48,143]. An alternative approach to reducing the minZ. We normally only
        recommend doing this when it is known that there will be no objects farther away
        than MaxZ, such as having a depth camera mounted above a table pointing down at
        the table surface.
        """
    def setExtendedDisparity(self, enable: bool) -> StereoDepthConfig:
        """setExtendedDisparity(self: depthai.StereoDepthConfig, enable: bool) -> depthai.StereoDepthConfig

        Disparity range increased from 95 to 190, combined from full resolution and
        downscaled images. Suitable for short range objects
        """
    def setFiltersComputeBackend(self, filtersBackend: ProcessorType) -> StereoDepthConfig:
        """setFiltersComputeBackend(self: depthai.StereoDepthConfig, filtersBackend: depthai.ProcessorType) -> depthai.StereoDepthConfig

        Set filters compute backend
        """
    def setLeftRightCheck(self, enable: bool) -> StereoDepthConfig:
        """setLeftRightCheck(self: depthai.StereoDepthConfig, enable: bool) -> depthai.StereoDepthConfig

        Computes and combines disparities in both L-R and R-L directions, and combine
        them.

        For better occlusion handling, discarding invalid disparity values
        """
    def setLeftRightCheckThreshold(self, sigma: int) -> StereoDepthConfig:
        """setLeftRightCheckThreshold(self: depthai.StereoDepthConfig, sigma: int) -> depthai.StereoDepthConfig

        Parameter ``threshold``:
            Set threshold for left-right, right-left disparity map combine, 0..255
        """
    def setMedianFilter(self, median: filters.params.MedianFilter) -> StereoDepthConfig:
        """setMedianFilter(self: depthai.StereoDepthConfig, median: depthai.filters.params.MedianFilter) -> depthai.StereoDepthConfig

        Parameter ``median``:
            Set kernel size for disparity/depth median filtering, or disable
        """
    def setNumInvalidateEdgePixels(self, arg0: int) -> StereoDepthConfig:
        """setNumInvalidateEdgePixels(self: depthai.StereoDepthConfig, arg0: int) -> depthai.StereoDepthConfig

        Invalidate X amount of pixels at the edge of disparity frame. For right and
        center alignment X pixels will be invalidated from the right edge, for left
        alignment from the left edge.
        """
    def setSubpixel(self, enable: bool) -> StereoDepthConfig:
        """setSubpixel(self: depthai.StereoDepthConfig, enable: bool) -> depthai.StereoDepthConfig

        Computes disparity with sub-pixel interpolation (3 fractional bits by default).

        Suitable for long range. Currently incompatible with extended disparity
        """
    def setSubpixelFractionalBits(self, subpixelFractionalBits: int) -> StereoDepthConfig:
        """setSubpixelFractionalBits(self: depthai.StereoDepthConfig, subpixelFractionalBits: int) -> depthai.StereoDepthConfig

        Number of fractional bits for subpixel mode. Default value: 3. Valid values:
        3,4,5. Defines the number of fractional disparities: 2^x. Median filter
        postprocessing is supported only for 3 fractional bits.
        """

class StereoDepthProperties:
    """Specify properties for StereoDepth"""

    class RectificationMesh:
        meshLeftUri: str
        meshRightUri: str
        meshSize: int | None
        stepHeight: int
        stepWidth: int
        def __init__(self, *args, **kwargs) -> None:
            """Initialize self.  See help(type(self)) for accurate signature."""
    alphaScaling: float | None
    baseline: float | None
    depthAlignCamera: CameraBoardSocket
    depthAlignmentUseSpecTranslation: bool | None
    disparityToDepthUseSpecTranslation: bool | None
    enableRectification: bool
    enableRuntimeStereoModeSwitch: bool
    focalLength: float | None
    focalLengthFromCalibration: bool
    height: int | None
    initialConfig: StereoDepthConfig
    mesh: StereoDepthProperties.RectificationMesh
    numFramesPool: int
    numPostProcessingMemorySlices: int
    numPostProcessingShaves: int
    outHeight: int | None
    outKeepAspectRatio: bool
    outWidth: int | None
    rectificationUseSpecTranslation: bool | None
    rectifyEdgeFillColor: int
    useHomographyRectification: bool | None
    width: int | None
    def __init__(self, *args, **kwargs) -> None:
        """Initialize self.  See help(type(self)) for accurate signature."""

class StereoPair:
    """Describes which camera sockets can be used for stereo and their baseline."""
    baseline: float
    isVertical: bool
    left: CameraBoardSocket
    right: CameraBoardSocket
    def __init__(self) -> None:
        """__init__(self: depthai.StereoPair) -> None"""

class StereoRectification:
    """StereoRectification structure"""
    leftCameraSocket: CameraBoardSocket
    rectifiedRotationLeft: list[list[float]]
    rectifiedRotationRight: list[list[float]]
    rightCameraSocket: CameraBoardSocket
    def __init__(self) -> None:
        """__init__(self: depthai.StereoRectification) -> None"""

class SyncProperties:
    """Specify properties for Sync."""
    processor: ProcessorType
    syncAttempts: int
    syncThresholdNs: int
    def __init__(self, *args, **kwargs) -> None:
        """Initialize self.  See help(type(self)) for accurate signature."""

class SystemInformation(Buffer):
    """SystemInformation message. Carries memory usage, cpu usage and chip
    temperatures."""
    chipTemperature: ChipTemperature
    cmxMemoryUsage: MemoryInfo
    ddrMemoryUsage: MemoryInfo
    leonCssCpuUsage: CpuUsage
    leonCssMemoryUsage: MemoryInfo
    leonMssCpuUsage: CpuUsage
    leonMssMemoryUsage: MemoryInfo
    def __init__(self) -> None:
        """__init__(self: depthai.SystemInformation) -> None"""

class SystemInformationRVC4(Buffer):
    """SystemInformation message Carries memory usage, cpu usage and chip temperatures."""
    chipTemperature: ChipTemperatureRVC4
    cpuAvgUsage: CpuUsage
    cpuUsages: list[CpuUsage]
    ddrMemoryUsage: MemoryInfo
    processCpuAvgUsage: CpuUsage
    processMemoryUsage: int
    def __init__(self) -> None:
        """__init__(self: depthai.SystemInformationRVC4) -> None"""

class SystemLoggerProperties:
    """SystemLoggerProperties structure"""
    rateHz: float
    def __init__(self, *args, **kwargs) -> None:
        """Initialize self.  See help(type(self)) for accurate signature."""

class TensorInfo:
    """TensorInfo structure"""

    class DataType:
        """Members:

          FP16

          U8F

          INT

          FP32

          I8

          FP64"""
        __members__: ClassVar[dict] = ...  # read-only
        FP16: ClassVar[TensorInfo.DataType] = ...
        FP32: ClassVar[TensorInfo.DataType] = ...
        FP64: ClassVar[TensorInfo.DataType] = ...
        I8: ClassVar[TensorInfo.DataType] = ...
        INT: ClassVar[TensorInfo.DataType] = ...
        U8F: ClassVar[TensorInfo.DataType] = ...
        __entries: ClassVar[dict] = ...
        def __init__(self, value: int) -> None:
            """__init__(self: depthai.TensorInfo.DataType, value: int) -> None"""
        def __eq__(self, other: object) -> bool:
            """__eq__(self: object, other: object) -> bool"""
        def __hash__(self) -> int:
            """__hash__(self: object) -> int"""
        def __index__(self) -> int:
            """__index__(self: depthai.TensorInfo.DataType) -> int"""
        def __int__(self) -> int:
            """__int__(self: depthai.TensorInfo.DataType) -> int"""
        def __ne__(self, other: object) -> bool:
            """__ne__(self: object, other: object) -> bool"""
        @property
        def name(self) -> str:
            """name(self: object) -> str

            name(self: object) -> str
            """
        @property
        def value(self) -> int:
            """(arg0: depthai.TensorInfo.DataType) -> int"""

    class StorageOrder:
        """Members:

          NHWC

          NHCW

          NCHW

          HWC

          CHW

          WHC

          HCW

          WCH

          CWH

          NC

          CN

          C

          H

          W"""
        __members__: ClassVar[dict] = ...  # read-only
        C: ClassVar[TensorInfo.StorageOrder] = ...
        CHW: ClassVar[TensorInfo.StorageOrder] = ...
        CN: ClassVar[TensorInfo.StorageOrder] = ...
        CWH: ClassVar[TensorInfo.StorageOrder] = ...
        H: ClassVar[TensorInfo.StorageOrder] = ...
        HCW: ClassVar[TensorInfo.StorageOrder] = ...
        HWC: ClassVar[TensorInfo.StorageOrder] = ...
        NC: ClassVar[TensorInfo.StorageOrder] = ...
        NCHW: ClassVar[TensorInfo.StorageOrder] = ...
        NHCW: ClassVar[TensorInfo.StorageOrder] = ...
        NHWC: ClassVar[TensorInfo.StorageOrder] = ...
        W: ClassVar[TensorInfo.StorageOrder] = ...
        WCH: ClassVar[TensorInfo.StorageOrder] = ...
        WHC: ClassVar[TensorInfo.StorageOrder] = ...
        __entries: ClassVar[dict] = ...
        def __init__(self, value: int) -> None:
            """__init__(self: depthai.TensorInfo.StorageOrder, value: int) -> None"""
        def __eq__(self, other: object) -> bool:
            """__eq__(self: object, other: object) -> bool"""
        def __hash__(self) -> int:
            """__hash__(self: object) -> int"""
        def __index__(self) -> int:
            """__index__(self: depthai.TensorInfo.StorageOrder) -> int"""
        def __int__(self) -> int:
            """__int__(self: depthai.TensorInfo.StorageOrder) -> int"""
        def __ne__(self, other: object) -> bool:
            """__ne__(self: object, other: object) -> bool"""
        @property
        def name(self) -> str:
            """name(self: object) -> str

            name(self: object) -> str
            """
        @property
        def value(self) -> int:
            """(arg0: depthai.TensorInfo.StorageOrder) -> int"""
    dataType: TensorInfo.DataType
    dims: list[int]
    name: str
    numDimensions: int
    offset: int
    order: TensorInfo.StorageOrder
    qpScale: float
    qpZp: float
    quantization: bool
    strides: list[int]
    def __init__(self) -> None:
        """__init__(self: depthai.TensorInfo) -> None"""
    def getChannelStride(self) -> int:
        """getChannelStride(self: depthai.TensorInfo) -> int"""
    def getChannels(self) -> int:
        """getChannels(self: depthai.TensorInfo) -> int"""
    def getDataTypeSize(self) -> int:
        """getDataTypeSize(self: depthai.TensorInfo) -> int"""
    def getHeight(self) -> int:
        """getHeight(self: depthai.TensorInfo) -> int"""
    def getHeightStride(self) -> int:
        """getHeightStride(self: depthai.TensorInfo) -> int"""
    def getTensorSize(self) -> int:
        """getTensorSize(self: depthai.TensorInfo) -> int"""
    def getWidth(self) -> int:
        """getWidth(self: depthai.TensorInfo) -> int"""
    def getWidthStride(self) -> int:
        """getWidthStride(self: depthai.TensorInfo) -> int"""

class TextAnnotation:
    backgroundColor: Color
    fontSize: float
    position: Point2f
    text: str
    textColor: Color
    def __init__(self) -> None:
        """__init__(self: depthai.TextAnnotation) -> None"""

class ThermalAmbientParams:
    """Ambient factors that affect the temperature measurement of a Thermal sensor."""
    atmosphericTemperature: int | None
    atmosphericTransmittance: int | None
    distance: int | None
    gainMode: ThermalGainMode | None
    reflectionTemperature: int | None
    targetEmissivity: int | None
    def __init__(self) -> None:
        """__init__(self: depthai.ThermalAmbientParams) -> None"""

class ThermalConfig(Buffer):
    """ThermalConfig message. Currently unused."""
    ambientParams: ThermalAmbientParams
    ffcParams: ThermalFFCParams
    imageParams: ThermalImageParams
    def __init__(self) -> None:
        """__init__(self: depthai.ThermalConfig) -> None"""

class ThermalFFCParams:
    antiFallProtectionThresholdHighGainMode: int | None
    antiFallProtectionThresholdLowGainMode: int | None
    autoFFC: bool | None
    autoFFCTempThreshold: int | None
    closeManualShutter: bool | None
    fallProtection: bool | None
    maxFFCInterval: int | None
    minFFCInterval: int | None
    minShutterInterval: int | None
    def __init__(self) -> None:
        """__init__(self: depthai.ThermalFFCParams) -> None"""

class ThermalGainMode:
    """Thermal sensor gain mode. Use low gain in high energy environments.

    Members:

      LOW : 

      HIGH : """
    __members__: ClassVar[dict] = ...  # read-only
    HIGH: ClassVar[ThermalGainMode] = ...
    LOW: ClassVar[ThermalGainMode] = ...
    __entries: ClassVar[dict] = ...
    def __init__(self, value: int) -> None:
        """__init__(self: depthai.ThermalGainMode, value: int) -> None"""
    def __eq__(self, other: object) -> bool:
        """__eq__(self: object, other: object) -> bool"""
    def __hash__(self) -> int:
        """__hash__(self: object) -> int"""
    def __index__(self) -> int:
        """__index__(self: depthai.ThermalGainMode) -> int"""
    def __int__(self) -> int:
        """__int__(self: depthai.ThermalGainMode) -> int"""
    def __ne__(self, other: object) -> bool:
        """__ne__(self: object, other: object) -> bool"""
    @property
    def name(self) -> str:
        """name(self: object) -> str

        name(self: object) -> str
        """
    @property
    def value(self) -> int:
        """(arg0: depthai.ThermalGainMode) -> int"""

class ThermalImageParams:
    brightnessLevel: int | None
    contrastLevel: int | None
    digitalDetailEnhanceLevel: int | None
    orientation: Incomplete
    spatialNoiseFilterLevel: int | None
    timeNoiseFilterLevel: int | None
    def __init__(self) -> None:
        """__init__(self: depthai.ThermalImageParams) -> None"""

class ThermalProperties:
    """Specify properties for Thermal"""
    boardSocket: CameraBoardSocket
    fps: float
    initialConfig: ThermalConfig
    numFramesPool: int
    def __init__(self, *args, **kwargs) -> None:
        """Initialize self.  See help(type(self)) for accurate signature."""

class ThreadedNode(Node):
    def __init__(self, *args, **kwargs) -> None:
        """Initialize self.  See help(type(self)) for accurate signature."""
    def blockEvent(self, type: PipelineEvent.Type, source: str) -> BlockPipelineEvent:
        """blockEvent(self: depthai.ThreadedNode, type: depthai.PipelineEvent.Type, source: str) -> depthai.BlockPipelineEvent

        Creates a scoped event that sends start and end events for a custom block event

        Parameter ``type``:
            Type of the event

        Parameter ``source``:
            Source name of the event
        """
    def critical(self, arg0: str) -> None:
        """critical(self: depthai.ThreadedNode, arg0: str) -> None"""
    def debug(self, arg0: str) -> None:
        """debug(self: depthai.ThreadedNode, arg0: str) -> None"""
    def error(self, arg0: str) -> None:
        """error(self: depthai.ThreadedNode, arg0: str) -> None"""
    def getLogLevel(self) -> LogLevel:
        """getLogLevel(self: depthai.ThreadedNode) -> depthai.LogLevel

        Gets the logging severity level for this node.

        Returns:
            Logging severity level
        """
    def info(self, arg0: str) -> None:
        """info(self: depthai.ThreadedNode, arg0: str) -> None"""
    def inputBlockEvent(self) -> BlockPipelineEvent:
        """inputBlockEvent(self: depthai.ThreadedNode) -> depthai.BlockPipelineEvent

        Creates a scoped event that sends start and end events for the getting inputs
        state
        """
    def isRunning(self) -> bool:
        """isRunning(self: depthai.ThreadedNode) -> bool"""
    def mainLoop(self) -> bool:
        """mainLoop(self: depthai.ThreadedNode) -> bool"""
    def outputBlockEvent(self) -> BlockPipelineEvent:
        """outputBlockEvent(self: depthai.ThreadedNode) -> depthai.BlockPipelineEvent

        Creates a scoped event that sends start and end events for the sending outputs
        state
        """
    def setLogLevel(self, arg0: LogLevel) -> None:
        """setLogLevel(self: depthai.ThreadedNode, arg0: depthai.LogLevel) -> None

        Sets the logging severity level for this node.

        Parameter ``level``:
            Logging severity level
        """
    def trace(self, arg0: str) -> None:
        """trace(self: depthai.ThreadedNode, arg0: str) -> None"""
    def warn(self, arg0: str) -> None:
        """warn(self: depthai.ThreadedNode, arg0: str) -> None"""
    @property
    def pipelineEventOutput(self) -> Node.Output:
        """(self: depthai.ThreadedNode) -> depthai.Node.Output"""

class Timestamp:
    """Timestamp structure"""
    nsec: int
    sec: int
    def __init__(self) -> None:
        """__init__(self: depthai.Timestamp) -> None"""
    def get(self) -> datetime.timedelta:
        """get(self: depthai.Timestamp) -> datetime.timedelta"""

class ToFConfig(Buffer):
    """ToFConfig message. Carries config for feature tracking algorithm"""
    enableBurstMode: bool
    enableDistortionCorrection: bool
    enableFPPNCorrection: bool | None
    enableOpticalCorrection: bool | None
    enablePhaseShuffleTemporalFilter: bool
    enablePhaseUnwrapping: bool | None
    enableTemperatureCorrection: bool | None
    enableWiggleCorrection: bool | None
    median: filters.params.MedianFilter
    phaseUnwrapErrorThreshold: int
    phaseUnwrappingLevel: int
    def __init__(self) -> None:
        """__init__(self: depthai.ToFConfig) -> None"""
    def setMedianFilter(self, arg0: filters.params.MedianFilter) -> ToFConfig:
        """setMedianFilter(self: depthai.ToFConfig, arg0: depthai.filters.params.MedianFilter) -> depthai.ToFConfig

        Parameter ``median``:
            Set kernel size for median filtering, or disable
        """
    def setProfilePreset(self, arg0: ImageFiltersPresetMode) -> None:
        """setProfilePreset(self: depthai.ToFConfig, arg0: depthai.ImageFiltersPresetMode) -> None

        Set preset mode for ToFConfig.

        Parameter ``presetMode``:
            Preset mode for ToFConfig.
        """

class ToFDepthConfidenceFilterConfig(Buffer):
    confidenceThreshold: float
    def __init__(self) -> None:
        """__init__(self: depthai.ToFDepthConfidenceFilterConfig) -> None"""
    def setProfilePreset(self, arg0) -> None:
        """setProfilePreset(self: depthai.ToFDepthConfidenceFilterConfig, arg0: dai::ImageFiltersPresetMode) -> None

        Set preset mode for ImageFiltersPresetMode.

        Parameter ``presetMode``:
            Preset mode for ImageFiltersPresetMode.
        """

class ToFDepthConfidenceFilterProperties:
    def __init__(self, *args, **kwargs) -> None:
        """Initialize self.  See help(type(self)) for accurate signature."""

class ToFProperties:
    """Specify properties for ToF"""
    initialConfig: ToFConfig
    numFramesPool: int
    numShaves: int | None
    warpHwIds: list[int]
    def __init__(self, *args, **kwargs) -> None:
        """Initialize self.  See help(type(self)) for accurate signature."""

class TrackedFeature:
    """TrackedFeature structure"""
    age: int
    descriptor: Incomplete
    harrisScore: float
    id: int
    position: Point2f
    trackingError: float
    def __init__(self) -> None:
        """__init__(self: depthai.TrackedFeature) -> None"""

class TrackedFeatures(Buffer):
    """TrackedFeatures message. Carries position (X, Y) of tracked features and their
    ID."""
    trackedFeatures: list[TrackedFeature]
    def __init__(self) -> None:
        """__init__(self: depthai.TrackedFeatures) -> None"""
    def getSequenceNum(self) -> int:
        """getSequenceNum(self: depthai.TrackedFeatures) -> int

        Retrieves image sequence number
        """
    def getTimestamp(self) -> datetime.timedelta:
        """getTimestamp(self: depthai.TrackedFeatures) -> datetime.timedelta

        Retrieves timestamp related to dai::Clock::now()
        """
    def getTimestampDevice(self) -> datetime.timedelta:
        """getTimestampDevice(self: depthai.TrackedFeatures) -> datetime.timedelta

        Retrieves timestamp directly captured from device's monotonic clock, not
        synchronized to host time. Used mostly for debugging
        """

class TrackerIdAssignmentPolicy:
    """Members:

      UNIQUE_ID

      SMALLEST_ID"""
    __members__: ClassVar[dict] = ...  # read-only
    SMALLEST_ID: ClassVar[TrackerIdAssignmentPolicy] = ...
    UNIQUE_ID: ClassVar[TrackerIdAssignmentPolicy] = ...
    __entries: ClassVar[dict] = ...
    def __init__(self, value: int) -> None:
        """__init__(self: depthai.TrackerIdAssignmentPolicy, value: int) -> None"""
    def __eq__(self, other: object) -> bool:
        """__eq__(self: object, other: object) -> bool"""
    def __hash__(self) -> int:
        """__hash__(self: object) -> int"""
    def __index__(self) -> int:
        """__index__(self: depthai.TrackerIdAssignmentPolicy) -> int"""
    def __int__(self) -> int:
        """__int__(self: depthai.TrackerIdAssignmentPolicy) -> int"""
    def __ne__(self, other: object) -> bool:
        """__ne__(self: object, other: object) -> bool"""
    @property
    def name(self) -> str:
        """name(self: object) -> str

        name(self: object) -> str
        """
    @property
    def value(self) -> int:
        """(arg0: depthai.TrackerIdAssignmentPolicy) -> int"""

class TrackerType:
    """Members:

      SHORT_TERM_KCF : Kernelized Correlation Filter tracking

      SHORT_TERM_IMAGELESS : Short term tracking without using image data

      ZERO_TERM_IMAGELESS : Ability to track the objects without accessing image data.

      ZERO_TERM_COLOR_HISTOGRAM : Tracking using image data too."""
    __members__: ClassVar[dict] = ...  # read-only
    SHORT_TERM_IMAGELESS: ClassVar[TrackerType] = ...
    SHORT_TERM_KCF: ClassVar[TrackerType] = ...
    ZERO_TERM_COLOR_HISTOGRAM: ClassVar[TrackerType] = ...
    ZERO_TERM_IMAGELESS: ClassVar[TrackerType] = ...
    __entries: ClassVar[dict] = ...
    def __init__(self, value: int) -> None:
        """__init__(self: depthai.TrackerType, value: int) -> None"""
    def __eq__(self, other: object) -> bool:
        """__eq__(self: object, other: object) -> bool"""
    def __hash__(self) -> int:
        """__hash__(self: object) -> int"""
    def __index__(self) -> int:
        """__index__(self: depthai.TrackerType) -> int"""
    def __int__(self) -> int:
        """__int__(self: depthai.TrackerType) -> int"""
    def __ne__(self, other: object) -> bool:
        """__ne__(self: object, other: object) -> bool"""
    @property
    def name(self) -> str:
        """name(self: object) -> str

        name(self: object) -> str
        """
    @property
    def value(self) -> int:
        """(arg0: depthai.TrackerType) -> int"""

class Tracklet:
    """Tracklet structure

    Contains tracklets from object tracker output."""

    class TrackingStatus:
        """

        Members:

          NEW

          TRACKED

          LOST

          REMOVED"""
        __members__: ClassVar[dict] = ...  # read-only
        LOST: ClassVar[Tracklet.TrackingStatus] = ...
        NEW: ClassVar[Tracklet.TrackingStatus] = ...
        REMOVED: ClassVar[Tracklet.TrackingStatus] = ...
        TRACKED: ClassVar[Tracklet.TrackingStatus] = ...
        __entries: ClassVar[dict] = ...
        def __init__(self, value: int) -> None:
            """__init__(self: depthai.Tracklet.TrackingStatus, value: int) -> None"""
        def __eq__(self, other: object) -> bool:
            """__eq__(self: object, other: object) -> bool"""
        def __hash__(self) -> int:
            """__hash__(self: object) -> int"""
        def __index__(self) -> int:
            """__index__(self: depthai.Tracklet.TrackingStatus) -> int"""
        def __int__(self) -> int:
            """__int__(self: depthai.Tracklet.TrackingStatus) -> int"""
        def __ne__(self, other: object) -> bool:
            """__ne__(self: object, other: object) -> bool"""
        @property
        def name(self) -> str:
            """name(self: object) -> str

            name(self: object) -> str
            """
        @property
        def value(self) -> int:
            """(arg0: depthai.Tracklet.TrackingStatus) -> int"""
    age: int
    id: int
    label: int
    roi: Rect
    spatialCoordinates: Point3f
    srcImgDetection: ImgDetection
    status: Tracklet.TrackingStatus
    def __init__(self) -> None:
        """__init__(self: depthai.Tracklet) -> None"""

class Tracklets(Buffer):
    """Tracklets message. Carries object tracking information."""
    tracklets: list[Tracklet]
    def __init__(self) -> None:
        """__init__(self: depthai.Tracklets) -> None"""
    def getSequenceNum(self) -> int:
        """getSequenceNum(self: depthai.Tracklets) -> int

        Retrieves image sequence number
        """
    def getTimestamp(self) -> datetime.timedelta:
        """getTimestamp(self: depthai.Tracklets) -> datetime.timedelta

        Retrieves timestamp related to dai::Clock::now()
        """
    def getTimestampDevice(self) -> datetime.timedelta:
        """getTimestampDevice(self: depthai.Tracklets) -> datetime.timedelta

        Retrieves timestamp directly captured from device's monotonic clock, not
        synchronized to host time. Used mostly for debugging
        """
    def getTransformation(self) -> ImgTransformation:
        """getTransformation(self: depthai.Tracklets) -> depthai.ImgTransformation"""
    def setTransformation(self, arg0: ImgTransformation) -> None:
        """setTransformation(self: depthai.Tracklets, arg0: depthai.ImgTransformation) -> None"""

class TransformData(Buffer):
    """TransformData message. Carries transform in x,y,z,qx,qy,qz,qw format."""
    def __init__(self) -> None:
        """__init__(self: depthai.TransformData) -> None"""
    def getQuaternion(self) -> Quaterniond:
        """getQuaternion(self: depthai.TransformData) -> depthai.Quaterniond"""
    def getRotationEuler(self) -> Point3d:
        """getRotationEuler(self: depthai.TransformData) -> depthai.Point3d"""
    def getTranslation(self) -> Point3d:
        """getTranslation(self: depthai.TransformData) -> depthai.Point3d"""

class UVCProperties:
    """Properties for UVC node"""
    gpioInit: dict[int, int]
    gpioStreamOff: dict[int, int]
    gpioStreamOn: dict[int, int]
    def __init__(self, *args, **kwargs) -> None:
        """Initialize self.  See help(type(self)) for accurate signature."""

class UsbSpeed:
    """Get USB Speed

    Members:

      UNKNOWN

      LOW

      FULL

      HIGH

      SUPER

      SUPER_PLUS"""
    __members__: ClassVar[dict] = ...  # read-only
    FULL: ClassVar[UsbSpeed] = ...
    HIGH: ClassVar[UsbSpeed] = ...
    LOW: ClassVar[UsbSpeed] = ...
    SUPER: ClassVar[UsbSpeed] = ...
    SUPER_PLUS: ClassVar[UsbSpeed] = ...
    UNKNOWN: ClassVar[UsbSpeed] = ...
    __entries: ClassVar[dict] = ...
    def __init__(self, value: int) -> None:
        """__init__(self: depthai.UsbSpeed, value: int) -> None"""
    def __eq__(self, other: object) -> bool:
        """__eq__(self: object, other: object) -> bool"""
    def __hash__(self) -> int:
        """__hash__(self: object) -> int"""
    def __index__(self) -> int:
        """__index__(self: depthai.UsbSpeed) -> int"""
    def __int__(self) -> int:
        """__int__(self: depthai.UsbSpeed) -> int"""
    def __ne__(self, other: object) -> bool:
        """__ne__(self: object, other: object) -> bool"""
    @property
    def name(self) -> str:
        """name(self: object) -> str

        name(self: object) -> str
        """
    @property
    def value(self) -> int:
        """(arg0: depthai.UsbSpeed) -> int"""

class VectorCircleAnnotation:
    @overload
    def __init__(self) -> None:
        """__init__(*args, **kwargs)
        Overloaded function.

        1. __init__(self: depthai.VectorCircleAnnotation) -> None

        2. __init__(self: depthai.VectorCircleAnnotation, arg0: depthai.VectorCircleAnnotation) -> None

        Copy constructor

        3. __init__(self: depthai.VectorCircleAnnotation, arg0: Iterable) -> None
        """
    @overload
    def __init__(self, arg0: VectorCircleAnnotation) -> None:
        """__init__(*args, **kwargs)
        Overloaded function.

        1. __init__(self: depthai.VectorCircleAnnotation) -> None

        2. __init__(self: depthai.VectorCircleAnnotation, arg0: depthai.VectorCircleAnnotation) -> None

        Copy constructor

        3. __init__(self: depthai.VectorCircleAnnotation, arg0: Iterable) -> None
        """
    @overload
    def __init__(self, arg0: Iterable) -> None:
        """__init__(*args, **kwargs)
        Overloaded function.

        1. __init__(self: depthai.VectorCircleAnnotation) -> None

        2. __init__(self: depthai.VectorCircleAnnotation, arg0: depthai.VectorCircleAnnotation) -> None

        Copy constructor

        3. __init__(self: depthai.VectorCircleAnnotation, arg0: Iterable) -> None
        """
    def append(self, x: CircleAnnotation) -> None:
        """append(self: depthai.VectorCircleAnnotation, x: depthai.CircleAnnotation) -> None

        Add an item to the end of the list
        """
    def clear(self) -> None:
        """clear(self: depthai.VectorCircleAnnotation) -> None

        Clear the contents
        """
    @overload
    def extend(self, L: VectorCircleAnnotation) -> None:
        """extend(*args, **kwargs)
        Overloaded function.

        1. extend(self: depthai.VectorCircleAnnotation, L: depthai.VectorCircleAnnotation) -> None

        Extend the list by appending all the items in the given list

        2. extend(self: depthai.VectorCircleAnnotation, L: Iterable) -> None

        Extend the list by appending all the items in the given list
        """
    @overload
    def extend(self, L: Iterable) -> None:
        """extend(*args, **kwargs)
        Overloaded function.

        1. extend(self: depthai.VectorCircleAnnotation, L: depthai.VectorCircleAnnotation) -> None

        Extend the list by appending all the items in the given list

        2. extend(self: depthai.VectorCircleAnnotation, L: Iterable) -> None

        Extend the list by appending all the items in the given list
        """
    def insert(self, i: int, x: CircleAnnotation) -> None:
        """insert(self: depthai.VectorCircleAnnotation, i: int, x: depthai.CircleAnnotation) -> None

        Insert an item at a given position.
        """
    @overload
    def pop(self) -> CircleAnnotation:
        """pop(*args, **kwargs)
        Overloaded function.

        1. pop(self: depthai.VectorCircleAnnotation) -> depthai.CircleAnnotation

        Remove and return the last item

        2. pop(self: depthai.VectorCircleAnnotation, i: int) -> depthai.CircleAnnotation

        Remove and return the item at index ``i``
        """
    @overload
    def pop(self, i: int) -> CircleAnnotation:
        """pop(*args, **kwargs)
        Overloaded function.

        1. pop(self: depthai.VectorCircleAnnotation) -> depthai.CircleAnnotation

        Remove and return the last item

        2. pop(self: depthai.VectorCircleAnnotation, i: int) -> depthai.CircleAnnotation

        Remove and return the item at index ``i``
        """
    def __bool__(self) -> bool:
        """__bool__(self: depthai.VectorCircleAnnotation) -> bool

        Check whether the list is nonempty
        """
    @overload
    def __delitem__(self, arg0: int) -> None:
        """__delitem__(*args, **kwargs)
        Overloaded function.

        1. __delitem__(self: depthai.VectorCircleAnnotation, arg0: int) -> None

        Delete the list elements at index ``i``

        2. __delitem__(self: depthai.VectorCircleAnnotation, arg0: slice) -> None

        Delete list elements using a slice object
        """
    @overload
    def __delitem__(self, arg0: slice) -> None:
        """__delitem__(*args, **kwargs)
        Overloaded function.

        1. __delitem__(self: depthai.VectorCircleAnnotation, arg0: int) -> None

        Delete the list elements at index ``i``

        2. __delitem__(self: depthai.VectorCircleAnnotation, arg0: slice) -> None

        Delete list elements using a slice object
        """
    @overload
    def __getitem__(self, s: slice) -> VectorCircleAnnotation:
        """__getitem__(*args, **kwargs)
        Overloaded function.

        1. __getitem__(self: depthai.VectorCircleAnnotation, s: slice) -> depthai.VectorCircleAnnotation

        Retrieve list elements using a slice object

        2. __getitem__(self: depthai.VectorCircleAnnotation, arg0: int) -> depthai.CircleAnnotation
        """
    @overload
    def __getitem__(self, arg0: int) -> CircleAnnotation:
        """__getitem__(*args, **kwargs)
        Overloaded function.

        1. __getitem__(self: depthai.VectorCircleAnnotation, s: slice) -> depthai.VectorCircleAnnotation

        Retrieve list elements using a slice object

        2. __getitem__(self: depthai.VectorCircleAnnotation, arg0: int) -> depthai.CircleAnnotation
        """
    def __iter__(self) -> Iterator[CircleAnnotation]:
        """__iter__(self: depthai.VectorCircleAnnotation) -> Iterator[depthai.CircleAnnotation]"""
    def __len__(self) -> int:
        """__len__(self: depthai.VectorCircleAnnotation) -> int"""
    @overload
    def __setitem__(self, arg0: int, arg1: CircleAnnotation) -> None:
        """__setitem__(*args, **kwargs)
        Overloaded function.

        1. __setitem__(self: depthai.VectorCircleAnnotation, arg0: int, arg1: depthai.CircleAnnotation) -> None

        2. __setitem__(self: depthai.VectorCircleAnnotation, arg0: slice, arg1: depthai.VectorCircleAnnotation) -> None

        Assign list elements using a slice object
        """
    @overload
    def __setitem__(self, arg0: slice, arg1: VectorCircleAnnotation) -> None:
        """__setitem__(*args, **kwargs)
        Overloaded function.

        1. __setitem__(self: depthai.VectorCircleAnnotation, arg0: int, arg1: depthai.CircleAnnotation) -> None

        2. __setitem__(self: depthai.VectorCircleAnnotation, arg0: slice, arg1: depthai.VectorCircleAnnotation) -> None

        Assign list elements using a slice object
        """

class VectorColor:
    @overload
    def __init__(self) -> None:
        """__init__(*args, **kwargs)
        Overloaded function.

        1. __init__(self: depthai.VectorColor) -> None

        2. __init__(self: depthai.VectorColor, arg0: depthai.VectorColor) -> None

        Copy constructor

        3. __init__(self: depthai.VectorColor, arg0: Iterable) -> None
        """
    @overload
    def __init__(self, arg0: VectorColor) -> None:
        """__init__(*args, **kwargs)
        Overloaded function.

        1. __init__(self: depthai.VectorColor) -> None

        2. __init__(self: depthai.VectorColor, arg0: depthai.VectorColor) -> None

        Copy constructor

        3. __init__(self: depthai.VectorColor, arg0: Iterable) -> None
        """
    @overload
    def __init__(self, arg0: Iterable) -> None:
        """__init__(*args, **kwargs)
        Overloaded function.

        1. __init__(self: depthai.VectorColor) -> None

        2. __init__(self: depthai.VectorColor, arg0: depthai.VectorColor) -> None

        Copy constructor

        3. __init__(self: depthai.VectorColor, arg0: Iterable) -> None
        """
    def append(self, x: Color) -> None:
        """append(self: depthai.VectorColor, x: depthai.Color) -> None

        Add an item to the end of the list
        """
    def clear(self) -> None:
        """clear(self: depthai.VectorColor) -> None

        Clear the contents
        """
    @overload
    def extend(self, L: VectorColor) -> None:
        """extend(*args, **kwargs)
        Overloaded function.

        1. extend(self: depthai.VectorColor, L: depthai.VectorColor) -> None

        Extend the list by appending all the items in the given list

        2. extend(self: depthai.VectorColor, L: Iterable) -> None

        Extend the list by appending all the items in the given list
        """
    @overload
    def extend(self, L: Iterable) -> None:
        """extend(*args, **kwargs)
        Overloaded function.

        1. extend(self: depthai.VectorColor, L: depthai.VectorColor) -> None

        Extend the list by appending all the items in the given list

        2. extend(self: depthai.VectorColor, L: Iterable) -> None

        Extend the list by appending all the items in the given list
        """
    def insert(self, i: int, x: Color) -> None:
        """insert(self: depthai.VectorColor, i: int, x: depthai.Color) -> None

        Insert an item at a given position.
        """
    @overload
    def pop(self) -> Color:
        """pop(*args, **kwargs)
        Overloaded function.

        1. pop(self: depthai.VectorColor) -> depthai.Color

        Remove and return the last item

        2. pop(self: depthai.VectorColor, i: int) -> depthai.Color

        Remove and return the item at index ``i``
        """
    @overload
    def pop(self, i: int) -> Color:
        """pop(*args, **kwargs)
        Overloaded function.

        1. pop(self: depthai.VectorColor) -> depthai.Color

        Remove and return the last item

        2. pop(self: depthai.VectorColor, i: int) -> depthai.Color

        Remove and return the item at index ``i``
        """
    def __bool__(self) -> bool:
        """__bool__(self: depthai.VectorColor) -> bool

        Check whether the list is nonempty
        """
    @overload
    def __delitem__(self, arg0: int) -> None:
        """__delitem__(*args, **kwargs)
        Overloaded function.

        1. __delitem__(self: depthai.VectorColor, arg0: int) -> None

        Delete the list elements at index ``i``

        2. __delitem__(self: depthai.VectorColor, arg0: slice) -> None

        Delete list elements using a slice object
        """
    @overload
    def __delitem__(self, arg0: slice) -> None:
        """__delitem__(*args, **kwargs)
        Overloaded function.

        1. __delitem__(self: depthai.VectorColor, arg0: int) -> None

        Delete the list elements at index ``i``

        2. __delitem__(self: depthai.VectorColor, arg0: slice) -> None

        Delete list elements using a slice object
        """
    @overload
    def __getitem__(self, s: slice) -> VectorColor:
        """__getitem__(*args, **kwargs)
        Overloaded function.

        1. __getitem__(self: depthai.VectorColor, s: slice) -> depthai.VectorColor

        Retrieve list elements using a slice object

        2. __getitem__(self: depthai.VectorColor, arg0: int) -> depthai.Color
        """
    @overload
    def __getitem__(self, arg0: int) -> Color:
        """__getitem__(*args, **kwargs)
        Overloaded function.

        1. __getitem__(self: depthai.VectorColor, s: slice) -> depthai.VectorColor

        Retrieve list elements using a slice object

        2. __getitem__(self: depthai.VectorColor, arg0: int) -> depthai.Color
        """
    def __iter__(self) -> Iterator[Color]:
        """__iter__(self: depthai.VectorColor) -> Iterator[depthai.Color]"""
    def __len__(self) -> int:
        """__len__(self: depthai.VectorColor) -> int"""
    @overload
    def __setitem__(self, arg0: int, arg1: Color) -> None:
        """__setitem__(*args, **kwargs)
        Overloaded function.

        1. __setitem__(self: depthai.VectorColor, arg0: int, arg1: depthai.Color) -> None

        2. __setitem__(self: depthai.VectorColor, arg0: slice, arg1: depthai.VectorColor) -> None

        Assign list elements using a slice object
        """
    @overload
    def __setitem__(self, arg0: slice, arg1: VectorColor) -> None:
        """__setitem__(*args, **kwargs)
        Overloaded function.

        1. __setitem__(self: depthai.VectorColor, arg0: int, arg1: depthai.Color) -> None

        2. __setitem__(self: depthai.VectorColor, arg0: slice, arg1: depthai.VectorColor) -> None

        Assign list elements using a slice object
        """

class VectorImgAnnotation:
    @overload
    def __init__(self) -> None:
        """__init__(*args, **kwargs)
        Overloaded function.

        1. __init__(self: depthai.VectorImgAnnotation) -> None

        2. __init__(self: depthai.VectorImgAnnotation, arg0: depthai.VectorImgAnnotation) -> None

        Copy constructor

        3. __init__(self: depthai.VectorImgAnnotation, arg0: Iterable) -> None
        """
    @overload
    def __init__(self, arg0: VectorImgAnnotation) -> None:
        """__init__(*args, **kwargs)
        Overloaded function.

        1. __init__(self: depthai.VectorImgAnnotation) -> None

        2. __init__(self: depthai.VectorImgAnnotation, arg0: depthai.VectorImgAnnotation) -> None

        Copy constructor

        3. __init__(self: depthai.VectorImgAnnotation, arg0: Iterable) -> None
        """
    @overload
    def __init__(self, arg0: Iterable) -> None:
        """__init__(*args, **kwargs)
        Overloaded function.

        1. __init__(self: depthai.VectorImgAnnotation) -> None

        2. __init__(self: depthai.VectorImgAnnotation, arg0: depthai.VectorImgAnnotation) -> None

        Copy constructor

        3. __init__(self: depthai.VectorImgAnnotation, arg0: Iterable) -> None
        """
    def append(self, x: ImgAnnotation) -> None:
        """append(self: depthai.VectorImgAnnotation, x: depthai.ImgAnnotation) -> None

        Add an item to the end of the list
        """
    def clear(self) -> None:
        """clear(self: depthai.VectorImgAnnotation) -> None

        Clear the contents
        """
    @overload
    def extend(self, L: VectorImgAnnotation) -> None:
        """extend(*args, **kwargs)
        Overloaded function.

        1. extend(self: depthai.VectorImgAnnotation, L: depthai.VectorImgAnnotation) -> None

        Extend the list by appending all the items in the given list

        2. extend(self: depthai.VectorImgAnnotation, L: Iterable) -> None

        Extend the list by appending all the items in the given list
        """
    @overload
    def extend(self, L: Iterable) -> None:
        """extend(*args, **kwargs)
        Overloaded function.

        1. extend(self: depthai.VectorImgAnnotation, L: depthai.VectorImgAnnotation) -> None

        Extend the list by appending all the items in the given list

        2. extend(self: depthai.VectorImgAnnotation, L: Iterable) -> None

        Extend the list by appending all the items in the given list
        """
    def insert(self, i: int, x: ImgAnnotation) -> None:
        """insert(self: depthai.VectorImgAnnotation, i: int, x: depthai.ImgAnnotation) -> None

        Insert an item at a given position.
        """
    @overload
    def pop(self) -> ImgAnnotation:
        """pop(*args, **kwargs)
        Overloaded function.

        1. pop(self: depthai.VectorImgAnnotation) -> depthai.ImgAnnotation

        Remove and return the last item

        2. pop(self: depthai.VectorImgAnnotation, i: int) -> depthai.ImgAnnotation

        Remove and return the item at index ``i``
        """
    @overload
    def pop(self, i: int) -> ImgAnnotation:
        """pop(*args, **kwargs)
        Overloaded function.

        1. pop(self: depthai.VectorImgAnnotation) -> depthai.ImgAnnotation

        Remove and return the last item

        2. pop(self: depthai.VectorImgAnnotation, i: int) -> depthai.ImgAnnotation

        Remove and return the item at index ``i``
        """
    def __bool__(self) -> bool:
        """__bool__(self: depthai.VectorImgAnnotation) -> bool

        Check whether the list is nonempty
        """
    @overload
    def __delitem__(self, arg0: int) -> None:
        """__delitem__(*args, **kwargs)
        Overloaded function.

        1. __delitem__(self: depthai.VectorImgAnnotation, arg0: int) -> None

        Delete the list elements at index ``i``

        2. __delitem__(self: depthai.VectorImgAnnotation, arg0: slice) -> None

        Delete list elements using a slice object
        """
    @overload
    def __delitem__(self, arg0: slice) -> None:
        """__delitem__(*args, **kwargs)
        Overloaded function.

        1. __delitem__(self: depthai.VectorImgAnnotation, arg0: int) -> None

        Delete the list elements at index ``i``

        2. __delitem__(self: depthai.VectorImgAnnotation, arg0: slice) -> None

        Delete list elements using a slice object
        """
    @overload
    def __getitem__(self, s: slice) -> VectorImgAnnotation:
        """__getitem__(*args, **kwargs)
        Overloaded function.

        1. __getitem__(self: depthai.VectorImgAnnotation, s: slice) -> depthai.VectorImgAnnotation

        Retrieve list elements using a slice object

        2. __getitem__(self: depthai.VectorImgAnnotation, arg0: int) -> depthai.ImgAnnotation
        """
    @overload
    def __getitem__(self, arg0: int) -> ImgAnnotation:
        """__getitem__(*args, **kwargs)
        Overloaded function.

        1. __getitem__(self: depthai.VectorImgAnnotation, s: slice) -> depthai.VectorImgAnnotation

        Retrieve list elements using a slice object

        2. __getitem__(self: depthai.VectorImgAnnotation, arg0: int) -> depthai.ImgAnnotation
        """
    def __iter__(self) -> Iterator[ImgAnnotation]:
        """__iter__(self: depthai.VectorImgAnnotation) -> Iterator[depthai.ImgAnnotation]"""
    def __len__(self) -> int:
        """__len__(self: depthai.VectorImgAnnotation) -> int"""
    @overload
    def __setitem__(self, arg0: int, arg1: ImgAnnotation) -> None:
        """__setitem__(*args, **kwargs)
        Overloaded function.

        1. __setitem__(self: depthai.VectorImgAnnotation, arg0: int, arg1: depthai.ImgAnnotation) -> None

        2. __setitem__(self: depthai.VectorImgAnnotation, arg0: slice, arg1: depthai.VectorImgAnnotation) -> None

        Assign list elements using a slice object
        """
    @overload
    def __setitem__(self, arg0: slice, arg1: VectorImgAnnotation) -> None:
        """__setitem__(*args, **kwargs)
        Overloaded function.

        1. __setitem__(self: depthai.VectorImgAnnotation, arg0: int, arg1: depthai.ImgAnnotation) -> None

        2. __setitem__(self: depthai.VectorImgAnnotation, arg0: slice, arg1: depthai.VectorImgAnnotation) -> None

        Assign list elements using a slice object
        """

class VectorPoint2f:
    @overload
    def __init__(self) -> None:
        """__init__(*args, **kwargs)
        Overloaded function.

        1. __init__(self: depthai.VectorPoint2f) -> None

        2. __init__(self: depthai.VectorPoint2f, arg0: depthai.VectorPoint2f) -> None

        Copy constructor

        3. __init__(self: depthai.VectorPoint2f, arg0: Iterable) -> None
        """
    @overload
    def __init__(self, arg0: VectorPoint2f) -> None:
        """__init__(*args, **kwargs)
        Overloaded function.

        1. __init__(self: depthai.VectorPoint2f) -> None

        2. __init__(self: depthai.VectorPoint2f, arg0: depthai.VectorPoint2f) -> None

        Copy constructor

        3. __init__(self: depthai.VectorPoint2f, arg0: Iterable) -> None
        """
    @overload
    def __init__(self, arg0: Iterable) -> None:
        """__init__(*args, **kwargs)
        Overloaded function.

        1. __init__(self: depthai.VectorPoint2f) -> None

        2. __init__(self: depthai.VectorPoint2f, arg0: depthai.VectorPoint2f) -> None

        Copy constructor

        3. __init__(self: depthai.VectorPoint2f, arg0: Iterable) -> None
        """
    def append(self, x: Point2f) -> None:
        """append(self: depthai.VectorPoint2f, x: depthai.Point2f) -> None

        Add an item to the end of the list
        """
    def clear(self) -> None:
        """clear(self: depthai.VectorPoint2f) -> None

        Clear the contents
        """
    @overload
    def extend(self, L: VectorPoint2f) -> None:
        """extend(*args, **kwargs)
        Overloaded function.

        1. extend(self: depthai.VectorPoint2f, L: depthai.VectorPoint2f) -> None

        Extend the list by appending all the items in the given list

        2. extend(self: depthai.VectorPoint2f, L: Iterable) -> None

        Extend the list by appending all the items in the given list
        """
    @overload
    def extend(self, L: Iterable) -> None:
        """extend(*args, **kwargs)
        Overloaded function.

        1. extend(self: depthai.VectorPoint2f, L: depthai.VectorPoint2f) -> None

        Extend the list by appending all the items in the given list

        2. extend(self: depthai.VectorPoint2f, L: Iterable) -> None

        Extend the list by appending all the items in the given list
        """
    def insert(self, i: int, x: Point2f) -> None:
        """insert(self: depthai.VectorPoint2f, i: int, x: depthai.Point2f) -> None

        Insert an item at a given position.
        """
    @overload
    def pop(self) -> Point2f:
        """pop(*args, **kwargs)
        Overloaded function.

        1. pop(self: depthai.VectorPoint2f) -> depthai.Point2f

        Remove and return the last item

        2. pop(self: depthai.VectorPoint2f, i: int) -> depthai.Point2f

        Remove and return the item at index ``i``
        """
    @overload
    def pop(self, i: int) -> Point2f:
        """pop(*args, **kwargs)
        Overloaded function.

        1. pop(self: depthai.VectorPoint2f) -> depthai.Point2f

        Remove and return the last item

        2. pop(self: depthai.VectorPoint2f, i: int) -> depthai.Point2f

        Remove and return the item at index ``i``
        """
    def __bool__(self) -> bool:
        """__bool__(self: depthai.VectorPoint2f) -> bool

        Check whether the list is nonempty
        """
    @overload
    def __delitem__(self, arg0: int) -> None:
        """__delitem__(*args, **kwargs)
        Overloaded function.

        1. __delitem__(self: depthai.VectorPoint2f, arg0: int) -> None

        Delete the list elements at index ``i``

        2. __delitem__(self: depthai.VectorPoint2f, arg0: slice) -> None

        Delete list elements using a slice object
        """
    @overload
    def __delitem__(self, arg0: slice) -> None:
        """__delitem__(*args, **kwargs)
        Overloaded function.

        1. __delitem__(self: depthai.VectorPoint2f, arg0: int) -> None

        Delete the list elements at index ``i``

        2. __delitem__(self: depthai.VectorPoint2f, arg0: slice) -> None

        Delete list elements using a slice object
        """
    @overload
    def __getitem__(self, s: slice) -> VectorPoint2f:
        """__getitem__(*args, **kwargs)
        Overloaded function.

        1. __getitem__(self: depthai.VectorPoint2f, s: slice) -> depthai.VectorPoint2f

        Retrieve list elements using a slice object

        2. __getitem__(self: depthai.VectorPoint2f, arg0: int) -> depthai.Point2f
        """
    @overload
    def __getitem__(self, arg0: int) -> Point2f:
        """__getitem__(*args, **kwargs)
        Overloaded function.

        1. __getitem__(self: depthai.VectorPoint2f, s: slice) -> depthai.VectorPoint2f

        Retrieve list elements using a slice object

        2. __getitem__(self: depthai.VectorPoint2f, arg0: int) -> depthai.Point2f
        """
    def __iter__(self) -> Iterator[Point2f]:
        """__iter__(self: depthai.VectorPoint2f) -> Iterator[depthai.Point2f]"""
    def __len__(self) -> int:
        """__len__(self: depthai.VectorPoint2f) -> int"""
    @overload
    def __setitem__(self, arg0: int, arg1: Point2f) -> None:
        """__setitem__(*args, **kwargs)
        Overloaded function.

        1. __setitem__(self: depthai.VectorPoint2f, arg0: int, arg1: depthai.Point2f) -> None

        2. __setitem__(self: depthai.VectorPoint2f, arg0: slice, arg1: depthai.VectorPoint2f) -> None

        Assign list elements using a slice object
        """
    @overload
    def __setitem__(self, arg0: slice, arg1: VectorPoint2f) -> None:
        """__setitem__(*args, **kwargs)
        Overloaded function.

        1. __setitem__(self: depthai.VectorPoint2f, arg0: int, arg1: depthai.Point2f) -> None

        2. __setitem__(self: depthai.VectorPoint2f, arg0: slice, arg1: depthai.VectorPoint2f) -> None

        Assign list elements using a slice object
        """

class VectorPointsAnnotation:
    @overload
    def __init__(self) -> None:
        """__init__(*args, **kwargs)
        Overloaded function.

        1. __init__(self: depthai.VectorPointsAnnotation) -> None

        2. __init__(self: depthai.VectorPointsAnnotation, arg0: depthai.VectorPointsAnnotation) -> None

        Copy constructor

        3. __init__(self: depthai.VectorPointsAnnotation, arg0: Iterable) -> None
        """
    @overload
    def __init__(self, arg0: VectorPointsAnnotation) -> None:
        """__init__(*args, **kwargs)
        Overloaded function.

        1. __init__(self: depthai.VectorPointsAnnotation) -> None

        2. __init__(self: depthai.VectorPointsAnnotation, arg0: depthai.VectorPointsAnnotation) -> None

        Copy constructor

        3. __init__(self: depthai.VectorPointsAnnotation, arg0: Iterable) -> None
        """
    @overload
    def __init__(self, arg0: Iterable) -> None:
        """__init__(*args, **kwargs)
        Overloaded function.

        1. __init__(self: depthai.VectorPointsAnnotation) -> None

        2. __init__(self: depthai.VectorPointsAnnotation, arg0: depthai.VectorPointsAnnotation) -> None

        Copy constructor

        3. __init__(self: depthai.VectorPointsAnnotation, arg0: Iterable) -> None
        """
    def append(self, x: PointsAnnotation) -> None:
        """append(self: depthai.VectorPointsAnnotation, x: depthai.PointsAnnotation) -> None

        Add an item to the end of the list
        """
    def clear(self) -> None:
        """clear(self: depthai.VectorPointsAnnotation) -> None

        Clear the contents
        """
    @overload
    def extend(self, L: VectorPointsAnnotation) -> None:
        """extend(*args, **kwargs)
        Overloaded function.

        1. extend(self: depthai.VectorPointsAnnotation, L: depthai.VectorPointsAnnotation) -> None

        Extend the list by appending all the items in the given list

        2. extend(self: depthai.VectorPointsAnnotation, L: Iterable) -> None

        Extend the list by appending all the items in the given list
        """
    @overload
    def extend(self, L: Iterable) -> None:
        """extend(*args, **kwargs)
        Overloaded function.

        1. extend(self: depthai.VectorPointsAnnotation, L: depthai.VectorPointsAnnotation) -> None

        Extend the list by appending all the items in the given list

        2. extend(self: depthai.VectorPointsAnnotation, L: Iterable) -> None

        Extend the list by appending all the items in the given list
        """
    def insert(self, i: int, x: PointsAnnotation) -> None:
        """insert(self: depthai.VectorPointsAnnotation, i: int, x: depthai.PointsAnnotation) -> None

        Insert an item at a given position.
        """
    @overload
    def pop(self) -> PointsAnnotation:
        """pop(*args, **kwargs)
        Overloaded function.

        1. pop(self: depthai.VectorPointsAnnotation) -> depthai.PointsAnnotation

        Remove and return the last item

        2. pop(self: depthai.VectorPointsAnnotation, i: int) -> depthai.PointsAnnotation

        Remove and return the item at index ``i``
        """
    @overload
    def pop(self, i: int) -> PointsAnnotation:
        """pop(*args, **kwargs)
        Overloaded function.

        1. pop(self: depthai.VectorPointsAnnotation) -> depthai.PointsAnnotation

        Remove and return the last item

        2. pop(self: depthai.VectorPointsAnnotation, i: int) -> depthai.PointsAnnotation

        Remove and return the item at index ``i``
        """
    def __bool__(self) -> bool:
        """__bool__(self: depthai.VectorPointsAnnotation) -> bool

        Check whether the list is nonempty
        """
    @overload
    def __delitem__(self, arg0: int) -> None:
        """__delitem__(*args, **kwargs)
        Overloaded function.

        1. __delitem__(self: depthai.VectorPointsAnnotation, arg0: int) -> None

        Delete the list elements at index ``i``

        2. __delitem__(self: depthai.VectorPointsAnnotation, arg0: slice) -> None

        Delete list elements using a slice object
        """
    @overload
    def __delitem__(self, arg0: slice) -> None:
        """__delitem__(*args, **kwargs)
        Overloaded function.

        1. __delitem__(self: depthai.VectorPointsAnnotation, arg0: int) -> None

        Delete the list elements at index ``i``

        2. __delitem__(self: depthai.VectorPointsAnnotation, arg0: slice) -> None

        Delete list elements using a slice object
        """
    @overload
    def __getitem__(self, s: slice) -> VectorPointsAnnotation:
        """__getitem__(*args, **kwargs)
        Overloaded function.

        1. __getitem__(self: depthai.VectorPointsAnnotation, s: slice) -> depthai.VectorPointsAnnotation

        Retrieve list elements using a slice object

        2. __getitem__(self: depthai.VectorPointsAnnotation, arg0: int) -> depthai.PointsAnnotation
        """
    @overload
    def __getitem__(self, arg0: int) -> PointsAnnotation:
        """__getitem__(*args, **kwargs)
        Overloaded function.

        1. __getitem__(self: depthai.VectorPointsAnnotation, s: slice) -> depthai.VectorPointsAnnotation

        Retrieve list elements using a slice object

        2. __getitem__(self: depthai.VectorPointsAnnotation, arg0: int) -> depthai.PointsAnnotation
        """
    def __iter__(self) -> Iterator[PointsAnnotation]:
        """__iter__(self: depthai.VectorPointsAnnotation) -> Iterator[depthai.PointsAnnotation]"""
    def __len__(self) -> int:
        """__len__(self: depthai.VectorPointsAnnotation) -> int"""
    @overload
    def __setitem__(self, arg0: int, arg1: PointsAnnotation) -> None:
        """__setitem__(*args, **kwargs)
        Overloaded function.

        1. __setitem__(self: depthai.VectorPointsAnnotation, arg0: int, arg1: depthai.PointsAnnotation) -> None

        2. __setitem__(self: depthai.VectorPointsAnnotation, arg0: slice, arg1: depthai.VectorPointsAnnotation) -> None

        Assign list elements using a slice object
        """
    @overload
    def __setitem__(self, arg0: slice, arg1: VectorPointsAnnotation) -> None:
        """__setitem__(*args, **kwargs)
        Overloaded function.

        1. __setitem__(self: depthai.VectorPointsAnnotation, arg0: int, arg1: depthai.PointsAnnotation) -> None

        2. __setitem__(self: depthai.VectorPointsAnnotation, arg0: slice, arg1: depthai.VectorPointsAnnotation) -> None

        Assign list elements using a slice object
        """

class VectorTextAnnotation:
    @overload
    def __init__(self) -> None:
        """__init__(*args, **kwargs)
        Overloaded function.

        1. __init__(self: depthai.VectorTextAnnotation) -> None

        2. __init__(self: depthai.VectorTextAnnotation, arg0: depthai.VectorTextAnnotation) -> None

        Copy constructor

        3. __init__(self: depthai.VectorTextAnnotation, arg0: Iterable) -> None
        """
    @overload
    def __init__(self, arg0: VectorTextAnnotation) -> None:
        """__init__(*args, **kwargs)
        Overloaded function.

        1. __init__(self: depthai.VectorTextAnnotation) -> None

        2. __init__(self: depthai.VectorTextAnnotation, arg0: depthai.VectorTextAnnotation) -> None

        Copy constructor

        3. __init__(self: depthai.VectorTextAnnotation, arg0: Iterable) -> None
        """
    @overload
    def __init__(self, arg0: Iterable) -> None:
        """__init__(*args, **kwargs)
        Overloaded function.

        1. __init__(self: depthai.VectorTextAnnotation) -> None

        2. __init__(self: depthai.VectorTextAnnotation, arg0: depthai.VectorTextAnnotation) -> None

        Copy constructor

        3. __init__(self: depthai.VectorTextAnnotation, arg0: Iterable) -> None
        """
    def append(self, x: TextAnnotation) -> None:
        """append(self: depthai.VectorTextAnnotation, x: depthai.TextAnnotation) -> None

        Add an item to the end of the list
        """
    def clear(self) -> None:
        """clear(self: depthai.VectorTextAnnotation) -> None

        Clear the contents
        """
    @overload
    def extend(self, L: VectorTextAnnotation) -> None:
        """extend(*args, **kwargs)
        Overloaded function.

        1. extend(self: depthai.VectorTextAnnotation, L: depthai.VectorTextAnnotation) -> None

        Extend the list by appending all the items in the given list

        2. extend(self: depthai.VectorTextAnnotation, L: Iterable) -> None

        Extend the list by appending all the items in the given list
        """
    @overload
    def extend(self, L: Iterable) -> None:
        """extend(*args, **kwargs)
        Overloaded function.

        1. extend(self: depthai.VectorTextAnnotation, L: depthai.VectorTextAnnotation) -> None

        Extend the list by appending all the items in the given list

        2. extend(self: depthai.VectorTextAnnotation, L: Iterable) -> None

        Extend the list by appending all the items in the given list
        """
    def insert(self, i: int, x: TextAnnotation) -> None:
        """insert(self: depthai.VectorTextAnnotation, i: int, x: depthai.TextAnnotation) -> None

        Insert an item at a given position.
        """
    @overload
    def pop(self) -> TextAnnotation:
        """pop(*args, **kwargs)
        Overloaded function.

        1. pop(self: depthai.VectorTextAnnotation) -> depthai.TextAnnotation

        Remove and return the last item

        2. pop(self: depthai.VectorTextAnnotation, i: int) -> depthai.TextAnnotation

        Remove and return the item at index ``i``
        """
    @overload
    def pop(self, i: int) -> TextAnnotation:
        """pop(*args, **kwargs)
        Overloaded function.

        1. pop(self: depthai.VectorTextAnnotation) -> depthai.TextAnnotation

        Remove and return the last item

        2. pop(self: depthai.VectorTextAnnotation, i: int) -> depthai.TextAnnotation

        Remove and return the item at index ``i``
        """
    def __bool__(self) -> bool:
        """__bool__(self: depthai.VectorTextAnnotation) -> bool

        Check whether the list is nonempty
        """
    @overload
    def __delitem__(self, arg0: int) -> None:
        """__delitem__(*args, **kwargs)
        Overloaded function.

        1. __delitem__(self: depthai.VectorTextAnnotation, arg0: int) -> None

        Delete the list elements at index ``i``

        2. __delitem__(self: depthai.VectorTextAnnotation, arg0: slice) -> None

        Delete list elements using a slice object
        """
    @overload
    def __delitem__(self, arg0: slice) -> None:
        """__delitem__(*args, **kwargs)
        Overloaded function.

        1. __delitem__(self: depthai.VectorTextAnnotation, arg0: int) -> None

        Delete the list elements at index ``i``

        2. __delitem__(self: depthai.VectorTextAnnotation, arg0: slice) -> None

        Delete list elements using a slice object
        """
    @overload
    def __getitem__(self, s: slice) -> VectorTextAnnotation:
        """__getitem__(*args, **kwargs)
        Overloaded function.

        1. __getitem__(self: depthai.VectorTextAnnotation, s: slice) -> depthai.VectorTextAnnotation

        Retrieve list elements using a slice object

        2. __getitem__(self: depthai.VectorTextAnnotation, arg0: int) -> depthai.TextAnnotation
        """
    @overload
    def __getitem__(self, arg0: int) -> TextAnnotation:
        """__getitem__(*args, **kwargs)
        Overloaded function.

        1. __getitem__(self: depthai.VectorTextAnnotation, s: slice) -> depthai.VectorTextAnnotation

        Retrieve list elements using a slice object

        2. __getitem__(self: depthai.VectorTextAnnotation, arg0: int) -> depthai.TextAnnotation
        """
    def __iter__(self) -> Iterator[TextAnnotation]:
        """__iter__(self: depthai.VectorTextAnnotation) -> Iterator[depthai.TextAnnotation]"""
    def __len__(self) -> int:
        """__len__(self: depthai.VectorTextAnnotation) -> int"""
    @overload
    def __setitem__(self, arg0: int, arg1: TextAnnotation) -> None:
        """__setitem__(*args, **kwargs)
        Overloaded function.

        1. __setitem__(self: depthai.VectorTextAnnotation, arg0: int, arg1: depthai.TextAnnotation) -> None

        2. __setitem__(self: depthai.VectorTextAnnotation, arg0: slice, arg1: depthai.VectorTextAnnotation) -> None

        Assign list elements using a slice object
        """
    @overload
    def __setitem__(self, arg0: slice, arg1: VectorTextAnnotation) -> None:
        """__setitem__(*args, **kwargs)
        Overloaded function.

        1. __setitem__(self: depthai.VectorTextAnnotation, arg0: int, arg1: depthai.TextAnnotation) -> None

        2. __setitem__(self: depthai.VectorTextAnnotation, arg0: slice, arg1: depthai.VectorTextAnnotation) -> None

        Assign list elements using a slice object
        """

class Version:
    """Version structure"""
    @overload
    def __init__(self, v: str) -> None:
        """__init__(*args, **kwargs)
        Overloaded function.

        1. __init__(self: depthai.Version, v: str) -> None

        Construct Version from string

        2. __init__(self: depthai.Version, major: int, minor: int, patch: int) -> None

        Construct Version major, minor, patch, and pre-release information
        """
    @overload
    def __init__(self, major: int, minor: int, patch: int) -> None:
        """__init__(*args, **kwargs)
        Overloaded function.

        1. __init__(self: depthai.Version, v: str) -> None

        Construct Version from string

        2. __init__(self: depthai.Version, major: int, minor: int, patch: int) -> None

        Construct Version major, minor, patch, and pre-release information
        """
    def getBuildInfo(self) -> str:
        """getBuildInfo(self: depthai.Version) -> str

        Get build info
        """
    def toStringSemver(self) -> str:
        """toStringSemver(self: depthai.Version) -> str

        Convert Version to semver (no build information) string
        """
    def __eq__(self, arg0: Version) -> bool:
        """__eq__(self: depthai.Version, arg0: depthai.Version) -> bool"""
    def __gt__(self, arg0: Version) -> bool:
        """__gt__(self: depthai.Version, arg0: depthai.Version) -> bool"""
    def __lt__(self, arg0: Version) -> bool:
        """__lt__(self: depthai.Version, arg0: depthai.Version) -> bool"""

class VideoEncoderProperties:
    """Specify properties for VideoEncoder such as profile, bitrate, ..."""

    class Profile:
        """Encoding profile, H264 (AVC), H265 (HEVC) or MJPEG

        Members:

          H264_BASELINE

          H264_HIGH

          H264_MAIN

          H265_MAIN

          MJPEG"""
        __members__: ClassVar[dict] = ...  # read-only
        H264_BASELINE: ClassVar[VideoEncoderProperties.Profile] = ...
        H264_HIGH: ClassVar[VideoEncoderProperties.Profile] = ...
        H264_MAIN: ClassVar[VideoEncoderProperties.Profile] = ...
        H265_MAIN: ClassVar[VideoEncoderProperties.Profile] = ...
        MJPEG: ClassVar[VideoEncoderProperties.Profile] = ...
        __entries: ClassVar[dict] = ...
        def __init__(self, value: int) -> None:
            """__init__(self: depthai.VideoEncoderProperties.Profile, value: int) -> None"""
        def __eq__(self, other: object) -> bool:
            """__eq__(self: object, other: object) -> bool"""
        def __hash__(self) -> int:
            """__hash__(self: object) -> int"""
        def __index__(self) -> int:
            """__index__(self: depthai.VideoEncoderProperties.Profile) -> int"""
        def __int__(self) -> int:
            """__int__(self: depthai.VideoEncoderProperties.Profile) -> int"""
        def __ne__(self, other: object) -> bool:
            """__ne__(self: object, other: object) -> bool"""
        @property
        def name(self) -> str:
            """name(self: object) -> str

            name(self: object) -> str
            """
        @property
        def value(self) -> int:
            """(arg0: depthai.VideoEncoderProperties.Profile) -> int"""

    class RateControlMode:
        """Rate control mode specifies if constant or variable bitrate should be used (H264
        / H265)

        Members:

          CBR

          VBR"""
        __members__: ClassVar[dict] = ...  # read-only
        CBR: ClassVar[VideoEncoderProperties.RateControlMode] = ...
        VBR: ClassVar[VideoEncoderProperties.RateControlMode] = ...
        __entries: ClassVar[dict] = ...
        def __init__(self, value: int) -> None:
            """__init__(self: depthai.VideoEncoderProperties.RateControlMode, value: int) -> None"""
        def __eq__(self, other: object) -> bool:
            """__eq__(self: object, other: object) -> bool"""
        def __hash__(self) -> int:
            """__hash__(self: object) -> int"""
        def __index__(self) -> int:
            """__index__(self: depthai.VideoEncoderProperties.RateControlMode) -> int"""
        def __int__(self) -> int:
            """__int__(self: depthai.VideoEncoderProperties.RateControlMode) -> int"""
        def __ne__(self, other: object) -> bool:
            """__ne__(self: object, other: object) -> bool"""
        @property
        def name(self) -> str:
            """name(self: object) -> str

            name(self: object) -> str
            """
        @property
        def value(self) -> int:
            """(arg0: depthai.VideoEncoderProperties.RateControlMode) -> int"""
    bitrate: int
    keyframeFrequency: int
    maxBitrate: int
    numBFrames: int
    numFramesPool: int
    outputFrameSize: int
    profile: VideoEncoderProperties.Profile
    quality: int
    rateCtrlMode: VideoEncoderProperties.RateControlMode
    def __init__(self, *args, **kwargs) -> None:
        """Initialize self.  See help(type(self)) for accurate signature."""

class VioConfig:
    mapper_bow_num_bits: int
    mapper_detection_num_points: int
    mapper_frames_to_match_threshold: float
    mapper_lm_lambda_max: float
    mapper_lm_lambda_min: float
    mapper_max_hamming_distance: float
    mapper_min_matches: float
    mapper_min_track_length: float
    mapper_min_triangulation_dist: float
    mapper_no_factor_weights: bool
    mapper_num_frames_to_match: float
    mapper_obs_huber_thresh: float
    mapper_obs_std_dev: float
    mapper_ransac_threshold: float
    mapper_second_best_test_ratio: float
    mapper_use_factors: bool
    mapper_use_lm: bool
    optical_flow_detection_grid_size: int
    optical_flow_detection_max_threshold: int
    optical_flow_detection_min_threshold: int
    optical_flow_detection_nonoverlap: bool
    optical_flow_detection_num_points_cell: int
    optical_flow_epipolar_error: float
    optical_flow_image_safe_radius: float
    optical_flow_levels: int
    optical_flow_matching_default_depth: float
    optical_flow_matching_guess_type: Incomplete
    optical_flow_max_iterations: int
    optical_flow_max_recovered_dist2: float
    optical_flow_pattern: int
    optical_flow_recall_all_cams: bool
    optical_flow_recall_enable: bool
    optical_flow_recall_max_patch_dist: float
    optical_flow_recall_max_patch_norms: list[float]
    optical_flow_recall_num_points_cell: bool
    optical_flow_recall_over_tracking: bool
    optical_flow_recall_update_patch_viewpoint: bool
    optical_flow_skip_frames: int
    optical_flow_type: str
    vio_debug: bool
    vio_enforce_realtime: bool
    vio_extended_logging: bool
    vio_fix_long_term_keyframes: bool
    vio_init_ba_weight: float
    vio_init_bg_weight: float
    vio_init_pose_weight: float
    vio_kf_marg_criteria: Incomplete
    vio_kf_marg_feature_ratio: float
    vio_linearization_type: Incomplete
    vio_lm_lambda_initial: float
    vio_lm_lambda_max: float
    vio_lm_lambda_min: float
    vio_marg_lost_landmarks: bool
    vio_max_iterations: int
    vio_max_kfs: int
    vio_max_states: int
    vio_min_frames_after_kf: int
    vio_min_triangulation_dist: float
    vio_new_kf_keypoints_thresh: float
    vio_obs_huber_thresh: float
    vio_obs_std_dev: float
    vio_scale_jacobian: bool
    vio_sqrt_marg: bool
    vio_use_lm: bool
    def __init__(self) -> None:
        """__init__(self: depthai.VioConfig) -> None"""

class VppConfig(Buffer):
    """VppConfig message. Carries config for Virtual Projection Pattern algorithm"""

    class InjectionParameters:
        confidenceThreshold: float
        kernelSize: int
        morphologyIterations: int
        textureThreshold: float
        useInjection: bool
        useMorphology: bool
        def __init__(self) -> None:
            """__init__(self: depthai.VppConfig.InjectionParameters) -> None"""

    class PatchColoringType:
        """

        Members:

          RANDOM : Random patch coloring

          MAXDIST : Color with most distant color"""
        __members__: ClassVar[dict] = ...  # read-only
        MAXDIST: ClassVar[VppConfig.PatchColoringType] = ...
        RANDOM: ClassVar[VppConfig.PatchColoringType] = ...
        __entries: ClassVar[dict] = ...
        def __init__(self, value: int) -> None:
            """__init__(self: depthai.VppConfig.PatchColoringType, value: int) -> None"""
        def __eq__(self, other: object) -> bool:
            """__eq__(self: object, other: object) -> bool"""
        def __hash__(self) -> int:
            """__hash__(self: object) -> int"""
        def __index__(self) -> int:
            """__index__(self: depthai.VppConfig.PatchColoringType) -> int"""
        def __int__(self) -> int:
            """__int__(self: depthai.VppConfig.PatchColoringType) -> int"""
        def __ne__(self, other: object) -> bool:
            """__ne__(self: object, other: object) -> bool"""
        @property
        def name(self) -> str:
            """name(self: object) -> str

            name(self: object) -> str
            """
        @property
        def value(self) -> int:
            """(arg0: depthai.VppConfig.PatchColoringType) -> int"""
    MAXDIST: ClassVar[VppConfig.PatchColoringType] = ...
    RANDOM: ClassVar[VppConfig.PatchColoringType] = ...
    blending: float
    distanceGamma: float
    injectionParameters: VppConfig.InjectionParameters
    maxFPS: int
    maxNumThreads: int
    maxPatchSize: int
    patchColoringType: VppConfig.PatchColoringType
    uniformPatch: bool
    def __init__(self) -> None:
        """__init__(self: depthai.VppConfig) -> None"""

class VppProperties:
    """Specify properties for Vpp node"""
    initialConfig: VppConfig
    numFramesPool: int
    def __init__(self, *args, **kwargs) -> None:
        """Initialize self.  See help(type(self)) for accurate signature."""

class WarpProperties:
    """Specify properties for Warp"""
    def __init__(self, *args, **kwargs) -> None:
        """Initialize self.  See help(type(self)) for accurate signature."""

class XLinkConnection:
    """Represents connection between host and device over XLink protocol"""
    @overload
    def __init__(self, arg0: DeviceInfo, arg1, std) -> None:
        """__init__(*args, **kwargs)
        Overloaded function.

        1. __init__(self: depthai.XLinkConnection, arg0: depthai.DeviceInfo, arg1: std::vector<unsigned char, std::allocator<unsigned char> >) -> None

        2. __init__(self: depthai.XLinkConnection, arg0: depthai.DeviceInfo, arg1: str) -> None

        3. __init__(self: depthai.XLinkConnection, arg0: depthai.DeviceInfo) -> None
        """
    @overload
    def __init__(self, arg0: DeviceInfo, arg1: str) -> None:
        """__init__(*args, **kwargs)
        Overloaded function.

        1. __init__(self: depthai.XLinkConnection, arg0: depthai.DeviceInfo, arg1: std::vector<unsigned char, std::allocator<unsigned char> >) -> None

        2. __init__(self: depthai.XLinkConnection, arg0: depthai.DeviceInfo, arg1: str) -> None

        3. __init__(self: depthai.XLinkConnection, arg0: depthai.DeviceInfo) -> None
        """
    @overload
    def __init__(self, arg0: DeviceInfo) -> None:
        """__init__(*args, **kwargs)
        Overloaded function.

        1. __init__(self: depthai.XLinkConnection, arg0: depthai.DeviceInfo, arg1: std::vector<unsigned char, std::allocator<unsigned char> >) -> None

        2. __init__(self: depthai.XLinkConnection, arg0: depthai.DeviceInfo, arg1: str) -> None

        3. __init__(self: depthai.XLinkConnection, arg0: depthai.DeviceInfo) -> None
        """
    @staticmethod
    def bootBootloader(devInfo: DeviceInfo) -> DeviceInfo:
        """bootBootloader(devInfo: depthai.DeviceInfo) -> depthai.DeviceInfo"""
    @staticmethod
    def getAllConnectedDevices(state: XLinkDeviceState = ..., skipInvalidDevices: bool = ..., timeoutMs: int = ...) -> list[DeviceInfo]:
        """getAllConnectedDevices(state: depthai.XLinkDeviceState = <XLinkDeviceState.X_LINK_ANY_STATE: 0>, skipInvalidDevices: bool = True, timeoutMs: int = 500) -> list[depthai.DeviceInfo]"""
    @staticmethod
    def getDeviceById(deviceId: str, state: XLinkDeviceState = ..., skipInvalidDevice: bool = ...) -> tuple[bool, DeviceInfo]:
        """getDeviceById(deviceId: str, state: depthai.XLinkDeviceState = <XLinkDeviceState.X_LINK_ANY_STATE: 0>, skipInvalidDevice: bool = True) -> tuple[bool, depthai.DeviceInfo]"""
    @staticmethod
    def getFirstDevice(state: XLinkDeviceState = ..., skipInvalidDevice: bool = ...) -> tuple[bool, DeviceInfo]:
        """getFirstDevice(state: depthai.XLinkDeviceState = <XLinkDeviceState.X_LINK_ANY_STATE: 0>, skipInvalidDevice: bool = True) -> tuple[bool, depthai.DeviceInfo]"""
    @staticmethod
    def getGlobalProfilingData() -> ProfilingData:
        """getGlobalProfilingData() -> depthai.ProfilingData

        Get current accumulated profiling data

        Returns:
            ProfilingData from the specific connection
        """

class XLinkDeviceState:
    """Members:

      X_LINK_ANY_STATE

      X_LINK_BOOTED

      X_LINK_UNBOOTED

      X_LINK_BOOTLOADER

      X_LINK_FLASH_BOOTED

      X_LINK_BOOTED_NON_EXCLUSIVE

      X_LINK_GATE

      X_LINK_GATE_BOOTED

      X_LINK_GATE_SETUP"""
    __members__: ClassVar[dict] = ...  # read-only
    X_LINK_ANY_STATE: ClassVar[XLinkDeviceState] = ...
    X_LINK_BOOTED: ClassVar[XLinkDeviceState] = ...
    X_LINK_BOOTED_NON_EXCLUSIVE: ClassVar[XLinkDeviceState] = ...
    X_LINK_BOOTLOADER: ClassVar[XLinkDeviceState] = ...
    X_LINK_FLASH_BOOTED: ClassVar[XLinkDeviceState] = ...
    X_LINK_GATE: ClassVar[XLinkDeviceState] = ...
    X_LINK_GATE_BOOTED: ClassVar[XLinkDeviceState] = ...
    X_LINK_GATE_SETUP: ClassVar[XLinkDeviceState] = ...
    X_LINK_UNBOOTED: ClassVar[XLinkDeviceState] = ...
    __entries: ClassVar[dict] = ...
    def __init__(self, value: int) -> None:
        """__init__(self: depthai.XLinkDeviceState, value: int) -> None"""
    def __eq__(self, other: object) -> bool:
        """__eq__(self: object, other: object) -> bool"""
    def __hash__(self) -> int:
        """__hash__(self: object) -> int"""
    def __index__(self) -> int:
        """__index__(self: depthai.XLinkDeviceState) -> int"""
    def __int__(self) -> int:
        """__int__(self: depthai.XLinkDeviceState) -> int"""
    def __ne__(self, other: object) -> bool:
        """__ne__(self: object, other: object) -> bool"""
    @property
    def name(self) -> str:
        """name(self: object) -> str

        name(self: object) -> str
        """
    @property
    def value(self) -> int:
        """(arg0: depthai.XLinkDeviceState) -> int"""

class XLinkError(RuntimeError): ...

class XLinkError_t:
    """Members:

      X_LINK_SUCCESS

      X_LINK_ALREADY_OPEN

      X_LINK_COMMUNICATION_NOT_OPEN

      X_LINK_COMMUNICATION_FAIL

      X_LINK_COMMUNICATION_UNKNOWN_ERROR

      X_LINK_DEVICE_NOT_FOUND

      X_LINK_TIMEOUT

      X_LINK_ERROR

      X_LINK_OUT_OF_MEMORY

      X_LINK_INSUFFICIENT_PERMISSIONS

      X_LINK_DEVICE_ALREADY_IN_USE

      X_LINK_NOT_IMPLEMENTED

      X_LINK_INIT_USB_ERROR

      X_LINK_INIT_TCP_IP_ERROR

      X_LINK_INIT_PCIE_ERROR"""
    __members__: ClassVar[dict] = ...  # read-only
    X_LINK_ALREADY_OPEN: ClassVar[XLinkError_t] = ...
    X_LINK_COMMUNICATION_FAIL: ClassVar[XLinkError_t] = ...
    X_LINK_COMMUNICATION_NOT_OPEN: ClassVar[XLinkError_t] = ...
    X_LINK_COMMUNICATION_UNKNOWN_ERROR: ClassVar[XLinkError_t] = ...
    X_LINK_DEVICE_ALREADY_IN_USE: ClassVar[XLinkError_t] = ...
    X_LINK_DEVICE_NOT_FOUND: ClassVar[XLinkError_t] = ...
    X_LINK_ERROR: ClassVar[XLinkError_t] = ...
    X_LINK_INIT_PCIE_ERROR: ClassVar[XLinkError_t] = ...
    X_LINK_INIT_TCP_IP_ERROR: ClassVar[XLinkError_t] = ...
    X_LINK_INIT_USB_ERROR: ClassVar[XLinkError_t] = ...
    X_LINK_INSUFFICIENT_PERMISSIONS: ClassVar[XLinkError_t] = ...
    X_LINK_NOT_IMPLEMENTED: ClassVar[XLinkError_t] = ...
    X_LINK_OUT_OF_MEMORY: ClassVar[XLinkError_t] = ...
    X_LINK_SUCCESS: ClassVar[XLinkError_t] = ...
    X_LINK_TIMEOUT: ClassVar[XLinkError_t] = ...
    __entries: ClassVar[dict] = ...
    def __init__(self, value: int) -> None:
        """__init__(self: depthai.XLinkError_t, value: int) -> None"""
    def __eq__(self, other: object) -> bool:
        """__eq__(self: object, other: object) -> bool"""
    def __hash__(self) -> int:
        """__hash__(self: object) -> int"""
    def __index__(self) -> int:
        """__index__(self: depthai.XLinkError_t) -> int"""
    def __int__(self) -> int:
        """__int__(self: depthai.XLinkError_t) -> int"""
    def __ne__(self, other: object) -> bool:
        """__ne__(self: object, other: object) -> bool"""
    @property
    def name(self) -> str:
        """name(self: object) -> str

        name(self: object) -> str
        """
    @property
    def value(self) -> int:
        """(arg0: depthai.XLinkError_t) -> int"""

class XLinkPlatform:
    """Members:

      X_LINK_ANY_PLATFORM

      X_LINK_MYRIAD_2

      X_LINK_MYRIAD_X

      X_LINK_RVC3

      X_LINK_RVC4"""
    __members__: ClassVar[dict] = ...  # read-only
    X_LINK_ANY_PLATFORM: ClassVar[XLinkPlatform] = ...
    X_LINK_MYRIAD_2: ClassVar[XLinkPlatform] = ...
    X_LINK_MYRIAD_X: ClassVar[XLinkPlatform] = ...
    X_LINK_RVC3: ClassVar[XLinkPlatform] = ...
    X_LINK_RVC4: ClassVar[XLinkPlatform] = ...
    __entries: ClassVar[dict] = ...
    def __init__(self, value: int) -> None:
        """__init__(self: depthai.XLinkPlatform, value: int) -> None"""
    def __eq__(self, other: object) -> bool:
        """__eq__(self: object, other: object) -> bool"""
    def __hash__(self) -> int:
        """__hash__(self: object) -> int"""
    def __index__(self) -> int:
        """__index__(self: depthai.XLinkPlatform) -> int"""
    def __int__(self) -> int:
        """__int__(self: depthai.XLinkPlatform) -> int"""
    def __ne__(self, other: object) -> bool:
        """__ne__(self: object, other: object) -> bool"""
    @property
    def name(self) -> str:
        """name(self: object) -> str

        name(self: object) -> str
        """
    @property
    def value(self) -> int:
        """(arg0: depthai.XLinkPlatform) -> int"""

class XLinkProtocol:
    """Members:

      X_LINK_USB_VSC

      X_LINK_USB_CDC

      X_LINK_PCIE

      X_LINK_TCP_IP

      X_LINK_IPC

      X_LINK_NMB_OF_PROTOCOLS

      X_LINK_ANY_PROTOCOL"""
    __members__: ClassVar[dict] = ...  # read-only
    X_LINK_ANY_PROTOCOL: ClassVar[XLinkProtocol] = ...
    X_LINK_IPC: ClassVar[XLinkProtocol] = ...
    X_LINK_NMB_OF_PROTOCOLS: ClassVar[XLinkProtocol] = ...
    X_LINK_PCIE: ClassVar[XLinkProtocol] = ...
    X_LINK_TCP_IP: ClassVar[XLinkProtocol] = ...
    X_LINK_USB_CDC: ClassVar[XLinkProtocol] = ...
    X_LINK_USB_VSC: ClassVar[XLinkProtocol] = ...
    __entries: ClassVar[dict] = ...
    def __init__(self, value: int) -> None:
        """__init__(self: depthai.XLinkProtocol, value: int) -> None"""
    def __eq__(self, other: object) -> bool:
        """__eq__(self: object, other: object) -> bool"""
    def __hash__(self) -> int:
        """__hash__(self: object) -> int"""
    def __index__(self) -> int:
        """__index__(self: depthai.XLinkProtocol) -> int"""
    def __int__(self) -> int:
        """__int__(self: depthai.XLinkProtocol) -> int"""
    def __ne__(self, other: object) -> bool:
        """__ne__(self: object, other: object) -> bool"""
    @property
    def name(self) -> str:
        """name(self: object) -> str

        name(self: object) -> str
        """
    @property
    def value(self) -> int:
        """(arg0: depthai.XLinkProtocol) -> int"""

class XLinkReadError(XLinkError): ...

class XLinkWriteError(XLinkError): ...

class YoloDecodingFamily:
    """Members:

      TLBR

      v5AB

      v3AB

      R1AF"""
    __members__: ClassVar[dict] = ...  # read-only
    R1AF: ClassVar[YoloDecodingFamily] = ...
    TLBR: ClassVar[YoloDecodingFamily] = ...
    __entries: ClassVar[dict] = ...
    v3AB: ClassVar[YoloDecodingFamily] = ...
    v5AB: ClassVar[YoloDecodingFamily] = ...
    def __init__(self, value: int) -> None:
        """__init__(self: depthai.YoloDecodingFamily, value: int) -> None"""
    def __eq__(self, other: object) -> bool:
        """__eq__(self: object, other: object) -> bool"""
    def __hash__(self) -> int:
        """__hash__(self: object) -> int"""
    def __index__(self) -> int:
        """__index__(self: depthai.YoloDecodingFamily) -> int"""
    def __int__(self) -> int:
        """__int__(self: depthai.YoloDecodingFamily) -> int"""
    def __ne__(self, other: object) -> bool:
        """__ne__(self: object, other: object) -> bool"""
    @property
    def name(self) -> str:
        """name(self: object) -> str

        name(self: object) -> str
        """
    @property
    def value(self) -> int:
        """(arg0: depthai.YoloDecodingFamily) -> int"""

class connectionInterface:
    """

    Members:

      USB

      ETHERNET

      WIFI"""
    __members__: ClassVar[dict] = ...  # read-only
    ETHERNET: ClassVar[connectionInterface] = ...
    USB: ClassVar[connectionInterface] = ...
    WIFI: ClassVar[connectionInterface] = ...
    __entries: ClassVar[dict] = ...
    def __init__(self, value: int) -> None:
        """__init__(self: depthai.connectionInterface, value: int) -> None"""
    def __eq__(self, other: object) -> bool:
        """__eq__(self: object, other: object) -> bool"""
    def __hash__(self) -> int:
        """__hash__(self: object) -> int"""
    def __index__(self) -> int:
        """__index__(self: depthai.connectionInterface) -> int"""
    def __int__(self) -> int:
        """__int__(self: depthai.connectionInterface) -> int"""
    def __ne__(self, other: object) -> bool:
        """__ne__(self: object, other: object) -> bool"""
    @property
    def name(self) -> str:
        """name(self: object) -> str

        name(self: object) -> str
        """
    @property
    def value(self) -> int:
        """(arg0: depthai.connectionInterface) -> int"""

def downloadModelsFromZoo(path: os.PathLike, cacheDirectory: os.PathLike = ..., apiKey: str = ..., progressFormat: str = ...) -> bool:
    '''downloadModelsFromZoo(path: os.PathLike, cacheDirectory: os.PathLike = \'\', apiKey: str = \'\', progressFormat: str = \'none\') -> bool

    Helper function allowing one to download all models specified in yaml files in
    the given path and store them in the cache directory

    Parameter ``path:``:
        Path to the directory containing yaml files

    Parameter ``cacheDirectory:``:
        Cache directory where the cached models are stored, default is "". If
        cacheDirectory is set to "", this function checks the DEPTHAI_ZOO_CACHE_PATH
        environment variable and uses that if set, otherwise the default is used
        (see getDefaultCachePath).

    Parameter ``apiKey:``:
        API key for the model zoo, default is "". If apiKey is set to "", this
        function checks the DEPTHAI_ZOO_API_KEY environment variable and uses that
        if set. Otherwise, no API key is used.

    Parameter ``progressFormat:``:
        Format to use for progress output (possible values: pretty, json, none),
        default is "pretty"

    Returns:
        bool: True if all models were downloaded successfully, false otherwise
    '''
def getModelFromZoo(modelDescription: NNModelDescription, useCached: bool = ..., cacheDirectory: os.PathLike = ..., apiKey: str = ..., progressFormat: str = ...) -> os.PathLike:
    '''getModelFromZoo(modelDescription: depthai.NNModelDescription, useCached: bool = True, cacheDirectory: os.PathLike = \'\', apiKey: str = \'\', progressFormat: str = \'none\') -> os.PathLike

    Get model from model zoo

    Parameter ``modelDescription:``:
        Model description

    Parameter ``useCached:``:
        Use cached model if present, default is true

    Parameter ``cacheDirectory:``:
        Cache directory where the cached models are stored, default is "". If
        cacheDirectory is set to "", this function checks the DEPTHAI_ZOO_CACHE_PATH
        environment variable and uses that if set, otherwise the default value is
        used (see getDefaultCachePath).

    Parameter ``apiKey:``:
        API key for the model zoo, default is "". If apiKey is set to "", this
        function checks the DEPTHAI_ZOO_API_KEY environment variable and uses that
        if set. Otherwise, no API key is used.

    Parameter ``progressFormat:``:
        Format to use for progress output (possible values: pretty, json, none),
        default is "pretty"

    Returns:
        std::filesystem::path: Path to the model in cache
    '''
def isDatatypeSubclassOf(arg0: DatatypeEnum, arg1: DatatypeEnum) -> bool:
    """isDatatypeSubclassOf(arg0: depthai.DatatypeEnum, arg1: depthai.DatatypeEnum) -> bool"""
def platform2string(arg0: Platform) -> str:
    """platform2string(arg0: depthai.Platform) -> str

    Convert Platform enum to string

    Parameter ``platform``:
        Platform enum

    Returns:
        std::string String representation of Platform
    """
def readModelType(modelPath: os.PathLike) -> ModelType:
    """readModelType(modelPath: os.PathLike) -> depthai.ModelType

    Read model type from model path

    Parameter ``modelPath``:
        Path to model

    Returns:
        ModelType
    """
def string2platform(arg0: str) -> Platform:
    """string2platform(arg0: str) -> depthai.Platform

    Convert string to Platform enum

    Parameter ``platform``:
        String representation of Platform

    Returns:
        Platform Platform enum
    """
